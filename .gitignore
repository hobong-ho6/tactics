.DS_Store
.claude/settings.json
.claude/settings.local.json

# 2026-08-13 사용자 확정 — 다른 세션/PC에서 유입된 것으로 추정되는 디렉터리.
# 삭제하지 않고 추적에서만 제외한다(세션 #14 판단 유지, 세션 #15에서 처분 확정).
handoff-hooks/
.claude(구독)/

# 빌드 산출물 — 파이썬 버전마다 갈려서 머신 간 충돌을 만든다(3.9 vs 3.14 실증).
__pycache__/
*.pyc
.venv/

# cron 실행 로그 — 산출물이 아니다.
logs/
