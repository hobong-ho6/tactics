# HANDOFF

<!--
════════════════════════════════════════════════════════════
이 파일은 세션 간 핸드오프 문서입니다. (사람 + Claude 공용)

■ Claude 사용 규칙
1. [세션 시작] 이 파일을 가장 먼저 읽고, "현재 상태"와
   "다음 할 일"을 확인한 뒤 작업을 시작한다.
   시작 시 사용자에게 한 줄로 브리핑한다.
   예) "지난 세션: X 완료. 오늘 예정: Y. 시작할까요?"
2. [세션 종료] 사용자가 "핸드오프 갱신" / "세션 정리" /
   "handoff"라고 하면 이 파일을 아래 규칙으로 갱신한다.
   - "현재 상태" 블록을 새로 작성 (덮어쓰기)
   - "다음 할 일"에서 완료 항목은 [x] 처리 후
     "최근 세션 기록"의 완료 목록으로 이동
   - 새로 발견된 할 일을 "다음 할 일"에 우선순위와 함께 추가
   - "최근 세션 기록" 맨 위에 오늘 세션 엔트리 추가
   - "최근 세션 기록"은 최대 5개 유지. 넘치면 가장 오래된
     엔트리를 "아카이브 요약"에 1~2줄로 압축해 병합
3. [작성 원칙]
   - 다음 세션의 Claude가 이 파일만 읽고 즉시 작업을
     이어갈 수 있을 만큼 구체적으로 쓴다.
     (파일 경로, 커밋 해시, 이슈 번호, 명령어 포함)
   - "왜 그렇게 결정했는지"를 결과보다 우선해서 남긴다.
   - 추측과 사실을 구분한다. 미확인 사항은 (미확인) 표기.
   - 전체 파일이 300줄을 넘지 않게 유지한다.
════════════════════════════════════════════════════════════
-->

## 프로젝트 정보

- **프로젝트명**: Aston Villa → FC 전술 구현 (2026-07-30부터 **3팀 체제**)
- **한 줄 설명**: 실제 감독의 전술을 실측 데이터로 분해해 FC26 인게임 전술(포메이션·역할·포커스)로 재현한다. 대상: **에메리(빌라) / 알론소(첼시) / 이라올라(리버풀)**
- **주요 경로/저장소**: `/Users/user/Documents/tactics` (git, main)
  - DB `data/avl_analysis.db` = single source of truth / 툴 `fc26-heatmap.html` = DB 파생
  - 문서 맵은 `CLAUDE.md` 참조. 신규: `docs/11-alonso-tactics.md`, `docs/12-iraola-tactics.md`
- **관련 링크**: 스케줄 작업 `villa-transfer-watch` (매일 09/21시), 스킬 `.claude/skills/transfer-watch/SKILL.md`

---

## 현재 상태

> 마지막 갱신: **2026-08-11 (심야)** · 세션 **#13** · 브랜치 `main` · 마지막 커밋 **`746600e`** · obs **152개**.
> ⭐⭐ 선수 페이지가 스카우팅 도구가 됐고, **스쿼드 자체의 결손 8명을 찾아 메웠다**(AVL 19명 → 27명).
>
> **이 세션의 산출**: ⑴ **종합 평가 56건**(`player_evaluations`, migration 002) — 3팀 전원 + 영입 후보 4명.
> ⑵ **경기 스탯 탭化** — 수집 풀(3팀) / 리그 전체(Fotmob 40지표). ⑶ **시즌 성적 표**(`v_player_season_stats`,
> 004 — 대회 라벨은 원본 불변, 뷰에서만 정규화). ⑷ **Fotmob 수집**(005·006): traits 288행 + **상세 스탯
> 53명 2006행**(지표별 값·90분당·리그 동포지션 백분위) + `players.fotmob_id`. ⑸ **AVL 전수 재수집**
> (소표본 7명 266행 + 누락 8명분). ⑹ **용어 사전 93항목**(`site/assets/glossary.js`) — 음차 표기를 풀어 쓰고
> 본문에 점선 밑줄 툴팁. ⑺ UI 수리 5건(폼 hover·sofifa 영문·레이더 잘림·좁은 폭 오버플로·report.html 죽은 코드).
>
> **⭐⭐ obs#152 — 스쿼드에 8명이 통째로 빠져 있었다.** 사용자가 밍스·비조를 짚은 뒤 Fotmob 팀 명단과
> 전수 대조해 확인했다. 누락 8명 중 **6명은 DB에 이미 실측 경기가 있었다** — 데이터 결손이 아니라
> `squad_entries` 등재 단계가 빠진 것이다. ⇒ **스쿼드 완전성은 내부 데이터로 검증되지 않는다.**
> 반대 방향 1건: **산초는 Fotmob 명단에 없다**(임대 종료·무소속).
>
> **⭐ obs#151 — 맥긴 RM '인사이드 포워드' 결론은 기각된다.** 그리드가 반증한다: 맥긴 RM 실측은 우측
> 터치라인 열에 48~60%가 몰렸고 insidefwd 커널은 그 열에 19~27%뿐이다(방향 반대), 4표본 전부 5~9위.
> 다만 '가운데로 들어온다'는 관찰 자체는 옳고 **그 표본이 RM이 아니라 CAM 출전분**이다(터치라인 .12).
> 현 처방 `wm_widemid/Support`도 실측 최적은 아니다(최적은 `wm_wideplm/Attack` .761, Δ.033=노이즈) —
> 결정권은 뱅크(자기진영 복귀량 0.60 vs 0.00)에 있고 이는 obs#39 기준이다. **처방 변경 없음.**
>
> ⚠️ **FBref는 못 쓴다** — 스카우팅 백분위를 무료 노출하지 않는다(브라우저로 열어도 표 0개).
> `fbref_percentiles`(003)는 스키마만 남겼다. 대체 소스는 Fotmob이고 오히려 더 촘촘하다.
>
> **다음 세션 우선순위**:
> 1. **08-12 슈퍼컵(PSG) 실측** — 26/27 첫 공식전. 08-13 09시 스케줄 런 확인.
> 2. **산초 행 처분 결정** — Fotmob 무소속인데 `squad_entries`에 남아 있다(사용자가 재집계 제외 지시).
>    이탈 등재 여부는 사용자 판단 사항.
> 3. **비조 처방 충돌 해소** — 처방은 `gk_goalkeeper/Defend`("스위퍼 아님")인데 Fotmob 스위퍼 행동이
>    90분 기준 상위 5%로 방향이 반대다(신규 평가에서 발견).
> 4. **CHE·LIV 소표본 전수 재수집** — LIV는 `player_matches`가 대부분 n=1이다. AVL과 같은 절차로.
> 5. `fotmob_detail_stats` 결손 3명(포파나·에스테방·알리송) 보충 + 네델코비치·마조 표본 축적.
> 6. 게임 시스템 페이지 EA 정의문 85건 한글 번역(사용자 요청 잔여분).
>
> ⚠️ 미커밋 `.gitignore`·`README.md` + untracked는 사용자 소유 — `git add -A` 금지.

## 다음 할 일

> 08-10 종료. ⏰ **다음 세션 첫 작업 권고: 08-12 PSG 슈퍼컵(잘츠부르크) 실측** — **소멸성 기회**다.
> 26/27 빌라 첫 공식전이라 커버리지가 확실하고(obs#133), **obs#134(우측 결손)·obs#135(엔드릭 우측)의
> 공통 1차 검증**이며 마조 표본을 3/6 → 4/6으로 올린다. 그 다음이 아래 **P0**(obs#132 파급, 미착수).

- [ ] **⭐⭐ P0 · obs#132 파급 재검토** (08-05 발생, 미착수) — SofaScore가 **0일 때 키를 생략**한다는 것이
  밝혀져 전수 재수집(142/142) 후 **인터셉트·태클 1위가 카마라 → 오나나**로 바뀌었다. 그런데 이 값을
  근거로 삼은 앞선 판정들을 아직 재검토하지 않았다. 최소 확인 대상:
  ⓐ **obs#129 ③ "3선 배급 주도는 카마라"** — 근거였던 이벤트 스탯이 바뀌었다. 성립하는지 재판정.
  ⓑ **`FUNC_AXIS` 카마라 `require(dm_holding)`** — obs#130이 보류 결정을 내린 근거도 옛 값이다.
  ⓒ `v_event_profile`을 인용한 다른 obs 전수 — **뷰의 `n` 컬럼이 지표별 표본 수가 아니다**(obs#131).

- [x] ~~**P1 · R축 검정**~~ — 기각(obs#123). **재개 조건: 표본 2~3배**(쌍당 n≥60~100), `docs/60` 재사용.

- [ ] **P1 · 국면 축 확장** (obs#101 → obs#122 후속) — possession 행이 늘어(로저스 42·콘사 38·디뉴 26·
  틸레만스 24) obs#101이 8쌍에서 멈춘 것을 다시 돌릴 수 있다. ⚠️ 게상 12/26·일링 0/15은 결손(니스·임대분).
  ⚠️ obs#101 ③(**rest-defense 칸은 저점유가 옳은 기준**)이 확장 표본에서도 성립하는지 재확인할 것.

- [ ] **⭐ P1 · 첼시 피벗 처방 결정** (obs#116, **사용자 판단 사항**) — 기능축 실측이 확정한 배정
  (카이세도=볼위너 · 엔소=창조)과 현 처방(두 피벗 모두 `dm_dlp`)이 어긋난다. 선택지 2개:
  ⓐ **현행 유지** — 움직임 적합 +.126을 지키고 기능 정합을 포기(현 평균 .799)
  ⓑ **소스 배정 채택** — 엔소 LDM `dm_dlp`/Roaming .698 + 카이세도 RDM `dm_holding`/Roaming .647
     (평균 .673, −.126). 카이세도 개인 지불액은 **.246~.294**로 카마라의 .147보다 크다.
  ⚠️ 표본이 **알론소 체제가 아니다**(25/26 마레스카·로즈니어) — 8/24 풀럼 후 재판정 가능.

- [ ] **P2 · 리버풀 기능축 최초 수집** (obs#116의 첼시 방식 그대로) — `appearances` team=LIV **0행** ·
  `player_match_positions` team=LIV **0행**이다. obs#120이 12명을 재산출했으나 **피벗 3인이 전부
  `dm_dlp`/Roaming**으로 나와 **기능 분화를 가릴 수단이 없다**. 프리셋보다 이것이 먼저다.
  경로: `/api/v1/event/{eid}/player/{pid}/statistics` → `player_match_positions.stats_json`.

- [ ] **P2 · SSOT 4~7단계** — 4단계 `CMP_SLOTS`+`SQUAD_SLOTS` 6건+`PLAYER_BEST`(obs#84 ⑨의 원래 타깃) →
  5~7단계 `SLOT_EMERY_ROLE`+`FUNC_AXIS` · `FORMATIONS` · `SQ_STATS`.
  ⚠️ **4단계 선행 필수 2건**: ⓐ **실제 드리프트** — `CMP_SLOTS` 맥긴 CAM 그리드가 **DB 어느 값과도 다른**
  제3의 값인데 툴·DB가 같은 표본("CAM 4경기 평균 6.90")을 주장한다. orphan 그리드 10건 처분도 함께.
  ⓑ **별칭 축**(obs#115 ④) — 축약↔풀네임 14쌍은 규칙 도출 불가 → `player_fc_stats.tool_label`(NULL 허용)이
  최소 침습안. 툴 내부 `베리발`/`베르그발` 2중 표기도 통일.
  ⚠️ **이관 금지**(obs#105 ⑥, 사유는 결정표 참조): `ANIM_*` · `COLL_CAL` · `FORMATION_DESC` · 초기추정 프리셋 · `TEAMS`.
  ⚠️ **별 단계로 분리**: 상태 접미 소유자 통일(런타임 vs 하드코딩이 둘 다 `(합류확정)` 생산, 키 200여 개
  파괴적 변경) · `UNIQUE(game_version, sofifa_id)` 추가(없으면 이름 변형 중복 재발).

- [ ] **P2 · 애니메이션 궤적** (obs#117 ⑵ 잔여) — 마커는 커널 파생이나 **화살표(`ANIM_PHASES.ar`)·
  경로(`AN_PATTERN`)는 옛 상수**라 어긋날 수 있다(`전술 요구역할만` 모드에서 LM이 20칸 이동).
  대안: 화살표를 **파생 좌표 사이 직선**으로 대체(정합성 ↑, 표현력 ↓) — 사용자 판단 필요.

- [ ] **P3 · 잔여 4건** — ⓐ **세트피스 득점 비중 축**(obs#112·#113 결손): DB에 세트피스 전용 컬럼이 없어
  obs#49(알론소 40%)·obs#74(이라올라 33%)와 **동축 비교 불가**. 경로는 `/event/{eid}/incidents`(브라우저 필수)
  또는 `appearances` 자유서술 파싱(⚠️ 커버리지 불균등 → "비중" 금지, "확인된 골 목록"까지만).
  ⓑ **리버풀 프리셋**(8/23 개막 후 · **기능축 수집이 선행**)
  ⓒ ⚠️ **transfer-watch 소스 1.5개 손실** — Sky 빌라 라이브 블로그(누적 5회+ 실패) + **Fotmob은 헤드리스
     스케줄 런에서만 차단**(`preview_start` 권한 분류기). **대화형에서는 정상**이며 08-10 심야 회차에서
     30행 수집 확인. ⚠️ 단 **Fotmob 라벨은 티어뿐 아니라 날짜도 틀린다**(루헤리 "Today" → 실제 08-08).
  ⓓ [x] ~~바투리나·리세 STALE 동시 전환 집행~~ — 완료(둘 다 `DEAD (STALE …)`로 DB 확인).

- [ ] **P2 · 이적창 마감 추적** — 마감 **2026-09-01(화) 23:00 BST**(PL 공식, 08-31은 오기였음).
  ⓐ **루헤리**: €20m 클럽 합의 + 선수 그린라이트, **메디컬·공식발표 0건**(Romano "few more steps") → 나오면 `CONFIRMED`
  ⓑ **베일리**: 공식 발표 9일째 0건. "완료" 서사는 **헐 구단주 SNS 발**이라 근거 아님
  ⓒ **콘사**: 아스날 2차 오퍼 **미제출**(전 매체 "준비" 시제, 원출처 Daily Mail 단독). 리버풀은 5번째 재순환
  ⓓ ⭐ **잭슨 45일 (b) 조건 정밀화** — 로저스 07-22 확정 → 만료 **09-05**로 잔여 창 전체가 구간 안. 단
     Annex G.3.5는 **(a)45일 AND (b)동일 지급구조**의 연언 조건 + **substance over form**이라 45일은
     자동 판정선이 **아니다**(회피는 (b)로 하며 실증이 가르나초). ⚠️ **잭슨은 임대 슬롯 소진으로 (b)가 막혔다** — 미확인.
  ⓔ [x] ~~Emerson Royal STALE 전환~~ — 완료(**삭제 안 함**, fit_sim 0.95 보존). ⇒ **RB 후보가 0이 됐다.**

### 차단 요소 / 대기 중

- **3팀 모두 공식전 0경기** — 첼시 **8/24 풀럼**, 리버풀 **8/23 뉴캐슬 원정**.
- **EA가 수치를 공개하지 않는다** — Role+/++ 정량 효과, OOP 페널티 크기, 포커스 위치 델타,
  역할별 능력치 가중치. **AcceleRATE는 분류 공식만 공개**(효과 크기는 여전히 비공개).
- **국면 분리는 보유 선수만** — 영입 4명은 빌라 경기 0이라 구조적으로 불가.
- ⚠️ **SofaScore 실측 제공 여부는 "친선이냐"가 아니라 "그 대회를 커버하느냐"로 갈린다** —
  **obs#133(08-10)이 obs#126의 일반화를 정정했다.** 양방향 반증이 다 나왔다:
  **친선인데 있음** = 빌라 vs 바이에른(08-07, event `16284981`) 마조 **히트맵 28pt·평점·스탯 정상** /
  **공식전인데 없음** = 마조의 U17 유럽선수권 예선 3경기(90·36·90분) **히트맵 전건 0**.
  08-01 인도네시아전이 null이었던 진짜 원인은 친선이라서가 아니라 **상대가 올스타 선발팀**이라 커버리지 밖이었던 것.
  ⇒ **프리시즌이라고 미리 포기하지 말 것 — 경기별로 수집을 시도해 보는 것이 유일한 판별법이다.**
  ⇒ 다만 **커버되는 경기라도 표본 기준(§3: 6경기 45분+)은 별도**다. 마조는 현재 **3/6**이라 그리드 미산출.
- ⚠️ **`tactic_code`가 16행 전부 비어 있다** — 어느 팀도 실제 인게임 슬라이더/코드가 DB에 없다.
  처방은 "역할·포커스·라인"까지만 내려간다. `ingame_checks`도 **0행**(게임 내 검증 폐기, docs/50).
- ⚠️ **적합도가 전 국면 평균 기준**이고 obs#101은 그 어긋남을 **빌라 표본으로만** 측정했다 —
  첼시·리버풀에 같은 노이즈 크기가 성립한다는 근거는 없다.

---

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-08-10 | **웹 검색보다 DB 선조회를 먼저 한다** — SKILL.md **§1-0 신설** (`731d92f`) | "프리시즌 활약 유스"를 웹으로만 훑다 헤밍스·린치를 통째로 놓쳤는데 **obs#126 ③에 5명이 이름으로 적혀 있었다**(Lynch 어시 → Madjo 골까지). 검색 설계 문제인 줄 알았으나 실제 원인은 **파이프라인에 DB 선조회 단계가 없었던 것**. ⭐ 함께 넣은 규칙: **obs는 `claim`과 `confidence`를 같이 읽어라** — obs#126은 결론을 "친선이라 실측 없음"으로 썼지만 `confidence`에 진짜 원인("상대가 올스타 선발팀")이 이미 있었다. **근거 칸이 결론보다 정확할 수 있다** |
| 2026-08-10 | **obs#126은 폐기가 아니라 범위 축소** — obs#133 신설 (`b790539`) | 관측(11명 전건 null)은 옳고 과한 것은 **"프리시즌 친선의 구조적 한계로 기록한다"** 한 문장이다. 양방향 반증 확보: **친선인데 있음**(빌라 vs 바이에른, 히트맵 28pt) / **공식전인데 없음**(U17 유럽선수권 예선 90·36·90분, 히트맵 0). ⇒ 결정 변수는 친선 여부가 아니라 **SofaScore 대회 커버리지**이며, **사전 판별법이 없어 수집을 시도해 보는 것이 유일한 방법**이다. HANDOFF 차단요소도 "경기별로 시도하라"로 교체 |
| 2026-08-06 | **불변규칙 8 신설 — 히트맵은 시각화해서 보여준다** (`8707e44`) | `map25` 5×5는 공간 정보(열0=좌측 터치라인·행0=공격)인데 숫자열로는 좌우 편향 비교가 사실상 불가능하다. 실증: 가르나초 좌2컬럼 **90%** vs 로저스 **68%** — 이 대비는 그림에서만 즉시 보인다. 수치 결론은 위젯 **밖**(응답 본문)에 쓴다 |
| 2026-08-06 | **커널 적합도의 별도 구현을 조건부 허용** — 회귀 게이트 통과가 조건 | 컨텍스트 노트 「측정 기준 #2」는 "브라우저에서 툴 함수 직접 호출, 별도 구현 금지"였다(MAPS 파싱 함정 때문). 08-06에 파이썬으로 `decodeMap`/`placedMap`/`cmpCos`를 재현하고 **역할 수 37 + 회귀값 7개**를 통과시켜 브라우저 없이 산출했다. ⭐ 금지의 목적은 "브라우저 사용"이 아니라 **파싱 함정 방지**이므로, 게이트를 통과하면 목적이 충족된다. 게이트 없이 구현하는 것은 여전히 금지 |
| 2026-08-06 | **SKILL.md 회귀 기준값 정정** (하지무사 .923→**.821** · 은디아예 .883→**.825**) | 두 값이 obs#93·#94 커널 교체 **이전** 것이라 그대로 쓰면 **회귀 검증이 항상 실패**했다. ⭐ 전수 재산출 결과 **틀어진 것은 wide 역할(RM/LM)뿐**이고 ST·CAM 4개(Jackson .752·Quiñones .832·만잠비 .817·바투리나 .711)는 그대로 재현됐다 — 커널 교체가 터치라인 계열에만 영향을 줬다는 뜻. HANDOFF 컨텍스트 노트는 이미 .821로 옳았고 **SKILL.md만 낡아 있었다** |
| 2026-08-05 | **`v_event_profile` 인용 시 지표별 표본 수를 반드시 확인한다** (obs#131·#132, `448fbc6`) | 뷰의 `n` 컬럼이 **지표별 실제 표본 수가 아니다**(카마라 인터셉트 3.0은 2경기 평균, 1.40이 9경기 값). 더 근본적으로 **SofaScore는 값이 0이면 키 자체를 생략**하므로 DB의 '결손'은 미수집이 아니라 **0**이었다 — 전수 재수집으로 인터셉트·태클 **1위가 카마라 → 오나나**로 바뀌었다 |
| 2026-08-05 | **카마라 `require(dm_holding)` 하향 보류** (obs#130, 사용자 지시를 실행 직전 중단) | 지시는 prefer 하향이었으나, obs#121이 이 제약의 존재 이유를 "카마라를 창조 역할에 배정한 솔버 사고"로 명시한 것을 실행 직전 확인했다 — 내리면 그 사고가 재발한다. 계열 내 포커스 교정(Ball-Winning .556 → **Defend .703**)이 현 근거로 정당화되는 최대치 |
| 2026-08-03 | **이적 감시에서 발행일 검증을 티어 판정보다 먼저 한다**(SKILL.md §2-0 신설) | 하루에 **10건**을 이 검사로 걸렀고, 그중 Sancho/Lammens는 **BBC Sport 본체 + Romano 원트윗**이라 **매체 티어로는 원리적으로 안 잡힌다**. 여름 창은 매년 같은 구단·같은 포지션 루머가 돌아 **정확히 1년 오차**가 가장 흔하다. 실패한 지표(URL 슬러그·검색 순서·피드 "Nh ago") 대신 **본문 나이 표기**와 **병기된 타 구단 상황**을 1순위로 삼는다 |
| 2026-08-03 | **Fotmob은 복구하되 "리드 소스"로만 쓴다**(티어 근거 금지) | 인앱 브라우저로 열자 CSR 테이블이 완전히 렌더링됐다 = 07-31 진단(구조적 불일치)이 옳았고 `WebFetch`로는 여전히 불가. 고유 가치는 **루머별 출처·날짜 라벨**이지만, `Watkins→Fenerbahce`를 "Sky Sports"로 표시해 놓고 실제 원출처는 터키 매체였다 — 게다가 행이 **선수 페이지로만 링크**돼 라벨을 검증할 수 없다. 수집 주체는 **메인 세션**(브라우저 pane이 세션당 하나라 서브에이전트와 충돌) |
| 2026-08-01 | **R축 미신설(기각)** — C6는 소프트 유지 | 사전 등록한 처분표 그대로. FDR 생존 **0/5** + 대조군 **C 계수 음수**(−0.435, 방향 반대). ⭐ 그러나 **방향은 4/5가 예측대로 음수**였고 실패 원인은 효과 부재가 아니라 **검정력**이다(판정에 \|r\|≥0.33~0.45 필요, 실측 0.13~0.24) → "보완이 없다"가 아니라 **"이 도구로는 세울 수 없다"**가 정확한 진술이고, 표본 2~3배면 재검정 가치가 있다 (obs#123) |
| 2026-08-02 | **GK·CAM·ST 슬롯의 적합값을 좌우 편향 근거로 쓰지 않는다** | 그 슬롯에서 선택되는 커널 변형이 **좌우 대칭**이라 그리드를 뒤집어도 값이 **소수 셋째 자리까지 동일**하다(.686→.686). 코사인이 원리적으로 방향을 못 본다 — 우위가 말하는 것은 "퍼진 분포"까지다 (obs#127) |
| 2026-07-31 | **측정 기준 재정립 14건** (압축 — 세션 #3, obs#82~#96) | **슬롯 x 정본 = 툴 `SQUAD_SLOTS`**(코사인이 x의 계단 함수) · docs/20 §B 라인 보상 규칙 폐기(obs#27과 모순) · 전역 충돌 임계값 → **슬롯쌍 상대 지수 CI**(쌍 중앙값이 140배 범위) · 26/27 프리셋 5종 폐기 → 단일 `projected:emery-2627` · **A축 기각**(두 독립 검증 실패 + EA "Opta AI 모델" 명시) · **B축은 점수 아닌 플래그**(이산 속성) · 뱅크 포커스 기본 Support · RM 뱅크는 캐시(알리송은 뱅크 미숙련) · **`placedMap` 전면 교체 — 변형은 시프트가 아니라 중앙 대칭 미러** · 회귀 게이트 값 교체 · **프리셋 H안 채택**(사용자 확정) · **C5 기능축 신설**(솔버가 카마라를 창조 역할에 배정한 사고) · **C6는 소프트**(obs#95 근거가 억제 1건뿐) · 고점유 전환 보류(→ obs#101에서 기각으로 종결) |
| 2026-07-30 | **3팀 체제 도입 결정 9건** (압축) | 팀 축 = 텍스트 `team` 컬럼 + FK 없는 `teams`(불변규칙 2 준수) · UNIQUE 충돌 5테이블만 재작성 · **STALE = DEAD 숨김(삭제 아님)** — 툴이 `/^DEAD/i`를 필터하므로 행을 지우면 `map25`·`fit_sim`을 잃는다 · 등급 강등도 숨김으로 통일 · STALE 판정은 **빌라 링크 보도일**(`last_news_date`) 기준이고 `CONFIRMED`/`OWNED` 제외 · **CCB 슬롯 신설**(백3 3인이 각자 자기 자리 최고치) · CCB를 `CMP_SLOTS`에 의도적 미추가(빌라 화면 누출 방지) · 스캔은 서브에이전트·판정은 메인 · **obs#28 빌라 PPDA 9.88 폐기**(대표값 12.3/12.45, 불변규칙 3) · 로저스 첼시 행은 빌라 그리드로 적재(캐비앗 명기) |
| 2026-07-31 | **히트맵은 1차 필터로 옳으나 단독으로는 요구역할의 36%다** | 요구역할 69조항 중 위치 25(36.2%) · 온더볼 15 · 관계 14 · 타이밍 9 · 세트피스 4 · 기능 2. 히트맵이 옳은 이유는 **FC26의 Role이 정확히 위치 분포**이고(fut.gg가 heatmap을 준다) 우리가 재는 것과 게임이 구현하는 것의 **자료형이 같기** 때문이다. 못 담는 63.8%의 대부분은 **역할 선택으로 애초에 구현 불가**(PlayStyles·능력치·세트피스 시스템) — 결함이 아니라 레버가 다르다 (obs#121) |
| 2026-07-31 | **세션 #4 결정 12건** (압축 — obs#100~#122, 상세는 각 obs) | **SSOT 1순위=`MAPS` 커널**(모든 적합값의 뿌리인데 툴에만 있어 백업 없음) · **`ANIM_*`·`COLL_CAL`은 DB로 안 옮김**(전자는 삽화라 `source`를 정직히 못 채우고 DB에 넣으면 실측 권위를 얻는다, 후자는 그리드의 순함수라 박아두면 조용히 stale) · **전수 42,972로도 H안 유지**(297위·상위 0.69%, Δ.0131은 EA 노이즈 하한 미달) · **C6는 대가 0이어도 소프트**(대가가 없다는 것이 근거가 세졌다는 뜻이 아니다) · **고점유 전면 전환 기각**(rest-defense 칸은 부호 반대) · **요구역할 표는 실측에 맞춰 고치지 않음**(불일치는 대부분 obs#14의 의도적 예외) · **애니메이션 위치만 커널 파생, 궤적은 삽화** · **sync가 프리뷰 미러 자동 갱신**(사람이 기억할 절차는 결국 빠뜨린다) |
| 2026-07-31 | **측정·수집 규약 6건** (압축 — obs#97·#98·#103·#104·#100·#102) | **슬롯 판정 원천 = `/lineups` `players[]` 배열 순서**(`formationPlace` 전 이벤트 NULL, 좌표 밴딩은 RB·RAM이 겹쳐 원리적 불가) · **`pos_class` 보존 + `lineup_pos` 신설**(덮어쓰기 아님, 두 파생을 나란히 둬야 밴딩 오탐을 계속 감사할 수 있다) · **C5에 `focus` 선택 필드 추가**(LCB에서 갈리는 것은 role이 아니라 focus였다 — 결손은 근거가 아니라 표현력) · **AcceleRATE 경계는 보가르드·카마라 2명**(불일치 4명 ≠ 키 민감 2명) · **SOFIFA `en` 정본 = `id` 보유 항목** · **첼시 제임스는 포커스만 교체**(Δ.001 동률에서 움직임만 보고 역할을 바꾸는 것이 C5를 낳은 그 사고다) · **LCB는 `Build-Up` 유지**(Δ.049는 노이즈라 결정권이 duties로 넘어간다) · **obs#38은 폐기 아니라 개정** |

---

## 최근 세션 기록

### 2026-08-11 — 세션 #13: 스쿼드에 8명이 없었다 (obs#149~152, `15ef406`…`746600e`, 5커밋)

- **종합 평가 56건 신설**(`player_evaluations`) — 3팀 전원 + 영입 후보 4명. 서브에이전트 6개가 분담.
- **Fotmob 전환** — FBref가 백분위를 닫아서 대체했는데 오히려 더 촘촘하다(선수당 40지표 × 리그 동포지션 백분위).
- **스쿼드 누락 8명 복구**(obs#152) · **맥긴 IF 재검증 기각**(obs#151) · **아우안 FC26 로스터 부재**(obs#149) ·
  **AVL 소표본 7명 전수 재수집 266행**(obs#150).
- **용어 사전 93항목** — '프레스 저항 캐리' 같은 음차 표기를 풀어 쓰고 3개 페이지에 툴팁으로 적용.
- **발견 / 배운 것**:
  - ⭐⭐⭐ **스쿼드 완전성은 내부 데이터로 검증되지 않는다**(obs#152) — 누락 8명 중 6명은 실측 경기가
    이미 DB에 있었다. "데이터가 있다"와 "명단에 있다"는 다른 문제이고, 후자는 외부 명단 대조로만 잡힌다.
  - ⭐⭐ **모집단을 바꾸면 결론이 뒤집힌다** — 산초 xG는 우리 3팀 풀에서 백분위 68인데 리그 윙어
    기준으로는 **하위 13%**다. 시세·만잠비는 반대 방향으로 뒤집혔다. 백분위는 모집단과 함께 읽어야 한다.
  - ⭐⭐ **FBref가 막힌 게 아니라 소스가 사라졌다** — WebFetch 403은 차단 신호였지만 브라우저로 열어도
    표가 없었다. "도구가 막혔나"와 "소스가 없어졌나"는 브라우저로 확인하기 전엔 구분되지 않는다.
  - ⭐⭐ **DB에 없는 주장이 페이지에 떠 있었다** — 가르나초 '트릭스터'가 수집값에도 근거 obs에도 없었다
    (사용자가 발견). 서브에이전트에 "조회하지 않은 주장 금지"를 명시하게 된 계기.
  - ⭐ **표시 라벨을 조인 키로 쓰면 조용히 빠진다** — '아브라함(보유)' vs '타미 아브라함'.
    불변규칙 6(라벨 조인 금지)은 DB만이 아니라 프런트에도 적용된다.
  - ⭐ **표본이 10배 늘어도 판정은 대개 그대로다** — 재수집 7명 중 커널 판정이 바뀐 것은 린델뢰프 하나.
    재수집의 값은 판정 변경이 아니라 신뢰구간 축소다.
  - ⭐ **좁은 화면에서 `overflow-wrap:anywhere`는 표를 파괴한다** — 셀 글자가 세로로 쪼개졌다.
    긴 식별자 줄바꿈과 표 가로 스크롤은 다른 처방이다.

### 2026-08-10 — 세션 #11: 판정을 세 번 뒤집은 날 (obs#133~135, `d1ce280`…`095d1f0`, 17커밋)

- **이적 감시 4회차**(09시 스케줄 · 21시 · 심야 `/transfer-watch` · 사용자 지시 추가 8건).
  전 회차 리포트는 `reports/transfer-watch/2026-08-10.md` 한 파일에 누적.
- **obs 3건 신설** — #133(친선 실측 일반화 정정) · #134(26/27 결손 판정) · #135(엔드릭 우측 77.1%).
- **SKILL.md 2곳 개정** — §1-0 신설 · §2-0 지표 2개 추가. **HANDOFF 차단요소 1건 정정**(친선 실측).
- **실측 2건** — 마조(SofaScore `2070311`, **3/6 임계치 미달로 그리드 미산출**) ·
  엔드릭(`1174937`, 20경기 980pt, `map25` obs#135에 보존).
- **이적**: 에머슨 로열 **STALE 전환**(fit_sim 0.95 보존, 삭제 안 함) · 루헤리 HIGH 유지(메디컬 0건) ·
  팔리냐 경쟁강도 하향 정정 · 베일리 상충 원인 특정(구단주 SNS 발) · 콘사 2차 오퍼 미제출 확인.
  신규는 전부 MEDIUM 미달 미등재(엔드릭 · 조나단 데이비드 · Seys · Louis Page · Gabriel Sara · Catamo).
- **진행 중 / 중단 지점**: 없음. 미커밋 2건은 이전부터 있던 것(위 「현재 상태」 참조).
- **발견 / 배운 것**:
  - ⭐⭐ **근거 칸이 결론보다 정확할 수 있다**(obs#133) — 답이 이미 `confidence`에 적혀 있었다.
  - ⭐⭐ **필터를 좁게 걸면 DB를 봐도 놓친다**(obs#134) — `CONFIRMED`를 빼고 세어 결손을 오판했다.
  - ⭐⭐ **적합값이 못 보는 축이 있다**(obs#135) — 좌우 방향은 그리드 그림에서만 보인다.
  - ⭐ **같은 매체도 최신순 정렬이 필요하다** — 연도가 맞아도 "이미 뒤집힌 중간 기사"일 수 있다(에스투피냔·사라).
  - ⭐ **Fotmob 라벨 금지는 날짜에도 적용된다** — "Today"가 붙은 루헤리 항목의 원문은 08-08이었다.

### 2026-08-04~06 — 세션 #8~10: 저장된 값을 의심한 날들 (obs#128~132, 커밋 `3b1ae6d`…`8707e44`)

- **08-04** 이적 감시 티어 무관 전수 수집(12건) · **루헤리 LB 등재**(`fb_att_wb`/Support **.949**).
- **08-05** ⭐⭐ **obs#131→#132 인터셉트 전수 재수집**(142/142, 1위 카마라 → **오나나**) · obs#128 좌측백
  `fb_wingback/Balanced` · obs#129 카마라 RDM·고메스 LDM · ⭐ **obs#130 지시 실행 직전 보류** ·
  바클리 실측(RDM .836) · 이적: 에스투피냔 **DEAD**(발행일 오류 자기수정)·왓킨스 하향·잭슨 정체 플래그.
- **08-06** ⭐⭐ **가르나초 W 행 obs#94 누락 교정**(.601 → **.936**) · **파이샹 실측**(SofaScore 981815) ·
  **불변규칙 8 신설** · SKILL.md 회귀값 정정 · 이적 감시(루제리 HIGH 상향).
- **발견 / 배운 것**:
  - ⭐⭐ **일괄 교체는 "적용된 곳"이 아니라 "적용되지 않은 곳"을 세어야 한다** — obs#94가 놓친 W 행이
    **8개 중 최저값**을 정답처럼 들고 있었다. 교체의 검증은 argmax 재계산으로.
  - ⭐⭐ **결손과 0은 다르다 — API가 어느 쪽인지 먼저 확인하라**(obs#132).
  - ⭐ **서사가 실측을 세 번째로 이기지 못했다**(파이샹: 프로필 "컷인" vs 실측 터치라인형, 좌2컬럼 89%).
  - ⭐ **회귀 게이트가 낡으면 검증이 아니라 소음이다** — 값을 바꾼 커밋과 게이트를 같이 고쳐야 한다.

---

## 아카이브 요약

- **세션 #5~7 (2026-08-01~03, `bd527a3`…`504e312`, obs#123~127)** — **R축 검정 → 기각**(obs#123,
  사전등록 후 FDR 생존 0/5·대조군 계수 음수. 재개 조건: 표본 2~3배) · ⭐⭐ **obs#127 코사인 방향 맹점**
  (GK·CAM·ST는 커널이 좌우 대칭이라 그리드를 뒤집어도 적합값이 같다) · 잭슨 **PL 동일구단 임대 1명 규정**
  확인 · **Fotmob 복구** · **§2-0 발행일 우선 검증 신설**. 교훈: **기각도 산출물이고 기각의 정확한 문장이
  다음 작업을 정한다** · **적합값이 무엇을 말하지 *않는지* 검사하라** · **티어는 발행일을 보증하지 않는다**.
- **세션 #4 (2026-07-31, `1c51818`…`7cd40ba`, obs#97~122)** — 백로그 비우기 + 방법론 한계 정량화.
  **SSOT 1~3단계** · 전수 42,972 라인업 · 첼시 기능축 23행 · **A축 폐기** · **⭐#121 히트맵 커버리지 36.2%**.
  교훈: **기록을 실행 전에 검증하라**(11건이 틀렸고 3건은 내가 만든 것) · **"표 값 ≠ 현 처방"을 "표가 낡음"으로
  읽은 것이 오판**(obs#14의 의도적 예외였다) — **차이의 관찰과 판정은 다르다** · **"근거 확보"는 새 데이터가
  아니라 검증 설계다** · 사용자 지시도 검증 대상이며 **기각이 아니라 정교화로 답한다**.
- **세션 #3 (2026-07-31, `924fc4e`…`8cb9cda`)** — **FC26을 정면으로 판 날**. EA 피치노트 확보(obs#91: 커널은
  **의도적 비결정성** · **Δ 0.02~0.05는 노이즈**) · `placedMap`을 **EA 정본 변형**으로 교체(obs#93·#94: 변형은
  시프트가 아니라 **중앙 대칭 미러**) · 프리셋 H안 · C5·C6 신설 · `game_role_focus` 85행(obs#92).
- **세션 #1 (2026-07-30, `68444a3`…`ae2c976`)** — **3팀 체제** 개시(알론소·첼시 obs 23건 `docs/11` ·
  이라올라·리버풀 16건 `docs/12`). 교훈: **실측이 서사를 5번 정정**(팔머 CAM→RM .845 등) · 탭 프리즈는
  레이트리밋이 아니라 **홈 라이브 스크립트** 탓. 그 이전(7/14~7/29)은 리포트·git log에 있다.

---

## 컨텍스트 노트

**⚠️ SSOT (obs#105·#107·#115 — 1~3단계 완료)**
- **DB가 정본인 블록 3개**: `MAPS` 85커널 → `game_role_focus.kernel25` · `ROLE_VARIANTS` 217변형 →
  `game_role_variants` · 라벨 → `players.name_kr`(보유 22명, 툴 키와 **100% 일치**).
  이 블록들은 `AUTOGEN:*`로 자동 생성되므로 **직접 편집하지 말 것** — DB → `python3 scripts/sync_transfer_ui.py`.
- **AUTOGEN 마커 문자열은 정확히 일치해야 한다**(`/* AUTOGEN:NAME:START */`). 설명은 마커 **밖**에 쓸 것 —
  마커 줄에 덧붙이면 `replace_block`이 `sys.exit`한다.
- 회귀 게이트: **DB 무변경 상태에서 sync를 두 번 돌려 `git diff`가 비어야** 한다.
- ⭐ **sync가 프리뷰 미러(`/private/tmp/tactics-preview/`)를 자동 갱신한다** — 손으로 `cp` 하지 않아도 된다.
- ⚠️ 4단계 이후는 **순수 additive로 불가**하다(obs#115 ④) — 축약↔풀네임 **별칭 축**이 필요하다.
- ⚠️ **신규 이적 후보를 등재할 때 `opt_role`·`opt_focus`를 반드시 채울 것** — `injectTransferCandidates()`가
  이 둘이 있어야 `SQUAD_SLOTS`에 후보를 넣는다. 비우면 `CMP_SLOTS`·`XI_POOL`에는 뜨는데 **스쿼드 슬롯
  후보에서만 조용히 빠진다**(08-01 아우안·리세가 이 상태였다). 규약은 **`opt` = `fit`**(실측 argmax).

**⚠️ 방법론의 커버리지 (obs#121 — 과도한 기대와 과도한 자기비판을 둘 다 막는 기준)**
- 요구역할 69조항 중 **히트맵이 직접 담는 것은 36.2%**. 온더볼 21.7% · **관계 20.3%** · 타이밍 13.0% ·
  세트피스 5.8% · 기능 2.9%. 재분류: 역할·포커스 직접 통제 36% · 제약으로 부분 커버 33% ·
  **다른 레버 31%**(PlayStyles·능력치·세트피스 시스템).
- ⭐ **커버리지 0%인 슬롯이 정확히 C5를 붙인 슬롯**이다(파우 0% · 콘사 0% · 카마라 40% · 오나나 25%) —
  커버리지가 낮은 칸에서 **duties가 적합을 이기는 것은 구조적 필연**이다.
- ⚠️ **63.8%를 "구현 실패"로 읽지 말 것** — 상당 부분이 게임의 다른 레버이거나 AI가 알아서 한다.
- ⛔ **R(관계) 20.3%는 축이 없고, 세우려다 기각됐다(obs#123, 08-01).** 도구(잔차 상관)도 표본(14명/91쌍)도
  있었지만 FDR 생존 0/5 · 대조군 계수 음수였다. **재시도하려면 표본 2~3배가 먼저다** — 기준을 다시
  만들지 말고 `docs/60-r-axis-prereg.md`를 재사용할 것.
- ⛔ **방향(좌/우) 판별은 GK·CAM·ST에서 원리적으로 불가**(obs#127) — 그 슬롯 커널 변형이 좌우 대칭이라
  그리드를 뒤집어도 적합값이 같다. 그 칸의 우위는 "퍼진 분포"까지만 말한다.

**⚠️ 새 축을 세울 때의 4요건** (obs#32 성공 / obs#95 약함 / **obs#85·#123 기각**에서 역산 — 2승 2패다)
1. **독립 출처 2~3개가 같은 방향** — obs#32가 HIGH인 이유. ⭐ obs#123은 확증쌍을 `player_duties`
   **영문 서술에서만** 뽑아 이 요건을 **선정 단계에서** 만족시켰다(움직임 자료 미참조).
2. **교란 통제** — obs#95: LB↔RB 원시 **+0.687** → 경기별 평균을 뺀 잔차 **−0.168**. 원시 상관 금지.
3. ⭐ **대조군과 사전 성공 기준을 먼저 정한다** — obs#85·#123이 기각될 수 있었던 이유. 기준이 없으면
   그 추측이 정답처럼 출력된다. ⭐ **검정력도 미리 계산할 것** — obs#123은 n이 필요치의 1/2~1/3이라
   효과 유무를 애초에 판정할 수 없었다(사후에야 알았다).
4. **n · p · 다중비교 상태를 `confidence`에 명시** — 없으면 하드 제약으로 못 쓴다(obs#95가 소프트인 이유).

**📍 규약은 docs로 이관됐다 (2026-08-06)** — HANDOFF는 "지금 무슨 상태인가"만 담고, 재사용 규칙은
아래 문서가 정본이다. 값이 충돌하면 **docs가 이긴다**.

| 필요한 것 | 문서 |
|---|---|
| 슬롯 x 정본표 · **회귀 게이트 11항** · 측정 기준 7항 · 별도 구현 허용 조건 | [docs/20 「슬롯 x 정본표」](docs/20-fc-game-system.md) |
| FC26 대상 시스템 · EA 피치노트(비결정성·숙련·개명) | [docs/20 「EA 공식 피치노트」](docs/20-fc-game-system.md) |
| SofaScore 수집 절차 · **수집 함정 7종** · 좌표/그리드 규약 | [docs/30 「수집 함정」](docs/30-data-rules.md) |
| `git add -A` 금지 · sync/dump 순서 · 툴 편집 사고 사례 | [docs/40 「git 규칙」](docs/40-pipeline.md) |

특히 자주 잊는 3개만 여기 남긴다:
- ⛔ **`git add -A` 금지** — `.claude/settings.json`(Figma PAT)이 있어 푸시가 차단된다. 명시 스테이징만.
  ⚠️ 이 규칙은 **핸드오프 스킬의 wip 보존 절차보다 우선**한다.
- ⭐ **적합값 Δ 0.02~0.05는 EA가 공개한 노이즈 구간**(obs#91) — 그 차이로 인선을 가르지 않는다.
- ⭐ **결손과 0은 다르다**(obs#132) — SofaScore는 0이면 키를 생략한다. `v_event_profile`의 `n`은
  지표별 표본 수가 **아니다**.

**⚠️ 국면 축 상태 (obs#96 → obs#101 종결)**
- 공격 칸은 argmax 7/8 불변·|Δ|≤.049라 **전 국면 평균을 계속 써도 결론이 안 바뀐다**.
- ⚠️ 단 **rest-defense 칸은 저점유가 옳은 기준**(카마라 .629 / .692 / **.759**) — 고점유로 재면
  목적에 부합할수록 깎인다. 축 라벨에 `poss-hi`/`poss-lo`를 병기할 것.
- ⚠️ **경기별 그리드는 (선수, 경기) 단위라 포지션이 섞인다** — 슬롯별 비교 전에 **`lineup_pos` 필터**가
  반드시 선행돼야 한다(obs#96 ②의 자기 정정 사례. `pos_class`는 RB와 RAM을 못 가른다 — docs/30 ④).
