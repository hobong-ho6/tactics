# HANDOFF

<!--
════════════════════════════════════════════════════════════
이 파일은 세션 간 핸드오프 문서입니다. (사람 + Claude 공용)

■ Claude 사용 규칙
1. [세션 시작] 이 파일을 가장 먼저 읽고, "현재 상태"와
   "다음 할 일"을 확인한 뒤 작업을 시작한다.
   시작 시 사용자에게 한 줄로 브리핑한다.
   예) "지난 세션: X 완료. 오늘 예정: Y. 시작할까요?"
2. [세션 종료] 사용자가 "핸드오프 갱신" / "세션 정리" /
   "handoff"라고 하면 이 파일을 아래 규칙으로 갱신한다.
   - "현재 상태" 블록을 새로 작성 (덮어쓰기)
   - "다음 할 일"에서 완료 항목은 [x] 처리 후
     "최근 세션 기록"의 완료 목록으로 이동
   - 새로 발견된 할 일을 "다음 할 일"에 우선순위와 함께 추가
   - "최근 세션 기록" 맨 위에 오늘 세션 엔트리 추가
   - "최근 세션 기록"은 최대 5개 유지. 넘치면 가장 오래된
     엔트리를 "아카이브 요약"에 1~2줄로 압축해 병합
3. [작성 원칙]
   - 다음 세션의 Claude가 이 파일만 읽고 즉시 작업을
     이어갈 수 있을 만큼 구체적으로 쓴다.
     (파일 경로, 커밋 해시, 이슈 번호, 명령어 포함)
   - "왜 그렇게 결정했는지"를 결과보다 우선해서 남긴다.
   - 추측과 사실을 구분한다. 미확인 사항은 (미확인) 표기.
   - 전체 파일이 300줄을 넘지 않게 유지한다.
════════════════════════════════════════════════════════════
-->

## 프로젝트 정보

- **프로젝트명**: Aston Villa → FC 전술 구현 (2026-07-30부터 **3팀 체제**)
- **한 줄 설명**: 실제 감독의 전술을 실측 데이터로 분해해 FC26 인게임 전술(포메이션·역할·포커스)로 재현한다. 대상: **에메리(빌라) / 알론소(첼시) / 이라올라(리버풀)**
- **주요 경로/저장소**: `/Users/user/Documents/tactics` (git, main)
  - DB `db/tactics.db` = single source of truth / `site/data/`·툴 = DB 파생
  - 문서 맵은 `CLAUDE.md` 참조. 신규: `docs/11-alonso-tactics.md`, `docs/12-iraola-tactics.md`
- **관련 링크**: 스케줄 작업 `villa-transfer-watch` (매일 09/21시)

### 프로젝트 스킬 (`.claude/skills/`)

| 스킬 | 언제 쓰나 |
|---|---|
| `transfer-watch` | 3팀 이적 루머 정기 감시. 스케줄(09/21시) 또는 `/transfer-watch` |
| `match-watch` | 경기 수집 — 실측·팀스탯·경기 리포트·게임 구현 판정 |
| `player-collect` | ⭐ **한 선수의 모든 데이터 축(14축) 전수 수집.** "○○ 데이터 수집"이면 이것 |
| `hadoff` | 세션 인수인계 — HANDOFF.md 갱신·Git 동기화 |

---

## 현재 상태

> 마지막 갱신: **2026-08-20** · 세션 **#32** · 작업 PC `AL02359162.local` · 브랜치 `main` ·
> 최신 커밋 **`58eeddb`**(데이터 최신 `3e39ef0`) · observations **257행**(최신 obs#257) ·
> match_reports **20행**(완료 5 · 초안 15). DB는 v2(`db/tactics.db`), 3팀 체제 유지.
> ⚠️ **작업 PC가 바뀌었다** — #31은 `AD03230205ui-iMac.local`이었다.
> ⚠️ HANDOFF가 #31(`e1921c3`)에서 멈춘 뒤 **20커밋**이 쌓였다 — 08-19 저녁의 다른 세션 작업 18건과
> 08-20 이적 감시 2회차다. 이 세션이 직접 한 것은 **08-20 14시 회차 + 이 핸드오프**이고,
> 나머지는 **`git log`를 근거로 함께 반영**했다(스킬 규칙).
>
> **⭐⭐⭐ 이번 세션의 최대 정정 — SofaScore 차단은 08-19에 해제됐다(obs#256 · `d7fcef9`).**
> 3팀 프리시즌 전수 재수집으로 **map25 보유 경기가 2 → 10**으로 늘었다. 그런데 **08-20 이적 감시
> 두 회차가 모두 「403이 7~8일째」로 잘못 보고**했다 — 원인은 `.claude/skills/transfer-watch/SKILL.md`의
> 「⛔⛔ [08-14 확인] **현재** 전면 차단」 블록을 현재 상태로 읽은 것이고, 그 사이 다른 세션이 복구를
> 커밋해 뒀다. ⇒ **런북의 「현재」는 작성 시점의 현재다. DB·커밋 로그가 그보다 최신일 수 있다.**
> SKILL.md를 이력으로 강등 표기했고 리포트에 정정 절을 달았다.
> ⚠️ **obs#256 이월 결함 2건**: **이중 기록 21쌍**(같은 출전이 SofaScore·FotMob 두 소스로 들어가
> **행 수 기반 질의가 이중 계상**한다 — 예: CHE 유벤투스 35행 = SofaScore 14 + FotMob 21. 삭제 전
> **두 소스의 평점 불일치를 어느 쪽으로 채택할지 결정이 선행**돼야 한다) · AVL `2026-08-01 Indonesia
> All Stars` 중복 matches 행(event_id NULL·0행).
>
> **⭐⭐ 첼시·리버풀 전술 정본을 26/27 실측으로 이동**(08-19 다른 세션 · obs#254·#255·#257).
> 첼시 인게임 포메이션이 **5-4-1 → 3-4-3 → 3-4-2-1**로 두 번 전환됐고(**사용자 지시**), 알론소·이라올라
> **26/27 실측 그리드로 교체 + 슬롯 정본 11행씩**(`slot_canon_roles`)이 들어갔다. LIV는 **ST 슬롯 신설**.
> ⭐ obs#255: **포메이션은 총합 적합도로 판정할 수 없다**(격차 0.022 = 노이즈 이내) · obs#257: 레버쿠젠→
> 첼시 역할 매칭을 알론소 실측으로 검증(확증 7 · 정정 3).
>
> **⭐ 화면 3건**(08-19 다른 세션): `pos_only` 신설로 **좌우 쌍 슬롯 한쪽 제한**(→ **좌우 경고 0건**) ·
> `(합류확정)` 라벨 접미 제거 · 이적 페이지 **접이식 근거 행 + DEAD 포함 토글**.
>
> **⭐⭐ 수지 원장을 상각 기준으로 개편**(08-20 09시 회차 · 사용자 요청 · 상세는 그날 리포트).
> `transfer_ledger.contract_years` 신설 + 영입/매각 분리 + **연 상각액 = 이적료 ÷ 계약연수**, ⭐ **PL 5년
> 상각 상한**(계약 7~8년이어도 **분모는 5** — 첼시 장기계약 분산 기법을 무력화한 그 규정).
> 부담 **AVL £6.0m(계약 확인 1/3건) · CHE £54.2m · LIV £18.9m**.
> ⛔ **「가용 금액」 절대치는 산출 불가** — 26/27부터 PSR이 **SCR**(임금+상각+수수료 ÷ 축구수익+매각
> 순손익 ≤ 85%)로 대체됐는데 **DB에 축구수익·임금 총액이 없다.** ⚠️ 그리고 **임대 영입의 비용이 구조적으로
> 0으로 표시된다**(아라우호 임금 €11m·완비사카 임대는 SCR 분자인데 상각표는 0) = **과소 표시**.
>
> **이적(08-20 14시 회차 기준)**: ⭐ **톰모리 신규 MEDIUM-HIGH**(AVL RCB — 사용자 요청 「콘사 대체 전수
> 수집」의 산출물. Bianchin/Gazzetta 1티어 + talkSPORT 2티어 · 계약 2027 만료로 €10~15m · fuori rosa 등
> 행동 신호 4건) · **콘사 이적료 정본 £51m + 애드온**(1티어 3:1 — 양쪽 다 「plus add-ons」라 총액/고정분
> 해석은 오류였다) · **완비사카 구조 = 의무매입 확정**(Romano 본계정 · 단 **£18m이 4회차 연속 미확인**이라
> `squad_entries` 승격 보류) · **무드리크 dest 첫 특정**(PL 3개 클럽/스트라스부르 · STALE 09-01로) ·
> **바디아실 €3m+€27m diritto**(보너스 삭제 수렴) · **엔드릭 AS 축 종결**(DEAD 유지) ·
> **음바예·바르콜라 MEDIUM-HIGH 유지**(Pearce 귀속 오류 정정) · **아라우호는 완전이적이 아니다**
> (임대+€55m 옵션·임금 100% 리버풀).
>
> **다음 우선순위**:
> 1. ⏰⏰ **08-21(금) 18:00 FC27 1단계 수집** — 3팀 전 스쿼드(docs/21 2단계 계획). ⚠️ sofifa 로그인 확인 선행.
>    **09-10판이 정본**이고 08-21판은 PlayStyles 없음·이적창 미반영이다.
> 2. ⏰⏰ **CONFIRMED 전환 4건** — 콘사(08-20 메디컬·등록 마감) · 존스(밀라노 서명 · 인터 개막 08-22) ·
>    바디아실(08-20 서명) · 완비사카(개막 08-23 전 등록). ⚠️ 완비사카는 **£18m 확정이 승격의 선결**이다.
> 3. ⭐ **톰모리·토신 실측** — **차단이 풀렸으므로 지금 가능하다**(PENDING 사유가 「소스 차단」에서
>    「미수행」으로 바뀌었다). ⚠️ 클럽 표본은 **25/26 시즌분**을 써야 한다(리그 개막 08-23·08-24).
> 4. ⭐ **개막 3경기** — LIV·AVL **08-23**, CHE **08-24**. 공식전 표본이 여기서 처음 쌓인다(docs/30 §4 Δ 산출).
> 5. **obs#256 이월 결함 정리**(이중 기록 21쌍 · Indonesia All Stars 중복) · **AVL 원장 감사 미실시** ·
>    **잭슨 커널 앵커 표본의 팀 축 확인**(다수 매체가 25/26을 바이에른 임대로 적는다 — 값 .724는 안정적이나
>    표본 팀 표기가 틀렸다면 불변규칙 7 위반).
## 다음 할 일

> ⏰ **첫 작업: 08-21 18:00 FC27 1단계 수집**(위 「현재 상태」 1번). 그다음 **CONFIRMED 전환 4건**과
> **톰모리·토신 실측**(⭐ SofaScore가 풀렸으므로 이제 소스 결손이 아니다). **08-23 리그 개막**이 곧이다.
> ⚠️ **톰모리·토신 실측은 2026-08-20에 완료됐다**(각 34·18경기) — 위 문장은 #32 시점 기준이라 낡았다.

- [ ] **⏰⏰ P0 · R2(08-23 ATM vs 비야레알) 수집 시 「그리말도 실측이 채워지는지」 확인**
  (2026-08-21 **사용자 지시**로 신설 · 세션 #33)
  **무엇을**: `squad_entries` regime_id=4의 **그리말도(Alejandro Grimaldo, LB)** 행이 현재
  `map25` = **0 25칸 자리표시** · `fit_role/focus/sim` = **NULL** · `sort_order`=**93**이다.
  R2 수집 후 그가 **실측 그리드를 얻는지**를 확인하고, 얻으면 자리표시를 교체하라.
  **왜 중요한가**: `a8b25eb`에서 ATM `sort_order`를 「실측 보유 → 자리표시」 순으로 부여했다.
  그 결과 **깊이 차트가 「실제 서열」이 아니라 「그릴 수 있는 그리드가 있는 순서」**가 됐고,
  **그리말도(€40m 레버쿠젠 영입, 실질 LB 1옵션)가 다니 마르티네스(0.968) 뒤로 밀려 있다.**
  같은 이유로 RB도 R1 실제 선발 마르코스 요렌테(0.891)가 호르헤 도밍게스(0.917) 뒤다.
  ⇒ **그리말도 그리드가 채워지면 이 왜곡이 자동 해소된다.** 안 채워지면 캐비앗이 유지된다.
  **어떻게**: ⚠️ **ATM 그리드는 SofaScore가 아니라 WhoScored `matchCentreData`(Opta 원천)** 다
  (기존 31행의 `source` 필드가 정본 · R1 matchId **1993898** · `isTouch` 이벤트만 사용 ·
  중괄호 균형 스캔으로 추출 · 좌표는 Opta 규약이라 소파와 동일해 변환 불필요).
  ⛔ 2026-08-21 시점 **SofaScore는 전 엔드포인트 403**이라 그 경로로는 대체 불가였다 — 상태 재확인 필요.
  **조건**: ⑴ 그가 실제로 출전해야 한다(로테이션·부상 시 자리표시 유지)
  ⑵ 의미 있는 그리드가 나올 만큼 터치가 있어야 한다. ⚠️ 단 **§3의 45분+·터치15+ 기준을
  `squad_entries` 제외 사유로 쓰지 말 것** — `736fbb9`가 정확히 그 오류를 정정했다(표본 얕음은 캐비앗이지 제외 사유가 아니다).
  ⭐⭐ **연쇄 작업(이게 더 크다)**: `reports/match-watch/2026-08-19-atm-malaga.md` §4가
  **「R2 이후 2경기 합의값으로 슬롯 기하를 재산출한다」**(CHE 3-4-2-1 선례)고 예고했다.
  ⇒ R2 수집은 **ATM `slots`의 x값을 바꾸고**, 그러면 **ATM 전 행의 `fit_sim`을 재산출**해야 하고,
  `sort_order`도 **재도출 대상**이다. 그리말도 한 명 문제가 아니라 **ATM 실측 축 전체의 1차 갱신**이다.
  ⚠️ 현 ATM 자리표시는 **31행 중 15행**이다(기존 7 = `736fbb9` 미출전 벤치 / 신규 8 = `382c03f`
  R1 명단 밖 1군). R2 출전자 전원이 교체 대상이다.
  ⚠️ 잠정성이 실재하는 슬롯은 **LM 24 · RM 76 · LCM 36 · RCM 64**다 — **ST 50·GK 50은 좌우 대칭
  중앙값이라 재산출에서 움직이지 않는다**(ATM 잭슨 행 `fit_sim` 0.724 유지 근거, `0c039c3` 참조).

- [ ] **P1 · 프리시즌 리포트 15건의 처분 + 서사 수집 미수행분** (2026-08-16 신설 · 두 항목 병합) —
  match_reports 20행 중 **15행이 draft**이고 사유는 전부 「히트맵이 없어 단일 경기 fit을 못 낸다」는 **소스 결손**이다
  (자료가 얕아서가 아니다). ⇒ 현 상태로 complete로 올릴 수 있는 초안은 **0건**이다.
  선택지: ⓐ 현행 유지(친선은 영구 draft) ⓑ 히트맵 없는 경기용 **완료 기준 별도 정의** — **사용자 판단 사항.**
  ⚠️ 함께: 그 8개 리포트의 §8(영상·전술글·기사·감독 발언)이 **4종 전부 미수행**이다. 사유는
  「친선 커버리지 부재 + 회차 범위 밖」이고, **실측이 없는 경기일수록 서사 가치가 크므로 우선순위가 낮지 않다.**

- [ ] **P1 · 국면 축 확장** (obs#101 → obs#122 후속) — possession 행이 늘어(로저스 42·콘사 38·디뉴 26·
  틸레만스 24) obs#101이 8쌍에서 멈춘 것을 다시 돌릴 수 있다. ⚠️ 게상 12/26·일링 0/15은 결손(니스·임대분).
  ⚠️ obs#101 ③(**rest-defense 칸은 저점유가 옳은 기준**)이 확장 표본에서도 성립하는지 재확인할 것.
  (**R축은 기각됨**(obs#123) — 재개 조건은 표본 2~3배, 쌍당 n≥60~100, `docs/60` 재사용.)

- [ ] **⭐ P1 · 첼시 피벗 처방 결정** (obs#116, **사용자 판단 사항**) — 기능축 실측이 확정한 배정
  (카이세도=볼위너 · 엔소=창조)과 현 처방(두 피벗 모두 `dm_dlp`)이 어긋난다. 선택지 2개:
  ⓐ **현행 유지** — 움직임 적합 +.126을 지키고 기능 정합을 포기(현 평균 .799)
  ⓑ **소스 배정 채택** — 엔소 LDM `dm_dlp`/Roaming .698 + 카이세도 RDM `dm_holding`/Roaming .647
     (평균 .673, −.126). 카이세도 개인 지불액은 **.246~.294**로 카마라의 .147보다 크다.
  ⚠️ 표본이 **알론소 체제가 아니다**(25/26 마레스카·로즈니어) — 8/24 풀럼 후 재판정 가능.

- [ ] **P2 · 리버풀 기능축 최초 수집** (obs#116의 첼시 방식 그대로) — `appearances` team=LIV **0행** ·
  `player_match_positions` team=LIV **0행**이다. obs#120이 12명을 재산출했으나 **피벗 3인이 전부
  `dm_dlp`/Roaming**으로 나와 **기능 분화를 가릴 수단이 없다**. 프리셋보다 이것이 먼저다.
  경로: `/api/v1/event/{eid}/player/{pid}/statistics` → `player_match_positions.stats_json`.

- [ ] **P2 · SSOT 4~7단계** — 4단계 `CMP_SLOTS`+`SQUAD_SLOTS` 6건+`PLAYER_BEST` → 5~7단계
  `SLOT_EMERY_ROLE`+`FUNC_AXIS` · `FORMATIONS` · `SQ_STATS`. ⚠️ **4단계 선행 2건**(상세는 obs#115 ④):
  ⓐ **실제 드리프트** — `CMP_SLOTS` 맥긴 CAM 그리드가 DB 어느 값과도 다른 제3의 값이다(orphan 그리드 10건 처분 포함)
  ⓑ **별칭 축** — 축약↔풀네임 14쌍은 규칙 도출 불가 → `player_fc_stats.tool_label`(NULL 허용)이 최소 침습안.
  ⚠️ **이관 금지**(obs#105 ⑥): `ANIM_*` · `COLL_CAL` · `FORMATION_DESC` · 초기추정 프리셋 · `TEAMS`.
  ⚠️ **별 단계로 분리**: 상태 접미 소유자 통일 · `UNIQUE(game_version, sofifa_id)`.

- [ ] **P2 · 애니메이션 궤적** (obs#117 ⑵ 잔여) — 마커는 커널 파생이나 **화살표(`ANIM_PHASES.ar`)·경로(`AN_PATTERN`)는
  옛 상수**라 어긋날 수 있다. 대안은 화살표를 **파생 좌표 사이 직선**으로 대체(정합성 ↑, 표현력 ↓) — **사용자 판단 필요.**

- [ ] **P3 · 잔여 3건** — ⓐ **세트피스 득점 비중 축**(obs#112·#113 결손): DB에 세트피스 전용 컬럼이 없어
  obs#49(알론소 40%)·obs#74(이라올라 33%)와 **동축 비교 불가**. 경로는 `/event/{eid}/incidents`(브라우저 필수)
  또는 `appearances` 자유서술 파싱(⚠️ 커버리지 불균등 → "비중" 금지, "확인된 골 목록"까지만).
  ⓑ **리버풀 프리셋**(개막 후 · **기능축 수집이 선행**) ⓒ **소스 인프라 미해소**는 위 이적창 블록에 통합했다.


### 차단 요소 / 대기 중

- **공식전**: AVL은 **08-12 슈퍼컵 1경기**(PSG 2-1 AVL) 45분+ 11명 실측·심화 서사 완료(obs#194·196),
  마조 유효 표본 **4/6**. ⏰ **개막 임박** — AVL **8/23 브라이턴 원정** · LIV **8/23 뉴캐슬 원정** · CHE **8/24 풀럼**.
  CHE·LIV는 아직 **공식전 0경기**이고 여기서 처음 표본이 쌓인다(docs/30 §4 Δ 산출의 선결 조건).
- ⚠️ **커버리지 결손은 「친선이냐」가 아니라 「그 대회를 커버하느냐」로 갈린다**(obs#133이 obs#126을 정정).
  양방향 반증: **친선인데 있음**(빌라 vs 바이에른 08-07, 마조 히트맵 28pt) / **공식전인데 없음**(마조 U17
  유럽선수권 예선 3경기 전건 0). ⇒ **사전 판별법이 없으므로 프리시즌이라고 미리 포기하지 말고 경기별로
  시도**하는 것이 유일한 방법이다. 커버돼도 표본 기준(6경기 45분+)은 별개다(마조 4/6).
  ⭐ **2026-08-19에 3팀 프리시즌 전수 재수집이 성공해 map25가 2→10이 됐다**(obs#256) — 「프리시즌 8경기는
  양쪽 소스 모두 결손」이라던 08-16 판정은 **차단 기간의 관측이었다.**
- **EA가 수치를 공개하지 않는다** — Role+/++ 정량 효과, OOP 페널티 크기, 포커스 위치 델타,
  역할별 능력치 가중치. **AcceleRATE는 분류 공식만 공개**(효과 크기는 여전히 비공개).
- **국면 분리는 보유 선수만** — 영입 4명은 빌라 경기 0이라 구조적으로 불가.
- ⚠️ **`tactic_code`가 16행 전부 비어 있다** — 어느 팀도 실제 인게임 슬라이더/코드가 DB에 없다.
  처방은 "역할·포커스·라인"까지만 내려간다. `ingame_checks`도 **0행**(게임 내 검증 폐기, docs/50).
- ⚠️ **적합도가 전 국면 평균 기준**이고 obs#101은 그 어긋남을 **빌라 표본으로만** 측정했다 —
  첼시·리버풀에 같은 노이즈 크기가 성립한다는 근거는 없다.

---

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-08-20 | **런북의 「현재」를 현재 상태로 읽지 않는다 — SKILL.md의 상태 서술은 날짜를 붙여 이력으로 강등한다** (`3e39ef0` 후속) | `transfer-watch/SKILL.md`가 「⛔⛔ [08-14 확인] SofaScore가 **현재** 전면 차단」이라고 적어 뒀고, **08-19에 다른 세션이 복구를 커밋**(obs#256, map25 2→10)했는데 **08-20 두 회차가 모두 「403이 8일째」로 오보**했다. 에이전트 보고와 교차되자 확정으로 굳혔다 — 그러나 에이전트도 같은 런북을 읽는다(순환 확증). ⇒ **소스 접근성·차단 여부 같은 「상태」는 런북이 아니라 DB(obs)·커밋 로그가 정본**이고, 런북에 남길 때는 유효 기간을 명기한다. 실질 피해: 톰모리·토신을 **실측 가능한데도 PENDING으로 남기고** 불필요한 「대체 경로 전환 회차」를 우선순위에 올렸다 |
| 2026-08-20 | **이적료가 「A + 애드온」 ↔ 「B + 애드온」으로 갈리면 총액/고정분 해석으로 화해시키지 않는다 — 다수결로 정본을 정하고 충돌을 기록한다** (콘사 £51m, `3e39ef0`) | Romano 「£50m plus add-ons」 ↔ Ornstein 「£51m plus add-ons」를 보고 「£50m 고정 + 애드온 = £51m 총액」으로 읽으려 했으나 **양쪽 다 「plus add-ons」라 그 해석은 성립하지 않는다.** 실체는 **고정분 수치의 1티어 간 £1m 불일치**다. Ornstein(담당 기자)·Sky·ESPN **3:1**로 £51m 채택. ⚠️ 애드온 조건은 **1티어 어느 쪽도 분해하지 않았다** ⇒ CONFIRMED 시 원장에 **보장분/애드온 분리** 필수(산투스 £50m = £48m+£2m+셀온 10%가 그 선례) |
| 2026-08-20 | **`transfer_outgoing` 등재 기준의 비대칭을 인지한다 — 기준은 목적지 유형이 아니라 「`players`에 있는지」다** (사용자 판단 대기) | 원장 감사로 **Nallo→HJK(구단 공식 07-24)·로버트슨→토트넘(스퍼스 공식 06-05)** 두 CONFIRMED 결손을 찾았으나 둘 다 `players` 미등재라 2026-08-13 방침에 걸려 보류했다. ⚠️ 그런데 **코나테→레알(자유이적)은 등재돼 있다** — 즉 「자유이적이라 뺀다」가 아니라 **분석 대상 스쿼드인지**가 실제 기준이고, 그 사실이 문서화돼 있지 않아 감사 때마다 재판단이 필요하다 |
| 2026-08-19 | **선수 확인은 이름 + 소속팀·국적·포지션 3요소로 한다** (사용자 지시 · docs/30 정본 · obs#250, `6366641`) | 이름만으로는 사람이 특정되지 않는다는 것이 이 세션에 3번 실증됐다 — `Alysson`(빌라·**브라질**·**RM**) ↔ `Alisson Becker`(리버풀·**브라질**·**GK**)는 **국적까지 같아 소속·포지션만이 구분자**다 · `Mamadou Sarr`(첼시·CB) ↔ `Pape Matar Sarr`(토트넘·CM)는 성·국적이 같다 · `Miloš Kerkez`↔`Milos Kerkez` 표기 차이로 **매칭이 조용히 깨졌다**. `player_game_stats`가 `UNIQUE(game_version, roster_date, name_kr)`이라 충돌 시 **행이 사라진다**. 규칙 4개: name_kr 유일성(충돌 시 더 긴 표기로 분리 — `알리송 베케르` 선례) · id 조인 · 국적/풀네임 기록 · **브리핑·리포트에도 3요소 표기**(08-16·08-18 브리핑 오류가 이 누락에서 나왔다) |
| 2026-08-19 | **FC27 수집은 2단계로 나눈다 — 09-10판이 정본이다** (obs#249·#251, `2192906`·`529c472`) | EA 공식 스케줄이 **08-21 = 전체 DB이지만 PlayStyles 없음 + 이적창 미반영**, **09-10 = PlayStyles 포함 + 이적 마감 후 갱신**으로 갈라 놨다. 우리가 실제로 쓰는 `playstyles`와 스즈키·완비사카·루헤리·네델코비치의 새 소속은 **09-10에야** 들어온다. ⛔ 그리고 **델타는 기준 시점을 밝히지 않으면 부호가 뒤집힌다** — fut.gg Δ는 FC26 **출시판(2025-09)** 기준이고 우리는 **시즌 말 라이브판**이다(왓킨스 −1↔**+1** · 반다이크 −2↔**0** · 만잠비 +13↔**+3**). ⚠️ 능력치 변화는 **적합도·처방을 바꾸지 않는다**(커널은 히트맵 기반, obs#121) |
| 2026-08-16 | **GK 슬롯은 fit_sim 대신 평균 위치를 표시한다** (obs#220, `8f8e4dc`) | 측정된 GK 5명의 적합이 **.961~.975(폭 0.014)** 에 전원 수렴하고 **그리드 상호 코사인이 모두 ≥0.986**이다(스즈키~마르티네스 0.995). GK 히트맵은 골문 앞 한 칸이 최대값을 독점해 5×5 해상도에서 서로 구분되지 않는다 — 숫자를 노출하면 **없는 우열**을 읽게 된다. 변별은 평균 위치에서 나온다(tool_y 폭 **2.49**, 팀 라인 높이와 방향 일치). ⛔ 이 결정은 "적합을 GK에 쓰지 말라"가 아니라 **"적합 차이를 우열 근거로 쓰지 말라"** 다 — 슬롯 배정(`gk_goalkeeper` 수렴)은 정상 작동한다. squad·compare의 GK **정렬도 평점순으로** 바꿨다(★가 .001 차이로 붙던 문제) |
| 2026-08-16 | **히트맵 없는 친선은 `complete`로 올리지 않는다 — 영구 draft가 기본값** (`557d8c5`) | 프리시즌 8경기를 적재하며 확인: 이 경기들은 SofaScore·FotMob **양쪽 모두** 선수 스탯·평점·히트맵·팀 스탯이 없다(양성 대조군으로 프로브 오류 배제). 히트맵이 없으면 **단일 경기 fit을 산출할 수 없고**, 런북은 그 상태에서 역할·포커스를 채우는 것을 **과대 표시**로 금지한다. ⇒ 자료를 더 파도 해결되지 않는 **소스 결손**이라 draft가 정확한 상태 표기다. 기존 초안 7건도 같은 이유였다(자료가 얕아서가 아니다). ⚠️ 결손은 0이 아니라 **NULL**로 적재한다 |
| 2026-08-16 | **`last_news_date`에 재확인 날짜를 넣은 행을 정정한다 — 베일리 08-14 → 08-05** | §4-3이 명시적으로 금지한 오류가 실제로 들어가 있었다. rationale의 `[2026-08-14] …확인됐다`는 **우리 분석 날짜**이고 인용된 기사는 08-04~08-05다. 이 값을 두면 **그 행은 영원히 정체되지 않는다**(STALE이 14일 기준이므로). ⇒ 매 회차 이 컬럼은 **보도 자체의 날짜**인지 재검증한다 |
| 2026-08-16 | **서브에이전트 브리핑의 선수·id·소속은 §1-0 조회 출력에서 그대로 복사한다** | 3팀 스캔 에이전트가 각각 "DB 정정 필요"를 올렸는데 **셋 다 DB가 맞고 브리핑이 틀렸다**(루헤리를 아탈란타로, LIV `id 53`을 엘리엇으로, CHE `id 82`를 펠릭스로 적었다 — 실제로는 아틀레티코·각포·무드리크). §1-0 DB 선조회는 수행했으나 **브리핑을 조회 결과가 아니라 기억으로 썼다.** 오류를 넣으면 에이전트가 그 확인에 토큰을 쓰고 **존재하지 않는 정정을 권고**한다 |
| 2026-08-13 | **양 측면을 쓰는 자원은 좌우를 나눠 재고, 혼합값을 쓰지 않는다** (obs#156 규약을 영입 후보에도 확장, `d72897d`) | 무뇨스를 34경기 **혼합**으로 재면 LM **.615**인데, 좌 23·우 11이 섞여 그리드가 중앙으로 뭉개진 결과다. 나누면 **좌 .729 / 우 .772로 양쪽 다 올라간다** — 혼합값은 두 값의 중간이 아니라 **둘 다 아닌 값**이다. 자케도 같은 구조(혼합 RCB .838 → 우측만 **.903**). ⚠️ 반대로 **완비사카는 25/25 전 경기 우측 우세(우2컬럼 91.7%)** 라 분리가 불필요했다 — 분리 여부는 미리 정하는 게 아니라 **경기별 좌우 비중을 세어 판정**한다 |
| 2026-08-13 | **`transfer_outgoing`은 `players`에 있는 선수만 등재한다** — 첼시 신규 유출 후보 5명(토신·델랍·디사시·기우·사르) 미등재 | 다섯 명 전원이 1티어(Sky·The Athletic·BBC) 근거로 MEDIUM-HIGH 요건을 **충족**했으나 `transfer_outgoing.player_id`는 `players` FK가 필수이고 **5명 전원이 `players`에 없다** = 전술 분석 대상 스쿼드가 아니다. 이들이 나가도 `squad_entries`·XI·슬롯이 **바뀌지 않는다**. 행을 만들려고 `players`를 5개 늘리는 것은 추적 가치 대비 **스키마 오염**이라 판단하고 근거는 리포트에만 보존했다. ⚠️ 단 **센터백 감축의 누적 효과는 별도 추적 대상**(찰로바 CONFIRMED + 바디아실 HIGH + 이 3명) |
| 2026-08-13 | **`handoff-hooks/`·`".claude(구독)/"` 는 삭제하지 않고 `.gitignore`에 넣는다** (사용자 확정) | 세션 #14가 "다른 세션/PC에서 유입 추정"으로 판단해 untracked로 남겨뒀던 미결 항목. **git에 없는 파일이라 삭제하면 복구 불가**이고, 유입 경위가 확정되지 않았다. 추적에서만 제외하면 매 세션 `git status` 노이즈가 사라지면서 파일은 보존된다 — 되돌리기 쉬운 쪽을 택했다 |
| 2026-08-10 | **웹 검색보다 DB 선조회를 먼저 한다** — SKILL.md **§1-0 신설** (`731d92f`) | "프리시즌 활약 유스"를 웹으로만 훑다 헤밍스·린치를 통째로 놓쳤는데 **obs#126 ③에 5명이 이름으로 적혀 있었다**(Lynch 어시 → Madjo 골까지). 검색 설계 문제인 줄 알았으나 실제 원인은 **파이프라인에 DB 선조회 단계가 없었던 것**. ⭐ 함께 넣은 규칙: **obs는 `claim`과 `confidence`를 같이 읽어라** — obs#126은 결론을 "친선이라 실측 없음"으로 썼지만 `confidence`에 진짜 원인("상대가 올스타 선발팀")이 이미 있었다. **근거 칸이 결론보다 정확할 수 있다** |
| 2026-08-10 | **obs#126은 폐기가 아니라 범위 축소** — obs#133 신설 (`b790539`) | 관측(11명 전건 null)은 옳고 과한 것은 **"프리시즌 친선의 구조적 한계로 기록한다"** 한 문장이다. 양방향 반증 확보: **친선인데 있음**(빌라 vs 바이에른, 히트맵 28pt) / **공식전인데 없음**(U17 유럽선수권 예선 90·36·90분, 히트맵 0). ⇒ 결정 변수는 친선 여부가 아니라 **SofaScore 대회 커버리지**이며, **사전 판별법이 없어 수집을 시도해 보는 것이 유일한 방법**이다. HANDOFF 차단요소도 "경기별로 시도하라"로 교체 |
| 2026-08-06 | **불변규칙 8 신설 — 히트맵은 시각화해서 보여준다** (`8707e44`) | `map25` 5×5는 공간 정보(열0=좌측 터치라인·행0=공격)인데 숫자열로는 좌우 편향 비교가 사실상 불가능하다. 실증: 가르나초 좌2컬럼 **90%** vs 로저스 **68%** — 이 대비는 그림에서만 즉시 보인다. 수치 결론은 위젯 **밖**(응답 본문)에 쓴다 |
| 2026-08-06 | **커널 적합도의 별도 구현을 조건부 허용** — 회귀 게이트 통과가 조건 | 컨텍스트 노트 「측정 기준 #2」는 "브라우저에서 툴 함수 직접 호출, 별도 구현 금지"였다(MAPS 파싱 함정 때문). 08-06에 파이썬으로 `decodeMap`/`placedMap`/`cmpCos`를 재현하고 **역할 수 37 + 회귀값 7개**를 통과시켜 브라우저 없이 산출했다. ⭐ 금지의 목적은 "브라우저 사용"이 아니라 **파싱 함정 방지**이므로, 게이트를 통과하면 목적이 충족된다. 게이트 없이 구현하는 것은 여전히 금지 |
| 2026-08-06 | **SKILL.md 회귀 기준값 정정** (하지무사 .923→**.821** · 은디아예 .883→**.825**) | 두 값이 obs#93·#94 커널 교체 **이전** 것이라 그대로 쓰면 **회귀 검증이 항상 실패**했다. 당시 6경기 Jackson .752 등 ST·CAM은 커널 교체 전후 불변이었다. ⚠️ Jackson 앵커는 2026-08-13 전수 28경기 정형화로 **.724**로 교체됐다. |
| 2026-08-05 | **`v_event_profile` 인용 시 지표별 표본 수를 반드시 확인한다** (obs#131·#132, `448fbc6`) | 뷰의 `n` 컬럼이 **지표별 실제 표본 수가 아니다**(카마라 인터셉트 3.0은 2경기 평균, 1.40이 9경기 값). 더 근본적으로 **SofaScore는 값이 0이면 키 자체를 생략**하므로 DB의 '결손'은 미수집이 아니라 **0**이었다 — 전수 재수집으로 인터셉트·태클 **1위가 카마라 → 오나나**로 바뀌었다 |
| 2026-08-05 | **카마라 `require(dm_holding)` 하향 보류** (obs#130, 사용자 지시를 실행 직전 중단) | 지시는 prefer 하향이었으나, obs#121이 이 제약의 존재 이유를 "카마라를 창조 역할에 배정한 솔버 사고"로 명시한 것을 실행 직전 확인했다 — 내리면 그 사고가 재발한다. 계열 내 포커스 교정(Ball-Winning .556 → **Defend .703**)이 현 근거로 정당화되는 최대치 |
| 2026-08-03 | **이적 감시에서 발행일 검증을 티어 판정보다 먼저 한다**(SKILL.md §2-0 신설) | 하루에 **10건**을 이 검사로 걸렀고, 그중 Sancho/Lammens는 **BBC Sport 본체 + Romano 원트윗**이라 **매체 티어로는 원리적으로 안 잡힌다**. 여름 창은 매년 같은 구단·같은 포지션 루머가 돌아 **정확히 1년 오차**가 가장 흔하다. 실패한 지표(URL 슬러그·검색 순서·피드 "Nh ago") 대신 **본문 나이 표기**와 **병기된 타 구단 상황**을 1순위로 삼는다 |
| 2026-08-03 | **Fotmob은 복구하되 "리드 소스"로만 쓴다**(티어 근거 금지) | 인앱 브라우저로 열자 CSR 테이블이 완전히 렌더링됐다 = 07-31 진단(구조적 불일치)이 옳았고 `WebFetch`로는 여전히 불가. 고유 가치는 **루머별 출처·날짜 라벨**이지만, `Watkins→Fenerbahce`를 "Sky Sports"로 표시해 놓고 실제 원출처는 터키 매체였다 — 게다가 행이 **선수 페이지로만 링크**돼 라벨을 검증할 수 없다. 수집 주체는 **메인 세션**(브라우저 pane이 세션당 하나라 서브에이전트와 충돌) |
| 2026-08-01 | **R축 미신설(기각)** — C6는 소프트 유지 | 사전 등록한 처분표 그대로. FDR 생존 **0/5** + 대조군 **C 계수 음수**(−0.435, 방향 반대). ⭐ 그러나 **방향은 4/5가 예측대로 음수**였고 실패 원인은 효과 부재가 아니라 **검정력**이다(판정에 \|r\|≥0.33~0.45 필요, 실측 0.13~0.24) → "보완이 없다"가 아니라 **"이 도구로는 세울 수 없다"**가 정확한 진술이고, 표본 2~3배면 재검정 가치가 있다 (obs#123) |
| 2026-08-02 | **GK·CAM·ST 슬롯의 적합값을 좌우 편향 근거로 쓰지 않는다** | 그 슬롯에서 선택되는 커널 변형이 **좌우 대칭**이라 그리드를 뒤집어도 값이 **소수 셋째 자리까지 동일**하다(.686→.686). 코사인이 원리적으로 방향을 못 본다 — 우위가 말하는 것은 "퍼진 분포"까지다 (obs#127) |
| 2026-07-31 | **측정 기준 재정립 14건** (압축 — 세션 #3, obs#82~#96) | **슬롯 x 정본 = 툴 `SQUAD_SLOTS`**(코사인이 x의 계단 함수) · docs/20 §B 라인 보상 규칙 폐기(obs#27과 모순) · 전역 충돌 임계값 → **슬롯쌍 상대 지수 CI**(쌍 중앙값이 140배 범위) · 26/27 프리셋 5종 폐기 → 단일 `projected:emery-2627` · **A축 기각**(두 독립 검증 실패 + EA "Opta AI 모델" 명시) · **B축은 점수 아닌 플래그**(이산 속성) · 뱅크 포커스 기본 Support · RM 뱅크는 캐시(알리송은 뱅크 미숙련) · **`placedMap` 전면 교체 — 변형은 시프트가 아니라 중앙 대칭 미러** · 회귀 게이트 값 교체 · **프리셋 H안 채택**(사용자 확정) · **C5 기능축 신설**(솔버가 카마라를 창조 역할에 배정한 사고) · **C6는 소프트**(obs#95 근거가 억제 1건뿐) · 고점유 전환 보류(→ obs#101에서 기각으로 종결) |
| 2026-07-30 | **3팀 체제 도입 결정 9건** (압축) | 팀 축 = 텍스트 `team` 컬럼 + FK 없는 `teams`(불변규칙 2 준수) · UNIQUE 충돌 5테이블만 재작성 · **STALE = DEAD 숨김(삭제 아님)** — 툴이 `/^DEAD/i`를 필터하므로 행을 지우면 `map25`·`fit_sim`을 잃는다 · 등급 강등도 숨김으로 통일 · STALE 판정은 **빌라 링크 보도일**(`last_news_date`) 기준이고 `CONFIRMED`/`OWNED` 제외 · **CCB 슬롯 신설**(백3 3인이 각자 자기 자리 최고치) · CCB를 `CMP_SLOTS`에 의도적 미추가(빌라 화면 누출 방지) · 스캔은 서브에이전트·판정은 메인 · **obs#28 빌라 PPDA 9.88 폐기**(대표값 12.3/12.45, 불변규칙 3) · 로저스 첼시 행은 빌라 그리드로 적재(캐비앗 명기) |
| 2026-07-31 | **히트맵은 1차 필터로 옳으나 단독으로는 요구역할의 36%다** | 요구역할 69조항 중 위치 25(36.2%) · 온더볼 15 · 관계 14 · 타이밍 9 · 세트피스 4 · 기능 2. 히트맵이 옳은 이유는 **FC26의 Role이 정확히 위치 분포**이고(fut.gg가 heatmap을 준다) 우리가 재는 것과 게임이 구현하는 것의 **자료형이 같기** 때문이다. 못 담는 63.8%의 대부분은 **역할 선택으로 애초에 구현 불가**(PlayStyles·능력치·세트피스 시스템) — 결함이 아니라 레버가 다르다 (obs#121) |
| 2026-07-31 | **세션 #4 결정 12건** (압축 — obs#100~#122, 상세는 각 obs) | **SSOT 1순위=`MAPS` 커널**(모든 적합값의 뿌리인데 툴에만 있어 백업 없음) · **`ANIM_*`·`COLL_CAL`은 DB로 안 옮김**(전자는 삽화라 `source`를 정직히 못 채우고 DB에 넣으면 실측 권위를 얻는다, 후자는 그리드의 순함수라 박아두면 조용히 stale) · **전수 42,972로도 H안 유지**(297위·상위 0.69%, Δ.0131은 EA 노이즈 하한 미달) · **C6는 대가 0이어도 소프트**(대가가 없다는 것이 근거가 세졌다는 뜻이 아니다) · **고점유 전면 전환 기각**(rest-defense 칸은 부호 반대) · **요구역할 표는 실측에 맞춰 고치지 않음**(불일치는 대부분 obs#14의 의도적 예외) · **애니메이션 위치만 커널 파생, 궤적은 삽화** · **sync가 프리뷰 미러 자동 갱신**(사람이 기억할 절차는 결국 빠뜨린다) |
| 2026-07-31 | **측정·수집 규약 6건** (압축 — obs#97·#98·#103·#104·#100·#102) | **슬롯 판정 원천 = `/lineups` `players[]` 배열 순서**(`formationPlace` 전 이벤트 NULL, 좌표 밴딩은 RB·RAM이 겹쳐 원리적 불가) · **`pos_class` 보존 + `lineup_pos` 신설**(덮어쓰기 아님, 두 파생을 나란히 둬야 밴딩 오탐을 계속 감사할 수 있다) · **C5에 `focus` 선택 필드 추가**(LCB에서 갈리는 것은 role이 아니라 focus였다 — 결손은 근거가 아니라 표현력) · **AcceleRATE 경계는 보가르드·카마라 2명**(불일치 4명 ≠ 키 민감 2명) · **SOFIFA `en` 정본 = `id` 보유 항목** · **첼시 제임스는 포커스만 교체**(Δ.001 동률에서 움직임만 보고 역할을 바꾸는 것이 C5를 낳은 그 사고다) · **LCB는 `Build-Up` 유지**(Δ.049는 노이즈라 결정권이 duties로 넘어간다) · **obs#38은 폐기 아니라 개정** |

---

## 최근 세션 기록

### 2026-08-19~20 — 세션 #32: SofaScore 해제 정정·26/27 전술 정본 이동·콘사 대체 축 (`38e9305`~`3e39ef0`)

상세는 위 「현재 상태」. 여기에는 **판단 이력**만 남긴다.
- ⚠️ **이 엔트리는 3개 세션분을 합친다** — 08-19 저녁 18커밋(다른 세션, HANDOFF 미갱신)과 08-20 09시 회차는
  `git log`·리포트를 근거로 반영했고, **이 세션이 직접 한 것은 14시 회차 + 핸드오프**다.
- ⭐⭐⭐ **최대 정정: SofaScore 해제(08-19, obs#256)를 08-20 두 회차가 놓쳐 「403이 8일째」로 오보**했다.
  원인은 런북의 「현재 차단」 서술이고, 에이전트도 같은 런북을 읽어 **순환 확증**이 됐다. SKILL.md를 이력으로 강등.
- ⚠️ **자기 정정 4건**: 위 오보 · **브리핑에 완비사카 소속을 맨유로 오기 — #31에서 이미 한 실수의 재발**
  (DB는 두 번 다 맞았다) · Pearce 귀속 오류(음바예) · 비홈그론 17명 상한이 음바예에는 미적용(U21 면제).
- ⭐ **에이전트 보고를 액면가로 받지 않은 사례**: CHE 원장 누락 7건 보고가 **전부 오탐**이었다 —
  공식 페이지는 정확히 읽었지만 **우리 DB를 볼 수 없어 「없다」는 판정을 할 수 없다.**
- ⭐ **사용자 요청 처리**: 「콘사 대체자 기사 전수 수집」에 전담 에이전트를 별도로 띄웠다(정기 스캔과 분리).
  9개 언어·20여 이름 중 **1~2티어로 특정된 것은 톰모리 하나**였고, 나머지는 3티어·과거 창·타 구단 건의 전용이었다.

---

## 아카이브 요약

- **#31 (08-18~19, `8d79944`~`28f4255`·`e1921c3`)**: 스즈키 CONFIRMED 승격(obs#234·#241) ·
  `site/assets/prose.js` 신설(**DB의 볼드가 한 번도 렌더된 적 없었다**, obs#240) · FC26 베이스라인 3팀 86행 +
  **선수 동일성 3요소 규약**(obs#250) · FC27 온보딩 2단계 확정(obs#249·#251) · obs#245(사칭 계정 HWG 3중 검증).
- **#30 (08-16, `475cfb9`·`8f8e4dc`·`557d8c5`)**: SofaScore 403 해소 확인 + 스즈키 39경기 전수,
  obs#220(GK는 적합이 변별하지 못한다 → 툴 5화면을 평균 위치로), 프리시즌 8경기 적재(경기 분석 19경기), obs#221.
- **#28~29 (08-14, `539b60e`·`f1b3761`)**: 슈퍼컵 사후 16명 자료 재감사(obs#201, Madjo 표본 4/6 유지)와
  완비사카 FotMob 36지표 적재(obs#202, RB `.932` 유지·공격 생산성으로 확대 안 함).
- **#26~27 (08-13~14, `ecebf05`·`3b4e539`·`9652e91`)**: 영상 분석 누락 5명 보강·duties 표본 메타·G10 강화,
  슈퍼컵 교체 5명 수집. Codex cron TCC 실증, obs#197~199(AVL 슬롯 x 유지·CHE CCB 공석·LIV 설정 유지).
- **#25 (08-13, `703c612`)**: `match_game_setups`·`match_player_prescriptions`로 MATCH ONLY 설정을 신설하고
  슈퍼컵 팀 설정·선발 11명을 이관. 시즌 정본과 경기 전용 프리셋의 분리가 여기서 시작됐다.
- **#5~24 (08-01~13, obs#123~196)**: R축 기각(재개 표본 2~3배)·GK·CAM·ST 좌우 판별 불가·Fotmob 복구·
  **발행일 우선 검증 신설** · **결손과 0은 다르다**(인터셉트 1위 카마라→오나나, 가르나초 W .936) ·
  친선 일반화 정정·Codex 이식·3팀 이적 실측·슈퍼컵 심화·경기별 리포트·**DB 선조회 원칙**.
- **#1~4 (07-30~31, obs#82~122)**: 3팀 체제, EA 정본 중앙 대칭 커널, H안·C5·C6, SSOT 1~3단계,
  42,972 라인업, A축 폐기, 히트맵 커버리지 36.2%. 이전 기록은 리포트·git log 참조.

---

## 컨텍스트 노트

**⚠️ SSOT (obs#105·#107·#115 — 1~3단계 완료)**
- **DB가 정본인 블록 3개**: `MAPS` 85커널 → `game_role_focus.kernel25` · `ROLE_VARIANTS` 217변형 →
  `game_role_variants` · 라벨 → `players.name_kr`. `AUTOGEN:*`로 자동 생성되므로 **직접 편집 금지** —
  DB → `python3 scripts/sync_transfer_ui.py`. 마커 문자열은 **정확히 일치**해야 하고(`/* AUTOGEN:NAME:START */`)
  설명은 마커 **밖**에 쓴다(마커 줄에 덧붙이면 `replace_block`이 `sys.exit`).
- 회귀 게이트: **DB 무변경 상태에서 sync를 두 번 돌려 `git diff`가 비어야** 한다.
  ⭐ sync가 프리뷰 미러(`/private/tmp/tactics-preview/`)를 **자동 갱신**한다.
- ⚠️ 4단계 이후는 **순수 additive로 불가**하다(obs#115 ④) — 축약↔풀네임 **별칭 축**이 필요하다.
- ⚠️ **신규 이적 후보 등재 시 `opt_role`·`opt_focus`를 반드시 채울 것** — 비우면 `CMP_SLOTS`·`XI_POOL`에는
  뜨는데 **`SQUAD_SLOTS` 후보에서만 조용히 빠진다**. 규약은 **`opt` = `fit` = 슬롯 유형 제한 argmax**
  (⚠️ 2026-08-13 기준 음바예 행이 이 규약을 어기고 있다 — 「다음 할 일」 P0 ⓑ).

**⚠️ 방법론의 커버리지 (obs#121 — 과도한 기대와 과도한 자기비판을 둘 다 막는 기준)**
- 요구역할 69조항 중 **히트맵이 직접 담는 것은 36.2%**(온더볼 21.7 · **관계 20.3** · 타이밍 13.0 · 세트피스 5.8 · 기능 2.9).
  ⚠️ **63.8%를 "구현 실패"로 읽지 말 것** — 상당 부분이 게임의 다른 레버이거나 AI가 알아서 한다.
- ⭐ **커버리지 0%인 슬롯이 정확히 C5를 붙인 슬롯**(파우 0 · 콘사 0 · 카마라 40 · 오나나 25) —
  낮은 칸에서 **duties가 적합을 이기는 것은 구조적 필연**이다.
- ⛔ **R(관계) 축은 세우려다 기각**(obs#123, FDR 생존 0/5) — 재시도 조건은 **표본 2~3배** + `docs/60` 재사용.
- ⛔ **방향(좌/우) 판별은 GK·CAM·ST에서 원리적으로 불가**(obs#127, 커널 변형이 좌우 대칭) ·
  ✅ **FB·CB·WM에서는 코사인이 방향을 본다**(완비사카 RB **.932** vs 같은 그리드 LB **.022**).

**⚠️ 새 축을 세울 때의 4요건** (obs#32 성공 / obs#95 약함 / **obs#85·#123 기각**에서 역산 — 2승 2패)
1. **독립 출처 2~3개가 같은 방향** — ⭐ obs#123은 확증쌍을 `player_duties` **영문 서술에서만** 뽑아
   이 요건을 **선정 단계에서** 만족시켰다(움직임 자료 미참조).
2. **교란 통제** — obs#95: LB↔RB 원시 **+0.687** → 경기별 평균을 뺀 잔차 **−0.168**. 원시 상관 금지.
3. ⭐ **대조군·사전 성공 기준·검정력을 먼저 정한다** — 없으면 그 추측이 정답처럼 출력된다.
   obs#123은 n이 필요치의 1/2~1/3이라 효과 유무를 **애초에 판정할 수 없었다**(사후에야 알았다).
4. **n · p · 다중비교 상태를 `confidence`에 명시** — 없으면 하드 제약으로 못 쓴다(obs#95가 소프트인 이유).

**📍 규약은 docs로 이관됐다 (2026-08-06)** — HANDOFF는 "지금 무슨 상태인가"만 담고, 재사용 규칙은
아래 문서가 정본이다. 값이 충돌하면 **docs가 이긴다**.

| 필요한 것 | 문서 |
|---|---|
| 슬롯 x 정본표 · **회귀 게이트 11항** · 측정 기준 7항 · 별도 구현 허용 조건 | [docs/20 「슬롯 x 정본표」](docs/20-fc-game-system.md) |
| FC26 대상 시스템 · EA 피치노트(비결정성·숙련·개명) | [docs/20 「EA 공식 피치노트」](docs/20-fc-game-system.md) |
| SofaScore 수집 절차 · **수집 함정 7종** · 좌표/그리드 규약 | [docs/30 「수집 함정」](docs/30-data-rules.md) |
| `git add -A` 금지 · sync/dump 순서 · 툴 편집 사고 사례 | [docs/40 「git 규칙」](docs/40-pipeline.md) |

특히 자주 잊는 3개만 여기 남긴다:
- ⛔ **`git add -A` 금지** — `.claude/settings.json`(Figma PAT)이 있어 푸시가 차단된다. 명시 스테이징만.
  ⚠️ 이 규칙은 **핸드오프 스킬의 wip 보존 절차보다 우선**한다.
- ⭐ **적합값 Δ 0.02~0.05는 EA가 공개한 노이즈 구간**(obs#91) — 그 차이로 인선을 가르지 않는다.
- ⭐ **결손과 0은 다르다**(obs#132) — SofaScore는 0이면 키를 생략한다. `v_event_profile`의 `n`은
  지표별 표본 수가 **아니다**.

**⚠️ 국면 축 상태 (obs#96 → obs#101 종결)**
- 공격 칸은 argmax 7/8 불변·|Δ|≤.049라 **전 국면 평균을 계속 써도 결론이 안 바뀐다**.
- ⚠️ 단 **rest-defense 칸은 저점유가 옳은 기준**(카마라 .629 / .692 / **.759**) — 고점유로 재면
  목적에 부합할수록 깎인다. 축 라벨에 `poss-hi`/`poss-lo`를 병기할 것.
- ⚠️ **경기별 그리드는 (선수, 경기) 단위라 포지션이 섞인다** — 슬롯별 비교 전에 **`lineup_pos` 필터**가
  반드시 선행돼야 한다(obs#96 ②의 자기 정정 사례. `pos_class`는 RB와 RAM을 못 가른다 — docs/30 ④).
