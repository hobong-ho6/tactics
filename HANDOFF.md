# HANDOFF

<!--
════════════════════════════════════════════════════════════
이 파일은 세션 간 핸드오프 문서입니다. (사람 + Claude 공용)

■ Claude 사용 규칙
1. [세션 시작] 이 파일을 가장 먼저 읽고, "현재 상태"와
   "다음 할 일"을 확인한 뒤 작업을 시작한다.
   시작 시 사용자에게 한 줄로 브리핑한다.
   예) "지난 세션: X 완료. 오늘 예정: Y. 시작할까요?"
2. [세션 종료] 사용자가 "핸드오프 갱신" / "세션 정리" /
   "handoff"라고 하면 이 파일을 아래 규칙으로 갱신한다.
   - "현재 상태" 블록을 새로 작성 (덮어쓰기)
   - "다음 할 일"에서 완료 항목은 [x] 처리 후
     "최근 세션 기록"의 완료 목록으로 이동
   - 새로 발견된 할 일을 "다음 할 일"에 우선순위와 함께 추가
   - "최근 세션 기록" 맨 위에 오늘 세션 엔트리 추가
   - "최근 세션 기록"은 최대 5개 유지. 넘치면 가장 오래된
     엔트리를 "아카이브 요약"에 1~2줄로 압축해 병합
3. [작성 원칙]
   - 다음 세션의 Claude가 이 파일만 읽고 즉시 작업을
     이어갈 수 있을 만큼 구체적으로 쓴다.
     (파일 경로, 커밋 해시, 이슈 번호, 명령어 포함)
   - "왜 그렇게 결정했는지"를 결과보다 우선해서 남긴다.
   - 추측과 사실을 구분한다. 미확인 사항은 (미확인) 표기.
   - 전체 파일이 300줄을 넘지 않게 유지한다.
════════════════════════════════════════════════════════════
-->

## 프로젝트 정보

- **프로젝트명**: Aston Villa → FC 전술 구현 (2026-07-30부터 **3팀 체제**)
- **한 줄 설명**: 실제 감독의 전술을 실측 데이터로 분해해 FC26 인게임 전술(포메이션·역할·포커스)로 재현한다. 대상: **에메리(빌라) / 알론소(첼시) / 이라올라(리버풀)**
- **주요 경로/저장소**: `/Users/user/Documents/tactics` (git, main)
  - DB `db/tactics.db` = single source of truth / `site/data/`·툴 = DB 파생
  - 문서 맵은 `CLAUDE.md` 참조. 신규: `docs/11-alonso-tactics.md`, `docs/12-iraola-tactics.md`
- **관련 링크**: 스케줄 작업 `villa-transfer-watch` (매일 09/21시), 스킬 `.claude/skills/transfer-watch/SKILL.md`

---

## 현재 상태

> 마지막 갱신: **2026-08-13** · 세션 **#20** · 작업 PC `AL02359162.local` · 브랜치 `main` ·
> 최신 데이터 커밋 **`f1a2867`** · observations **196행**(최신 obs#196).
> DB는 v2(`db/tactics.db`), 3팀 스쿼드·실측·평가 체제 유지.
>
> **선수 후보 SSOT 통합(`f1a2867`).** DB `v_slot_candidates` → export `slot_candidates` → 공용 JS 함수로
> 비교·스쿼드·히트맵·리포트·선수 화면을 단일화했다. 가르나초 LM 중복은 1건으로 정리됐고 Hemmings는
> `squad_entries.id=66` WM에 승격돼 LM에 노출된다. G8이 중복·도달불가·이적누락 0을 강제한다.
> `scripts/serve.sh`는 no-store 서버를 쓰며 JSON에도 timestamp+`cache:'no-store'`를 붙이고 G9가 지킨다.
>
> **Codex 이식 검증 완료.** 병렬 3팀 스캔·실제 `transfer-watch.sh` end-to-end를 모두 통과했다.
> `workspace-write`는 `.git/FETCH_HEAD`를 막으므로 cron을
> `codex exec --ephemeral --sandbox danger-full-access`로 교정(`e71bc38`), 고정 프롬프트에서 fetch 성공을 확인했다.
> 남은 환경 검증은 **macOS TCC 해제 후 예약 cron 기동 1건뿐**이다.
>
> **결손 수리 완료(`486eada`).** 중복·오류 차바리아 `squad_entries.id=62/player_id=87`만 제거하고 정상 행
> id=63/player_id=80을 보존했다. `gates.py`에 G6 `foreign_key_check`를 추가했다. 팔리냐·잭슨·알란·음바예의
> 전수 실측을 정형 필드로 옮겼고 슬롯 유형 불일치도 교정했다.
>
> **슈퍼컵 실측 완료(`ef0fc1b`).** event `16260286`, PSG 2-1 AVL의 45분+ 11명·팀 스탯을 적재했다.
> 자연 우측 윙어 대신 McGinn이 RAM(평균 y=15.81, 4키패스·1도움)을 맡아 obs#134의 우측 결손을 지지했다.
> Madjo는 ST 72분·1골로 유효 표본 **4/6**. 상세는 `reports/match-watch/2026-08-12-avl-psg-super-cup.md`.
> Jørgensen의 Chelsea·Strasbourg 08-11 공식 발표도 확인해 **CONFIRMED**로 전환했다.
>
> **슈퍼컵 심화 완료(`0174191`, obs#196).** 11명 전원의 실제 스탯·평균 위치·맵25·단일경기 커널 적합을
> 리포트에 한 표로 합치고, 경기 후 Emery 기자회견 자동 자막·El País·AS·Villa 팟캐스트·talkSPORT를
> 직접 대조했다. 유스 **George Hemmings도 프로필 이력이 아니라 event 16260286 경로로** 90분·평점 6.2·
> 히트포인트 51·평균 (58.11,73.31)·맵25를 확보해 `player_duties` LM에 추가했다. 2026-27 역할 행이 없던
> Bizot·Lindelöf·Pau·Maatsen·Hemmings·Madjo 6명만 추가했고 기존 처방은 한 경기라 바꾸지 않았다.
>
> **15시 transfer-watch 완료(`f69d2db`).** Fotmob 109행(AVL 29/CHE 44/LIV 36), 신규 DB 등재·등급·
> STALE·PENDING 변화 0. 김민재 문의는 선수·Bayern 잔류 의사가 강해 MEDIUM 리포트 전용. Palhinha
> Romano 게시물은 X status ID상 08-12 UTC라 DB 날짜를 08-13으로 잘못 밀지 않았다. Enzo 시한 불변.
>
> **obs#132 파급 감사 완료(`f929652`).** 배급 우위는 원천 재수집으로 재현했지만 카마라의 “스쿼드 최고
> 볼위너” 근거는 폐기했다. `dm_holding/Defend`는 백4 스크린·창조 역할 오배정 방지 근거로 유지한다.
> 함께 발견한 v1→v2 `appearances` 기능 스탯 누락을 수리해 116행을 복구하고 G7 병합 앵커를 추가했다.
>
> **다음 우선순위**:
> 1. ⏰ **08-14(금) 17:00 BST = 08-15(토) 01:00 KST — 엔소 페르난데스 시한.** 직후 확인해
>    오퍼 없으면 이번 창 DEAD, 있으면 HIGH로 재판정한다.
> 2. ⭐ **`slots` 표의 LAM/RAM x 재검토** — 정본 14/85 vs 실측 30.9/76.1. **LAM Δ+16.9는 노이즈로 보기 어렵다**.
>    WM군은 불변 확인, 타 역할군 미검증.
> 3. **찰로바 코모 이적 → CHE의 CCB 슬롯 공석**(obs#102 재검토)과 산초 스쿼드 행 처분.
> 4. **이라올라 이식 리스크 3건** + `fotmob_detail_stats` 결손 3명 보충 + 네델코비치·마조 표본 축적.
>
> **Codex 잔여 확인 1건 — 예약 cron 실제 기동.** 사용자가 `/usr/sbin/cron` 전체 디스크 접근 권한은
> 2026-08-13에 부여했다. 다음 09/21시 회차에서 로그·커밋·push까지 실제 기동만 확인하면 이식 검증이 닫힌다.

## 다음 할 일

> ⏰ **첫 작업: 08-15(토) 01:00 KST 직후 엔소 시한 확인**. 그 전에는 LAM/RAM 슬롯 x 재검토부터 진행한다.

- [ ] **P1 · 국면 축 확장** (obs#101 → obs#122 후속) — possession 행이 늘어(로저스 42·콘사 38·디뉴 26·
  틸레만스 24) obs#101이 8쌍에서 멈춘 것을 다시 돌릴 수 있다. ⚠️ 게상 12/26·일링 0/15은 결손(니스·임대분).
  ⚠️ obs#101 ③(**rest-defense 칸은 저점유가 옳은 기준**)이 확장 표본에서도 성립하는지 재확인할 것.
  (**R축은 기각됨**(obs#123) — 재개 조건은 표본 2~3배, 쌍당 n≥60~100, `docs/60` 재사용.)

- [ ] **⭐ P1 · 첼시 피벗 처방 결정** (obs#116, **사용자 판단 사항**) — 기능축 실측이 확정한 배정
  (카이세도=볼위너 · 엔소=창조)과 현 처방(두 피벗 모두 `dm_dlp`)이 어긋난다. 선택지 2개:
  ⓐ **현행 유지** — 움직임 적합 +.126을 지키고 기능 정합을 포기(현 평균 .799)
  ⓑ **소스 배정 채택** — 엔소 LDM `dm_dlp`/Roaming .698 + 카이세도 RDM `dm_holding`/Roaming .647
     (평균 .673, −.126). 카이세도 개인 지불액은 **.246~.294**로 카마라의 .147보다 크다.
  ⚠️ 표본이 **알론소 체제가 아니다**(25/26 마레스카·로즈니어) — 8/24 풀럼 후 재판정 가능.

- [ ] **P2 · 리버풀 기능축 최초 수집** (obs#116의 첼시 방식 그대로) — `appearances` team=LIV **0행** ·
  `player_match_positions` team=LIV **0행**이다. obs#120이 12명을 재산출했으나 **피벗 3인이 전부
  `dm_dlp`/Roaming**으로 나와 **기능 분화를 가릴 수단이 없다**. 프리셋보다 이것이 먼저다.
  경로: `/api/v1/event/{eid}/player/{pid}/statistics` → `player_match_positions.stats_json`.

- [ ] **P2 · SSOT 4~7단계** — 4단계 `CMP_SLOTS`+`SQUAD_SLOTS` 6건+`PLAYER_BEST` → 5~7단계
  `SLOT_EMERY_ROLE`+`FUNC_AXIS` · `FORMATIONS` · `SQ_STATS`.
  ⚠️ **4단계 선행 2건**: ⓐ **실제 드리프트** — `CMP_SLOTS` 맥긴 CAM 그리드가 **DB 어느 값과도 다른** 제3의
  값인데 툴·DB가 같은 표본("CAM 4경기 평균 6.90")을 주장한다(orphan 그리드 10건 처분 포함). ⓑ **별칭 축**
  (obs#115 ④) — 축약↔풀네임 14쌍은 규칙 도출 불가 → `player_fc_stats.tool_label`(NULL 허용)이 최소 침습안.
  ⚠️ **이관 금지**(obs#105 ⑥): `ANIM_*` · `COLL_CAL` · `FORMATION_DESC` · 초기추정 프리셋 · `TEAMS`.
  ⚠️ **별 단계로 분리**: 상태 접미 소유자 통일(키 200여 개 파괴적 변경) · `UNIQUE(game_version, sofifa_id)`.

- [ ] **P2 · 애니메이션 궤적** (obs#117 ⑵ 잔여) — 마커는 커널 파생이나 **화살표(`ANIM_PHASES.ar`)·
  경로(`AN_PATTERN`)는 옛 상수**라 어긋날 수 있다(`전술 요구역할만` 모드에서 LM이 20칸 이동).
  대안: 화살표를 **파생 좌표 사이 직선**으로 대체(정합성 ↑, 표현력 ↓) — 사용자 판단 필요.

- [ ] **P3 · 잔여 3건** — ⓐ **세트피스 득점 비중 축**(obs#112·#113 결손): DB에 세트피스 전용 컬럼이 없어
  obs#49(알론소 40%)·obs#74(이라올라 33%)와 **동축 비교 불가**. 경로는 `/event/{eid}/incidents`(브라우저 필수)
  또는 `appearances` 자유서술 파싱(⚠️ 커버리지 불균등 → "비중" 금지, "확인된 골 목록"까지만).
  ⓑ **리버풀 프리셋**(8/23 개막 후 · **기능축 수집이 선행**)
  ⓒ ⚠️ **transfer-watch 소스 1개 손실** — Sky 빌라 라이브 블로그(누적 5회+ 실패). 대체 소스 확보가 백로그.
     ✅ **Fotmob은 대화형에서 정상**(08-13 3팀 수집 확인) — 헤드리스 스케줄 런에서만 차단된다.

- [ ] **P2 · 이적창 마감 추적** — 마감 **2026-09-01(화) 23:00 BST**(PL 공식, 08-31은 오기였음).
  상태는 2026-08-13 기준. 전체 로그는 `reports/transfer-watch/2026-08-13.md`.
  ⓐ **루헤리**: Romano "here we go"(08-11) → **메디컬 이동**(08-12) → **공식 발표는 여전히 0건**.
     €25m + €1.5m. **CONFIRMED 전환 1순위.**
  ⓑ **엔소 → 맨시티**: ⏰ **08-14(금) 17:00 시한**. £120m, 오퍼 0건. 위 「현재 상태」 1번.
  ⓒ **콘사 ↔ 마르티네스는 배타 관계**("둘 중 하나는 창 마감 전 이탈", 2티어 1곳 단독). 콘사는 BBC(1티어)
     기준 아스날 오퍼 **£30m**·빌라 거절이고 **£64.1m은 폐기 확정**(3티어 단독이었다). 신규 동인 =
     **살리바 부상**으로 관심 재점화·추가 회담. 빌라 평가액 £60m, 격차 미해소. 유벤투스는 GK 레이스 철수.
  ⓓ ⭐ **잭슨 45일 (b) 조건** — 로저스 07-22 확정 → 만료 **09-05**로 잔여 창 전체가 구간 안. 단 Annex
     G.3.5는 **(a)45일 AND (b)동일 지급구조**의 연언 조건 + **substance over form**이라 45일은 자동
     판정선이 **아니다**(회피는 (b)로 하며 실증이 가르나초). ⚠️ **임대 슬롯 소진으로 (b)가 막혔다** — 미확인.
     토트넘 경쟁 링크는 **약함**(전담 Alasdair Gold 정면 부인) — 빌라 우위 유지.
  ⓔ 완료분: 베일리 헐 시티 **결렬**(사우디가 유일 경로) · 게상 팰리스 **CONFIRMED** ·
     에머슨 로열 STALE(삭제 안 함) → RB 결손은 **완비사카(.932) 등재로 해소**.

### 차단 요소 / 대기 중

- **공식전**: AVL은 **08-12 슈퍼컵 1경기**(PSG 2-1 AVL) 45분+ 11명 실측·심화 서사 완료(obs#194·196),
  마조 유효 표본 **4/6**. 첼시 **8/24 풀럼**, 리버풀 **8/23 뉴캐슬 원정**으로 두 팀은 아직 0경기다.
- **EA가 수치를 공개하지 않는다** — Role+/++ 정량 효과, OOP 페널티 크기, 포커스 위치 델타,
  역할별 능력치 가중치. **AcceleRATE는 분류 공식만 공개**(효과 크기는 여전히 비공개).
- **국면 분리는 보유 선수만** — 영입 4명은 빌라 경기 0이라 구조적으로 불가.
- ⚠️ **SofaScore 실측 제공 여부는 "친선이냐"가 아니라 "그 대회를 커버하느냐"로 갈린다**(obs#133이
  obs#126을 정정). 양방향 반증: **친선인데 있음**(빌라 vs 바이에른 08-07, 마조 히트맵 28pt) /
  **공식전인데 없음**(마조 U17 유럽선수권 예선 3경기 전건 0). ⇒ **프리시즌이라고 미리 포기하지 말 것 —
  경기별로 시도해 보는 것이 유일한 판별법이다.** 단 커버돼도 표본 기준(6경기 45분+)은 별개(마조 4/6).
- ⚠️ **`tactic_code`가 16행 전부 비어 있다** — 어느 팀도 실제 인게임 슬라이더/코드가 DB에 없다.
  처방은 "역할·포커스·라인"까지만 내려간다. `ingame_checks`도 **0행**(게임 내 검증 폐기, docs/50).
- ⚠️ **적합도가 전 국면 평균 기준**이고 obs#101은 그 어긋남을 **빌라 표본으로만** 측정했다 —
  첼시·리버풀에 같은 노이즈 크기가 성립한다는 근거는 없다.

---

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-08-13 | **양 측면을 쓰는 자원은 좌우를 나눠 재고, 혼합값을 쓰지 않는다** (obs#156 규약을 영입 후보에도 확장, `d72897d`) | 무뇨스를 34경기 **혼합**으로 재면 LM **.615**인데, 좌 23·우 11이 섞여 그리드가 중앙으로 뭉개진 결과다. 나누면 **좌 .729 / 우 .772로 양쪽 다 올라간다** — 혼합값은 두 값의 중간이 아니라 **둘 다 아닌 값**이다. 자케도 같은 구조(혼합 RCB .838 → 우측만 **.903**). ⚠️ 반대로 **완비사카는 25/25 전 경기 우측 우세(우2컬럼 91.7%)** 라 분리가 불필요했다 — 분리 여부는 미리 정하는 게 아니라 **경기별 좌우 비중을 세어 판정**한다 |
| 2026-08-13 | **`transfer_outgoing`은 `players`에 있는 선수만 등재한다** — 첼시 신규 유출 후보 5명(토신·델랍·디사시·기우·사르) 미등재 | 다섯 명 전원이 1티어(Sky·The Athletic·BBC) 근거로 MEDIUM-HIGH 요건을 **충족**했으나 `transfer_outgoing.player_id`는 `players` FK가 필수이고 **5명 전원이 `players`에 없다** = 전술 분석 대상 스쿼드가 아니다. 이들이 나가도 `squad_entries`·XI·슬롯이 **바뀌지 않는다**. 행을 만들려고 `players`를 5개 늘리는 것은 추적 가치 대비 **스키마 오염**이라 판단하고 근거는 리포트에만 보존했다. ⚠️ 단 **센터백 감축의 누적 효과는 별도 추적 대상**(찰로바 CONFIRMED + 바디아실 HIGH + 이 3명) |
| 2026-08-13 | **`handoff-hooks/`·`".claude(구독)/"` 는 삭제하지 않고 `.gitignore`에 넣는다** (사용자 확정) | 세션 #14가 "다른 세션/PC에서 유입 추정"으로 판단해 untracked로 남겨뒀던 미결 항목. **git에 없는 파일이라 삭제하면 복구 불가**이고, 유입 경위가 확정되지 않았다. 추적에서만 제외하면 매 세션 `git status` 노이즈가 사라지면서 파일은 보존된다 — 되돌리기 쉬운 쪽을 택했다 |
| 2026-08-10 | **웹 검색보다 DB 선조회를 먼저 한다** — SKILL.md **§1-0 신설** (`731d92f`) | "프리시즌 활약 유스"를 웹으로만 훑다 헤밍스·린치를 통째로 놓쳤는데 **obs#126 ③에 5명이 이름으로 적혀 있었다**(Lynch 어시 → Madjo 골까지). 검색 설계 문제인 줄 알았으나 실제 원인은 **파이프라인에 DB 선조회 단계가 없었던 것**. ⭐ 함께 넣은 규칙: **obs는 `claim`과 `confidence`를 같이 읽어라** — obs#126은 결론을 "친선이라 실측 없음"으로 썼지만 `confidence`에 진짜 원인("상대가 올스타 선발팀")이 이미 있었다. **근거 칸이 결론보다 정확할 수 있다** |
| 2026-08-10 | **obs#126은 폐기가 아니라 범위 축소** — obs#133 신설 (`b790539`) | 관측(11명 전건 null)은 옳고 과한 것은 **"프리시즌 친선의 구조적 한계로 기록한다"** 한 문장이다. 양방향 반증 확보: **친선인데 있음**(빌라 vs 바이에른, 히트맵 28pt) / **공식전인데 없음**(U17 유럽선수권 예선 90·36·90분, 히트맵 0). ⇒ 결정 변수는 친선 여부가 아니라 **SofaScore 대회 커버리지**이며, **사전 판별법이 없어 수집을 시도해 보는 것이 유일한 방법**이다. HANDOFF 차단요소도 "경기별로 시도하라"로 교체 |
| 2026-08-06 | **불변규칙 8 신설 — 히트맵은 시각화해서 보여준다** (`8707e44`) | `map25` 5×5는 공간 정보(열0=좌측 터치라인·행0=공격)인데 숫자열로는 좌우 편향 비교가 사실상 불가능하다. 실증: 가르나초 좌2컬럼 **90%** vs 로저스 **68%** — 이 대비는 그림에서만 즉시 보인다. 수치 결론은 위젯 **밖**(응답 본문)에 쓴다 |
| 2026-08-06 | **커널 적합도의 별도 구현을 조건부 허용** — 회귀 게이트 통과가 조건 | 컨텍스트 노트 「측정 기준 #2」는 "브라우저에서 툴 함수 직접 호출, 별도 구현 금지"였다(MAPS 파싱 함정 때문). 08-06에 파이썬으로 `decodeMap`/`placedMap`/`cmpCos`를 재현하고 **역할 수 37 + 회귀값 7개**를 통과시켜 브라우저 없이 산출했다. ⭐ 금지의 목적은 "브라우저 사용"이 아니라 **파싱 함정 방지**이므로, 게이트를 통과하면 목적이 충족된다. 게이트 없이 구현하는 것은 여전히 금지 |
| 2026-08-06 | **SKILL.md 회귀 기준값 정정** (하지무사 .923→**.821** · 은디아예 .883→**.825**) | 두 값이 obs#93·#94 커널 교체 **이전** 것이라 그대로 쓰면 **회귀 검증이 항상 실패**했다. 당시 6경기 Jackson .752 등 ST·CAM은 커널 교체 전후 불변이었다. ⚠️ Jackson 앵커는 2026-08-13 전수 28경기 정형화로 **.724**로 교체됐다. |
| 2026-08-05 | **`v_event_profile` 인용 시 지표별 표본 수를 반드시 확인한다** (obs#131·#132, `448fbc6`) | 뷰의 `n` 컬럼이 **지표별 실제 표본 수가 아니다**(카마라 인터셉트 3.0은 2경기 평균, 1.40이 9경기 값). 더 근본적으로 **SofaScore는 값이 0이면 키 자체를 생략**하므로 DB의 '결손'은 미수집이 아니라 **0**이었다 — 전수 재수집으로 인터셉트·태클 **1위가 카마라 → 오나나**로 바뀌었다 |
| 2026-08-05 | **카마라 `require(dm_holding)` 하향 보류** (obs#130, 사용자 지시를 실행 직전 중단) | 지시는 prefer 하향이었으나, obs#121이 이 제약의 존재 이유를 "카마라를 창조 역할에 배정한 솔버 사고"로 명시한 것을 실행 직전 확인했다 — 내리면 그 사고가 재발한다. 계열 내 포커스 교정(Ball-Winning .556 → **Defend .703**)이 현 근거로 정당화되는 최대치 |
| 2026-08-03 | **이적 감시에서 발행일 검증을 티어 판정보다 먼저 한다**(SKILL.md §2-0 신설) | 하루에 **10건**을 이 검사로 걸렀고, 그중 Sancho/Lammens는 **BBC Sport 본체 + Romano 원트윗**이라 **매체 티어로는 원리적으로 안 잡힌다**. 여름 창은 매년 같은 구단·같은 포지션 루머가 돌아 **정확히 1년 오차**가 가장 흔하다. 실패한 지표(URL 슬러그·검색 순서·피드 "Nh ago") 대신 **본문 나이 표기**와 **병기된 타 구단 상황**을 1순위로 삼는다 |
| 2026-08-03 | **Fotmob은 복구하되 "리드 소스"로만 쓴다**(티어 근거 금지) | 인앱 브라우저로 열자 CSR 테이블이 완전히 렌더링됐다 = 07-31 진단(구조적 불일치)이 옳았고 `WebFetch`로는 여전히 불가. 고유 가치는 **루머별 출처·날짜 라벨**이지만, `Watkins→Fenerbahce`를 "Sky Sports"로 표시해 놓고 실제 원출처는 터키 매체였다 — 게다가 행이 **선수 페이지로만 링크**돼 라벨을 검증할 수 없다. 수집 주체는 **메인 세션**(브라우저 pane이 세션당 하나라 서브에이전트와 충돌) |
| 2026-08-01 | **R축 미신설(기각)** — C6는 소프트 유지 | 사전 등록한 처분표 그대로. FDR 생존 **0/5** + 대조군 **C 계수 음수**(−0.435, 방향 반대). ⭐ 그러나 **방향은 4/5가 예측대로 음수**였고 실패 원인은 효과 부재가 아니라 **검정력**이다(판정에 \|r\|≥0.33~0.45 필요, 실측 0.13~0.24) → "보완이 없다"가 아니라 **"이 도구로는 세울 수 없다"**가 정확한 진술이고, 표본 2~3배면 재검정 가치가 있다 (obs#123) |
| 2026-08-02 | **GK·CAM·ST 슬롯의 적합값을 좌우 편향 근거로 쓰지 않는다** | 그 슬롯에서 선택되는 커널 변형이 **좌우 대칭**이라 그리드를 뒤집어도 값이 **소수 셋째 자리까지 동일**하다(.686→.686). 코사인이 원리적으로 방향을 못 본다 — 우위가 말하는 것은 "퍼진 분포"까지다 (obs#127) |
| 2026-07-31 | **측정 기준 재정립 14건** (압축 — 세션 #3, obs#82~#96) | **슬롯 x 정본 = 툴 `SQUAD_SLOTS`**(코사인이 x의 계단 함수) · docs/20 §B 라인 보상 규칙 폐기(obs#27과 모순) · 전역 충돌 임계값 → **슬롯쌍 상대 지수 CI**(쌍 중앙값이 140배 범위) · 26/27 프리셋 5종 폐기 → 단일 `projected:emery-2627` · **A축 기각**(두 독립 검증 실패 + EA "Opta AI 모델" 명시) · **B축은 점수 아닌 플래그**(이산 속성) · 뱅크 포커스 기본 Support · RM 뱅크는 캐시(알리송은 뱅크 미숙련) · **`placedMap` 전면 교체 — 변형은 시프트가 아니라 중앙 대칭 미러** · 회귀 게이트 값 교체 · **프리셋 H안 채택**(사용자 확정) · **C5 기능축 신설**(솔버가 카마라를 창조 역할에 배정한 사고) · **C6는 소프트**(obs#95 근거가 억제 1건뿐) · 고점유 전환 보류(→ obs#101에서 기각으로 종결) |
| 2026-07-30 | **3팀 체제 도입 결정 9건** (압축) | 팀 축 = 텍스트 `team` 컬럼 + FK 없는 `teams`(불변규칙 2 준수) · UNIQUE 충돌 5테이블만 재작성 · **STALE = DEAD 숨김(삭제 아님)** — 툴이 `/^DEAD/i`를 필터하므로 행을 지우면 `map25`·`fit_sim`을 잃는다 · 등급 강등도 숨김으로 통일 · STALE 판정은 **빌라 링크 보도일**(`last_news_date`) 기준이고 `CONFIRMED`/`OWNED` 제외 · **CCB 슬롯 신설**(백3 3인이 각자 자기 자리 최고치) · CCB를 `CMP_SLOTS`에 의도적 미추가(빌라 화면 누출 방지) · 스캔은 서브에이전트·판정은 메인 · **obs#28 빌라 PPDA 9.88 폐기**(대표값 12.3/12.45, 불변규칙 3) · 로저스 첼시 행은 빌라 그리드로 적재(캐비앗 명기) |
| 2026-07-31 | **히트맵은 1차 필터로 옳으나 단독으로는 요구역할의 36%다** | 요구역할 69조항 중 위치 25(36.2%) · 온더볼 15 · 관계 14 · 타이밍 9 · 세트피스 4 · 기능 2. 히트맵이 옳은 이유는 **FC26의 Role이 정확히 위치 분포**이고(fut.gg가 heatmap을 준다) 우리가 재는 것과 게임이 구현하는 것의 **자료형이 같기** 때문이다. 못 담는 63.8%의 대부분은 **역할 선택으로 애초에 구현 불가**(PlayStyles·능력치·세트피스 시스템) — 결함이 아니라 레버가 다르다 (obs#121) |
| 2026-07-31 | **세션 #4 결정 12건** (압축 — obs#100~#122, 상세는 각 obs) | **SSOT 1순위=`MAPS` 커널**(모든 적합값의 뿌리인데 툴에만 있어 백업 없음) · **`ANIM_*`·`COLL_CAL`은 DB로 안 옮김**(전자는 삽화라 `source`를 정직히 못 채우고 DB에 넣으면 실측 권위를 얻는다, 후자는 그리드의 순함수라 박아두면 조용히 stale) · **전수 42,972로도 H안 유지**(297위·상위 0.69%, Δ.0131은 EA 노이즈 하한 미달) · **C6는 대가 0이어도 소프트**(대가가 없다는 것이 근거가 세졌다는 뜻이 아니다) · **고점유 전면 전환 기각**(rest-defense 칸은 부호 반대) · **요구역할 표는 실측에 맞춰 고치지 않음**(불일치는 대부분 obs#14의 의도적 예외) · **애니메이션 위치만 커널 파생, 궤적은 삽화** · **sync가 프리뷰 미러 자동 갱신**(사람이 기억할 절차는 결국 빠뜨린다) |
| 2026-07-31 | **측정·수집 규약 6건** (압축 — obs#97·#98·#103·#104·#100·#102) | **슬롯 판정 원천 = `/lineups` `players[]` 배열 순서**(`formationPlace` 전 이벤트 NULL, 좌표 밴딩은 RB·RAM이 겹쳐 원리적 불가) · **`pos_class` 보존 + `lineup_pos` 신설**(덮어쓰기 아님, 두 파생을 나란히 둬야 밴딩 오탐을 계속 감사할 수 있다) · **C5에 `focus` 선택 필드 추가**(LCB에서 갈리는 것은 role이 아니라 focus였다 — 결손은 근거가 아니라 표현력) · **AcceleRATE 경계는 보가르드·카마라 2명**(불일치 4명 ≠ 키 민감 2명) · **SOFIFA `en` 정본 = `id` 보유 항목** · **첼시 제임스는 포커스만 교체**(Δ.001 동률에서 움직임만 보고 역할을 바꾸는 것이 C5를 낳은 그 사고다) · **LCB는 `Build-Up` 유지**(Δ.049는 노이즈라 결정권이 duties로 넘어간다) · **obs#38은 폐기 아니라 개정** |

---

## 최근 세션 기록

### 2026-08-13 — 세션 #20: 선수 후보 SSOT + 캐시 없는 프리뷰 (`f1a2867`)

- `v_slot_candidates`를 신설해 squad 우선·활성 transfer 병합·player_id 중복 제거를 DB에서 고정했다.
- 모든 선수 목록 화면을 공용 export/JS 함수로 교체. 가르나초 LM 1건, Hemmings LM 노출을 실제 브라우저로 확인했다.
- `scripts/serve.py` no-store 헤더와 JSON timestamp 요청을 추가해 재실행 때 낡은 데이터가 보이지 않게 했다.
  export→dump→G1~G9와 비교·스쿼드·히트맵·리포트·선수 UI 검증 완료.

### 2026-08-13 — 세션 #19: 15시 이적 감시 + 슈퍼컵 선수별 심화 (`f69d2db`·`0174191`)

- transfer-watch 3팀/Fotmob 109행을 재검증. 신규 DB 행·등급·STALE 변화는 없고 김민재 문의는 MEDIUM
  리포트 전용, Palhinha 원 게시일은 08-12 UTC 유지, Enzo 08-15 01:00 KST 트리거 유지.
- 슈퍼컵 11명의 스탯·평균 위치·맵25·경기 적합을 통합하고 영상 자동 자막과 기사·감독 발언을 교차검증했다.
- 유스 Hemmings도 event별 API 경로로 90분 히트맵을 회수해 LM 지원/압박 가설을 기록했다. 신규 duties 6행,
  obs#196 1행. 처방 변경 없음. export→dump→G1~G7 통과 후 main push 완료.

### 2026-08-13 — 세션 #18: Codex 이식 마무리 + 슈퍼컵 실측 (`e71bc38`·`486eada`·`ef0fc1b`·`f929652`)

- cron 샌드박스를 `danger-full-access`로 교정해 fetch를 포함한 비대화형 동기화를 검증했고, 병렬 3팀 스캔도 통과했다.
- 차바리아 고아 FK와 전수 이적 적합 정형 필드를 수리하고 G6 FK 게이트를 추가했다.
- PSG 슈퍼컵 45분+ 11명·팀 스탯 적재, obs#194 신설. McGinn RAM 임시 해법은 우측 결손을 지지했다.
- Jørgensen의 양 구단 공식 발표를 확인해 CONFIRMED 전환. export→dump→G1~G6 전항 통과.
- obs#132 파급 감사로 카마라 기능축 근거를 교정하고, 이 과정에서 찾은 v1→v2 스탯 누락 116행을 복구(G7).

### 2026-08-13 — 세션 #17: 비대화형 정기 회차 end-to-end (`afe002f`)

- Fotmob 109행을 AVL→CHE→LIV 순으로 스캔. 신규 0, 근거 통과 8, 실패/보류 2, STALE·PENDING 0.
- 커티스 존스의 프라테시 선결 조건 진전을 근거에 반영하되 HIGH·기사일 08-12는 유지했다.
  export→dump→게이트→명시 스테이징→push 완료. `squad_entries.id=62` FK 결손은 후속으로 남겼다.

### 2026-08-13 — 세션 #16: Codex 이식 검증 (`eaa4f67`)

- `.venv`·Playwright 설치, 지정 완비사카 회귀를 자동 게이트화해 **RB .932**, 전체 `gates.py` 통과.
- 앱 번들 Codex `0.147.0-alpha.6.5`에서 `--full-auto` 제거를 확인하고 cron을
  `--ephemeral --approve-for-me` + 앱 경로 fallback으로 교체. smoke test 종료 0, 실제 전체 회차는 미검증.
- 엔소 시한 사전 점검: 오퍼 없음, **MEDIUM-HIGH 유지**, DB 무변경. 최종 확인은 08-15 01:00 KST 직후.

---

## 아카이브 요약

- **#11~15 (08-10~13, obs#133~158)**: 친선 일반화 정정, 26/27 결손, 스쿼드 누락 복구, 3팀 이적 실측,
  이적 감시 PENDING 중복과 README 혼입을 정리해 DB 선조회 원칙을 재확인.
- **#8~10 (08-04~06, obs#128~132)**: 결손값 재수집으로 인터셉트 1위 카마라→오나나, 가르나초 W .936 교정,
  히트맵 시각화 규칙 신설. **결손과 0은 다르다.**
- **#5~7 (08-01~03, obs#123~127)**: R축 기각(재개 표본 2~3배), GK·CAM·ST 좌우 판별 불가, Fotmob 복구,
  발행일 우선 검증 신설. **기각의 정확한 문장이 다음 작업을 정한다.**
- **#1~4 (07-30~31, obs#82~122)**: 3팀 체제, EA 정본 중앙 대칭 커널, H안·C5·C6, SSOT 1~3단계,
  42,972 라인업, A축 폐기, 히트맵 커버리지 36.2%. 이전 기록은 리포트·git log 참조.

---

## 컨텍스트 노트

**⚠️ SSOT (obs#105·#107·#115 — 1~3단계 완료)**
- **DB가 정본인 블록 3개**: `MAPS` 85커널 → `game_role_focus.kernel25` · `ROLE_VARIANTS` 217변형 →
  `game_role_variants` · 라벨 → `players.name_kr`. `AUTOGEN:*`로 자동 생성되므로 **직접 편집 금지** —
  DB → `python3 scripts/sync_transfer_ui.py`. 마커 문자열은 **정확히 일치**해야 하고(`/* AUTOGEN:NAME:START */`)
  설명은 마커 **밖**에 쓴다(마커 줄에 덧붙이면 `replace_block`이 `sys.exit`).
- 회귀 게이트: **DB 무변경 상태에서 sync를 두 번 돌려 `git diff`가 비어야** 한다.
  ⭐ sync가 프리뷰 미러(`/private/tmp/tactics-preview/`)를 **자동 갱신**한다.
- ⚠️ 4단계 이후는 **순수 additive로 불가**하다(obs#115 ④) — 축약↔풀네임 **별칭 축**이 필요하다.
- ⚠️ **신규 이적 후보 등재 시 `opt_role`·`opt_focus`를 반드시 채울 것** — 비우면 `CMP_SLOTS`·`XI_POOL`에는
  뜨는데 **`SQUAD_SLOTS` 후보에서만 조용히 빠진다**. 규약은 **`opt` = `fit` = 슬롯 유형 제한 argmax**
  (⚠️ 2026-08-13 기준 음바예 행이 이 규약을 어기고 있다 — 「다음 할 일」 P0 ⓑ).

**⚠️ 방법론의 커버리지 (obs#121 — 과도한 기대와 과도한 자기비판을 둘 다 막는 기준)**
- 요구역할 69조항 중 **히트맵이 직접 담는 것은 36.2%**(온더볼 21.7 · **관계 20.3** · 타이밍 13.0 ·
  세트피스 5.8 · 기능 2.9). 재분류: 직접 통제 36% · 제약으로 부분 커버 33% · **다른 레버 31%**.
  ⚠️ **63.8%를 "구현 실패"로 읽지 말 것** — 상당 부분이 게임의 다른 레버이거나 AI가 알아서 한다.
- ⭐ **커버리지 0%인 슬롯이 정확히 C5를 붙인 슬롯**(파우 0 · 콘사 0 · 카마라 40 · 오나나 25) —
  커버리지가 낮은 칸에서 **duties가 적합을 이기는 것은 구조적 필연**이다.
- ⛔ **R(관계) 20.3%는 축이 없다 — 세우려다 기각**(obs#123). FDR 생존 0/5 · 대조군 계수 음수.
  재시도하려면 **표본 2~3배가 먼저**이고 기준은 `docs/60-r-axis-prereg.md`를 재사용할 것.
- ⛔ **방향(좌/우) 판별은 GK·CAM·ST에서 원리적으로 불가**(obs#127) — 그 슬롯 커널 변형이 좌우 대칭이라
  그리드를 뒤집어도 적합값이 같다. ✅ **반대로 FB·CB·WM에서는 코사인이 방향을 실제로 본다**
  (2026-08-13 실증: 완비사카 RB **.932** vs 같은 그리드 LB **.022**).

**⚠️ 새 축을 세울 때의 4요건** (obs#32 성공 / obs#95 약함 / **obs#85·#123 기각**에서 역산 — 2승 2패)
1. **독립 출처 2~3개가 같은 방향** — ⭐ obs#123은 확증쌍을 `player_duties` **영문 서술에서만** 뽑아
   이 요건을 **선정 단계에서** 만족시켰다(움직임 자료 미참조).
2. **교란 통제** — obs#95: LB↔RB 원시 **+0.687** → 경기별 평균을 뺀 잔차 **−0.168**. 원시 상관 금지.
3. ⭐ **대조군·사전 성공 기준·검정력을 먼저 정한다** — 없으면 그 추측이 정답처럼 출력된다.
   obs#123은 n이 필요치의 1/2~1/3이라 효과 유무를 **애초에 판정할 수 없었다**(사후에야 알았다).
4. **n · p · 다중비교 상태를 `confidence`에 명시** — 없으면 하드 제약으로 못 쓴다(obs#95가 소프트인 이유).

**📍 규약은 docs로 이관됐다 (2026-08-06)** — HANDOFF는 "지금 무슨 상태인가"만 담고, 재사용 규칙은
아래 문서가 정본이다. 값이 충돌하면 **docs가 이긴다**.

| 필요한 것 | 문서 |
|---|---|
| 슬롯 x 정본표 · **회귀 게이트 11항** · 측정 기준 7항 · 별도 구현 허용 조건 | [docs/20 「슬롯 x 정본표」](docs/20-fc-game-system.md) |
| FC26 대상 시스템 · EA 피치노트(비결정성·숙련·개명) | [docs/20 「EA 공식 피치노트」](docs/20-fc-game-system.md) |
| SofaScore 수집 절차 · **수집 함정 7종** · 좌표/그리드 규약 | [docs/30 「수집 함정」](docs/30-data-rules.md) |
| `git add -A` 금지 · sync/dump 순서 · 툴 편집 사고 사례 | [docs/40 「git 규칙」](docs/40-pipeline.md) |

특히 자주 잊는 3개만 여기 남긴다:
- ⛔ **`git add -A` 금지** — `.claude/settings.json`(Figma PAT)이 있어 푸시가 차단된다. 명시 스테이징만.
  ⚠️ 이 규칙은 **핸드오프 스킬의 wip 보존 절차보다 우선**한다.
- ⭐ **적합값 Δ 0.02~0.05는 EA가 공개한 노이즈 구간**(obs#91) — 그 차이로 인선을 가르지 않는다.
- ⭐ **결손과 0은 다르다**(obs#132) — SofaScore는 0이면 키를 생략한다. `v_event_profile`의 `n`은
  지표별 표본 수가 **아니다**.

**⚠️ 국면 축 상태 (obs#96 → obs#101 종결)**
- 공격 칸은 argmax 7/8 불변·|Δ|≤.049라 **전 국면 평균을 계속 써도 결론이 안 바뀐다**.
- ⚠️ 단 **rest-defense 칸은 저점유가 옳은 기준**(카마라 .629 / .692 / **.759**) — 고점유로 재면
  목적에 부합할수록 깎인다. 축 라벨에 `poss-hi`/`poss-lo`를 병기할 것.
- ⚠️ **경기별 그리드는 (선수, 경기) 단위라 포지션이 섞인다** — 슬롯별 비교 전에 **`lineup_pos` 필터**가
  반드시 선행돼야 한다(obs#96 ②의 자기 정정 사례. `pos_class`는 RB와 RAM을 못 가른다 — docs/30 ④).
