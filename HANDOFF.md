# HANDOFF

## 프로젝트

- 실제 감독 전술을 실측으로 분해해 FC26 전술로 재현한다.
- 대상: Aston Villa/Unai Emery(주), Chelsea/Xabi Alonso, Liverpool/Andoni Iraola, Atlético/Diego Simeone.
- 저장소: `/Users/ad03230205/Documents/tactics`, 브랜치 `main`. DB 정본 `db/tactics.db`
  (`db/dump/`·`site/data/`는 파생물).
- 규약 정본: `CLAUDE.md`, `docs/00-overview.md`. 런북: `.claude/skills/{transfer-watch,match-watch,player-collect}/SKILL.md`.

## 현재 상태

> **2026-09-08 KST** · PC `AD03230205ui-iMac.local` · `main` · 마지막 내용 커밋 **`1274101`** · **✅ origin 푸시 완료**(`ca42991..1274101`).
> ⚠️ **PC 소유권 주의** — 09-07 오전은 `AL02359162.local`(신규 맥, brew 없음·yt-dlp pip --user). 두 맥이 같은 날 번갈아 작업.
> 09-08(이 세션, 스케줄 `-atm-athletic-d3`): ⭐⭐ **ATM 아틀레틱전 D+3로 3일 창 종결** — 전술 유튜브 2편 확보,
> **당일 판정 정정 3건**(obs#452 인과 철회 · 백3 전환 미채택 · 2실점 기점 이동), obs#506~516 · `player_duties` 202~208.
> 🟡 **워킹트리 비어 있지 않다 — 동시 실행 중인 AVL 축 스케줄 세션의 스테이징이 index에 남아 있다**(아래 WIP 절).
> 09-08 낮: 헐전 D+2(`ca42991`, obs#500~505 — 촐라키스 판정 철회).
> 09-07 밤: 1874 채널 규약·첫 수집(obs#499) · xG 혼합 정책 확정(obs#466) · 🔴 스케줄 2건 미푸시 구제(규칙 9).
> 09-05~07: 헐전 종결(report 31) · LIV R3·ATM R4(report 32·33) · 축 검정(obs#461~463) · 포켓 충돌 정정(obs#464) · migration 025(obs#465).

- DB: players **200** · player_matches **4,252** · team_match_stats **70** · match_reports **34**
  (**complete 19 · draft 15**) · match_player_reports **598** · squad_entries **133** ·
  prescriptions **469** · slots **88** · match_game_setups **19** · match_player_prescriptions **306** ·
  transfer_targets **44** · transfer_outgoing **67** · player_duties **208** ·
  player_shirt_numbers **116** · understat_player_matches **5,634** · observations **516** ·
  **teams 37** · **player_evaluations 120** · **transfer_summary 4**(023) · `game_role_focus.movement_kr` **85/85**(024) ·
  ⭐ `team_match_stats.xg_source` **62/70**(025 — 나머지 8행은 `xg_v` 자체가 없다) · `game_system_changes` **18**(FC26 9 · FC27 9, 커널 행 0).
- 회귀: **G1~G13 전항 통과**(2026-09-08 재확인). G13은 09-01 신설 후 **⑸「클럽코드 리그충돌」**이 추가돼 현재 5항이다.
- ✅ **2026-27 여름 이적창 감시 완전 종료** — 09-02 자동 마감 정산(CONFIRMED 11·DEAD 8, obs#394~399) + 사용자 재확인(obs#413) +
  총평·수지 원장 완결(obs#417~421). 미결 4건(에메날로·르마르·바르가스·응게상, ✅~~베일리~~)은 등급 동결, 정기 루틴 없음.
  ⭐ **스쿼드 프리즈는 DB 재작성이 아니라 아키텍처 검증으로 처리했다** — `squad_entries.lh`가 CONFIRMED 이적 후에도 `OWNED`로
  남는 건 정상이다. 모든 화면은 `slot_candidates()`가 `departed`(`transfer_outgoing WHERE likelihood='CONFIRMED'`, 매 export 계산)로
  런타임 필터링하고 G11이 검증한다. ⇒ **미결 건이 확정될 때마다 「등급 갱신 → export → G11 확인 → 공백 시 후임 선발 지정」을 반복한다.**

### 반복해서 사고를 냈던 규칙 (지우지 말 것)

- ⛔⛔ **xG 계열은 한 회차에 같은 스냅샷으로 수집하고 `xg_source`를 함께 채운다**(FotMob matchDetails 한 응답에 전부 있다).
  따로 채우면 Opta 사후 개정 때문에 「오픈플레이 > 전체」라는 불가능한 값이 된다. ⛔⛔ **검산(`xg − xg_op` ≈ PK 개수 × ~0.79)은
  스냅샷 동일성을 증명하지 않는다** — 비야레알전이 검산을 통과하면서 제공사가 갈려 있었다. **제공사는 `xg_source`로 확인**하고
  교차 경기 집계는 `WHERE xg_source='FotMob'`으로 고정한다(정본 `docs/30` ⑧ · match-watch SKILL §3).
- 🔴 **obs#110 솔버 산출을 인용할 때는 obs#111 이후인지 확인한다.** 09-03의 「세트 B(우측 전진) 유일 정합」 답변은
  obs#110 시점이고, 직후 obs#111이 C6를 하드화해 **정본은 좌측 전진**이다.
- 🔴 **커밋만 하고 푸시 안 함 — 두 번째 재발(09-07 밤, 스케줄 세션 2건).** 세션 시작 시 `git status -sb`의 **`[ahead N]`을 반드시 본다** —
  깨끗해 보여도 origin에 없는 커밋이 있을 수 있다(obs#382 유형). 발견하면 내용 확인 후 푸시하고 핸드오프에 기록한다.
- ⛔⛔ **`site/assets/*.js`를 고쳤으면 임포트의 `?v=`를 반드시 올려라.** heatmap 인라인 핸들러는
  HTML/JS에 no-store를 보내지 않는다. **G9가 막는다.**
- ⛔ **curl·WebFetch로 FotMob·SofaScore API를 치지 말 것** — 페이지 컨텍스트 밖에서 막힌다(404/403). 브라우저로 오리진을 연 뒤
  JS 안에서 fetch하면 200이다. **403 보고를 받으면 먼저 브라우저 경로로 재확인한다**(✅ SofaScore는 08-19 이후 재개통).
- ⛔⛔ **지연 발행 전술 분석을 검색엔진 질의로 「0건」 처리하지 말 것**(09-08 실증, obs#516). ATM 아틀레틱전은
  D+1·D+2에 스페인어·영어로 4회 질의해 전부 0건이었으나, 두 영상은 **각각 D+1·D+2에 이미 게시돼 있었다**(인덱싱만 늦었다).
  ⇒ **D+2~D+3에는 유튜브 사이트 내부 검색으로 직접 스윕한다**: `youtube.com/results?search_query=...&sp=EgIIAw%3D%3D`(업로드=이번 주)
  후 JS로 `watch?v=` href 회수.
- ⛔ **`observations.id`를 하드코딩하면 동시 실행 세션과 충돌한다**(09-08 실증). `max(id)` 읽고 INSERT 하는 사이에
  다른 스케줄 세션이 #500~505를 선점해 **트랜잭션 전체가 롤백**됐다(obs 번호 경합 3번째). ⇒ **id 생략(자동 배정)하거나
  `BEGIN IMMEDIATE` 직후 재확인**하고, 리포트·상호참조의 obs 번호는 **INSERT 성공 후에 기입**한다.
- ⭐ **유튜브 자동 생성 자막은 회수 가능하다** — `python3 scripts/yt_transcript.py VIDEO_ID LANG [메모]` → `reports/transcripts/`.
  영상·음성 자체는 시청 불가(화면 전술보드는 자막에 없다). 인용 시 confidence에 **auto-caption** 명기.
  ⏰ docs/30 「듣지 않은 영상은 인용 금지」에서 **「전사를 읽은 영상」의 지위는 아직 미정의** — 규약 갱신은 사용자 판단 대기.
  ⭐ **yt-dlp가 「The page needs to be reloaded」로 막히면** 브라우저 pot 경로 `scripts/yt_transcript_json3.py`(정본 docs/30).

## 자동화

- Codex heartbeat **id=3 「프리미어리그 3팀 경기 수집」**, 매일 08:00 KST (저장소 밖 · 별도 관리).
- ⚠️ **스케줄 정본은 `mcp__scheduled-tasks__list_scheduled_tasks`다** — `CronList`는 다른 시스템이라 0건으로 보인다
  (09-03 오전 세션이 이걸로 「cron 0건」이라 적었으나 오후 직접 조회로 **오판 확정**). 실제 활성:
  `match-watch-weekly`(월 10:06, 다음 09-07) · `weekly-unifi-now-sync`(월 10:03) ·
  ⭐ 헐시티전: 본수집 ✅(`45eb2e1`, report 31) · D+1 ✅(`7c358d1`, obs#481~485) · 잔여 `-followup-d2/d3`(09-08·09 11:00, 활성).
  ✅ CHE R3는 주간 태스크가 잡았다(report 34 `complete`, `b8b3116`). LIV R3·ATM R4는 09-06 수동 세션(report 32·33).
  ⭐ 09-06 밤 신설 일회성 3건 **전부 소진**: `-liv-ipswich-d3` ✅(`db7190f`) · `-atm-athletic-d2` ✅(`5e1ecde`) ·
  **`-atm-athletic-d3` ✅(09-08, `1274101` — 푸시까지 완료)**. ⇒ **세 건 모두 다음 세션에서 삭제 대상**(자기 삭제형 규약).
  🔴 앞선 두 회차는 커밋만 하고 끝났으나 **d3은 사용자 지시로 푸시까지 마쳤다** — 스케줄 프롬프트에 `git push` 확인 문구 점검은 여전히 미반영.
  ⚠️ 09-08은 헐 d2(11:00)·ATM d3(21:00)·AVL 세션이 겹쳤다 — **파일 충돌 0, 그러나 obs id 경합 1건 발생**(위 규칙).
  ⭐ AVL 영상 회차는 이제 **필수 채널 3곳**(docs/30 표)을 조회한다.
- **09-04 정리 완료**: 아스날 팔로업 3건 전부 삭제. ⭐ **규약: 자기를 띄운 스케줄 태스크는 그 세션 안에서 못 지운다** →
  자기 삭제형 일회성은 다음 세션에서 치운다. 소진된 나머지 일회성 7건은 `enabled:false`라 방치해도 무해.

## 최근 작업

### 2026-09-08 — ⭐⭐ ATM 아틀레틱전 D+3로 3일 창 종결 · 전술 유튜브 2편 · 당일 판정 정정 3건 (`1274101`, obs#506~516)

- ⭐⭐ **D+1·D+2가 두 번 「0건」으로 닫은 전술 분석 유튜브 해소** — 발행 지연이 아니라 **검색 방법** 문제였다(위 규칙 신설).
  **Proyecto Hadi**(스페인어 전술, 09-07) · **Atlético Stats**(ATM 전담, 09-06) 자막 전사 2편(618·807 cue). ⚠️ **auto-caption**.
- **당일 판정 정정 3건**: ⑴ ⚠️⚠️ **obs#452 인과 철회**(실측 PPDA 20.11 유지) — 초반 하이프레스는 **아틀레틱 쪽**이었고 **ATM이 측면 전환으로 깼다**,
  테르지치가 그 뒤 블록을 내렸다. 실점 2골은 「전술적 배경이 없다」(obs#506). ⑵ **「C.로메로 = 백3」 미채택**(Río Negro 단일 출처) — 최종은
  **4-4-2 유지 + 요렌테 전진**, ⭐ **요렌테 한 경기 5개 포지션**(CB·RB·윙백·FW·RW, obs#508). ⑶ **2실점 기점은 좌측 수비가 아니라 G.시메오네 피탈**,
  골-골 **인플레이 14초**(obs#512).
- **강화·정밀화**: obs#498 → **최종 5-4-1** 독립 확인 + 전환 목적(obs#507) · obs#495 → **우측 편중은 표류가 아니라 유일하게 작동한 진행 루트**,
  5-4-1이 봉쇄하자 공격 소멸(obs#509) · obs#460 → 그리말도 귀책이 **수비 커버 → 온더볼**(피탈 16)로 이동, 두 축 병존(obs#511) ·
  이강인 **39′ 경고 분기점 2소스 확정**(obs#513) · **「루크만 3분 골대」 확정 기각**(5소스 전부 이강인).
- ⭐⭐ **obs#459에 스페인어권 1차 근거** — 「뛰어난 헤더 마무리가 없다면 그리말도 같은 크로서가 무슨 소용인가」. 단 가설이 ⓒ**「진짜 9번 부재」**로
  재정의돼 **3가설 병존**(ⓐ장신 · ⓑ오프더볼 침투 · ⓒ진짜 9번, obs#510). ⭐⭐ **원정 공격 노선 축 해소** — 리포트 §9가 「서사 근거 0건」으로
  닫은 것을 ATM 전담 분석이 **의도된 새 원정 플랜**으로 규정(obs#514, 신뢰도 중간 = 팬 매체 해석). ATM은 현재 **리그 4번째 다실점 팀**.
- **미도달 확정**(재시도 금지): theobjective·atleticodemadrid.com(봇 검증, 우회 안 함) · liverpool.com(tollbit) · El Correo(⭐ 3회 실패 후
  **오리진 진입 성공**, 본문 **페이월 확정**) · EITB 바스크어(**본문 텍스트 없는 영상 임베드 구조**) · 슬로바키아어·덴마크어(⭐ 신규, 0건).
- ⛔ **감독 발언 3항목 「미확인 확정」 종결**: ①4장 교체 의도 ②로메로 이유 ③포메이션 — 회견에 **질문 자체가 없었고**(obs#493) 09-08 안필드
  프리뷰 회견은 **창 마감까지 미발행**. ⇒ **전술 채널·실측 축으로만 접근 가능**. `player_duties` **7행(202~208)** — ⭐ D+2가 「사용자 판단 필요」로
  남긴 **그리말도 항목을 덮어쓰기 없이 해소**(`UNIQUE(season,player_id,position)`을 `position` 경기 한정 라벨로 우회, 선례 id=197).

### 2026-09-07 밤 ⑵ — ⭐ 1874 : The Aston Villa Channel 규약·첫 수집 + 스케줄 2건 미푸시 구제 (`75f9c36`·`bdb465c`, obs#499)

- **사용자 지시**: 「AVL 영상 수집 목록에 1874 추가」 → docs/30 「AVL 필수 채널」 **2곳→3곳 표**(UTV·The Villans·**1874** `@The1874Podcast`) + SKILL §2-1 동기.
  1874의 가치 = 전술 보드가 아니라 **The Athletic 담당 Jacob Tanswell 정기 출연(기자 1차 발언 원출처, 1~2티어)** — 진행자·팬 패널은 3티어로 분리.
- **첫 수집 7편 전사**(Tanswell 3 · Brassell 1 · 리액션 2 · Lowdown 1) → `player_duties` #161·21·199·98·200·191·121 addendum. 신규 1차 사실:
  ⭐ **가르나초는 와이드 최우선 타깃이 아니었다 — 로저스 협상의 「시장 기회」**(여름 우선순위 = 우측 윙어 = 음바예) ⇒ 좌측 배정은 가용 인원의 결과 가능 ·
  ⭐ **에메리는 왓킨스+잭슨 2톱을 원했고 「잭슨은 왼쪽도 가능」**(obs#446 국면 반응과 정합) · 아브라함 정리 의사 5월부터·「훈련 강도 미적응」·마조>아브라함 ·
  하우드-벨리스 = 구단 「우측 파우 토레스」(1v1·후진 스텝 약점) · 고레츠카 「라인 브레이킹 패서 아님·박스 도착형」(Brassell) ⇒ **dm_dlp 정본과 긴장, 첫 출전 시 boxcrasher 대조**.
- 헐전 리포트는 **미편집** — 09-09 D+3 태스크가 SKILL(3곳)로 전사를 소비하도록 남김. yt-dlp 자막 429 → 45초 재시도로 회수(봇 차단 아님).

### 2026-09-07 밤 ⑴ — ⭐ xG 혼합 정책 확정 · docs/30 ⑧ 성문화 · 화면에 xG 원천 노출 (`04a307a`·`452d793`, obs#466)

- ⭐⭐ **사용자 확정: 스냅샷 혼합 3행은 값을 고치지 않고, 경기를 가로지르는 집계에서 `xg_source`로 거른다**(obs#466).
  대상 3행 = ATM 08-19 말라가 · ATM 08-23 비야레알(제공사까지 다르다) · LIV 08-23 뉴캐슬.
  근거: 두 행은 리포트가 `complete`이고 본문이 값을 인용한다(덮어쓰면 원문·obs 연쇄 수정) + 불변규칙 2 + `xg_source`가 이미 배제 조건을 준다.
  ⇒ **「미해결」에 오래 있던 xG 개정 정책이 이로써 닫혔다** — 저장된 값을 정본으로 두고 추종하지 않는다.
- **정본 위치**: `docs/30` **「⑧ xG는 제공사마다 다른 값이다」**(수집 함정 ①~⑦ 옆). 값 규약 표는 match-watch SKILL §3.
  ⛔⛔ 핵심 교훈: **부등식 검산은 스냅샷 동일성을 증명하지 않는다** — 비야레알전이 `3.60−2.02=1.58≈PK 2개`로
  검산을 통과하면서 실제로는 `xg_o`(SofaScore)와 `xg_op_o`(FotMob)가 갈려 있었다. 그 행이 SKILL §3의 모범 사례였다.
- **화면 노출**(`452d793`): `core/export.py`에 `ts.xg_source` + `match-report.html`에 「xG 원천」 행 — `MIXED`면 ⚠️ 배지 + 비교 금지 캐비앗.
  공유 asset 미변경 → G9 `?v=` 대상 아님. ⚠️ **브라우저 육안 확인 못 함**(무인 세션은 dev 서버 차단) — node로 4입력 렌더 검증.
  **`colspan=2` 긴 문자열 레이아웃은 유인 세션에서 한 번 볼 것.**
- ⚠️ 남은 위험: 게이트는 혼합 **개수만** 고정한다 — **집계 질의가 필터를 걸었는지는 검사하지 못한다**(현재 코드에 교차 경기 xG 집계는 없다).

### 2026-09-07 오전 — ⭐ AVL 필수 유튜브 채널 2곳 규약 + 헐전 D+2 유튜브 26편 전사 (obs#472~480)

- **사용자 지시**: UTVFANCHANNEL·TheVillans를 **빌라 영상 분석 시 항상 포함** → docs/30 「AVL 필수 채널」·match-watch §2-1·메모리.
  영상·음성은 어느 경로로도 보지 않고 자막만 읽는다(계정 불필요·계정 정보는 받지 않는다).
- **전사 26편**(UTV 12 · The Villans 7 · 헐 3 · 기타 4) — 헐전 리포트 **§8-8**. 헐 구단 「Meyler 프리뷰」는 2015년 영상이라 제외.
  수확: ⭐ 스즈키 배급 영어권 최초 구체 서술(「third center half in build-up」, obs#474) · **UCL 명단 23인 — THB·마조·시세 제외**(obs#476) ·
  만잠비 「pocket filler」≠정적 포켓(obs#477) · 풀백 비대칭 = 좌측 와이드 유형 함수 가설(obs#478) · 「맥긴 인버트」 실측 충돌(obs#473).
  `player_duties` 9행에 날짜 태그 블록 **덧붙임**(재작성 아님).
- ⚠️ **obs 번호 경합 재발**(스케줄 세션이 #467~471 선점, `b8b3116`) → **INSERT 직전 `max(id)` 재확인, obs 번호는 INSERT 후 기입**(09-08에 3번째 재발).
- 미해결: 하프타임 폭 조정 주장 2건(obs#480)은 전후반 좌표 부재로 검증 불가 → **다음 경기부터 전·후반 분리 좌표 저장** 제안. 그리스어·아랍어 미탐색.

### 2026-09-05~06 — 헐전 종결 · LIV/ATM 수집 · 축 검정 · ⭐ 포켓 충돌 정정 · migration 025 (`45eb2e1`~`eb435b3`, obs#446~465)

- **헐전 0-0(원정, 점유 74%) — 아스날전이 지목한 판별 경기**(`45eb2e1`): ⭐⭐ **잭슨 false9는 성향이 아니라 국면 반응**(obs#446) —
  아스날 39%에서 st_false9 .723 1위 → 헐 74%에서 st_advanced/Support .632 1위, false9 .368, Δ 노이즈 5배.
  obs#447 **무공 4-4-2 플랫 판정 보류**(이벤트 좌표로 2톱 판정 불가) — 최종라인으로 내려가는 와이드가 **경기마다 바뀐다**(맥긴→헤밍스).
  obs#448 **캐시 뒤 이음새 2경기 연속** + 양 풀백 동시 전진 + 맥긴 winger·캐시 att_wb **동측 → 크로스 0/5**.
  obs#449 ⚠️ **4경기 집계에서 양 풀백이 att_wb/Support로 역전** — 74% 1경기 혼입 의심, **정본·짝짓기 규칙 유지**. 시즌 정본 변경 0.
- **LIV R3 입스위치 0-2 · ATM R4 아틀레틱 0-3**(`9aa92db`) — 두 팀 모두 **압박 급락**(LIV PPDA 5.8→13.96 · ATM 전반 20.11).
  obs#453·455 ⭐ **이라올라 「모든 윙어 양쪽 다 뛰라」 선언 원칙** → obs#353 「학포 우측 이동」은 라운드별 가변으로 정정.
  obs#454 ATM `pos_class` 라벨 통일(집계 8건 신생). **4팀 축 검정**: 「점유 대비 박스 효율」 가설 **기각**(r=+0.23) —
  대신 ⭐ **AVL 유효슛 이상치**(막히지 않은 슛 19개 중 유효 1개, obs#462).
- ⭐⭐ **포켓 충돌 정정**(`9803601`, obs#464) — 「인사이드 포워드 포켓 .81 = CAM 충돌」은 **중앙형 10번 전제**. 현 만잠비 halfwinger와는
  겹침 **.329 최저**(보완). **「playmaker/Roaming = 스위칭」은 오독** — 사이드 스위칭 포커스는 `cam_halfwinger`/Roaming 하나.
  세트 A(insidefwd/B·halfwinger/R·widemid/S) / B(wideplm/A·playmaker/R·widemid/S) — **10번 유형이 와이드를 정한다**(docs/20 규칙 3).
- **migration 025 `team_match_stats.xg_source`**(`eb435b3`) — provenance 정형화 + G12 검사 2종. ⭐ obs#465 = **obs#463 정정**
  (ATM 비야레알전은 행 내부 스냅샷 혼합, 같은 패턴 2행 더). ✅ SKILL §3의 「obs#464」 오인용은 `obs#461·#465`로 정정됐다.

### 그 이전 (압축)

- **09-04 ⑷ FC27 사전 조사**(`9a06e8b`, obs#445, gsc #10~18 — 상세 docs/21) — EA 1차 피치노트 4건 verbatim → FC27 9행:
  Attacking Spatial Awareness(커널 결정성 추가 약화) · Triggered Runs 거리 제한 · AcceleRATE 축소 · PlayStyle 리밸런스 10종+PS+ 상한 3 ·
  **Team Press 수비 3분의 1 무효** · 수동 수비 강화 · 커리어 7포지션. ⛔ **역할·포커스 변화는 미공개** — fut.gg roles(09-18) 전 커널 행 금지.
- **09-04 ⑶ 역할×포커스 85조합 movement_kr**(migration 024) — **정본은 여전히 `description`(EA 원문)**, 커널 값이 바뀌면 해설도 고친다(docs/20 ⑧).
  이름과 실제가 갈리는 조합을 전부 ⚠️로 못 박았다(`cb_bpd`/Build-Up < Aggressive · `fb_fullback`/Defend는 보유 시 백3로 접힘 · `st_target`/A = `st_poacher`/A).
- **09-04 ⑵ FC26 윙 혼잡 회피**(obs#443~444, docs/20 ⑨) — EA 원문 「avoid congestion in the wings」 ⇒ 버그가 아니라 설계.
  ⚠️ **`fb_att_wb`/Support = 복귀 감소, 전진 강화는 Attack**. 짝짓기: 윙어 쪽 = `fb_wingback`/B(.553) · 전진 사이드 = `fb_att_wb` + 안쪽형 와이드.
  대가는 **뱅크 소멸**. 프리셋 미저장, ⛔ 인게임 A/B 미실시.
- **09-04 ⑴ 아스날전 D+3 종결 + 다국어 재점검**(obs#435~442, report 30) — ⭐⭐ 스즈키 롱볼은 **브라이턴전 하이턴오버 19회 대응 처방**(🇯🇵),
  상대 의존이라 시즌 빌드업 축 이동은 오재현. 5축(🇮🇹🇩🇪🇸🇪🇳🇱🇦🇷) 0건 = **「도달 불가」 확정**. 왓킨스 브라이턴전 **출전 거부**(🇩🇪🇫🇷, obs#439).

- **09-03** 스케줄 회차 2건·잭슨 프리셋(`ef81cea`~`d60e6ba`) — 아스날 D+2 정정 4건 · R2 4경기 D+3 종결(⭐ 세비야 감독 오기 정정) ·
  잭슨 프리셋 역할 유지(Δ0.30, 온볼 서사는 PlayStyle 층). **베일리 → 올림피아코스 CONFIRMED**(obs#422~423) — 추적 3축 전부 빗나감.
  ⭐⭐ 교훈: 「미결」 시한을 열린 창으로 잡지 말 것 · 총평에 예상 행선지 금지 · **현지 매체 활발 ≠ 축 생존**.
- **09-02 이적창 사후 정리·평가 축 완결·⭐docs/22 신설**(12커밋, obs#414~421) — G11 선발 공백 2건 충원 · ⚠️ 알렉사 푸리치 `sofascore_id`
  오매칭 폐기(이월) · `transfer_summary`(023) · `player_evaluations` 120행 · SKILL §2-1a 「감독 회견 원문+번역 필수」 ·
  ⭐⭐ **docs/22 = 온볼 층**, 커널 유사도에 넣지 않는다. ⚠️ 워싱턴(CHE) 이적료 3중 병기 미해소.
- **09-01 AVL 아스날전 신규 수집**(report 30, obs#350·383) — 스즈키·잭슨 데뷔. ⭐⭐ **SofaScore 403 재발** → WhoScored/Opta
  `matchCentreData`(인앱 오리진) 대체, 좌표 관례 동일 선검증.
- **09-01 무결성 정리 4연쇄**(obs#368~374) — 전부 「FK는 성립하는데 조용히 이중화된」 부류: 에수구 2-id 병합 · 이중 기록 78쌍 ·
  **`team_code` 739행** 재배정 · **G13 신설**(5항). 🔴 파급: **완비사카·잭슨·루제리 소속팀 실측 0**. ⭐ **team_code 오류는 게이트를 무력화한다**.
  같은 날 transfer-watch 마감 — ⭐⭐ **아하노르 = 아탈란타발 팰리스 임대 + 첼시 선계약(2027-07-01)**, 완전영입 서술은 오답.
  R3 D+1(obs#375~382) ⚠️ **동시 실행 회피 규칙 실증**: 타 세션 미커밋 DB가 보이면 `.db`를 내 커밋에서 뺀다. **잠금은 여전히 없다.**
- **08-30~31 PL Round 3**(`e175143`) — CHE 4–3 브라이턴(**PL 사상 최저 점유 26%인데 xG 2.93:1.41**) · LIV 2–2 포레스트(**69% 점유에
  빅찬스 1:5 — 이라올라 이식의 첫 반증**) · ATM 3–1 세비야. ⭐ **압박 상수 3건**(obs#350~352): 알론소 저압박(→22.67) ·
  이라올라 고압박(→**5.76, 최안정 상수**) · 시메오네 하이브리드(→8.26, **저블록 서사와 어긋난다**).
  ⚠️ **병렬 2회 실행으로 같은 4경기 중복 수집**(obs#354) — 선행 세션분이 정본.
- **08-31 빌라 영입 17축 전수** — 커널 회귀 14/14 재현 · ⭐⭐ **음바예 regime_id 3→1 정정**(불변규칙 7 위반, 적합 0.94→**0.878**).
- **08-24~30** ⭐ 사용자 확정 원칙(obs#348): **개별 경기 구현은 그 경기 전술의 게임 재현이므로 분석 내용을 전부 반영한다**(시즌 정본은
  경기 누적으로만) · 이벤트 축 수집기 · 적합값 드리프트 20→8→4 · 좌우 이봉 7명, **전후 이봉 0건**(obs#342) · ATM 4-4-2 슬롯.

## 진행 중 작업 (WIP)

**없음.** ATM D+3 `1274101` 커밋·푸시 완료, origin/main 일치.

⚠️ **단 이 세션 중에 AVL 축 세션이 동시 실행됐다** — 핸드오프 작성 도중 워킹트리에 그쪽 스테이징
(`db/tactics.db` + 브뤼헤 프리뷰 230줄)이 보였고, 그 세션이 `1211c43`(브뤼헤전 UCL MD1 프리뷰 · obs#503 고레츠카 결장 사유 정정)로
**스스로 커밋·푸시해 해소**됐다. 내 커밋은 그보다 먼저였고 그 시점 트리는 깨끗했다 — **순서상 오염 없음(확인함)**.
⇒ ⭐ **다음 세션은 `1211c43` 내용을 먼저 확인**할 것(내가 검증하지 않은 커밋이다).

## 다음 할 일

1. **P1 · obs#449 ⓐ/ⓑ 판별 — 양 풀백 att_wb 집계 역전이 체제 변화(ⓐ)인가 국면 혼입(ⓑ)인가.** 필요 조건: 점유 국면별
   (@dom/@tight) 분리 집계가 **버킷당 2경기**. 74% 표본은 헐전 1경기뿐 → 다음 우세 경기 후 판정. 그때까지 **obs#443 짝짓기 규칙·
   정본(좌 전진·우 절제) 유지**. 사용자 인게임 A/B 체감(윙어 쪽 wingback/B · 반대쪽 att_wb/**Attack**)은 여전히 미수집.
2. ⭐ **P1 · 09-09 안필드(리버풀·UCL, ATM R1) — 아틀레틱전 진단 4건의 직접 판별 경기다.** ⑴ **가설 ⓒ 우선 검증**: 알바레스 선발 예상 →
   **박스 터치·빅찬스가 회복되는가**(ⓐ장신은 쇠를로트 결장 확정으로 09-13로 밀렸다). ⑵ **obs#514**: PPDA **15 대역+60%대 점유**면
   원정 공격 노선, **8 대역 복귀**면 부산물 + 「촐로의 철회」. ⑶ **obs#460/#511**: **그리말도 제외 + 한츠코 좌측**이 실제로 나오는가
   (2소스 예상이나 상충 소스도 있어 obs#515는 결과 판정에 쓰지 않는다). ⑷ **감독 발언 3공란의 마지막 창구**.
   ⚠️ 확정 결장: 쇠를로트(근육) · 아르나우 오르티스(유럽대회 정지).
3. **P2 · 아틀레틱 5-4-1 전환 시각 미확정** — 2소스 모두 분(分)을 명시하지 않아 obs#498·#507이 구간 분할에 못 쓰인다.
   ⭐ 남은 방법: WhoScored 이벤트 타임라인으로 **평균 위치 변화 시점을 역산**.
4. **P1 · 헐전 D+1~D+3 추적**(09-07·08·09 자동, ✅ 유튜브 D+2 26편은 §8-8로 선반영) — obs#447 「4-4-2 플랫」 보류라 **국면 태그 영상·헐 관점**이 핵심.
   Squawka 「6-2-2」 원문 verbatim 미확보(403) — 재시도. ✅ ~~CHE R3 미수집~~ → 주간 태스크가 잡았다(**report 34 `complete`**, `b8b3116`).
   ⭐ 헐전이 남긴 재검증: 헤밍스 LM fit .613(위치 분산) — **wm_wideplm 대안** 검토(산출이 안쪽 진입에서 나왔다) · 부엔디아 CAM이
   classic10/Attack .879(정본 playmaker/Roaming과 다름) — 10번 유형에 따라 와이드 세트 A/B가 갈린다(obs#464).
5. **P1 · 미결 4건 개별 처리**(에메날로·르마르·바르가스·응게상, ✅~~베일리~~) — 실효 시한 도래분부터. 절차는 「현재 상태」 마지막 문단.
6. **P2 · 알렉사 푸리치(ATM) `sofascore_id` 재탐색**(obs#416, 오매칭 적발·폐기만 하고 이월) · **ATM 11명 map25
   전량 결손** — `player-collect` 필요, 값을 발명하지 않기로 확인됨(obs#415).
7. **P2 · obs#353 후속** — 학포 RM 배치로 「검증된 LM이 학포뿐」 리스크 재판정. ⭐ 09-02 **무뇨스 LIV RM 선발 지정**으로 전제가 바뀌었다(무뇨스 좌측 표본은 오사수나 — 불변규칙 7 캐비앗 유지).
8. **P2 · 경기 전용 처방 재판정을 match-watch 표준 파이프라인에 넣을지 확정** — R3 4경기에서 반복 적용됐다.
9. **P2 · 완비사카·잭슨·루제리의 소속팀 실측 0** — R4 이후 클럽 표본이 쌓이면 `avg_positions`가 되살아난다.
   그때까지 이 셋은 **히트맵 A(실측) 칩이 없는 상태**다.
10. **P3** · 병렬 중복 실행 가드 미수립(obs#354, 데이터 재발 감지는 G13) · 겨울창(2027-01) transfer-watch 재개는
   사용자 판단(런북은 저장소에 있어 스케줄만 만들면 된다) · 그리말도(ATM) 실측 채워지면 슬롯 기하·fit 재검증.

## 미해결 — 판단이 필요한 것

1. ✅ **[09-07 밤 종결] xG 개정 정책** — 사용자 확정: **저장된 값을 정본으로 두고 개정을 추종하지 않는다.**
   provenance를 `xg_source`에 기록하고 교차 경기 집계에서 거른다(obs#466 · docs/30 ⑧). 혼합 3행은 그대로 둔다.
2. ⏰ **ATM 포메이션 표기 충돌 — 4-4-2 쪽으로 기울었으나 미해소 존속.** 비야레알전에서 우리·El Desmarque·COPE는 **4-4-2**,
   **Infobae는 「4-3-3 de Simeone」**이었다(obs#333의 전제와 직결). ⭐ **09-08 진전**: ATM 전담 분석이 아틀레틱전 최종 포진을
   **4-4-2로 명시**하고(obs#508) 09-09 예상 XI도 **4-4-2 유지** ⇒ obs#454는 **강화 방향**. ⛔ 그러나 **감독 발언은 3일 창 내내 0건**이라
   확정 근거가 없다. ⭐ 실측 기하가 4-1-4-1에 더 가까워 처방을 이관하지 않은 판단은 여전히 정합적이다.
   ⚠️ 별건: 빌드업에서 「**salidas de tres**+라인 간 3명」 서술 — **보유 국면 기하는 별도 축**으로 본다.
3. ⏰ **FC27 커널 대기** — 사전 조사·준비물은 09-04 완료(obs#445, 「09-04 ⑷」). 잔여 트리거: **09-10** EA 전체 DB(PlayStyles) →
   **09-18** fut.gg `/api/fut/roles/` FC27 → `migrate_fc27.py --roles --check` → `EXPECTED["FC27"]`·gates 앵커 **새 행** → `--apply`(승인).
   ⛔ 역할 목록 변화는 **미공개** — 확정 전 커널 행 금지. ⚠️ 게임스탯 잔여 결손: **고레츠카 0행**(무소속이라 fut.gg에 없다 —
   결손이지 미출시 아님, obs#362) · **35속성은 완비사카·음바예 2명**(obs#364).
4. ⏰ **`transfer_targets` 아하노르 window 표기** — 「2026-summer 완전이적」이 아니라 **2026-summer 선계약 /
   2027-07-01 등록**. 분리 표현할 스키마 자리가 없어 `rationale`에만 적었다.

## 데이터 수집 상태와 결손

- 대량 수집: `collect_fotmob_players.py` · `collect_understat_shots.py` · `collect_event.py`(이벤트 축).
- 읽기 전용 진단: `check_fit_drift.py` · `check_side_bimodality.py` · `check_height_bimodality.py` —
  ⚠️ 셋 다 **진단만 한다**(그리드 재적재·`pos_only`·처방 변경은 사람이 판단). ⭐ `test_g13_regression.py`
  (결함 4종 합성 주입 + 클럽월드컵 오탐 검사) · ⭐ `db_diff.py`(스냅샷 대비 행 단위 대조 — **NOT NULL→NULL
  전이를 따로 센다**. 게이트도 FK 검사도 「값이 조용히 지워졌다」는 못 잡는다).
- ⛔⛔ **검색엔진 연도 혼입 주의.** 같은 상대·같은 8월·유사 스코어의 전년도 경기는 발행일을 반드시 확인한다.
  ⭐ **엔진 요약 자체가 오염되는 유형**도 있다(과거 시즌 사건을 현재 확정 사실로 제시) — **엔진 요약은
  근거로 채택 불가, 개별 URL 실물 확인만 사용.**
- ⚠️ **verbatim 인용은 요약 경유 시 열화된다.** 실질만 채택하고 인용문은 원문 확보 전까지 쓰지 않는다.
- Understat은 빅5(+RFPL)만(챔피언십·에레디비시·리가2 없음). Sofifa 35속성/playstyles·FBref 12축은 403 결손.
- ⏰ **AVL LM/RM 슬롯 x 재검토는 표본 부족으로 보류**(obs#349). 재시도 조건: 정상 경기 누적 + 해당 경기 `cells` 수집.
- ⭐ **1차 소스 메모**: `laliga.com/clubs/{club}/transfers`가 등록 기준 1차 소스로 유효(`atleticodemadrid.com`은
  403). 단 날짜가 **행정 등록일**이라 구단 발표일과 다를 수 있다.
- 결손과 0을 구분한다. `docs/30-data-rules.md` 규약을 따른다.

## 고정 작업 규칙

1. 시작 시 `git status --short`. 로컬 변경이 있으면 fetch/pull하지 말고 내용부터 확인한다.
2. 깨끗할 때만 `git fetch origin && git pull --rebase origin main`.
3. 다른 PC/세션 변경을 버리거나 덮지 않는다. 특히 `db/tactics.db`는 충돌 시 기계 병합 금지.
4. DB 변경 뒤 반드시: `.venv/bin/python scripts/export.py` → `scripts/db_dump.sh` → `.venv/bin/python scripts/gates.py`
5. `git add -A` 금지. `db/tactics.db`, 관련 `db/dump/`, `site/data/`, `reports/`, 문서를 명시 스테이징한다.
6. push 전 `origin/main` 이동 여부를 다시 확인한다.
7. 시즌 집계는 45분+·hit_points 15+만 사용하되, 경기 리포트는 짧은 교체 포함 실제 출전자 전원을 기록한다.
8. match 전용 fit/전술은 시즌 `prescriptions`/`team_tactic_setups`에 자동 병합하지 않는다.
9. ⚠️ **스케줄 세션이 돈 뒤에는 `git log`와 `git status`를 함께 본다.** 2026-09-01에 D+1 세션이 **커밋만 하고
   푸시하지 않은 채 종료**했다(obs#382). 또 **다른 세션의 미커밋 DB 변경이 보이면 `db/tactics.db`를 내
   커밋에서 빼라** — 그쪽이 export·dump를 안 돌렸다면 dump·site/data가 어긋난다(불변규칙 5 위반).
10. **파괴적 정리 전에는 `python3 scripts/db_diff.py --snapshot`으로 스냅샷을 뜬다** — 09-01 정리 4연쇄에서
   스냅샷 대비 행 단위 대조가 **「새로 NULL 된 행 0」을 증명하는 유일한 수단**이었다.
11. HANDOFF는 300줄 이하로 유지한다.

## 핵심 방법론

- 그리드 인코딩·집계·커널은 `core/`만 사용한다. 재구현 금지.
- 좌표: x는 공격 방향, y가 낮을수록 오른쪽. `cells_from_points` → `encode` → `Kernel.best_fit_slot`.
- 역할군 argmax 전에 슬롯 유형 필터가 필수다.
- 적합값 Δ 0.02~0.05는 EA가 공개한 노이즈 구간이므로 그 차이만으로 인선을 바꾸지 않는다.
- 히트맵은 요구 역할의 일부만 설명한다. duties·1차 발언·전술 영상이 명확하면 낮은 fit보다 우선할 수 있다.
  ⭐ 단 **경기 프리셋의 역할 코드는 그 경기의 위치 실측을 따른다**(09-03 잭슨 판정) — 서사가 뒤집은 것이 **온볼 기능**이면
  그건 PlayStyle 층(docs/22)이고, 커널 Δ가 노이즈 구간(≤0.05) 밖이면 서사로 역할을 덮지 않는다. 시즌 정본 판별은 별개.
  ⭐ **풀백 전진은 같은 쪽 와이드 역할이 결정한다**(docs/20 ⑨, EA 「open space in front」) — 윙어 뒤 풀백은 wingback/B, 전진은 C6 해방 사이드에서만.
  ⭐ **10번 포켓 충돌은 CAM 유형의 함수**(obs#464) — halfwinger 옆엔 insidefwd가 보완(.329), playmaker/classic10 옆엔 wideplm/widemid.
  사이드 스위칭 포커스는 `cam_halfwinger`/Roaming 하나(playmaker/Roaming은 중앙 회랑 자유). **와이드는 10번 유형을 먼저 정한 뒤 고른다.**
- 단일 경기·퇴장·저점유 같은 교란은 confidence와 보고서에 함께 쓴다.
- ⭐ **불변규칙 7(팀 축 혼선)은 실제로 세 번 터졌다** — 런북 슬롯 x 표(08-14·08-24) · 음바예 regime_id
  오배치(obs#355) · **`team_code` 739행 오배정**(obs#372·373). **타 팀 실측은 출처 팀·체제를 반드시 명기.**
- ⭐ **불변규칙 10(다국어)의 최고 실증**(09-01): 아하노르 거래 구조는 **이탈리아어에만**, 무드리크 £75m 매수옵션은 **우크라이나어에만**,
  응게상의 미충족 선행조건은 **프랑스어에만** 있었다 — 영어권만 봤으면 **거래 형태 자체를 오독**했을 건이다.
- ⭐⭐ **불변규칙 10의 운영 형식**(09-04 확정, obs#441·442) — 「다국어를 했다」는 자평은 믿지 않는다.
  ⑴ **선발 11명의 국적·경유 리그를 체크리스트로 만들어** 언어축을 강제한다(화제성을 따라가면 감독 모국어와 최대 화제 선수 축만 훑는다).
  ⑵ **언어축별로 기대 산출물이 다르다** — 감독 모국어·최대 화제 선수 축은 「경기 분석」, 나머지 국적 축은 **「이적 서사·배경」**에서 수확이 난다.
  ⑶ **「0건」은 구조적 사실일 수 있다**(🇸🇪 매체가 린델뢰프를 상시 커버하면서도 이 경기 기사를 안 썼다) —
  ⛔ **그러나 당일 「0건」은 이제 세 번 반전됐다**(D+1 유튜브 · D+2 Coaches' Voice · **D+3 전술 유튜브 2편, 09-08**).
  ⇒ **경로·검색어를 바꾼 재확인 전에는 종결로 적지 않는다.** ⭐ 특히 **유튜브는 검색엔진이 아니라 사이트 내부 검색으로 확인**한다(obs#516).
  ⑷ ⭐ **언어권이 저밀도라는 것도 결과다** — 슬로바키아어(한츠코)·덴마크어(율만)는 09-08에 처음 수행해 **0건/통신사 단문**으로 확정했다.
  ⇒ 그 축은 다음 라운드에서 **우선순위를 내리고 사유를 남긴다**(무한 재시도 금지).

## 참고 문서

- `docs/20-fc-game-system.md`: 슬롯 x, 역할·포커스, 게임 구현 규칙, **⑧ movement_kr 해설 · ⑨ 윙 혼잡·풀백 짝짓기**. · `docs/30-data-rules.md`: 수집·좌표·표본·결손 규칙.
- `docs/40`: DB/export/dump/git 파이프라인. · `docs/50`: 이적 등급·보존 정책. · `docs/60`: 새 축 검증과 통계 기준.
  · `docs/22`: PlayStyles·스탯·조작(온더볼 축) + §6 [FC27]. · `docs/21`: FC27 온보딩·사전 조사·트리거.
