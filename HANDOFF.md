# HANDOFF

## 프로젝트

- 실제 감독 전술을 실측으로 분해해 FC26 전술로 재현한다.
- 대상: Aston Villa/Unai Emery(주), Chelsea/Xabi Alonso, Liverpool/Andoni Iraola, Atlético/Diego Simeone.
- 저장소: `/Users/ad03230205/Documents/tactics`, 브랜치 `main`.
- DB 정본: `db/tactics.db`. `db/dump/`와 `site/data/`는 파생물이다.
- 규약 정본: `CLAUDE.md`, `docs/00-overview.md`, 세부 문서는 `docs/`.
- 정기 작업 런북: `.claude/skills/transfer-watch/SKILL.md`, `.claude/skills/match-watch/SKILL.md`, `.claude/skills/player-collect/SKILL.md`.

## 현재 상태

> 마지막 갱신: **2026-08-25 밤 KST** · 작업 PC `AD03230205ui-iMac.local` · 브랜치 `main` ·
> 마지막 커밋 `f05010f`(origin/main과 일치, push 완료). 이 문서 자체의 커밋은 그 다음에 얹힌다.

- DB: players 190 · player_matches **4,074** · team_match_stats **61** · match_reports **25**
  (**complete 10 · draft 15**) · match_player_reports **457** · squad_entries **126** ·
  prescriptions **408** · **slots 88** · match_game_setups **10** · match_player_prescriptions **164** ·
  transfer_targets **38** · transfer_outgoing **54** · player_duties **190** ·
  observations **345**(최신 obs#345).
- 회귀: **G1~G12 전항 통과**(2026-08-25 마지막 확인). ⭐ 이날 **게이트 3건 신설** —
  G12 `orphan_preset_slots`(프리셋 pos_label이 그 팀 slots에 없으면 실패) ·
  G12 `xg_openplay_violations`(오픈플레이 xG > 전체 xG면 실패) ·
  G9 무버전 JS 임포트 검사(공유 모듈은 `?v=` 필수).
- ⭐ **스키마 확장 2건**(2026-08-25): `match_reports.overall_assessment`(migration 021 — 총평·매체 평가,
  현재 **6/25 = 공식전 전수**) · `team_match_stats.xg_op_v/o`·`blocked_v/o`(migration 022 — 오픈플레이 xG·막힌 슛, **5경기**).
- ⛔⛔ **xG 계열은 한 회차에 같은 스냅샷으로 수집한다** — `xg_v`·`xg_o`·`xg_op_*`를 따로 채우면
  **Opta 사후 개정 때문에 「오픈플레이 > 전체」라는 불가능한 값**이 된다(2026-08-25 CHE 실발생, Δ+0.54).
  FotMob matchDetails 한 응답에 xG·open play·set play·non-penalty·xGOT·Blocked shots가 전부 있다.
  검산법: `xg − xg_op` = 세트피스+PK이고 **PK 개수 × ~0.79**와 대조한다. 규칙 정본은
  `match-watch/SKILL.md` §3. ⚠️ **G12는 부등식을 깨는 심한 경우만 잡는다**(AVL은 Δ−0.02로 통과했다).
- ⭐ **적합값 드리프트: 차이 20 → 8 → 4**(obs#316 → obs#330). 좌우 이봉 분포 전수 점검 완료 —
  아래 「종결된 항목」 참조.
- ⛔⛔ **`site/assets/*.js`를 고쳤으면 임포트의 `?v=`를 반드시 올려라.** launch.json의 heatmap
  인라인 핸들러는 **HTML/JS에 no-store를 보내지 않는다**(그 파일 상단 주석). 2026-08-25에 이것 때문에
  `decodeMap` 수정이 브라우저에 도달하지 못해 같은 버그를 두 번 보고받았다. 이제 **G9가 막는다**.
- ⛔⛔ **영상·음성은 시청·청취할 수 없다**(2026-08-25 확인, obs#345). 「회견 영상 전사」류 작업은 **수행 불가**다.
  가능한 대체 수단 2개: ⑴ **유튜브 챕터 목록**(텍스트 메타데이터 — 브라우저로 engagement panel에서 추출) ·
  ⑵ **텍스트 매체가 인용한 발언**. ⚠️ 자동 생성 자막은 패널이 렌더링되지 않아 **회수 실패**한다.
  ⇒ docs/30 「듣지 않은 영상은 인용 금지」를 지킬 것 — **챕터 제목은 「다룬 주제」로만 쓰고 발언 내용으로 확장하지 말 것.**
- ⛔ **curl·WebFetch로 FotMob·SofaScore API를 치지 말 것** — 페이지 컨텍스트 밖에서 막힌다(404/403).
  **브라우저(Playwright 또는 in-app 브라우저)로 해당 오리진 페이지를 연 뒤 `page.evaluate`/JS 콘솔
  안에서 fetch**하면 200이다. ⭐ 2026-08-25 확인: SofaScore API가 **서브에이전트(WebFetch) 컨텍스트에선
  403**이었지만 **메인 세션 브라우저(sofascore.com 오리진)에선 200**이었다 — 사이트 차단이 아니라
  호출 경로의 문제이므로, 403 보고를 받으면 먼저 브라우저 경로로 재확인한다.

### 2026-08-23 PL Round 1 수집

| 팀 | 경기 | 핵심 실측 | 상태/리포트 |
|---|---|---|---|
| AVL | Brighton 4–0 Aston Villa | 점유 27%, xG 0.31–3.67, 슛 6–21, PPDA 24.69. João Gomes 40′ 퇴장 | `draft` · `reports/match-watch/2026-08-23-avl-brighton.md` |
| LIV | Newcastle 2–2 Liverpool | 점유 61%, xG 2.98–1.58, 슛 27–13, PPDA 5.95. Gakpo 52′, Szoboszlai 90+9′ PK | **`complete`** · `reports/match-watch/2026-08-23-liv-newcastle.md` |
| CHE | Fulham 2–3 Chelsea | 점유 38%, **xG 2.24–1.33**(오픈플레이 **1.96**–1.28 · 세트피스 0.28 · **PK 0**), 슛 18–14, 막힌슛 7, PPDA 20.18. Palmer 1G1A | **`complete`** · `reports/match-watch/2026-08-24-che-fulham.md` |
| ATM | 비야레알 2-2 (라리가 R2, 홈) | xG 0.78–3.60, 76′ DOGSO 퇴장, 주전 첫 가동 4-4-2 | **`complete`** · `reports/match-watch/2026-08-23-atm-villarreal.md` |

✅ **CHE 풀럼전(matchId 5795372) 전체 적재 완료.** FotMob 팀·선수 실측 + WhoScored/Opta 1,659 이벤트로
출전 16명 좌표/map25와 PPDA를 만들었다. 실제 3-4-2-1에서 Palmer–João Pedro–Rogers 전원이 득점에 관여했고,
Lacroix는 CCB에서 박스까지 올라가 도움을 기록했다. ✅ **08-25 D+1 심층으로 `complete` 승격**(obs#340·344) — 전술 메커니즘·선수 평가·총평·xG에 이어
**공식 회견본(chelseafc.com + BBC MOTD)** 까지 확보했다. **남은 결손은 클럽 전술 영상 1건이고 비차단**이다.
다음은 08-27 EFL컵(루턴 홈, id 6005665) · 08-30 브라이턴 홈.

**D+N 창 정정** — 경기일이 08-23이므로 D+1은 **08-24**(완료)다. 본문에 08-25로 적혀 있던 것은
오기이며 AVL 리포트에서 이미 정정됐다. 남은 창은 **D+3 = 08-25~26**.
D+2는 **08-24 21시에 조기 실시 완료**, 08-25에 D+3 정기 검색도 완료했다.

### 이번 경기 판정

- AVL: 50분간 10명이어서 PPDA 24.69와 Deep/38은 감독 의도보다 경기 국면의 영향이 크다.
  Buendía ST는 공백을 메운 응급 배치이며 시즌 처방으로 병합하지 않는다.
- LIV: 61% 점유·27슛에도 오픈플레이 xG는 0.96, 막힌 슛은 16개다. 공격량과 박스 공략의 질을 분리한다.
- LIV 두 실점은 모두 공격 진영 소유권 상실 뒤 전환에서 발생했다. 이라올라 이식의 잔여수비 리스크는 유지한다.
- Víctor Muñoz는 교체 27분에 마지막 PK를 유도했다. Rio Ngumoha와 RM 선발 경쟁을 다시 연다.
  ⭐ **D+2에서 「직접 맞교체」로 확정됐다** — 63분 무뇨스 IN ↔ 응구모하 OUT(FotMob 이벤트 원본).
  같은 슬롯 수행 대비라는 간접 근거가 **감독이 그 자리에서 바꿨다**는 직접 근거가 됐다.
- Ryan Gravenberch는 Gakpo 골을 도왔지만 두 실점 전 소유권 상실에도 관여했다. Roaming 피벗의 양면성 표본이다.
- ⭐ **LIV D+3에서 Coaches' Voice 전술 보드와 Between The Posts 초록을 확보했다(obs#321).**
  Wirtz·Szoboszlai가 뉴캐슬의 선수지향 마커를 바깥으로 끌고 Gakpo가 중앙으로 들어간 동점골 구조가 확정됐다.
  지연 전문 분석·공식 회견·전 선수 처방이 갖춰져 `complete`로 승격했다.
- **AVL D+3은 팟캐스트 4건의 공개 설명을 수확했지만 전술 영상·전문 블로그 0건**(obs#322).
  오디오를 듣거나 전사하지 않았으므로 새 전술 문장은 채택하지 않았고 `draft`를 유지했다.
- **CHE**: 38% 점유에도 **xG 2.24**(오픈플레이 1.96)·18슛·박스 터치 37로 전환 효율이 높았다(obs#323 · xG는 08-25 D+2 스냅샷).
  반면 Fulham 620패스·Sánchez goals prevented -0.78로 중원/GK 리스크가 남았다(obs#325).
  ⭐ **[08-25 D+1 심층 · obs#340] 백3의 실제 메커니즘 = 리스 제임스의 가변 하강** — 그가 미드필드에서
  상습적으로 백3로 내려가 아체암퐁·라크루아 두 CB를 전진하게 풀어 준다(포파나 출장정지가 원인,
  포메이션은 직전 친선 3경기 모두 3-4-2-1). ⇒ 「라크루아 전진이 설계인가 단발인가」가 **「설계」로 해소**됐다.

## 자동화

- Codex heartbeat **id=3 「프리미어리그 3팀 경기 수집」**, 매일 08:00 KST.
- 매 회차 순서: 기존 draft 리포트 D+1~D+3 점검 → 새 종료 경기 확인 → match-watch 전체 파이프라인.
- 새 경기/후속 근거가 없으면 저장소를 변경하지 않는다.
- 이적 감시는 cron `transfer-watch.sh`가 09:00/21:00 각 1행. macOS TCC/marker probe 검증 완료.

## 종결된 항목

### 2026-08-25 세션 (transfer-watch 09시 + Dutch 스윕, match-watch 사이드 이봉 점검)

- ✅ **transfer-watch 09시 회차**(커밋 `d427b8d`) — 레앙 신규 MEDIUM-HIGH(실측 wm_insidefwd/Balanced **0.834**) ·
  각포 신규 유출 MEDIUM-HIGH(+네덜란드어 추가 스윕으로 원발굴자 Boualin 확인) · 파르도 강등 · 알바레스 부분 철회 ·
  ⭐ **원장 결함 삭제**: ATM 그린우드 행이 **우리 `player_tenures`로 직접 반증**됐다(ATM 시즌 없음).
  전문은 `reports/transfer-watch/2026-08-25.md`.
- ✅ **좌우 이봉 분포 전수 점검(obs#328~330)** — 40명 중 **7명 이봉**, 4명은 전체 집계가 사이드 능력을
  심각히 과소평가했다. **린델뢰프 0.75 → LCB .952/RCB .929.** `squad_entries` 6행 갱신 +
  `prescriptions`에 `measured:side:L/R` 12행 신설(UNIQUE 제약상 한 슬롯에 좌우 두 행 불가).
  무뇨스는 사용자 지시로 **LM→RM 재판정**(분량보다 최근 기용 우선). **파생 발견: LIV LM이 학포 1명뿐이다.**
  커밋 `a626c77`·`a24d0ad`·`f6c5116`.
- ✅ **응구모하(pid=114) `squad_entries` 결손 채움(obs#331)** — 개막전(08-23 뉴캐슬) RM 선발인데
  행이 없었다. 같은 좌우 이봉 패턴(친선 4경기+PL 1경기, hp≥15)이나 무뇨스보다 판단이 단순했다 —
  분량(우측 170분>좌측 90분)·최근기용·유일한 공식전 표본이 전부 우측을 가리켜 **RM wm_winger/Attack
  .812**를 그대로 채택. 좌측(LM .837·n=2·프리시즌뿐)은 `measured:side:L`에 보존. 커밋 `9e1a262`.
- ✅ **경기 재현 프리셋 칩 겹침 — 원인 3개 전부 수정(obs#332)** 커밋 `b72b778`·`79c158e`.
  좌표 JOIN이 실패하면 `left:undefined%`가 되고 CSS가 무시해 11명이 겹친다(조용히 깨진다).
  ⑴ formation 문자열 불일치(`'4-2-3-1'` vs slots `'4-2-3-1 Wide'`) ⑵ 그 팀에 해당 포메이션 없음
  (ATM) → `core/export.py`에 **기하 폴백** ⑶ ⭐ **존재하지 않는 슬롯으로 산출**(CHE 피벗이 LDM/RDM인데
  CHE엔 없다 — 역산으로 `LDM 50·RDM 61` 출처 특정, LCM/RCM으로 재산출).
  ⇒ **G12에 `orphan_preset_slots` 신설**(폴백이 ⑴은 흡수하지만 ⑶은 못 잡는다).
- ✅ **ATM 4-4-2 슬롯 신설(obs#333)** 커밋 `e1a720b`. 근거: R1 Opta가 4141→**442(61~96분)**, R2는 선발부터 4-4-2.
  기하는 ATM 자체 실측만(불변규칙 7). ⭐ **공유 pos 7개의 x를 4-1-4-1과 완전히 동일하게 넣어 커널이 결정적**이다
  (CHE 비결정 결함의 원인은 포메이션 개수가 아니라 **pos별 x 불일치**였다). 저장 fit 재산출 불일치 0.
  ⛔ **report 24 처방은 이관하지 않았다** — 측정이 반대를 가리킨다(명목 4-4-2 / 실측 4-1-4-1 기하).
  ⚠️ **[08-25 추가] 원천 R1이 로테이션 XI였다** — 「측면은 칸테라의 몫」이라 x값이 1군 기하가 아닐 수 있다(11행에 캐비앗).
- ✅ **경기 히트맵이 '전체 출전자'에서 안 나오던 버그(obs#334)** 커밋 `e1a720b`·`c4805a4`.
  1차 원인은 **`decodeMap(null)`이 TypeError를 던진 것** — 호출부가 `.filter(Boolean)`로 걸러내게 쓰여 있었는데
  `map()`에서 던져 렌더가 중단됐다(브라이턴전은 16명 중 15명이 유효 히트맵을 갖고도 0명분이 그려졌다).
  ⛔ 고쳤는데도 비었다 — **브라우저가 구 kernel.js를 캐시**하고 있었다. `?v=` + **G9 검사**로 닫았다.
  진단 단서는 **캡션이 셀렉터와 어긋난 것**이었다 ⇒ 캡션을 그리기 전에 쓰도록 순서를 바꿨다.
  **교체 선수 히트맵 8건 신규 수집** · 기본값 '선발 11명' · 드롭다운 공식전/프리시즌 분리.
- ✅ **경기 리포트에 「총평·매체 평가」 축 신설(obs#335)** — `match_reports.overall_assessment`(migration 021).
  전술 4필드가 다 전술 축이라 매체 총평·평점·여론을 넣을 자리가 없었다. **현재 6/25 채움**(22·23·24·25·21·1).
  ⭐ 교차 검증 3건(AVL 매체 인용 수치가 우리 값과 정확히 일치 · CHE 산체스 1/10 ↔ goals prevented −0.78 ·
  LIV 이라올라 회견 ↔ obs#326 동일 원천). ⚠️ 신규 발견: **ATM 비야레알전 포메이션 표기 충돌** — 아래 「미해결」.
- ✅ **LIV 뉴캐슬전 서사 보강(obs#336)** — 4개 필드가 **54/66/52/69자 → 652/905/584/834자**.
  원문 마크다운은 37KB인데 정형 필드가 한 줄이었다(자료 부족이 아니라 접히지 않은 것).
  **새 수집 0건** — obs#312·317·321·326·330 + team_match_stats를 재구성했고 값은 안 바꿨다.
  ⛔ 나머지 얇은 3건(CHE 밀란·LIV 모나코·AVL 바이에른)은 **같은 결함이 아니다** — 프리시즌 친선이고
  원문이 9~11KB뿐이라 필드 길이가 근거량에 비례한다. **사용자 확인 후 미보강 확정.**
  ⭐ 교훈: **G12는 `trim(field)=''`만 본다 — 밀도는 검사하지 않는다.**
- ✅ **오픈플레이 xG·막힌 슛 정형 필드 승격(obs#337·338)** — migration 022, 5경기.
  ⛔ 그 과정에서 **스냅샷 혼합 함정**을 만났다(CHE 오픈플레이 1.96 > 전체 1.70, 세트피스 −0.26).
  사용자 결정 **C**: 즉시 덮어쓰지 않고 **D+2에 동일 스냅샷으로 재수집** → 아래 CHE 항에서 실행 완료.
  ⭐ ATM 비야레알전에서 **독립 검산 성립**(3.60 − 2.02 = 1.58, PK 2개 ≈ 0.79×2).
- ✅ **transfer-watch 21시 회차** — 켈리먼 **CONFIRMED**(구단 공식) · 사르 **신규 HIGH**(Romano HWG) ·
  음바예 **강등**(09시 트리거가 L'Équipe 1티어로 발화) · **ATM 잭슨 DEAD→MEDIUM-HIGH 복원**(우리가 「종료」
  근거로 쓴 Romano 트윗 안에 「ATM keep pushing」이 같이 있었다) · 왓킨스 원장 정정(4차 £45m는 이미 제출·거절).
  ⭐⭐ **FIFA art.10 해소** — 「6칸 중 4칸」(총량)과 「스트라스부르 1칸」(동일구단 3명)은 다른 조항이고 둘 다 참이다.
  **국내 임대는 대상 밖** ⇒ 첼시가 헐을 선호하는 이유가 규정으로 설명된다. 파생: **에수구↔무드리크가 잔여 1칸을
  상호 배타적으로 다툰다.** ⚠️ 원장 결함 발견: **`players` 에수구 중복(103/140)** — 아래 「미해결」.
  전문은 `reports/transfer-watch/2026-08-25.md`.
- ✅ **FC27 09-10 선행과제(obs#339)** — 문서가 지목한 알리송 충돌은 **이미 해소돼 있었고**,
  지목하지 않은 **`스캔런` 충돌**(118 Calum / 145 Cody)이 남아 있었다 ⇒ `캘럼/코디 스캔런`으로 분리.
  `players` name_kr 중복 **0건**. ⭐ 오탐 주의: `sofifa_name`이 갈리는 80여 건은 **약칭/정식명 차이**다.
  베이스라인 수치 갱신 — 델타 제약은 팀 편중이 아니라 **교집합**이다(FC26∩FC27 **99** / FC27만 47 / FC26만 77).
- ✅ **CHE 풀럼전 D+1 심층 → `complete` 승격(obs#340·344·345)**
  ⭐⭐ **초판이 표면만 봤다** — Telegraph가 백3의 실제 메커니즘을 명시: **리스 제임스가 미드필드에서 상습적으로
  백3로 내려가 아체암퐁·라크루아 두 CB를 전진하게 풀어 준다**(원인은 포파나 출장정지). ⇒ 재검증 항목 #2가 「설계」로 해소.
  ⭐⭐ **공식 회견이 우리 PPDA 판독을 확인했다** — 알론소는 후반을 「공 없이 편안했고 경기를 통제했다」로 규정하고
  문제를 **「압박할 순간 선택」(동점골 직후 10분)**으로 특정한다 ⇒ **낮은 PPDA는 실패가 아니라 의도**였다.
  ⭐ 실측과 독립 수렴 3건(산체스 3중 정합 · 로저스 8.65↔SI 8.7 + 2·3번째 골 모두 관여 · 통제 실패 판정).
  ⚠️ 승격 전 **`game_implications` 90자 → 1,333자 보강**(obs#336 밀도 교훈 적용).
  ⛔ 남은 결손은 **클럽 전술 영상 1건(비차단)** — LIV 선례(영상 없이 승격)를 따랐다.

### 아카이브 (2026-08-24 이전)

- 2026-08-24: 드리프트 6건 전량 데이터 오류 0(원인은 `check_fit_drift.py`의 `pos_only`/포메이션 혼입) → 차이 20→8.
  NULL fit 13행은 최초 시드 누락으로 확정, 8행 백필. AVL/LIV D+2 조기 실시.

## 다음 할 일

### match-watch
1. ⏰ **CHE 풀럼전 — `complete` 승격 완료(obs#344). 남은 것은 「클럽 전술 영상」 1건뿐이고 비차단이다.**
   ⭐ 회견의 최대 수확: 알론소가 후반을 **「공 없이 편안했고 경기를 통제했다」**로 규정해 **낮은 PPDA(20.18)가
   실패가 아니라 의도**임을 1차 발언으로 확인했다. 문제는 총량이 아니라 **「압박할 순간 선택」이고 동점골 직후 10분**이다.
   또 **31초 선제골을 「심리적 우위와 자신감」으로 규정**했다.
   ⛔ **[08-25 확인] 경기 후 회견 영상은 존재하지 않는다** — 검색된 알론소 회견 영상 3건이 전부 **경기 전(08-20)**이다.
   ⛔ **한국어 전술 분석 영상도 0건**(박종윤 `JnRlU-Guk5U`는 3주 전 프리시즌 체크로 이 경기 분석이 아니다).
   한국 언론은 경기 리포트를 냈다(머니투데이·일간스포츠·네이트).
   ⏰ **사람이 시청해야 하는 항목 1건**: 경기 전 회견 `ZhA2s-Pz0ss` **6:43「Reece James role, versatility and
   captaincy impact」** — D+1에 확인한 **백3 가변 하강이 사전 설계였는지 확정할 수 있는 유일한 경로**로 보인다.
   (챕터 제목만 확보했고 발언 내용은 모른다.)
2. ✅ **[2026-08-25 완료] `overall_assessment` — 공식전 전수 채움 달성.**
   **6/25**이고 그 6건이 **공식전 전부**다(UEFA 슈퍼컵 1 · LaLiga 21·24 · PL 22·23·25).
   나머지 19건은 전부 프리시즌 친선으로 **사용자 확인상 미수집 확정**(일반 매체 총평이 희박하다).
   ⛔ 게이트는 걸지 않았다 — 프리시즌이 정당하게 빈 상태라 필수화하면 오탐이 된다.
3. **AVL Brighton전은 비정기 재시도 조건부 draft** — 공식 전체 회견/클럽 전술 영상,
   경기 전용 전문 분석 또는 팟캐스트 공식 전사가 나오면 다시 연다. 정기 D+1~D+3은 완료.
3. **PL 다음 라운드**: AVL/LIV/CHE 실제 경기마다 동일 파이프라인을 반복한다.
   가장 가까운 CHE 08-30 Brighton 홈에 앞서 08-27 Luton EFL컵도 match-watch 범위 여부를 확인한다.
4. Atlético 이후 경기는 별도 match-watch 일정. 그리말도 실측이 채워지면 2경기 슬롯 기하·fit·sort_order 연쇄 재검증.
5. ✅ **[2026-08-25 검토 완료 · 경고 정정] LIV LM은 「후보 0명」이 아니다(obs#343).**
   좌측 전용 실측으로 재계산: **학포 LM 0.773**(n=60·4655분) vs **무뇨스 LM 0.771**(n=22·1707분) = **Δ0.002**로
   우리 지표로는 구별되지 않는다. 「LM 1명뿐」은 08-25에 무뇨스 주 사이드를 LM→RM으로 바꾼
   **우리 라벨링의 산물**이었다(값은 `measured:side:L`에 보존돼 있다).
   ⛔⛔ **단 fit은 형태 일치도이고 품질이 아니다** — 학포(LIV 65경기 51골)와 무뇨스(LIV 공식전 0경기)를
   등가로 읽으면 지표 오용이다. 게다가 무뇨스의 좌측 표본은 **오사수나 시절**이다(불변규칙 7).
   ⭐ **바르콜라 LM 0.751은 둘보다 낮다** ⇒ €120~140m 영입은 **fit으로 정당화되지 않는다**
   (천장·나이·1군 검증된 질로 정당화해야 한다. fit을 근거로 쓰면 자기모순이 된다).
   ⚠️ **RM 자원의 낮은 LM 값(0.03~0.24)은 「LM 불가」의 증거가 아니다** — 그리드가 우측 전용이라 당연하다.
   판정에는 **좌측 전용 표본이 필수**다. 엘리엇은 n=2(둘 다 우측)로 **판정 불가**.
   ⇒ 정확한 리스크: **「LM 0명」이 아니라 「이라올라 체제에서 검증된 LM이 학포뿐」**이다. RM은 두텁다
   (엘리엇 .858·무뇨스 .834·응구모하 .812) — 민테까지 오면 **RM 과잉·LM 얇음** 구도다.
6. ✅ **[2026-08-25 완료] 전후(x축, 라인 높이) 이봉 점검 — 0건이다(obs#342).**
   좌우는 40명 중 **7명(17.5%)** 이봉이었는데 전후는 **0명**이다. 민감도 4단계 완화(간극 8→4)에도
   0→0→0→1명이고 그 1명도 두 군집이 아니라 좌측 꼬리라 **임계값 인공물이 아니다.**
   ⭐ **방법론 결론**: **사이드는 범주형**(감독이 좌/우를 고른다 — 이산)이라 두 군집이 생겨 평균이 양쪽 다
   대표하지 못하고, **높이는 연속형**(경기 국면에 따라 매끄럽게 변한다)이라 단봉으로 퍼져 **평균이 정당하게 대표한다.**
   분산이 없는 게 아니다(캐시 34.1~73.4로 39단위) — **단봉의 분산은 평균으로 요약해도 되고 이봉의 분산은 안 된다.**
   ⇒ **현재 관행(높이는 시즌 집계, 사이드만 분리 적재)이 실측으로 정당화됐다.**
   ⏰ 표본부족 33명(hp≥15 경기 8건 미달, ATM·CHE 여름 영입 다수)은 시즌 진행 후 재실행.

### transfer-watch (마감 2026-09-01, D-7)
1. ⭐⭐⭐ **레앙(AVL) — 정식 오퍼 제출 여부.** Di Marzio(협상 중) ↔ Moretto(단순 아이디어) 상충은
   오퍼 하나로 갈린다. 밀란 재개선선 €50m.
2. ⭐⭐⭐ **고레츠카(AVL)·디사시(CHE) 구단 공식 발표** — 둘 다 Romano HWG+메디컬 완료 단계에서
   구단 공식만 미발화. 발화 즉시 CONFIRMED 승격.
3. ⭐⭐ **각포(LIV 유출) — Elfrink 「vorige week」 vs Romano 「today」 시점 대조** + 시티/토트넘 중
   구단 간 대화로 올라가는 쪽 확인.
4. **민테(LIV) 3차 입찰**·**바르콜라(LIV) 2차 서면 오퍼** — 둘 다 여러 회차 미발화이며 각포 매각의
    선행조건이다.
5. **왓킨스 이적 정황을 계속 추적한다** — 개막전 20인 제외가 부상이 아니고 에메리가 사유 답변을
    3회 거부했다. 08-24 훈련 복귀로 방향이 갈렸다. 이적 확정 시 `squad_entries` ST 행(fit 0.722)과
    9번 처방 전체가 재판정 대상이다.
6. Minteh는 2026-09-02에 개선 오퍼 0건이면 등급 재판정한다.
7. ✅ **[2026-08-25 21시 해소] FIFA art.10 수치 충돌** — 「6칸 중 4칸」(총량 ¶6)과 「스트라스부르 1칸」
    (동일구단 3명 ¶6 단서)은 **다른 조항이고 둘 다 참**이었다. U21 예외(¶7)는 **club-trained 누적 조건**이라
    에수구·무드리크·사르·워싱턴 전원 미충족. **국내 임대는 대상 밖** ⇒ 첼시가 헐을 선호하는 이유가 규정으로 설명된다.
    ⭐ 파생: **에수구와 무드리크가 스트라스부르 잔여 1칸을 상호 배타적으로 다툰다.**
    ⏰ 잔여: **RSTP 조문 verbatim 미확보**(PDF 바이너리) + PL 국내 규정 잉글랜드 수치 미확인.
8. ⏰⏰ **레앙 강등 트리거가 걸려 있다** — Sportmediaset이 예고한 **~€50m 오퍼가 미제출이면 MEDIUM**으로 내린다.
    ⚠️ Moretto(1티어)가 「빌라는 **선수·대리인과 접촉조차 하지 않았다**」로 09시 근거(Di Marzio 「선수 오픈」)를 반박했다.
9. ⏰⏰ **고레츠카 08-28(금) 컷오프** — 08-31 bank holiday로 09-01 아스날전 등록은 금요일까지다.
    ⭐ 「화요일 발표」 예고의 원출처는 **Lorenzo Lepore 3티어 단독**이었다(티어 근거 없음).
    구단이 08-25에 낸 「six summer signings」 총정리에 **부재**한다.
10. ⏰ **디사시·사르·워싱턴 구단 공식** — 셋 다 Romano HWG+메디컬 단계에서 공식만 남았다.
11. ⏰ **Marca 계열 도메인 차단 우회** — `marca/as/relevo/mundodeportivo/cadenaser`가 **HTTP 400**,
    `estoesatleti` **403**이다. ATM 1차 소스 전체가 릴레이 경유였고 **잭슨 복원 근거가 Marca 원문 1건에 걸려 있다**
    ⇒ 브라우저로 원문 확인 필요.
12. ⏰ **네덜란드어로 랑 → 아약스** — 이 딜 성립이 **CHE 기우 행의 등급을 직접 결정**한다.

## 미해결 — 판단이 필요한 것

1. ⚠️⚠️ **`players` 에수구 중복(103/140)** — 동일 선수가 두 id로 존재하고 **참조가 갈려 있다**:
   `103 Dário Essugo`(player_matches **4** · match_player_reports **4** · player_duties 1 · tenures 6) ·
   `140 Dario Essugo`(sofascore_id 1110006 · squad_entries **1** · **transfer_outgoing 1** · player_matches 1 · duties 1 · tenures 6).
   ⇒ **CHE 유출 행(140)이 실측 출전 기록(103)과 연결되지 않는다.** `players` 전수 스캔에서 **유일한 이 중복**이고
   **G6는 두 id가 모두 존재하므로 통과한다**(게이트 사각지대). 병합은 match 리포트·squad·prescriptions를 함께
   건드리므로 **어느 id를 살리고 FK를 어디로 재지정할지 판단이 필요하다.**
   ⭐ 참고: 같은 부류의 `스캔런` 충돌은 2026-08-25에 이미 분리했다(obs#339) — 그건 참조가 0이어서 안전했다.
2. ⏰ **CHE 풀럼전 `xg_v` 개정 정책** — 08-25에 1.70 → **2.24**로 통일했다(사용자 결정 C). 값 자체는 정합하나
   **Opta는 또 개정할 수 있다.** 「경기 직후 스냅샷을 정본으로 둘 것인가, 최신 개정을 따라갈 것인가」는
   아직 규칙이 없다 — 지금은 **draft면 최신, complete면 사용자 판단**으로 운용한다.
3. ⏰ **ATM 비야레알전 포메이션 표기 충돌** — 우리·리포트는 **4-4-2**(El Desmarque·COPE)인데
   **Infobae 크로니카는 「4-3-3 de Simeone」**이다. obs#333(ATM 4-4-2 슬롯 신설)의 전제와 직결된다.
   ⭐ 단 **실측 기하가 4-1-4-1에 더 가까워 처방을 이관하지 않은 판단은 이 충돌과 정합적**이다.
4. ⏰ **FC27 커널 미착수** — `game_roles`/`game_role_focus`/`game_role_variants`의 FC27 행이 **전부 0**이다.
   fut.gg `/api/fut/roles/` 수집은 발매(09-25) 전 역할 데이터 공개 여부가 불확실해 **09-10 이후로 미뤘다.**
   `core/kernel.py` EXPECTED·gates 앵커의 FC27 확장은 커널 행이 생긴 뒤에만 가능하다.

## 데이터 수집 상태와 결손

- 대량 수집: `collect_fotmob_players.py`, `collect_understat_shots.py`.
- 읽기 전용 진단 3종: `check_fit_drift.py` · `check_side_bimodality.py`(좌우) ·
  `check_height_bimodality.py`(전후, 2026-08-25 신설). ⚠️ 셋 다 **진단만 한다** —
  그리드 재적재·`pos_only`·처방 변경은 사람이 판단한다.
- ⛔⛔ **검색엔진 연도 혼입에 주의.** 2026-08-24 실측 사례 — 스웨덴어 검색 요약이 뉴캐슬–리버풀 2-2를
  「**Slots** radikala högalinje」로 설명했는데 26/27 리버풀 감독은 **이라올라**다. 원문 fetch 결과
  검색엔진이 **2025-08-25 동일 카드**(슬롯 체제, 응구모하 막판골) 기사를 섞은 것이었다.
  **같은 상대·같은 8월·유사 스코어의 전년도 경기는 반드시 발행일을 확인하고 쓴다.**
- ⚠️ **verbatim 인용은 요약 경유 시 열화된다.** 같은 기자회견 답변이 매체별로 3종(에메리·왓킨스 건),
  같은 「PL 매치센터」 성명이 2종(고메스 죄목)으로 갈렸다. **실질만 채택하고 인용문은 원문 확보 전까지 쓰지 않는다.**
- Understat은 빅5(+RFPL) 슛/창조 대체 원천이나 챔피언십·에레디비시·리가2는 없다.
- SofaScore 403이면 축2 실측/R2 그리드를 억지로 채우지 않는다. WhoScored 접근을 먼저 재프로브한다.
- Sofifa 35속성/playstyles, FBref 12축은 403 결손 상태다.
- 결손과 0을 구분한다. API 키 생략은 확정 0일 수 있으므로 `docs/30-data-rules.md` 규약을 따른다.

## 고정 작업 규칙

1. 시작 시 `git status --short`. 로컬 변경이 있으면 fetch/pull하지 말고 내용부터 확인한다.
2. 깨끗할 때만 `git fetch origin && git pull --rebase origin main`.
3. 다른 PC/세션 변경을 버리거나 덮지 않는다. 특히 `db/tactics.db`는 충돌 시 기계 병합 금지.
4. DB 변경 뒤 반드시:

   ```bash
   .venv/bin/python scripts/export.py
   scripts/db_dump.sh
   .venv/bin/python scripts/gates.py
   ```

5. `git add -A` 금지. `db/tactics.db`, 관련 `db/dump/`, `site/data/`, `reports/`, 문서를 명시 스테이징한다.
6. push 전 `origin/main` 이동 여부를 다시 확인한다.
7. 시즌 집계는 45분+·hit_points 15+만 사용하되, 경기 리포트는 짧은 교체 포함 실제 출전자 전원을 기록한다.
8. match 전용 fit/전술은 시즌 `prescriptions`/`team_tactic_setups`에 자동 병합하지 않는다.
9. HANDOFF는 300줄 이하로 유지한다.

## 핵심 방법론

- 그리드 인코딩·집계·커널은 `core/`만 사용한다. 재구현 금지.
- 좌표: x는 공격 방향, y가 낮을수록 오른쪽. `cells_from_points` → `encode` → `Kernel.best_fit_slot`.
- 역할군 argmax 전에 슬롯 유형 필터가 필수다.
- 적합값 Δ 0.02~0.05는 EA가 공개한 노이즈 구간이므로 그 차이만으로 인선을 바꾸지 않는다.
- 히트맵은 요구 역할의 일부만 설명한다. duties·1차 발언·전술 영상이 명확하면 낮은 fit보다 우선할 수 있다.
- 단일 경기·퇴장·저점유 같은 교란은 confidence와 보고서에 함께 쓴다.

## 참고 문서

- `docs/20-fc-game-system.md`: 슬롯 x, 역할·포커스, 게임 구현 규칙.
- `docs/30-data-rules.md`: 수집, 좌표, 그리드, 표본·결손 규칙.
- `docs/40-pipeline.md`: DB/export/dump/git 파이프라인.
- `docs/50-transfer-policy.md`: 이적 등급·보존 정책.
- `docs/60-research-methods.md`: 새 축 검증과 통계 기준.
