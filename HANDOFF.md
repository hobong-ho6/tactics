# HANDOFF

## 프로젝트

- 실제 감독 전술을 실측으로 분해해 FC26 전술로 재현한다. 대상: **AVL/에메리(주) · CHE/알론소 · LIV/이라올라 · ATM/시메오네**.
- 저장소 `/Users/ad03230205/Documents/tactics` · `main` · DB 정본 `db/tactics.db`(`db/dump/`·`site/data/`는 파생물).
- 규약 정본: `CLAUDE.md`, `docs/00-overview.md`. 런북: `.claude/skills/{transfer-watch,match-watch,player-collect}/SKILL.md`.

## 현재 상태

> **2026-09-17~18 KST ⑲** · ✅ **진화·내 구단 축 신설**(`36ea37e`·`7526afd`·`d14b0a3`·`9280639`, migration 034~037).
> **새 메뉴 `site/evolutions.html`「진화·내 구단」** 4탭: 내 구단(계정·보유·진화 적용 기록·판매·다음 추천) · 진화 카탈로그(팀별 적용 가능 선수, 13/14종 —
> 2495는 paths에 안 나오는 숨김 진화) · 선수별 진화 패스(단계 카드·6스탯 Δ·Role+/++ 해석·최적 제안) · **전술 구현·구매 우선순위**(slot_canon × starter 처방 × 카드 Role+/++ × 진화 × 시세).
> ⭐ **순위는 전부 화면이 계산한다** — DB(`fc_evolutions`·`player_evolutions`·`player_card_prices`)는 사실만 담는다(처방이 바뀌면 추천도 바뀌어야 하므로).
> ⭐ **쓰기 경로**: `scripts/fut_club.py` CLI = `scripts/serve.py` `POST /api/fut/*`(같은 함수) → DB → 곧바로 `export.py`(게이트 강제). 정적 배포본엔 API가 없어 CLI 명령을 대신 보여준다.
> 계정 `main`(PS5) 등록됨(검증 겸). ⛔ **EA 구단 자동 수집은 FC Community API 승인 파트너 전용** — fut.gg GG Club이 그 경로. 사용자가 브라우저 패널에서 직접 로그인·연결하면 그 세션을 읽는 임포터를 붙일 수 있다(미착수).
> 카드: FC27 99/103명(검색 폴백으로 고레츠카·카스티요·조나단 데이비드 확보 · **미해결 4명은 fut.gg에 없다**: 니콜자줄리·도밍게스·모레노·라하도) · 특별 카드 12장 · 이미지 결손 0.
> 진화: 경로 174행/53명(45명은 경로 0 — 대부분 OVR 상한) · 현 스냅샷 **Role++ 0건·유료 0건**(출시 전 도입용만). ⚠️ 진화는 기간제 — `pulled`가 정본.
> 시세: **FC27 시장 미형성(90장 전부 hasPrice=0 → NULL)**. 전용 가격 API는 Cloudflare 403 — 목록 API `currentDbPrice`만 쓴다. 시장 열리면 `collect_futgg_prices.py` 일일 실행 필요(스케줄 미등록 — 사용자 판단).
> `fc_role_familiarity_map` 98행(fut.gg roles `plusEaId/plusPlusEaId`)으로 FC27 Role+/++를 해석한다 — **FC27 `game_roles` 커널은 여전히 없다**(obs#629).
> 부수: 코번트리전(report 43) **영상 14편 전사·인덱싱·귀속 완료(총 17편)**.
>
> **2026-09-18 KST ⑳** · ✅ **코번트리전(report 43) 유튜브 전수 14편 반영** — 요약·key_points 14 · `video_impl_claims` 21(REJECTED 2·HELD 3·PENDING 6·NA 1·none 9) · obs#825~829 · `player_duties` 245(음바예 HELD) · `manager_profiles` rotation·set_pieces 덧붙임 · 리포트 「D+1 추적 보강」 절.
> ⭐⭐ **obs#825 — 「완비사카 하이브리드 백3」(UTV·1874 독립 2소스) 기각**: 실측 avg_y 14.69 → 툴x 85.3(터치라인). 전진 억제(avg_x 41 < LB 47)는 정합 ⇒ 억제형 풀백 유지, 「내재화」는 좌표가 부정. **2소스 일치가 실측을 이기지 않는다**(불변규칙 3) — 두 채널 모두 평균위치 그래픽의 좌우축을 깊이로 오독한 것으로 판단.
> ⭐ obs#826 — 음바예 새 판정 후보 **「선발 게이트 = 원정 수비 부담」**(3티어 단일) · 에메리 1차 인용 「his speed」 · 1874 「우측 폭 유지 지시」(실측 툴x 72.4 = 방향 정합·정도 중간). 검증 지점: **토트넘 원정 우측 선택(음바예/알리송)**.
> obs#827 — 27′ 토르프 FK 실점 **3설 대립 보류**(UTV GK 책임 ↔ 1874 면책 ↔ 코번트리 「벽이 물러났다」) · obs#828 — 바클리 「하이 피벗/10번」 처방은 실측이 이미 그 방향(LDM인데 avg_x 49.8 피벗 최전방) · 스즈키 롱볼→음바예 배후는 **제안**이지 관측 아님.
> ⭐⭐ obs#829 — **영상 스윕 3경로 규칙**(채널 목록 → 검색 → 채널명 재검색): 1874 리뷰는 채널 목록에서 누락(⇒ 「D+1 미게시」 판정 뒤집힘), UTV 음바예·코번트리 팬 영상은 채널명 재검색에서만 나왔다. AVFC 공식 핸들 `@avfcofficial`. 서브에이전트 헤더 순서 오류(「제목 - 채널」)로 인덱서가 제목을 채널로 읽었다 — 정규화 후 재인덱스. → docs/30 등재 후보.
> 이월: **Tanswell 0건**(브라우저 경로 미시도) · 음바예 모국어권(프랑스어·세네갈) 미시도 · 09-16 이전 음바예 영입 시점 영상 4편은 **선수 축**으로 별건.
>
> **2026-09-17 KST ⑱** · ✅ **`/match-watch` 회차 완료 — 3경기 수집·complete**(`c96ffc3`·`a756f23`, obs#812~824).
> **AVL 1-3 코번트리(EFL컵 3R)** report 43 · **LIV 3-1 토트넘(EFL컵 3R)** report 44 · **ATM 4-0 오사수나(라리가 J6)** report 45. CHE는 새 경기 없음(다음 09-18 브렌트퍼드).
> 실측 48명·국면 그리드 48·PPDA/def_x 3경기 · 경기 프리셋 48행 · measured 처방 17행 재산출 · T1 표본 47행 · 영상 요약 7편+구현 주장 11행 · manager_profiles 12축 · duties 31행+신규 3.
> ⭐⭐ **뒤집은 판정 3건**: ⑴ obs#812 — 주앙 고메스는 09-16에도 **징계 결장**(obs#787의 「복귀 예정」 전제 오류) → 「바클리 = 결원」 2경기 연속.
> ⑵ obs#821 — **「ATM 백3 = 유럽 원정 한정」 기각**(09-13 아노에타는 라리가 원정인데 백3였다, DB 자체 반증). 시메오네 「선발은 상대와의 관계로 정한다」 ⇒ **4-4-2도 「기본형」이 아니다**.
> ⑶ obs#819 — 「10인 변경 중 맥 알리스터만 잔류」는 **영어권 다수 오류**, 스페인어 EFE의 「아라우호가 유일한 생존자」가 옳다(우리 실측이 지지 — 불변규칙 10 실증).
> ⭐ **새 축 2건**: obs#816 — LIV **세트피스 수비 경고**(세트플레이 xG 0.08:1.18 = 상대 총 xG의 53%·공중볼 8/24인데 **구조로 지목한 매체 0건**, 실측 단독 주장·표본 1건).
> obs#820 — ATM **「진짜 9번 부재」의 답은 조나단 데이비드**(최종 라인 고정 → 박스터치 49 ↔ 안필드 알바레스 하강형 18, 같은 축의 양 끝). 서열은 **더비 09-20이 다음 표본**.
> ⭐ obs#817 — **obs#644(피벗 둘 다 전진)의 첫 반례 방향**: 뇨니가 잔류·우측 하강해 역할이 분화했고 **6번 공간 누출 지목이 3경기 만에 0건**. 결함은 성향이 아니라 **짝 선택**의 함수일 수 있다.
> 정정 2건: ATM `matches.stage` **Round 5 → 6**(FotMob 1차 API) · 조 고메스(108) `sofascore_id` 채움(리버풀 첫 실측).
> 부상 2건 추가: 비조 등 통증(하프타임 GK 교체 사유) · **알바레스**(DB 결손이었다 — 부위 미특정).
> ⏰ **D+2/D+3 추적 3건 예약**(09-18 21:00 LIV·21:30 AVL·22:00 ATM). ⚠️ 예약 전 **스케줄 목록이 완전히 비어 있었다** — 정기 태스크(match-watch 주간 등) 재생성은 사용자 판단.
> ⚠️ 이월: AVL **Tanswell 0건**(x.com 402 — 브라우저 미시도) · ATM **Marca·AS·Relevo·MD 4매체 동시 미도달** · LIV 상대 빅찬스 오픈/세트 분해 미수행 · 불변규칙 10 결손 3건(AVL 스페인어·일본어·프랑스어, ATM 데이비드 프랑스어·이탈리아어, LIV 바스크어 등).
> **2026-09-15 KST ⑰**(스케줄 세션) · ✅ **AVL 1-2 포레스트(09-12) 3일 추적 종결**(`0971716`, obs#787~792 · `player_duties` 239~241).
> 총 **6패스**(D+1 2 · D+2 3 · D+3 1) · obs 22건. **뒤집은 판정 0건**, D+3이 연 축은 둘이다.
> ⭐⭐ **obs#787 — 바클리 LDM은 「선택」이 아니라 「결원」이었다.** 주앙 고메스가 브라이턴 데뷔전 퇴장으로 **PL 2~4R 3경기 징계**(이 경기가 마지막),
> 징계가 **유럽 대회엔 미적용**이라 나흘 전 브뤼헤(CL)엔 고메스가 선발했고 **포레스트전은 브뤼헤 XI에서 딱 한 명 바뀐 것 — 그 한 명**이다
> (실측 matches 86·94·95 vs 99로 자기 검증). ⇒ 글라스너가 덫을 놓은 그 피벗(obs#711)은 **가용 인원의 산물**이며
> 포레스트 백3 강제(obs#641·727)와 **대칭**이다. → 규칙 목록에 부류 등재.
> ⭐ **obs#790 — 빌라 전반 최대 기회는 6′ 린델뢰프의 「골라인 미판정」 장면**(ESPN 1티어 「some of the ball had crossed the line」).
> **xg 0.4366 = 빌라 개인 최대**(전반 팀 xG 0.65의 2/3) — **D+1·D+2 5패스가 놓친 장면**이고 obs#715(허용된 점유)를 보강한다.
> 그 밖: obs#788(요구 자질 ↔ 커널 fit `.888` 동일값의 괴리) · obs#789(선제골 최종 저지선 = 10번 부엔디아) ·
> obs#791(알리송 골 = **26/27 시즌 첫 PL 득점**, 실측 자기완결) · obs#792(종결 판정·결측 확정 축 목록) · 신규 매체 1곳(할 일 0-3d).
> **`manager_profiles` `rotation` 갱신**(regime 1): 「**「유럽 = 별도 팀」 외형의 일부는 로테이션 원칙이 아니라 징계 적용 범위의 산물**이다 —
> 정량 표본을 만들 때 강제 변경을 분리하라.」 다른 축은 미갱신(이 경기는 **이탈 사례**이지 상수 변경 근거가 아니다 — 사유 리포트 기재).
>
> **2026-09-15 KST ⑮·⑯** · **남은 할 일 7건 + 사고/오탐 2건**(obs#779~786, `3c866b1`…`b8ce57c`).
> **G18 「표본 집계 정합」 신설**(공식전 한정 재집계 ↔ map25 대조) — ⛔⛔ `[표본판정` 표식이 **친선 혼입 5행을 가리고** 있었다(부류 등재).
> **`fc26:opt:*` 투영 fit 감사** 3행 `[투영판정]` 표기 · **채움 전용화 완료**(사람 손을 덮는 경로 3곳 전부 차단) ·
> **G16 사람 판정 90행 재검증** → **재판정 조건 충족 5행** 처리(APPLIED 4→**6**) · ⭐⭐⭐ **`canon()` 오탐 4쌍 적발**(접두 전부 제거).
> 최종: MATCH **142** · HELD 65 · REJECTED 19 · APPLIED 6 · PROSE 9.
> ✅ 방치가 구조적으로 막혔다(obs#786) — `refresh_duty_applied.py`가 회차마다 **「재판정 조건 충족 후보」를 보고**한다(상세는 아래 규칙 목록).
>
> **2026-09-15 KST ⑭** · ⭐⭐⭐ **ATM 표기 충돌(미해결 1)의 메커니즘 — 흔들리는 건 요렌테가 아니라 푸빌이다**(obs#775, 할 일 30):
> 요렌테는 **상수**(툴x 76.8~84.3)이고 **푸빌이 백4→백3에서 +16~17 이동**해 둘이 같은 대역에 선다 ⇒ **표기 충돌은 오류가 아니라 구조의 성질**.
> `reproduction_limits` #14 rest-defense 등록 · 영상 미수집 3건 해소 · ⭐⭐ **LIV 6번 축 미결**(층이 다르다 — 할 일 29).
>
> ---
> **⬇️ 2026-09-15 세션 ①~⑬ 압축** — 전문은 커밋·obs에 있다(규칙 8).
> ⑬ `pos_class` 백필 **1,180행** + 영상 요약 → **146편(94%)**. ⭐⭐⭐ 그 과정에서 **친선 혼입** 발견(부류 등재) —
> 「캐시가 wingback으로 뒤집혔다」는 친선 혼입 탓이고 공식전 한정은 `fb_att_wb/Support` .8919 그대로
> ⇒ **T6 판정 대상은 「att_wb/Support(실측) ↔ falseback(영상)」**이다.
> ⑫ `measured` 표본 감사 신설·전량 처리 → **감사 전항 0**(obs#769~772, 재선언 3건: 이강인·라비아·콜 파머 `ST`→`RAM` .5576→.8156).
> ⑪ LM 주전 **헤밍스** 확정·C1 뱅크 0.60 복구(obs#768) · ⑩ **맥긴 RM → `wm_winger/Attack`**(obs#766, Δ.0618) —
> ⛔ C1이 전제한 「RM 캐시 .835」는 **캐시 RM 통산 0경기**로 실체가 없었다. ⑩-a `applied_note` 덮어쓰기 사고(obs#767).
> ⑨ 축별 결정권 성문화 — docs/30 **7·8단계**(`video_impl_claims`)·**G17** · ⑧ 「수집이 구현을 바꿨나」 전수 측정(obs#762·763):
> 영상 obs 198건 중 구현을 바꾼 것 **3건** / 커널 **Δ>.05 단독 확정 54% · Δ≤.05 무결정 46%**.
> ⑦ 팟캐스트 RSS = 「발견 계층」 · ⑥ 전사 메타 84→138편 · ⑤ 요약 84/84 · ④ `match_videos` 신설 ·
> ③ ATM map25 결손 5→3 · ② PROSE 88행 → 0 · ① 반영 판정 5단계 성문화 · **09-14** G16(031)·FC27 카드 축(030).
> ---
>

### 반복해서 사고를 냈던 규칙 (지우지 말 것)

- ⛔⛔ **불변규칙 2(추가만·재작성 금지)는 리포트 파일뿐 아니라 DB 행에도 적용된다 — 09-08 실증(obs#517·#518).**
  `1211c43`이 obs#503의 4필드를 **`UPDATE`로 제자리 덮어썼고**, 파생 피해 2건이 실제로 났다: ㉠ `.replace()` 실수로 **텍스트 중복**
  ㉡ **UCL 사실을 `evidence`에만 넣고 `claim`의 PL 서술을 남겨 한 행에 두 대회 축이 섞임**(claim↔evidence 정반대 진술).
  ⇒ **정정은 언제나 새 obs 행으로 한다**(⛔ 위반을 위반으로 고치지 않는다 — obs#503도 되돌려 덮어쓰지 않았다).
  ✅ **이제 G14가 막는다.** 정당한 재작성은 `G14_ALLOW_REWRITE=<table>:<id>`로 통과시킨다. 보호 테이블은
  `observations`·`player_duties`뿐(`transfer_targets`·`prescriptions`·`match_reports`는 편집이 정상 작업 — 근거는 `gates.py` 주석).
- ⛔⛔ **파생 컬럼에 「어휘 정본」이 없으면 세션마다 다른 말을 써 넣는다 — 09-08 실증(obs#527).** `pos_class`는 값이 있는 839행 중
  **450행이 정본 어휘 밖**이었고(core 어휘 + 별도 스크립트 어휘 + 손으로 친 문장), `lineup_pos`도 같은 병이다(G/D/M/F ↔ 슬롯 라벨 ↔ NULL).
  ⇒ **새 파생 컬럼은 허용 값 집합을 먼저 정하고 게이트로 묶어라.** ⭐ **독스트링이 거짓말을 할 수 있다** — 옛 `pos_class`는
  「슬롯 x 정본표로 세분류한다」면서 `slots`를 한 번도 참조하지 않았다. **주석이 아니라 코드를 읽고 확인할 것.**
- ⛔ **팀 설정 3축(빌드업·수비접근·라인)을 산문으로 정하지 말 것 — 09-08 실증(obs#532).** 규칙(docs/20 「팀 설정 매핑 규칙」·`core/team_settings.py`)을
  먼저 돌리고 다르면 `rule_note='DIVERGE: 사유'`. 백필에서 **19행 중 14행이 편차**였다(같은 PPDA 대역에 High/72와 Balanced/55가 공존). G15가 사유 없는 편차를 막는다.
- ⛔ **게임에 입력할 수 없는 처방이 또 있었다 — 09-08 실증(obs#533).** CHE 선발 12명·슬롯 없는 LDM/RDM 4행·교체 6행 역할군 밖. **G8+가 막는다** —
  `prescriptions.kind`는 `KIND_RE` 형태만(새 접두는 게이트+docs/00 동시 수정).
- ⛔⛔ **xG 계열은 한 회차에 같은 스냅샷으로 수집하고 `xg_source`를 함께 채운다**(FotMob matchDetails 한 응답에 전부 있다).
  따로 채우면 Opta 사후 개정 때문에 「오픈플레이 > 전체」라는 불가능한 값이 된다. ⛔⛔ **검산(`xg − xg_op` ≈ PK × ~0.79)은
  스냅샷 동일성을 증명하지 않는다** — 비야레알전이 검산을 통과하면서 제공사가 갈려 있었다. **제공사는 `xg_source`로 확인**하고
  교차 경기 집계는 `WHERE xg_source='FotMob'`으로 고정한다(정본 `docs/30` ⑧ · match-watch SKILL §3).
- ⛔⛔ **지연 발행 자료를 검색엔진 질의로 「0건」 처리하지 말 것**(09-08 실증, obs#516) — 4회 질의해 0건이던 두 영상이 **이미 게시돼 있었다**
  (인덱싱만 늦었다). ⇒ **D+2~D+3엔 유튜브 사이트 내부 검색으로 스윕**: `youtube.com/results?search_query=...&sp=EgIIAw%3D%3D`(업로드=이번 주)
  후 JS로 `watch?v=` href 회수. **매체 기사도 같다 — 403/0건은 브라우저 경로로 재확인**(헐전에서 요크셔포스트·avfc.co.uk 성공).
- ⛔⛔ **「재판정 조건」을 걸어놨으면 조건 충족 여부를 회차마다 확인하라 — 09-15 실증(obs#785).**
  HELD 34행이 조건을 걸어놨는데 처방이 대거 바뀌어도 **아무도 확인하지 않아** 25행 중 **5행이 방치**됐다.
  ✅ **`refresh_duty_applied.py`가 이제 회차마다 후보를 보고한다**(obs#786, 보고만 — 판정은 안 바꾼다): **닿았을 수 있다**
  (HELD/REJECTED인데 분석 역할이 이제 처방에 있다) · **다시 갈렸다**(APPLIED인데 불일치) · **근거가 무효화됐다**.
  ⛔ 오탐 제외: 「처방이 아예 없다」(애초에 없는 선수 = `NO_RX`) · REJECTED의 처방 없음(스쿼드 밖이 사유).
  ⭐ 사유에 「무효화」를 적으면 억제 — 안 그러면 같은 행이 회차마다 떠서 **신호가 죽는다**.
- ⛔⛔ **선발 11명을 「감독의 선택」으로 읽기 전에 가용 인원부터 확인하라 — 09-15 실증(obs#787).**
  포레스트전 5패스가 **바클리 LDM을 인선 판단으로만 다뤘는데**, 실제로는 **주앙 고메스의 출전정지 대체**였다(PL 2~4R, 유럽 대회 미적용).
  ⇒ 상대가 표적으로 삼은 구조를 「에메리가 고른 것」으로 귀속하는 오독이 **6패스째에야 잡혔다**.
  ✅ 절차: 경기 수집 회차에 **⑴ 직전 경기 XI 대비 변경 명단 ⑵ 각 변경의 사유(징계/부상/로테이션)** 를 먼저 세운다 —
  **징계·부상 강제와 선택된 로테이션을 분리**해야 `rotation` 축의 정량 표본이 성립한다.
  ⭐ 부류: **「왜 이 선수가 여기 있나」는 전술 질문이기 전에 가용성 질문이다.**
- ⛔⛔ **부분 수정은 같은 부류의 나머지를 남긴다 — 09-15 실증(obs#785).**
  obs#743이 `canon()`의 `w_`/`wm_` **한 쌍만** 고쳤는데, `game_roles` 전수 확인 결과
  **어간 7개가 여러 접두에 걸쳐 있었다**(`dlp`·`holding` = cm/dm · `halfwinger`·`playmaker` = cam/cm ·
  `insidefwd`·`wideplm`·`winger` = w/wm) ⇒ **cm/dm·cam/cm 4쌍이 오탐으로 남아** 소보슬라이 `#195`를 불일치로 셌다.
  ✅ `canon()`이 이제 **접두를 전부 뗀다**(어간 충돌 없음 — 위 7개만 중복이고 전부 인접 슬롯군).
  ⇒ ⭐ **오탐 부류를 고칠 때는 어휘 전수로 같은 구조가 더 있는지 먼저 센다.**
- ⛔⛔ **판정 표식은 「그 시점의 규칙」에 묶인다 — 규칙을 강화하면 기존 표식을 재검증하라. 09-15 실증(obs#783).**
  `[표본판정]`은 **공식전 필터가 생기기 전**에 붙었는데 **G18이 표식 행을 건너뛰어** 30행이 새 규칙으로 검사되지 않았고
  **5행의 친선 혼입이 숨어 있었다**. ✅ G18이 이제 **「친선의존 N건」을 보고**한다(실패로는 안 센다).
  ✅ 같은 점검을 G16 사람 판정 90행에도 했다(obs#785) — **7단계가 바꾼 Δ≤.05 구간 근거 판정은 0건**이었고,
  대신 **재판정 조건 충족 5행**이 나왔다.
- ⛔⛔ **결손이 우연히 규칙을 지켜주고 있을 수 있다 — 09-15 실증(obs#773).** `player_matches.pos_class`가 친선 경기에서
  대부분 NULL이었고, 그 NULL이 **친선을 집계에서 빼주고 있었다**(obs#126 「친선경기는 실측을 주지 않는다」).
  결손을 백필하는 순간 그 규칙이 깨진다 — 점검하니 **이미 12행이 깨져 있었다**(기존 5행 포함).
  ⇒ ⭐ **결손을 채우기 전에 「그 결손이 무엇을 막고 있었는지」 먼저 묻는다.**
  ✅ 공식전 필터는 `scripts/audit_measured_samples.py`의 `EXCLUDE_COMP` 상수가 정본이다
  (`Club Friendly`·`Club Friendly Games`·`FIFA World Cup`·`UEFA Youth League`). ⛔ `core.aggregate`는 대회를 거르지 않는다 — **호출부가 건다.**
- ⛔⛔ **「자동 재계산」 스크립트는 자기가 쓰는 필드에 사람 손이 섞였는지 먼저 물어야 한다 — 09-15 실증(obs#767).**
  `refresh_duty_applied.py`의 「자동 4종만 재계산」은 **`applied_status`에만 참이고 `applied_note`에는 거짓**이었고,
  손으로 교정한 9행을 템플릿으로 덮었다. **G14는 그 컬럼을 보호하지 않는다**(migration 031로 나중에 추가 — 보호 목록이 마이그레이션과 함께 갱신되지 않는다).
  ⇒ 이제 **상태가 실제로 바뀔 때만 사유를 다시 쓴다**(`--force-note`로 강제). `index_transcripts.py`(채움 전용)·`write_video_summary.py`(`--force` 없이 불변)는 이미 그렇게 돼 있었다.
  ⏰ 같은 위험 잔존: `video_impl_claims`는 **DELETE+재삽입**이라 사람이 verdict를 고친 뒤 JSON을 다시 돌리면 날아간다(할 일 18).
- ⛔⛔ **처방 판정 전에 근거 행의 `season`·`sample_n`·`map25` 유무를 먼저 출력하라 — 09-15 실증(obs#766).**
  같은 회차에 **표본 혼용**(6경기 표본의 Δ를 시즌 정본 근거로 제시, obs#764)과 **시즌 혼용**(`season=2025-26` 행을 「시즌 전체」로 읽어 반대편 오류, obs#765)을
  연달아 냈다. `prescriptions`는 같은 선수·같은 `pos_label`에 **시즌·kind가 다른 행이 10개 이상 공존**한다 —
  **「시즌 그리드」라는 이름만 보고 현 시즌으로 간주하면 안 된다.**
  ⭐ 또한 **`fc26:opt:*` 계열은 fit 수치가 있어도 투영값일 수 있다**(캐시 RM .835 = RB 그리드를 RM 슬롯 x에 투영, **통산 RM 0경기**). 할 일 16.
- ⛔ **「포지션-순수」라는 라벨을 믿지 말 것 — 09-15 실증(obs#768·769).** `#470`이 스스로 그렇게 적고도 `pos_class=LDM` 경기를 포함했다(표본의 19.5%).
  ✅ **감사 상시화**: `scripts/audit_measured_samples.py`(읽기 전용) — ⑴ rationale 열거 일자 ↔ `pos_class` 대조 ⑵ `core.aggregate` 재집계로 map25 문자열 대조.
  ⭐ **오염이 전부 결함은 아니다** — 세 부류로 갈린다: ⓐ **어휘 불일치**(우리 슬롯 `RDM` ↔ SofaScore `RCM`) ⓑ **`pos_class`가 현상 그 자체**
  (평균 위치 파생이므로 **명목 9번이 내려오면 CAM으로 분류되는 것이 정상** — 잭슨·이삭·하토) ⓒ **진짜 위치 불일치**(재선언 대상).
  ⇒ 판정 끝난 행은 rationale에 **`[표본판정 YYYY-MM-DD]`** 를 남긴다(없으면 영구 오탐이 되어 게이트로 못 올린다).
  ⛔ 재집계할 때는 **「사용 경기 N건: 날짜(상대, 대회, pos_class, 분)」 목록을 반드시 기입**한다 — 출처 추적이 없으면 감사 ⑴이 성립하지 않는다.
- ⭐ **같은 회견은 2곳 이상에서 받는다 — 09-15 실증(obs#774).** 시메오네 안필드 회견 3종(스페인어 → 영어 통역[질문 추가] → 제3채널[**답변 확보**])을 거쳐야
  「백3 → 후반 변경」 답변을 얻었고, `TyE61f5CQ9I`는 전사가 **사실상 비어 있었으나**(라이브 광고·잡음) 같은 회견 4편 확보로 **손실 0**이었다.
  ⛔ **한계**: 고유명사 오인식은 **양쪽이 똑같이 틀린다**(같은 ASR) — 「Carnival Cup」(Carabao)·「Tyon Pow」가 두 전사에 동일.
  ⇒ **문장 구조는 복수 전사로 거르고, 인물명은 실측 라인업으로 건다.**
- ⛔ **`observations.id`를 하드코딩하면 동시 실행 세션과 충돌한다**(09-08 실증, 경합 3번째). `max(id)` 읽고 INSERT 하는 사이에
  다른 세션이 #500~505를 선점해 **트랜잭션 전체가 롤백**됐다. ⇒ **id 생략(자동 배정)하거나 `BEGIN IMMEDIATE` 직후 재확인**하고,
  리포트·상호참조의 obs 번호는 **INSERT 성공 후에 기입**한다.
- ⛔ **동시 세션이 있으면 새 파일도 Write 전에 `git log -- <path>`로 이력을 본다**(09-08 실증). 아침에 미추적(`??`)으로 보인
  `test_g14_regression.py`가 그 사이 다른 세션 커밋에 들어가 있었고, Write로 **덮어썼다가 `git show`로 복원**했다.
- 🔴 **커밋만 하고 푸시 안 함 — 2회 재발.** 세션 시작 시 `git status -sb`의 **`[ahead N]`을 반드시 본다**(obs#382 유형).
- ⛔ **curl·WebFetch로 FotMob·SofaScore API를 치지 말 것** — 페이지 컨텍스트 밖에서 막힌다(404/403). 브라우저로 오리진을 연 뒤
  JS 안에서 fetch하면 200이다(✅ SofaScore는 08-19 이후 재개통 — 차단으로 단정하지 말고 먼저 확인한다).
- ⛔⛔ **`site/assets/*.js`를 고쳤으면 임포트의 `?v=`를 반드시 올려라**(G9가 막는다).
- 🔴 **obs#110 솔버 산출을 인용할 때는 obs#111 이후인지 확인한다** — obs#111이 C6를 하드화해 **정본은 좌측 전진**이다.
- ⭐ **유튜브 자동 자막 회수**: `scripts/yt_transcript.py VIDEO_ID LANG` → `reports/transcripts/`. 봇 차단(「page needs to be reloaded」) 시
  브라우저 pot 경로 `scripts/yt_transcript_json3.py`(정본 docs/30). 영상·음성은 시청 불가 — 인용 시 confidence에 **auto-caption** 명기.
  ⏰ docs/30 「듣지 않은 영상은 인용 금지」에서 **「전사를 읽은 영상」의 지위는 미정의** — 규약 갱신은 사용자 판단 대기.

## 자동화

- ⚠️ **스케줄 정본은 `mcp__scheduled-tasks__list_scheduled_tasks`다** — `CronList`는 다른 시스템이라 0건으로 보인다(09-03 오판 확정).
- 🔴 **2026-09-17에 스케줄 목록이 통째로 비어 있는 것을 발견**했다(정기·일회성 전부). 원인 미확인 — 앱 재설치·설정 초기화 가능성. **정기 태스크가 사라져도 조용하다**는 뜻이므로 회차마다 목록을 확인한다.
- **정기 3건 재생성(09-17)**: `match-watch-weekly`(월 10:06 + 지터) · `henderson-che-debut-recollect`(매일 09:07 + 지터 — 헨더슨은 26/27 첼시 출전 0경기·손목 부상 상태라 여전히 유효, 데뷔 확인 시 재수집 후 **스스로 비활성화**) · `transfer-watch-daily`(**매일 09:05·21:05**, 사용자 승인 09-17 — SKILL 기재대로 2회).
  ⛔ **스케줄 태스크는 자기 자신을 삭제할 수 없다** — 비활성화만 되고 삭제는 일반 세션에서 `mcp__scheduled-tasks__delete_scheduled_task`로 한다.
- ⛔ **미복원 1건(사용자 결정 09-17: 복원하지 않음)**: `weekly-unifi-now-sync`(월 10:03) — **이 저장소 밖 타 프로젝트**라 프롬프트 내용을 모른다. 필요하면 해당 프로젝트 세션에서 직접 만든다.
- **일회성 3건(09-18)**: `mw-d2-liv-tottenham-0915`(21:00) · `mw-d2-avl-coventry-0916`(21:30) · `mw-d2-atm-osasuna-0916`(22:00).
- Codex heartbeat **id=3 「프리미어리그 3팀 경기 수집」**, 매일 08:00 KST (저장소 밖 · 별도 관리).
- ⭐ AVL 영상 회차는 **필수 채널 3곳**(UTV · The Villans · 1874, docs/30 표) + **Jacob Tanswell**(The Athletic, X `@J_Tanswell`)을 항상 조회한다.

## 최근 작업

⭐ 규칙 8에 따라 압축한다(전문은 커밋·obs) — 여기엔 **나중에 다시 물을 만한 것만** 남긴다.

- **09-11~12 ⑼·⑾**(`2205d24`…`4108bba` · `0aa7c79`·`1c6241b`, obs#616) — FC27 4팀 전량 수집 + FC25·FC26 **출시판 시계열**.
  ⭐ fut.gg 벌크 API `/api/fut/players/v2/27/?club_id=N` + `/all-versions/{eaId}`.
  ⛔ **동일성 사고**: 리스 제임스 행이 **로렌 제임스(첼시 위민)** 카드로 오염 — 이름 검색은 남녀를 섞는다, **EA id·gender로 특정**할 것.
- **09-11 ⑩**(`f4bb1cc`) — ⛔⛔ 신규 발견으로 오인한 영상이 D+1에 이미 반영돼 있었다(결번 #614) —
  **새 발견을 DB에 쓰기 전 `git log -- <파일>`로 기존 반영 여부부터 확인할 것.**
- **09-08~10 ⑹·⑺·⑻**(`1aeefa2`…`c970189`, obs#531~535·538·567·587) — 인게임 캡처 1차(Remote Play) · A/B 3·4·5 · 시뮬 1 ·
  `core/team_settings.py` 규칙화 → **G15** · `cells_poss/def`·`def_x` · `reproduction_limits` 12행.
  ⭐ **T1↔T2 = 「RM 맥긴 wideplm/Attack → widemid/Support」 단일 변수 실험**, 자기진영 5.9%→38.9%(obs#537).
  ⭐ **obs#702**(시뮬은 프레스티지가 가른다 — 승점으로 전술 비교 금지)가 여기서 나왔다.
- ⏰ 그 이전 이력은 `git log`와 `observations`에서 조회한다.

## 진행 중 작업 (WIP)

**없음.** ⑳ 코번트리 영상 14편 반영 완료(아래 현재 상태). 게이트 G1~G18 전항 통과.
⏰ `refresh_duty_applied.py` 「재판정 조건 충족 후보」 2건은 **둘 다 미충족으로 판정**: 알리송 #31(09-17 판정 그대로 — 유효표본 여전히 1경기, 09-16은 11분) · 음바예 #245(내가 방금 넣은 HELD — 클럽 표본 1경기, 조건은 2경기째).
📌 미커밋 잔여(이전 세션 산출): `reports/ingame/2026-09-0{8,9,10}-avl-*` 4폴더 · `reports/transfer-watch/2026-09-14.md`. 게이트 G1~G18 전항 통과 · 감사 전항 0 · 재판정 후보 0건.
⚠️ 미추적으로 남은 것: `reports/ingame/2026-09-0{8,9,10}-avl-*` 4폴더 · `reports/transfer-watch/2026-09-14.md` ·
**스페인어 전사 4편**(`RDHt-dGo644`·`UBaPgLg4kKI`·`_fKMMU1kcxM`·`uGlZbUGrHZs`, ATM 축 추정)
— **이전 세션 산출물이라 커밋 여부는 사용자 판단**이다(⑰ 세션은 손대지 않았다).
⏰ **남은 할 일은 전부 외부 입력 대기다** — 인게임 A/B(T6·C2/C6) · 맥 알리스터 공식전 2경기(LIV 6번) ·
ATM 3-4-2-1 슬롯 확정 · FC27 09-18 얼리액세스.

## 다음 할 일

00. ⭐⭐ **P1 · 진화·내 구단 축 후속(⑲)** — ⑴ **FC27 시장이 열리면 `collect_futgg_prices.py --games 27` 일일 실행**(스케줄 등록은 사용자 판단) → 「전술 구현」 탭 순위가 시세로 재정렬된다.
    ⑵ **출시(09-25) 후 진화 재수집**(`collect_futgg_evolutions.py --games 27 --fill-catalog`) — 지금은 도입용 무료 진화만 열려 Role++ 0건이다.
    ⑶ **GG Club 임포터**: 사용자가 브라우저 패널에서 fut.gg 로그인 + EA 연결을 직접 하면 그 세션의 GG Club 페이지를 읽어 `fut_club_players`로 옮긴다(로그인은 대행 불가).
    ⑷ 진화 카탈로그 2495(숨김) 확보 방법 · FC27 `game_roles` 커널(obs#629) 확보 후 `fc_role_familiarity_map`과 대조.
    ⑸ 카드 없는 4명(니콜자줄리·도밍게스·모레노·라하도)은 fut.gg에 없다 — FC27 미수록으로 확정할지 재확인.
0. ⭐⭐ **P1 · 다음 인게임 A/B(T6) = 「캐시 falseback」 단일 변수**(2026-09-13 사용자 결정)
   ⛔ 종전의 **2변수 동시 변경 계획은 폐기**됐다 — 귀속이 안 된다(obs#702 통제 실험의 교훈).
   ⭐⭐ **판정 지점이 명확해졌다**: 26/27 **공식전** 실측은 캐시 RB = **`fb_att_wb/Support` .8919**이고
   **`fb_falseback`은 상위 5위 밖**이다(obs#773 — 종전에 wingback으로 보였던 것은 **바이에른 친선 혼입** 탓, 교정됨).
   반대편 근거는 Brom Squad Gaming의 「**tucks inside to form a temporary back three**」(obs#774)인데 **25/26 체제 자료**다.
   ⇒ **실측 ↔ 영상이 정면으로 갈리는 지점을 T6가 판정한다.** 1차 판별 = **캐시 본인 그리드가 안쪽(열1~2)으로 이동하는가.**
   - 폴더 `reports/ingame/2026-09-1x-avl-test6/` · 전술 화면 · 히트맵 11장 · 팀 통계 · 조작 선수. ⚠️ 처리 전 썸네일 이름 대조.
   - ⛔ 캡처 단독으로 처방 변경 금지(docs/50). ⛔⛔ **시뮬 결과(승점)로 전술을 비교하지 말 것** — 프레스티지를 측정하게 된다(obs#702).
     팀 설정 2축(수비 접근·라인 높이)도 **사용자 직접 플레이 A/B**로만 검증한다.

0-2. **P2 · FC27 09-18 얼리액세스 후 4팀 역할·숙련도(Role+/++) 수집** — ⭐ **PlayStyles·키·몸무게·카드이미지는 09-11 완료**(4팀 131행, **FC25·FC26 출시판 시계열은 09-12 완료**),
    `2205d24`…`4108bba`). 남은 건 fut.gg `/api/fut/roles/` FC27 응답(역할·포커스 목록, docs/20 ② 타이브레이커의 전제) — 09-18 이전 확정 불가.
0-3b. **P1 · 부상 축 후속** — ⑴ 마첸(발목, 「maybe it is a long time」)·파우 토레스(햄스트링) **구단 공식 발표 재확인**(현재 등급 B, 감독 발언 인용) ⑵ premierinjuries·physioroom·transfermarkt 미조회 ⑶ 바리오스 부상 2소스 충돌(FotMob 09-12 Muscle ↔ 스페인 매체 교차확인 실패) ⑷ 스쿼드 밖 판정(발로건·민테·커티스 존스·조 고메스·키에사) 확인 필요.
0-3c. **P2 · 재검증 이월 — 포레스트전 3일 추적이 남긴 5건**(obs#792). ⑴ **재검증 ① 질문이 바뀌었다**(obs#714): 「잭슨이 내려오는가」
   → **「9번의 배후 침투가 빌드업의 폭을 만드는가」**. ⑵ **재검증 ③ 승격**(obs#725): 「4번째 표본 수집」 → **「이탈 조건 규명」** —
   글라스너가 **경기 전에** 빌라를 「compact 4-4-2 block」으로 스카우팅했으므로 **알려진 기본 형태의 이탈**이지 무형태가 아니다.
   ⑶ 후반 낙차 재발 여부(UTV 「45~60분 붕괴 = 에메리 체제 고질」은 3티어 단일, 신규 축 후보). ⑷ 바클리 선제골 행위의 성격
   미확정(obs#789 — 프레임 확인 전까지 obs#711 정본). ⑸ 파우 토레스·마첸 복귀 시점(→ 0-3b).
0-3d. **P3 · AVL 소스 2건 판단** — ⭐ **Aston Villa Review**(Chris Nee, 2티어 지역 분석, **유료벽** — 무료 도입부만) 09-15 신규 발견 ·
   ⚠️ **Read Aston Villa `HTTP 403` 4패스 연속**. docs/30 소스 표 등재 여부 / 브라우저 경로 재시도 여부는 사용자 판단.
0-4. **P2 · 선수 축 신규 517행 후처리** — formation·possession NULL, pos_class 좌표 폴백. 카스티요·미겔 요렌테 Primera Federación(아틀레티코 B) 행이 ATM 코드 — B팀 코드 분리 여부 판단. 무소·쇠를로트·르마르·바르가스 `squad_entries` map25·rate_v는 여전히 25/26 이전 값(표본 생겼으니 재산출 후보).
4. **P1 · obs#449 ⓐ/ⓑ 판별 — 양 풀백 att_wb 집계 역전이 체제 변화인가 국면 혼입인가.** 필요 조건: 점유 국면별(@dom/@tight) 분리 집계가 **버킷당 2경기**(74% 표본은 헐전 1경기뿐 → 다음 우세 경기 후 판정). 그때까지 **obs#443 짝짓기 규칙·정본(좌 전진·우 절제) 유지**. 사용자 인게임 A/B 체감(윙어 쪽 wingback/B · 반대쪽 att_wb/**Attack**)은 여전히 미수집.
5. **P1 · 미결 이적 4건 개별 처리**(에메날로·르마르·바르가스·응게상) — 실효 시한 도래분부터. 절차는 「현재 상태」 마지막 문단.
6. **P2 · 아틀레틱 5-4-1 전환 시각 미확정** — 2소스 모두 분(分) 미명시로 obs#498·#507이 구간 분할에 못 쓰인다. ⭐ 남은 방법: WhoScored 이벤트 타임라인으로 **평균 위치 변화 시점 역산**.
6-1. **P2 · G13 확장 후보 2건 사전등록** — ⑴ `team_code IS NULL` 0건 불변식(09-12 정리로 0 달성, obs#618 규약 「NULL은 미배정」) ⑵ 「선수×시즌 클럽↔클럽 혼입」(중간 이적 예외 설계 필요). ✅ 재산출 확인(obs#619): team_code 배정은 `v_player_profile`(팀 필터 없음)·`rate_v`·`prescriptions`에 무영향. ⏰ **별건**: `measured:season*` 처방 56건 중 **48건 sample_n이 현재 표본과 불일치**(8~9월 스냅샷 — 엔소 15→56·소보슬라이 18→66·비르츠 24→64). 재산출은 새 행으로, fit_sim Δ>0.05만 인선 재검토 — 사용자 판단 대기. · 팔레스트라·켄다·기튼스 **공식전 미출전 사유** 미수집.
8. **P2 · obs#353 후속** — 학포 RM 배치로 「검증된 LM이 학포뿐」 리스크 재판정. ⭐ 무뇨스 LIV RM 선발 지정으로 전제가 바뀌었다(무뇨스 좌측 표본은 오사수나 — 불변규칙 7 캐비앗 유지).
9. **P2 · 경기 전용 처방 재판정을 match-watch 표준 파이프라인에 넣을지 확정** — R3 4경기에서 반복 적용됐다.
11. **P3** · 병렬 중복 실행 가드 미수립(obs#354, 데이터 재발 감지는 G13) · 겨울창(2027-01) transfer-watch 재개는 사용자 판단
   (런북은 저장소에 있어 스케줄만 만들면 된다) · 그리말도(ATM) 실측 채워지면 슬롯 기하·fit 재검증.

15. **P2 · claims 미작성 91편**(요약 있는 행) — 회차마다 새 요약과 함께 채우고, 백로그는 §11-3 후보 축부터.
   ⏰ **PENDING 3건 마감 조건**: ATM 국면별 9번 교대 = 2소스 교차검증 · trampa pentagonal = reproduction_limits
   pressing 4번째 행 등록 여부 · CHE Counter = 저점유 2경기째 확인.

20. ⭐⭐ **P1 · C2/C6(풀백 비대칭) 재판정 — 해방 사이드 반전 때문**(obs#768).
   구성이 **우측 해방(맥긴 winger/Attack) + 좌측 뱅크(헤밍스 widemid/Support)**로 바뀌었다.
   현행 slot_canon은 LB `fb_att_wb/Support`(전진) · RB `fb_wingback/Balanced`(절제).
   ⑴ **obs#443**「윙어(터치라인형) 쪽 풀백 = wingback/Balanced」 ⇒ RB 절제 = **정합, 변경 불필요**
   ⑵ **obs#95**「해방 사이드의 풀백이 올라간다」 ⇒ RB가 **전진(att_wb)해야** 하고 LB는 절제로 뒤집어야 한다
   ⛔ 두 규칙이 이 구성에서 처음으로 갈린다 — 인게임 A/B(T7 후보 2순위) 없이는 못 닫는다.
29. ⭐⭐ **P1 · LIV 6번(홀딩) 축 확정**(obs#778) — 서사와 실측이 **정반대를 가리킨다.**
   서사 2소스(`N-oZ3Cn_XDc`「흐라벤베르흐는 천성적 6번이 아니다 · 맥 알리스터가 내 6번이다」 ·
   `BG7OGbbKmY0`「그가 돌아가자 팀이 돌아갔다」 48터치)는 **맥 알리스터**를 지목하는데,
   ⑴ 맥 알리스터 `#491`은 **role·fit이 전부 NULL**(공식전에 2경기 이상 가진 pos_class 없음, obs#770)
   ⑵ 흐라벤베르흐 `#368`은 LDM 순수 2경기에서 `dm_dlp/Roaming` **.7633, Δ.1148 단독 확정**이라 **커널은 잘 맞는다고 본다.**
   ⚠️ 영상은 「위치 적합」이 아니라 **「수비 인지」**를 말한다 — docs/22 PlayStyle·능력치 층이라 **층이 다르다.**
   ⏰ 마감 조건: 맥 알리스터가 PL·UCL에서 **같은 `pos_class` 2경기**를 채우면 재산출해 둘을 같은 슬롯에서 비교.
   ⛔ 그때까지 **어느 쪽도 처방으로 굳히지 않는다.**
30. **P2 · ATM 백3/백4 프리셋 병기 판단**(obs#775) — 표기 충돌의 원인이 **푸빌의 좌우 ±17 이동**으로 특정됐다.
   제안: `team_tactic_setups`에 **백3(3-4-2-1)와 백4(4-4-2) 두 프리셋을 병기**하고 **푸빌의 좌우값으로 구분**한다.
   ⚠️ 선행 조건 ⑴ 표본 5경기(백3는 2경기)로 얇다 ⑵ 08-29 요렌테 x=33.4는 다른 경기(46~51)와 달라 **국면 혼입 확인 필요**
   ⑶ ATM 3-4-2-1 슬롯 체계가 **LOW-MEDIUM 잠정**이라 슬롯 x 확정이 먼저다.
   ⭐ 이 결과는 `i_nG7i-vavk`·`Vcps1dALdnE`의 「요렌테를 전진시켜라」를 재평가하게 한다 — 그는 이미 x≈49로 전진해 있고,
   더 올리려면 **푸빌이 그 자리를 받아야** 한다(백3 경기에서 실제로 그렇게 하고 있다).
33. **P3 · LIV 슬롯에 LAM/RAM 신설 여부**(obs#784) — 비르츠 LAM 2경기 179분 · 소보슬라이 RAM 2경기 180분이
   **슬롯이 없어 축을 못 만든다**(LIV는 4-2-3-1 Wide 기준이라 LM/RM만 있다).
   ⚠️ 슬롯 신설은 기하(x·y)를 정해야 하므로 실측 2경기로는 얇다 — 표본 증가 후 판단.
## 미해결 — 판단이 필요한 것

1. ⏰ **ATM 포메이션 표기 충돌 — 4-4-2 쪽으로 기울었으나 미해소 존속.** 비야레알전에서 우리·El Desmarque·COPE는 **4-4-2**,
   **Infobae는 「4-3-3 de Simeone」**이었다(obs#333 전제와 직결). ⭐ **09-08 진전**: ATM 전담 분석이 아틀레틱전 최종 포진을
   **4-4-2로 명시**(obs#508)하고 09-09 예상 XI도 4-4-2 ⇒ obs#454는 **강화 방향**. ⛔ 그러나 **감독 발언은 3일 창 내내 0건**이라
   확정 근거가 없다. ⭐ 실측 기하가 4-1-4-1에 더 가까워 처방 미이관 판단은 여전히 정합적.
   ⚠️ 별건: 「**salidas de tres**+라인 간 3명」은 **보유 국면 별도 축**으로 본다.
2. ⏰ **FC27 커널 대기** — 사전 조사는 09-04 완료(obs#445). **PlayStyles·6대스탯·키·몸무게 EA 확정 트리거는 09-11에 완료**(4팀 전량 수집).
   잔여 트리거: **09-18** fut.gg `/api/fut/roles/` FC27 → `migrate_fc27.py --roles --check` → `EXPECTED["FC27"]`·gates 앵커 **새 행** → `--apply`(승인).
   ⛔ 역할 목록 변화는 **미공개** — 확정 전 커널 행 금지. ⚠️ **고레츠카 FC27 게임스탯 0행은 확정 결손이다**(09-11 재확인 — 이적 전 자유계약 공백기라
   fut.gg 전 버전에도 카드가 없음, all-versions API로 직접 대조 완료) ·
   **35속성은 완비사카·음바예 2명**(obs#364).
## 데이터 수집 상태와 결손

- 대량 수집: `collect_fotmob_players.py` · `collect_understat_shots.py` · `collect_event.py`(이벤트 축).
- ⭐ **읽기 전용 진단·감사**(전부 **진단만** 한다 — 판단은 사람이): `check_fit_drift.py` · `check_side_bimodality.py` ·
  `check_height_bimodality.py` · **`audit_measured_samples.py`**(09-15 신설 — 「포지션-순수」 선언 검증, `[표본판정]` 표식 존중) ·
  `db_diff.py`(**NOT NULL→NULL 전이를 따로 센다** — FK 검사는 「값이 조용히 지워졌다」를 못 잡는다).
  회귀 테스트 `test_g13/g14/g8x_g15_regression.py`(결함 합성 주입).
- ⭐ **채움 전용 스크립트**(사람 손을 덮지 않는다): `backfill_pos_class.py`(09-15 신설) · `index_transcripts.py`(report_id) ·
  `refresh_duty_applied.py`(`--force-note`) · `write_video_summary.py`(`--force`/`--force-claims`).
- ⛔⛔ **검색엔진 연도 혼입 주의.** 같은 상대·같은 달·유사 스코어의 전년도 경기는 발행일을 반드시 확인한다.
  ⭐ **엔진 요약 자체가 오염되는 유형**도 있다(과거 시즌 사건을 현재 확정 사실로 제시) — **엔진 요약은 근거로 채택 불가, 개별 URL 실물 확인만.**
  ⚠️ **verbatim 인용은 요약 경유 시 열화된다.** 실질만 채택하고 인용문은 원문 확보 전까지 쓰지 않는다.
- Understat은 빅5(+RFPL)만(챔피언십·에레디비시·리가2 없음). Sofifa 35속성/playstyles·FBref 12축은 403 결손.
- ⏰ **AVL LM/RM 슬롯 x 재검토는 표본 부족으로 보류**(obs#349). 재시도 조건: 정상 경기 누적 + 해당 경기 `cells` 수집.
- ⭐ **1차 소스 메모**: `laliga.com/clubs/{club}/transfers`는 등록 기준 1차 소스로 유효하나 날짜가 **행정 등록일**이라 구단 발표일과 다를 수 있다
  (`atleticodemadrid.com`은 403). 결손과 0을 구분한다(`docs/30`).

## 고정 작업 규칙

1. 시작 시 `git status --short`. 로컬 변경이 있으면 fetch/pull하지 말고 내용부터 확인한다. 깨끗할 때만 `git fetch && git pull --rebase origin main`.
2. 다른 PC/세션 변경을 버리거나 덮지 않는다. 특히 `db/tactics.db`는 충돌 시 기계 병합 금지.
3. DB 변경 뒤 반드시: `scripts/export.py` → `scripts/db_dump.sh` → `scripts/gates.py`. **`git add -A` 금지** — `db/tactics.db`,
   관련 `db/dump/`, `site/data/`, `reports/`, 문서를 명시 스테이징한다. push 전 `origin/main` 이동 여부를 다시 확인한다.
4. ⛔ **원장 행을 `UPDATE`로 고치지 않는다 — 정정은 새 obs 행으로 한다**(G14가 막는다).
5. 시즌 집계는 45분+·hit_points 15+만 쓰되, 경기 리포트는 짧은 교체 포함 **출전자 전원**을 기록한다.
   match 전용 fit/전술은 시즌 `prescriptions`/`team_tactic_setups`에 **자동 병합하지 않는다**.
6. ⚠️ **스케줄 세션이 돈 뒤에는 `git log`와 `git status`를 함께 본다**(커밋만 하고 푸시 안 한 사례, obs#382). 또 **다른 세션의 미커밋 DB
   변경이 보이면 `db/tactics.db`를 내 커밋에서 빼라** — 그쪽이 export·dump를 안 돌렸다면 dump·site/data가 어긋난다.
7. **파괴적 정리 전에는 `python3 scripts/db_diff.py --snapshot`** — 「새로 NULL 된 행 0」을 증명하는 유일한 수단이다(09-01 실증).
8. HANDOFF는 300줄 이하로 유지한다. 완료된 항목·해소된 미해결은 **지운다**(기록은 커밋과 obs에 남는다).

## 핵심 방법론

- 그리드 인코딩·집계·커널은 `core/`만 쓴다(재구현 금지). 좌표: x는 공격 방향, y가 낮을수록 오른쪽 —
  `cells_from_points` → `encode` → `Kernel.best_fit_slot`. **역할군 argmax 전에 슬롯 유형 필터 필수.**
  적합값 Δ 0.02~0.05는 EA 공개 노이즈 구간이라 그 차이만으로 인선을 바꾸지 않는다.
- 히트맵은 요구 역할의 일부만 설명한다. duties·1차 발언·전술 영상이 명확하면 낮은 fit보다 우선할 수 있다. ⭐ 단 **경기 프리셋의 역할 코드는
  그 경기의 위치 실측을 따른다**(09-03 잭슨) — 서사가 뒤집은 것이 **온볼 기능**이면 PlayStyle 층(docs/22)이고, 커널 Δ가 노이즈(≤0.05) 밖이면
  서사로 역할을 덮지 않는다.
  ⭐ **풀백 전진은 같은 쪽 와이드 역할이 결정한다**(docs/20 ⑨) — 윙어 뒤 풀백은 wingback/B, 전진은 C6 해방 사이드에서만.
  ⭐ **10번 포켓 충돌은 CAM 유형의 함수**(obs#464) — halfwinger 옆엔 insidefwd가 보완(.329), playmaker/classic10 옆엔 wideplm/widemid.
  사이드 스위칭 포커스는 `cam_halfwinger`/Roaming 하나. **와이드는 10번 유형을 먼저 정한 뒤 고른다.**
- 단일 경기·퇴장·저점유 같은 교란은 confidence와 보고서에 함께 쓴다.
- ⭐ **불변규칙 7(팀 축 혼선)은 세 번 터졌다** — 런북 슬롯 x 표 · 음바예 regime_id 오배치(obs#355) · **`team_code` 739행**(obs#372·373).
  **타 팀 실측은 출처 팀·체제를 반드시 명기.**
- ⭐ **불변규칙 10 최고 실증**(09-01): 아하노르 거래 구조는 **이탈리아어에만**, 무드리크 매수옵션은 **우크라이나어에만**, 응게상 선행조건은
  **프랑스어에만** 있었다 — 영어권만 봤으면 **거래 형태를 오독**했을 건이다.
- ⭐⭐ **불변규칙 10의 운영 형식**(obs#441·442) — 「다국어를 했다」는 자평은 믿지 않는다.
  ⑴ **선발 11명의 국적·경유 리그를 체크리스트로** 만들어 언어축을 강제한다(화제성만 따라가면 감독 모국어와 최대 화제 선수 축만 훑는다).
  ⑵ **언어축별로 기대 산출물이 다르다** — 감독 모국어·화제 선수 축은 「경기 분석」, 나머지 국적 축은 **「이적 서사·배경」**이 수확이다.
  ⑶ **「0건」은 구조적 사실일 수 있다** — ⛔ **그러나 당일 「0건」은 네 번 반전됐다**(D+1 유튜브 · D+2·D+3 전술 유튜브 · 헐전 Coaches' Voice).
  ⇒ **경로·검색어를 바꾼 재확인 전에는 종결로 적지 않는다.** ⭐ 유튜브는 **사이트 내부 검색**으로 확인한다(obs#516).
  ⑷ ⭐ **언어권이 저밀도라는 것도 결과다** ⇒ 다음 라운드에서 **우선순위를 내리고 사유를 남긴다**.
  ⛔ **단 「결장자 축」은 내리지 않는다** — 고레츠카는 D+2 당일 결장 확정 발표가 나왔다(09-08 실증).

## 참고 문서

- `docs/20`: 슬롯 x·역할·포커스·게임 구현 규칙(**⑧ movement_kr · ⑨ 윙 혼잡 · 팀 설정 매핑 규칙**) · `docs/30`: 수집·좌표·표본·결손(**⑧ xG 제공사 · 국면 그리드**) ·
  `docs/40`: DB/export/dump/git · `docs/50`: 인게임 경량 검증(캡처·전술 코드) · `docs/60`: 새 축 검증·통계 기준 · `docs/22`: PlayStyles·스탯(온더볼) ·
  `docs/21`: FC27 온보딩·트리거.
