# HANDOFF

## 프로젝트 정보

- **프로젝트명**: Aston Villa → FC 전술 구현 (2026-07-30 3팀 → **2026-08-20부터 4팀 체제**)
- **한 줄 설명**: 실제 감독의 전술을 실측 데이터로 분해해 FC26 인게임 전술(포메이션·역할·포커스)로 재현한다.
  대상: **에메리(빌라, 주) / 알론소(첼시) / 이라올라(리버풀) / 시메오네(아틀레티코)**
- **주요 경로/저장소**: `/Users/ad03230205/Documents/tactics` (git, main) — ⚠️ 「`/Users/user/`」는 오기였다
  - DB `db/tactics.db` = single source of truth / `site/data/`·툴 = DB 파생
  - 문서 맵은 `CLAUDE.md` 참조. 감독 문서: `docs/10`(에메리)·`11`(알론소)·`12`(이라올라)·`13`(시메오네)
- **관련 링크**: 스케줄 작업 `villa-transfer-watch` (매일 09/21시)

### 프로젝트 스킬 (`.claude/skills/`)

| 스킬 | 언제 쓰나 |
|---|---|
| `transfer-watch` | 4팀 이적 루머 정기 감시. 스케줄(09/21시) 또는 `/transfer-watch` |
| `match-watch` | 경기 수집 — 실측·팀스탯·경기 리포트·게임 구현 판정 |
| `player-collect` | ⭐ **한 선수의 모든 데이터 축(14축) 전수 수집.** "○○ 데이터 수집"이면 이것 |
| `hadoff` | 세션 인수인계 — HANDOFF.md 갱신·Git 동기화 |

### 대량 수집 스크립트 (`scripts/`) — ⭐ 에이전트 컨텍스트를 거치지 않는 경로

| 스크립트 | 언제 쓰나 |
|---|---|
| `collect_fotmob_players.py` | ⭐ **FotMob 2명 이상**이면 무조건 이것. 축 **1·5·6·7·8·15·16**을 한 번에. 멱등.<br>`.venv/bin/python scripts/collect_fotmob_players.py --team ATM [--include-targets] [--resolve-ids] [--adopt-fotmob-position] [--dry-run]`<br>`--resolve-ids`=fotmob_id 검색 해결 · `--adopt-fotmob-position`=포지션 표기 통일 |
| `collect_understat_shots.py` | ⭐ 축 **11**(슛맵)·**17**(경기 단위 창조). SofaScore 차단의 대체 원천. 멱등.<br>`.venv/bin/python scripts/collect_understat_shots.py --team AVL --include-targets --seasons 2025 2024` |
| `check_fit_drift.py` | 저장 적합값 vs 커널 재산출 대조(**읽기 전용**). 슬롯 기하 변경 후 필수 |
| `fetch_fotmob.py` | 루머 목록(리드 소스 전용 — 티어 근거 금지) |
| `collect_sofascore.py` · `import_fotmob_target.py` | 단일 선수·기존 경로 (⛔ sofascore는 403 중) |

⚠️ **Playwright(`.venv`)가 전제다** — 페이지 오리진에서 fetch해야 API가 200으로 열린다.
⛔ curl·WebFetch로는 안 된다. ⛔ **브라우저 MCP → localhost 전송은 전부 차단된다**
(`fetch`·form POST 모두 `ERR_BLOCKED_BY_CLIENT`, obs#273) — 그래서 이 스크립트가 있다.

---

## 현재 상태

> 마지막 갱신: **2026-08-21** · 세션 **#34** · 작업 PC `AD03230205ui-iMac.local` · 브랜치 `main` ·
> 최신 커밋 **`3377180`**(데이터 최신 **`74c7fda`**) · observations **278행**(최신 obs#278) ·
> match_reports **21행** · **4팀 체제**(AVL·CHE·LIV·ATM). DB는 v2(`db/tactics.db`). #33 이후 **8커밋**.
>
> ⛔⛔ **소스 접근 상태가 이 세션의 최대 변수다**(obs#274, 08-21 오후 4클라이언트 전수 프로브):
> **차단** — SofaScore 403 · **WhoScored 403** · FBref 403 · sofifa 403.
> **정상** — **FotMob 200 · Understat 200** · Transfermarkt 200 · premierleague.com 200.
> ⭐ 결정적: **`sofascore.com` 홈 HTML 자체가 403**이라 「페이지 오리진 fetch」 우회로가
> 원리적으로 안 통한다(curl·Playwright 헤드리스·**헤드풀**·브라우저 MCP 전부 동일) —
> 헤드리스 탐지가 아니라 **네트워크/IP 단위 차단**이다.
> ⇒ **P0 두 경로(축2 실측 / R2 그리드)가 동시에 닫혀 있다.**
> ⚠️ **이것은 「상태」다** — 08-21 오전엔 열려 있었다. 수집 착수 **전에 반드시 재프로브**한다.
>
> **⭐⭐ 대체 원천을 열었다 — Understat**: 슛 단위 좌표·xG·슛타입·상황을 커리어 전량으로 준다.
> `scripts/collect_understat_shots.py`(신설) · `players.understat_id` **105명**.
> ⛔ 한계: **빅5 리그(+RFPL) 한정** — 챔피언십·에레디비시·리가2 출전분은 없다.
>
> **⭐ 데이터 현황 — 17축(스킬 표 갱신됨)**
> `fotmob_detail_stats` **9,219행**(7,923→) · `season` **1,347** · `traits` **912** · `tenures` **1,283** ·
> `player_shot_profile` **85행**(9→) · **`player_market_values` 6,631**(신설) ·
> **`player_status` 106**(신설, 부상 보유 9명) · **`understat_player_matches` 4,817**(신설).
> `players` 신원: 생년 결손 **169→27** · 국적 **148→28** · 포지션 **71→12** · `fotmob_id` 결손 **5명**(리버풀 유스).
>
> **⭐ 데이터 현황 — ATM**: `squad_entries` **31행**(실측 16 + **자리표시 15**) · `player_duties` 31 ·
> `transfer_targets` 4 · `transfer_outgoing` 9 · `sort_order` 31/31.
> ⚠️ **자리표시 15행은 R2(08-23) 출전으로만 해소되는데 그 경로(WhoScored)가 지금 403이다.**
>
> **⚠️ 미해결 판단 사항 2건**(이번 세션 도구가 드러냈다)
> ⑴ **적합값 드리프트 6건** — `scripts/check_fit_drift.py`가 107행 중 88 일치, 6건 불일치를 보고한다
> (부엔디아 CAM +0.014 · 보가르드 FB +0.022 · 라크루아 CB +0.057 · 로저스 W +0.136 ·
> 니콜자줄리 CAM +0.224 · 에수구 CM +0.231). duty 제약(C5·C6)·사이드 고정일 수 있어 **행별 판정 필요**.
> ⑵ **저장값 NULL 13행**(AVL 주전) — 화면이 실시간 산출해서 비어 있는 것으로 보인다(**미확인**).
>
> ⛔ **여전히 미수집**: 축2 실측 3명(완비사카·톰모리·THB) · 축4 sofifa 35속성·playstyles ·
> 축12 FBref 0/12 — **전부 소스 403이라 스크립트로도 못 뚫는다.**

## 진행 중 작업(WIP)

**없음.** 작업 트리 clean · #34의 8커밋 전량 푸시됨.


## 다음 할 일

> ⏰ **첫 작업: 08-21 18:00 FC27 1단계 수집.** 이후 아래 PL 개막 3경기와 ATM R2를 `match-watch`로 수집한다.
> CONFIRMED 4건(콘사·완비사카·존스·바디아실)은 08-21 현재 모두 미발표다.

- [ ] **⏰⏰ P0 · PL 개막 실제 경기 3건 수집** (2026-08-21 사용자 지시) — 공식 일정 KST:
  **08-23 22:00 BHA–AVL · 08-24 00:30 NEW–LIV · 08-25 04:00 FUL–CHE.** 결과만 넣지 말고
  `match-watch` 전체 절차로 선발·교체, 선수·팀 실측, 전술·선수 분석, 경기 리포트까지 적재한다.
  Codex heartbeat 자동화 **id=3 「프리미어리그 3팀 경기 수집」**가 매일 **08:00 KST** 새 완료 경기를 확인한다.
  새 경기가 있으면 export/dump/G1~G12 후 명시 커밋·푸시하며, 소스 결손은 억지로 complete 처리하지 않는다.

- [ ] **⏰⏰ P0 · 수집 착수 전 소스 재프로브** (2026-08-21 신설 · obs#274) — 경기가 끝난 뒤
  `match-watch`를 시작하기 **전에** SofaScore·WhoScored 접근을 먼저 확인한다. 지금은 둘 다 403이라
  실측·그리드를 받을 데가 없고, 그 상태면 결과·라인업까지만 적재하고 **fit 산출은 불가**다.
  ⛔ 「런북에 열려 있다고 적혀 있다」를 근거로 쓰지 말 것(08-20 결정 사항).
  프로브는 4클라이언트 중 아무거나 1개로 충분하다 — 홈 HTML이 403이면 전부 막힌 것이다.

- [ ] **P1 · 적합값 드리프트 6건 처분** (2026-08-21 신설 · obs#278) —
  `.venv/bin/python scripts/check_fit_drift.py --min-delta 0.02`가 6건을 보고한다
  (에수구 +0.231 · 니콜자줄리 +0.224 · 로저스 +0.136 · 라크루아 +0.057 · 보가르드 +0.022 · 부엔디아 +0.014).
  ⚠️ **불일치가 오류라는 뜻이 아니다** — duty 제약(C5·C6)·사이드 고정 산출일 수 있다. 행별로 근거를 보고
  ⓐ 저장값 유지(제약 산출임을 confidence에 명기) ⓑ 재산출값 채택 중 하나로 닫는다.
  ⭐ 맥긴 DM만 「커널 최대 초과」라는 **불가능 조건**이라 이미 0.878→**0.717**로 정정했다.
  ⚠️ 함께: **저장값 NULL 13행**(AVL 주전)이 왜 비어 있는지 확인(화면 실시간 산출 추정 — **미확인**).

- [ ] **⏰⏰ P0 · R2(08-23 ATM vs 비야레알) 수집 시 「그리말도 실측이 채워지는지」 확인**
  (2026-08-21 **사용자 지시** · 상세 근거는 **obs#269** 및 `reports/transfer-watch/2026-08-21.md` 말미)
  현재 그리말도(LB)는 `map25` **0 자리표시** · `fit_*` **NULL** · `sort_order` **93**이다.
  ⚠️ `a8b25eb`의 sort_order 부여로 **깊이 차트가 「실제 서열」이 아니라 「그릴 수 있는 그리드 유무」를 반영**한다
  — 그리말도(€40m 실질 LB 1옵션)가 다니 마르티네스 뒤, R1 선발 마르코스 요렌테가 도밍게스 뒤다.
  그리드가 채워지면 자동 해소되고, 안 채워지면 캐비앗이 유지된다.
  ⚠️ **경로: SofaScore가 아니라 WhoScored `matchCentreData`(Opta)** — R1 matchId 1993898 · `isTouch`만 ·
  중괄호 균형 스캔 · 좌표 변환 불필요. 08-21 시점 SofaScore는 전 엔드포인트 403이었다(상태 재확인).
  ⛔ **§3의 45분+·터치15+를 `squad_entries` 제외 사유로 쓰지 말 것**(`736fbb9`가 정정한 오류).
  ⭐⭐ **연쇄가 더 크다**: match-watch 리포트 §4가 「R2 이후 **2경기 합의값으로 슬롯 기하 재산출**」을 예고했다
  ⇒ ATM `slots` x 변경 → **전 행 `fit_sim` 재산출** → **`sort_order` 재도출**. 자리표시 **15/31**이 대상이다.
  ⚠️ 잠정 슬롯은 **LM 24·RM 76·LCM 36·RCM 64**이고 **ST 50·GK 50은 움직이지 않는다**(`0c039c3` 근거).

- [ ] **⏰ P1 · 민테 등급 09-02 재판정** (2026-08-21 신설 · obs#272) — 하향 트리거 ⑵
  「창 마감까지 개선 오퍼 0건」이 **09-01이 지나면 자동 충족**된다. 그 시점에 MEDIUM으로 내릴지 판단할 것.
  ⚠️ 부상 복귀가 **Mid November 2026**이라 등록해도 시즌 개시 후 약 2.5개월 못 쓴다(비홈그론 슬롯 점유).

- [ ] **P1 · 프리시즌 리포트 15건의 처분 + 서사 수집 미수행분** (2026-08-16 신설 · 두 항목 병합) —
  match_reports 20행 중 **15행이 draft**이고 사유는 전부 「히트맵이 없어 단일 경기 fit을 못 낸다」는 **소스 결손**이다
  (자료가 얕아서가 아니다). ⇒ 현 상태로 complete로 올릴 수 있는 초안은 **0건**이다.
  선택지: ⓐ 현행 유지(친선은 영구 draft) ⓑ 히트맵 없는 경기용 **완료 기준 별도 정의** — **사용자 판단 사항.**
  ⚠️ 함께: 그 8개 리포트의 §8(영상·전술글·기사·감독 발언)이 **4종 전부 미수행**이다. 사유는
  「친선 커버리지 부재 + 회차 범위 밖」이고, **실측이 없는 경기일수록 서사 가치가 크므로 우선순위가 낮지 않다.**

- [ ] **P1 · 국면 축 확장** (obs#101 → obs#122 후속) — possession 표본이 늘어(로저스 42·콘사 38·
  디뉴 26·틸레만스 24) obs#101이 8쌍에서 멈춘 것을 다시 돌릴 수 있다. ⚠️ 게상 12/26·일링 0/15는
  결손(니스·임대분). ⚠️ obs#101 ③(**rest-defense 칸은 저점유가 옳다**)이 확장 표본에서도 성립하는지 재확인.
  (**R축은 기각**(obs#123) — 재개 조건은 표본 2~3배·쌍당 n≥60~100, `docs/60` 재사용.)

- [ ] **⭐ P1 · 첼시 피벗 처방 결정** (obs#116, **사용자 판단 사항**) — 기능축 실측(카이세도=볼위너·
  엔소=창조)과 현 처방(둘 다 `dm_dlp`)이 어긋난다. ⓐ 현행 유지(움직임 +.126, 평균 .799)
  ⓑ 소스 배정 채택(평균 .673, −.126 · 카이세도 개인 지불 .246~.294).
  ⚠️ 표본이 **알론소 체제가 아니다**(마레스카·로즈니어) — 8/24 풀럼 후 재판정 가능.

- [ ] **P2 · 리버풀 기능축 최초 수집** (obs#116의 첼시 방식) — `appearances`·`player_match_positions`
  team=LIV **0행**. obs#120이 12명을 재산출했으나 **피벗 3인 전부 `dm_dlp`/Roaming**으로 나와
  기능 분화를 가릴 수단이 없다. 프리셋보다 이것이 먼저다.
  경로: `/api/v1/event/{eid}/player/{pid}/statistics` → `player_match_positions.stats_json`
  (⛔ SofaScore 403 중 — 재프로브 선행).

- [ ] **P2 · SSOT 4~7단계** (상세는 obs#105·#115) — 4단계 `CMP_SLOTS`+`SQUAD_SLOTS`+`PLAYER_BEST` →
  5~7단계 `SLOT_EMERY_ROLE`+`FUNC_AXIS` · `FORMATIONS` · `SQ_STATS`.
  ⚠️ 선행 2건: ⓐ **실제 드리프트**(맥긴 CAM 그리드가 DB 어느 값과도 다르다 + orphan 그리드 10건)
  ⓑ **별칭 축**(축약↔풀네임 14쌍 규칙 도출 불가 → `player_fc_stats.tool_label`이 최소 침습안).
  ⛔ **이관 금지**: `ANIM_*`·`COLL_CAL`·`FORMATION_DESC`·초기추정 프리셋·`TEAMS`.

- [ ] **P2 · 애니메이션 궤적** (obs#117 잔여) — 마커는 커널 파생이나 화살표(`ANIM_PHASES.ar`)·
  경로(`AN_PATTERN`)는 옛 상수라 어긋날 수 있다. 대안=화살표를 파생 좌표 간 직선으로 대체
  (정합성↑·표현력↓) — **사용자 판단 필요.**

- [ ] **P3 · 잔여 2건** — ⓐ **세트피스 득점 비중 축**(obs#112·#113): 전용 컬럼이 없어 obs#49(알론소 40%)·
  obs#74(이라올라 33%)와 동축 비교 불가. 경로 `/event/{eid}/incidents`(⛔403) 또는 `appearances`
  파싱(⚠️ 커버리지 불균등 → "비중" 금지, "확인된 골 목록"까지만) ⓑ **리버풀 프리셋**(기능축 선행).

### 차단 요소 / 대기 중

- **공식전**: AVL은 **08-12 슈퍼컵 1경기**(PSG 2-1 AVL) 실측 완료(obs#194·196), 마조 유효 표본 **4/6**.
  ⏰ **개막** — AVL **8/23 브라이턴** · LIV **8/23 뉴캐슬** · CHE **8/24 풀럼**. CHE·LIV는 공식전 **0경기**라
  여기서 첫 표본이 쌓인다(docs/30 §4 Δ 산출의 선결 조건). ⛔ **단 지금은 실측 소스가 403이다**(obs#274).
- ⚠️ **커버리지 결손은 「친선이냐」가 아니라 「그 대회를 커버하느냐」로 갈린다**(obs#133이 obs#126 정정).
  양방향 반증 — 친선인데 있음(빌라 vs 바이에른, 히트맵 28pt) / 공식전인데 없음(마조 U17 예선 3경기 전건 0).
  ⇒ **사전 판별법이 없으니 경기별로 시도**한다. 08-19 프리시즌 전수 재수집으로 map25가 2→10이 됐다(obs#256).
- **EA가 수치를 공개하지 않는다** — Role+/++ 정량 효과·OOP 페널티·포커스 델타·역할별 가중치.
  AcceleRATE는 분류 공식만 공개.
- ⚠️ **`tactic_code`가 16행 전부 비어 있다** — 처방은 "역할·포커스·라인"까지만 내려간다.
  `ingame_checks`도 0행(게임 내 검증 폐기, docs/50). **국면 분리는 보유 선수만**(영입은 빌라 경기 0).
- ⚠️ **적합도가 전 국면 평균 기준**이고 obs#101은 그 어긋남을 **빌라 표본으로만** 측정했다 —
  첼시·리버풀에 같은 노이즈 크기가 성립한다는 근거는 없다.

---

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-08-21 | **소스 접근성은 「상태」다 — 수집 착수 전에 프로브하고, 결과는 런북이 아니라 obs에 날짜와 함께 남긴다** (obs#274) | SofaScore를 Playwright로 재시험하러 갔다가 **홈 HTML 자체가 403**임을 확인했다 — 4클라이언트(curl·헤드리스·헤드풀·브라우저 MCP) 전부 동일이라 **오리진 fetch 우회로가 원리적으로 무효**다. WhoScored·FBref·sofifa도 같이 403 ⇒ P0 두 경로 동시 차단. 대신 **Understat**을 열어 축11을 채웠다(빅5 한정). ⚠️ 08-21 오전엔 열려 있었다 — 「지금 막혀 있다」도 유효기간이 있는 관측이다 |
| 2026-08-21 | **「표기 통일」은 표기만 바꾼다 — 정규 코드끼리의 차이는 사실 판정이므로 덮지 않는다** (obs#276, `bb766dc`) | `--adopt-fotmob-position` 최초 구현이 **14건을 잘못 덮었다**(맥긴 CM→RW · 비르츠 CAM→LW · 가르시아 RB→ST) — FotMob의 「최근 출전 분포」 라벨이 우리 판정을 대체한 것이다. 규칙 확정(`canon_pos()`): **정규 코드면 유지 → 첫 토큰이 정규면 그것 → 정보 없으면(MF·DF·F·D·W) FotMob**. ⭐ 이 사고를 잡은 것은 **「덮지 않고 보고한다」 설계**다 ⇒ 덮어쓰기 옵션에는 **무엇을 덮었는지 출력**이 필수다. ⭐ 동명이인 검증의 최강 증거는 이름·소속이 아니라 **커리어 궤적**이다(가르시아 = 레반테→빌라 18경기→헤타페) |
| 2026-08-21 | **새 소스를 붙이기 전에 검증된 소스의 미사용 필드를 먼저 본다** (obs#275·#277, `4c97307`·`74c7fda`) | Transfermarkt 스크레이퍼를 짜려다 생년·국적·포지션·신장·**시장가·계약만료·부상**이 전부 FotMob `playerData`에 이미 있는 것을 확인했다. ⇒ TM 없이 축 3개를 열었다(`player_market_values` 6,631 · `player_status` 106 · `understat_player_matches` 4,817). 그 과정에서 **「이름+소속·국적·포지션 3요소」 규약이 DB에서 실행 불가능**했던 것도 드러났다(생년 169/182 결손) |
| 2026-08-21 | **표시 문자열 신고를 「문구 수정」으로 처리하지 않는다 — 그 값을 만드는 코드를 먼저 본다** (`6f9b26e`) | 사용자가 「비교 기준이 아직 3팀」이라고 지적했을 때 라벨만 고치면 끝날 일로 보였다. 그러나 **팀 코드가 하드코딩돼 ATM 선수 전원이 백분위·유사프로필 모집단에서 빠져 있었다**(POOL 88→104행, 15.4% 결손). 즉 라벨 「3팀」은 낡은 문구가 아니라 **정확한 고백**이었다. ⇒ 낡아 보이는 표시값은 **파생 경로를 역추적**한다. 재발 방지: 라벨은 「의도한 값」이 아니라 **「실제 로드된 값」**을 쓰고, 실패는 숨기지 않고 드러낸다(`.catch(()=>null)`가 로드 실패를 삼키고 있었다). |
| 2026-08-21 | **등급은 「보도 티어 × 단계」의 함수다 — 부상·구조 비용은 성사 확률 축이고 등급을 바꾸지 않는다** (obs#272, `9afc44f`) | 민테 복귀가 「최소 8주」→「Mid November 2026」으로 늘어 창 마감 후 2.5개월 못 쓰는 상태가 됐다. 그래도 **MEDIUM-HIGH를 유지**했다 — §2 등급표는 티어·단계로 정해지고 §2-1 보정은 경쟁 구도인데 부상은 그 어느 축도 아니다(단계 무변 · 경쟁 0 · 셀링클럽 기조 무변). 네투 선례와 동일 처리. ⛔ **내 추론으로 내리지 않는다** — 「리버풀이 식었다」는 1~2티어 보도 0건이고 리버풀은 부상을 알고 입찰했다. ⇒ 두 축(등급 / 성사 확률)을 **분리해 기록**하고, 자동 충족되는 트리거에는 **재판정 날짜**를 박는다. |
| 2026-08-21 | **라벨로 조인하는 맵은 id를 함께 실어야 한다 — `players.name_kr` ↔ `transfer_targets.name_kr`은 표기 규약이 다르다** (obs#270, `72bee37`) | 선수 페이지가 「데이터 미수집」을 12명 중 9명에게 표시했는데 **4건은 데이터가 있는데 키가 어긋난 것**이었다(토신: 「아다라비오요」 ↔ 「토신 아다라비오요」). `fbref`·`fotmob_season`·`form`·`season_stats`가 전부 라벨 단독 조인이었다. ⇒ export가 `player_id`를 행에 싣고 화면은 **id 우선·라벨 폴백**(`pickRows()`). ⚠️ **근본 원인(두 테이블의 표기 규약 불일치)은 회피했을 뿐 통일하지 않았다** — 라벨 단독 조인이 다른 곳에 또 있는지 미감사. |
| 2026-08-21 | **대량 수집은 손으로 하지 않는다 — Playwright 스크립트가 정확하고 컨텍스트 비용이 0이다** (`9145639`) | 브라우저 MCP→localhost 전송이 `fetch`·form POST 모두 `ERR_BLOCKED_BY_CLIENT`로 차단돼(하니스 정책, obs#273) ATM 1,285행을 **에이전트 컨텍스트 경유**로 옮겼다. Playwright는 그 제약 밖이다(페이지 오리진 fetch → 200). ⭐ 그리고 **스크립트가 수동 수집보다 정확했다** — 같은 대상에서 푸리치 30행(수동 0)·오르티스 LaLiga2 23/24를 더 찾았다. ⇒ `scripts/collect_fotmob_players.py`, 2명 이상이면 무조건 이것. ⛔ curl·WebFetch로는 안 된다. |
| 2026-08-20 | **런북의 「현재」를 현재 상태로 읽지 않는다 — SKILL.md의 상태 서술은 날짜를 붙여 이력으로 강등한다** (`3e39ef0` 후속) | `transfer-watch/SKILL.md`가 「⛔⛔ [08-14 확인] SofaScore가 **현재** 전면 차단」이라고 적어 뒀고, **08-19에 다른 세션이 복구를 커밋**(obs#256, map25 2→10)했는데 **08-20 두 회차가 모두 「403이 8일째」로 오보**했다. 에이전트 보고와 교차되자 확정으로 굳혔다 — 그러나 에이전트도 같은 런북을 읽는다(순환 확증). ⇒ **소스 접근성·차단 여부 같은 「상태」는 런북이 아니라 DB(obs)·커밋 로그가 정본**이고, 런북에 남길 때는 유효 기간을 명기한다. 실질 피해: 톰모리·토신을 **실측 가능한데도 PENDING으로 남기고** 불필요한 「대체 경로 전환 회차」를 우선순위에 올렸다 |
| 2026-08-20 | **이적료가 「A + 애드온」 ↔ 「B + 애드온」으로 갈리면 총액/고정분 해석으로 화해시키지 않는다 — 다수결로 정본을 정하고 충돌을 기록한다** (콘사 £51m, `3e39ef0`) | Romano 「£50m plus add-ons」 ↔ Ornstein 「£51m plus add-ons」를 보고 「£50m 고정 + 애드온 = £51m 총액」으로 읽으려 했으나 **양쪽 다 「plus add-ons」라 그 해석은 성립하지 않는다.** 실체는 **고정분 수치의 1티어 간 £1m 불일치**다. Ornstein(담당 기자)·Sky·ESPN **3:1**로 £51m 채택. ⚠️ 애드온 조건은 **1티어 어느 쪽도 분해하지 않았다** ⇒ CONFIRMED 시 원장에 **보장분/애드온 분리** 필수(산투스 £50m = £48m+£2m+셀온 10%가 그 선례) |
| 2026-08-20 | **`transfer_outgoing` 등재 기준의 비대칭을 인지한다 — 기준은 목적지 유형이 아니라 「`players`에 있는지」다** (사용자 판단 대기) | 원장 감사로 **Nallo→HJK(구단 공식 07-24)·로버트슨→토트넘(스퍼스 공식 06-05)** 두 CONFIRMED 결손을 찾았으나 둘 다 `players` 미등재라 2026-08-13 방침에 걸려 보류했다. ⚠️ 그런데 **코나테→레알(자유이적)은 등재돼 있다** — 즉 「자유이적이라 뺀다」가 아니라 **분석 대상 스쿼드인지**가 실제 기준이고, 그 사실이 문서화돼 있지 않아 감사 때마다 재판단이 필요하다 |
| 2026-08-19 | **선수 확인은 이름 + 소속팀·국적·포지션 3요소로 한다** (사용자 지시 · docs/30 정본 · obs#250, `6366641`) | 이름만으로는 사람이 특정되지 않는다는 것이 이 세션에 3번 실증됐다 — `Alysson`(빌라·**브라질**·**RM**) ↔ `Alisson Becker`(리버풀·**브라질**·**GK**)는 **국적까지 같아 소속·포지션만이 구분자**다 · `Mamadou Sarr`(첼시·CB) ↔ `Pape Matar Sarr`(토트넘·CM)는 성·국적이 같다 · `Miloš Kerkez`↔`Milos Kerkez` 표기 차이로 **매칭이 조용히 깨졌다**. `player_game_stats`가 `UNIQUE(game_version, roster_date, name_kr)`이라 충돌 시 **행이 사라진다**. 규칙 4개: name_kr 유일성(충돌 시 더 긴 표기로 분리 — `알리송 베케르` 선례) · id 조인 · 국적/풀네임 기록 · **브리핑·리포트에도 3요소 표기**(08-16·08-18 브리핑 오류가 이 누락에서 나왔다) |
| 2026-08-19 | **FC27 수집은 2단계로 나눈다 — 09-10판이 정본이다** (obs#249·#251, `2192906`·`529c472`) | EA 공식 스케줄이 **08-21 = 전체 DB이지만 PlayStyles 없음 + 이적창 미반영**, **09-10 = PlayStyles 포함 + 이적 마감 후 갱신**으로 갈라 놨다. 우리가 실제로 쓰는 `playstyles`와 스즈키·완비사카·루헤리·네델코비치의 새 소속은 **09-10에야** 들어온다. ⛔ 그리고 **델타는 기준 시점을 밝히지 않으면 부호가 뒤집힌다** — fut.gg Δ는 FC26 **출시판(2025-09)** 기준이고 우리는 **시즌 말 라이브판**이다(왓킨스 −1↔**+1** · 반다이크 −2↔**0** · 만잠비 +13↔**+3**). ⚠️ 능력치 변화는 **적합도·처방을 바꾸지 않는다**(커널은 히트맵 기반, obs#121) |
| 2026-08-16 | **GK 슬롯은 fit_sim 대신 평균 위치를 표시한다** (obs#220, `8f8e4dc`) | 측정된 GK 5명의 적합이 **.961~.975(폭 0.014)** 에 전원 수렴하고 **그리드 상호 코사인이 모두 ≥0.986**이다(스즈키~마르티네스 0.995). GK 히트맵은 골문 앞 한 칸이 최대값을 독점해 5×5 해상도에서 서로 구분되지 않는다 — 숫자를 노출하면 **없는 우열**을 읽게 된다. 변별은 평균 위치에서 나온다(tool_y 폭 **2.49**, 팀 라인 높이와 방향 일치). ⛔ 이 결정은 "적합을 GK에 쓰지 말라"가 아니라 **"적합 차이를 우열 근거로 쓰지 말라"** 다 — 슬롯 배정(`gk_goalkeeper` 수렴)은 정상 작동한다. squad·compare의 GK **정렬도 평점순으로** 바꿨다(★가 .001 차이로 붙던 문제) |
| 2026-08-16 | **히트맵 없는 친선은 `complete`로 올리지 않는다 — 영구 draft가 기본값** (`557d8c5`) | 프리시즌 8경기를 적재하며 확인: 이 경기들은 SofaScore·FotMob **양쪽 모두** 선수 스탯·평점·히트맵·팀 스탯이 없다(양성 대조군으로 프로브 오류 배제). 히트맵이 없으면 **단일 경기 fit을 산출할 수 없고**, 런북은 그 상태에서 역할·포커스를 채우는 것을 **과대 표시**로 금지한다. ⇒ 자료를 더 파도 해결되지 않는 **소스 결손**이라 draft가 정확한 상태 표기다. 기존 초안 7건도 같은 이유였다(자료가 얕아서가 아니다). ⚠️ 결손은 0이 아니라 **NULL**로 적재한다 |
| 2026-08-16 | **`last_news_date`에 재확인 날짜를 넣은 행을 정정한다 — 베일리 08-14 → 08-05** | §4-3이 명시적으로 금지한 오류가 실제로 들어가 있었다. rationale의 `[2026-08-14] …확인됐다`는 **우리 분석 날짜**이고 인용된 기사는 08-04~08-05다. 이 값을 두면 **그 행은 영원히 정체되지 않는다**(STALE이 14일 기준이므로). ⇒ 매 회차 이 컬럼은 **보도 자체의 날짜**인지 재검증한다 |
| 2026-08-16 | **서브에이전트 브리핑의 선수·id·소속은 §1-0 조회 출력에서 그대로 복사한다** | 3팀 스캔 에이전트가 각각 "DB 정정 필요"를 올렸는데 **셋 다 DB가 맞고 브리핑이 틀렸다**(루헤리를 아탈란타로, LIV `id 53`을 엘리엇으로, CHE `id 82`를 펠릭스로 적었다 — 실제로는 아틀레티코·각포·무드리크). §1-0 DB 선조회는 수행했으나 **브리핑을 조회 결과가 아니라 기억으로 썼다.** 오류를 넣으면 에이전트가 그 확인에 토큰을 쓰고 **존재하지 않는 정정을 권고**한다 |
| 2026-08-13 | **양 측면을 쓰는 자원은 좌우를 나눠 재고, 혼합값을 쓰지 않는다** (obs#156 규약을 영입 후보에도 확장, `d72897d`) | 무뇨스를 34경기 **혼합**으로 재면 LM **.615**인데, 좌 23·우 11이 섞여 그리드가 중앙으로 뭉개진 결과다. 나누면 **좌 .729 / 우 .772로 양쪽 다 올라간다** — 혼합값은 두 값의 중간이 아니라 **둘 다 아닌 값**이다. 자케도 같은 구조(혼합 RCB .838 → 우측만 **.903**). ⚠️ 반대로 **완비사카는 25/25 전 경기 우측 우세(우2컬럼 91.7%)** 라 분리가 불필요했다 — 분리 여부는 미리 정하는 게 아니라 **경기별 좌우 비중을 세어 판정**한다 |
| 2026-08-13 | **`transfer_outgoing`은 `players`에 있는 선수만 등재한다** — 첼시 신규 유출 후보 5명(토신·델랍·디사시·기우·사르) 미등재 | 다섯 명 전원이 1티어(Sky·The Athletic·BBC) 근거로 MEDIUM-HIGH 요건을 **충족**했으나 `transfer_outgoing.player_id`는 `players` FK가 필수이고 **5명 전원이 `players`에 없다** = 전술 분석 대상 스쿼드가 아니다. 이들이 나가도 `squad_entries`·XI·슬롯이 **바뀌지 않는다**. 행을 만들려고 `players`를 5개 늘리는 것은 추적 가치 대비 **스키마 오염**이라 판단하고 근거는 리포트에만 보존했다. ⚠️ 단 **센터백 감축의 누적 효과는 별도 추적 대상**(찰로바 CONFIRMED + 바디아실 HIGH + 이 3명) |
| 2026-08-13 | **`handoff-hooks/`·`".claude(구독)/"` 는 삭제하지 않고 `.gitignore`에 넣는다** (사용자 확정) | 세션 #14가 "다른 세션/PC에서 유입 추정"으로 판단해 untracked로 남겨뒀던 미결 항목. **git에 없는 파일이라 삭제하면 복구 불가**이고, 유입 경위가 확정되지 않았다. 추적에서만 제외하면 매 세션 `git status` 노이즈가 사라지면서 파일은 보존된다 — 되돌리기 쉬운 쪽을 택했다 |
| 2026-08-10 | **웹 검색보다 DB 선조회를 먼저 한다** — SKILL.md **§1-0 신설** (`731d92f`) | "프리시즌 활약 유스"를 웹으로만 훑다 헤밍스·린치를 통째로 놓쳤는데 **obs#126 ③에 5명이 이름으로 적혀 있었다**(Lynch 어시 → Madjo 골까지). 검색 설계 문제인 줄 알았으나 실제 원인은 **파이프라인에 DB 선조회 단계가 없었던 것**. ⭐ 함께 넣은 규칙: **obs는 `claim`과 `confidence`를 같이 읽어라** — obs#126은 결론을 "친선이라 실측 없음"으로 썼지만 `confidence`에 진짜 원인("상대가 올스타 선발팀")이 이미 있었다. **근거 칸이 결론보다 정확할 수 있다** |
| 2026-08-10 | **obs#126은 폐기가 아니라 범위 축소** — obs#133 신설 (`b790539`) | 관측(11명 전건 null)은 옳고 과한 것은 **"프리시즌 친선의 구조적 한계로 기록한다"** 한 문장이다. 양방향 반증 확보: **친선인데 있음**(빌라 vs 바이에른, 히트맵 28pt) / **공식전인데 없음**(U17 유럽선수권 예선 90·36·90분, 히트맵 0). ⇒ 결정 변수는 친선 여부가 아니라 **SofaScore 대회 커버리지**이며, **사전 판별법이 없어 수집을 시도해 보는 것이 유일한 방법**이다. HANDOFF 차단요소도 "경기별로 시도하라"로 교체 |
| 2026-08-01~06 | **측정·수집 규약 8건** (압축 — 상세는 obs#127·#130·#131·#132 및 각 커밋) | 불변규칙 8 신설(**히트맵은 시각화해서 보여준다** — 가르나초 좌2컬럼 90% vs 로저스 68%는 그림에서만 보인다) · **커널 적합도 별도 구현은 회귀 게이트 통과 시 조건부 허용**(금지의 목적은 브라우저 사용이 아니라 파싱 함정 방지) · SKILL.md 회귀 기준값 정정(하지무사 .821·은디아예 .825 — 구 커널 값이라 검증이 항상 실패했다) · **`v_event_profile`의 `n`은 지표별 표본 수가 아니다**(SofaScore는 값이 0이면 키를 생략 ⇒ '결손'이 실제로는 0) · 카마라 `require(dm_holding)` 하향 보류(제약의 존재 이유가 그 사고의 재발 방지였다) · **이적 감시는 발행일 검증을 티어 판정보다 먼저**(1년 오차가 가장 흔하고 매체 티어로는 원리적으로 안 잡힌다) · Fotmob은 리드 소스로만(출처 라벨을 검증할 수 없다) · **R축 미신설(기각)** — FDR 생존 0/5이나 방향은 4/5가 예측대로였고 실패 원인은 효과 부재가 아니라 **검정력**이다(obs#123, 표본 2~3배면 재검정 가치) · **GK·CAM·ST 적합값을 좌우 편향 근거로 쓰지 않는다**(커널 변형이 좌우 대칭) |
| 2026-07-31 | **측정 기준 재정립 14건** (압축 — 세션 #3, obs#82~#96) | **슬롯 x 정본 = 툴 `SQUAD_SLOTS`**(코사인이 x의 계단 함수) · docs/20 §B 라인 보상 규칙 폐기(obs#27과 모순) · 전역 충돌 임계값 → **슬롯쌍 상대 지수 CI**(쌍 중앙값이 140배 범위) · 26/27 프리셋 5종 폐기 → 단일 `projected:emery-2627` · **A축 기각**(두 독립 검증 실패 + EA "Opta AI 모델" 명시) · **B축은 점수 아닌 플래그**(이산 속성) · 뱅크 포커스 기본 Support · RM 뱅크는 캐시(알리송은 뱅크 미숙련) · **`placedMap` 전면 교체 — 변형은 시프트가 아니라 중앙 대칭 미러** · 회귀 게이트 값 교체 · **프리셋 H안 채택**(사용자 확정) · **C5 기능축 신설**(솔버가 카마라를 창조 역할에 배정한 사고) · **C6는 소프트**(obs#95 근거가 억제 1건뿐) · 고점유 전환 보류(→ obs#101에서 기각으로 종결) |
| 2026-07-30 | **3팀 체제 도입 결정 9건** (압축) | 팀 축 = 텍스트 `team` 컬럼 + FK 없는 `teams`(불변규칙 2 준수) · UNIQUE 충돌 5테이블만 재작성 · **STALE = DEAD 숨김(삭제 아님)** — 툴이 `/^DEAD/i`를 필터하므로 행을 지우면 `map25`·`fit_sim`을 잃는다 · 등급 강등도 숨김으로 통일 · STALE 판정은 **빌라 링크 보도일**(`last_news_date`) 기준이고 `CONFIRMED`/`OWNED` 제외 · **CCB 슬롯 신설**(백3 3인이 각자 자기 자리 최고치) · CCB를 `CMP_SLOTS`에 의도적 미추가(빌라 화면 누출 방지) · 스캔은 서브에이전트·판정은 메인 · **obs#28 빌라 PPDA 9.88 폐기**(대표값 12.3/12.45, 불변규칙 3) · 로저스 첼시 행은 빌라 그리드로 적재(캐비앗 명기) |
| 2026-07-31 | **히트맵은 1차 필터로 옳으나 단독으로는 요구역할의 36%다** | 요구역할 69조항 중 위치 25(36.2%) · 온더볼 15 · 관계 14 · 타이밍 9 · 세트피스 4 · 기능 2. 히트맵이 옳은 이유는 **FC26의 Role이 정확히 위치 분포**이고(fut.gg가 heatmap을 준다) 우리가 재는 것과 게임이 구현하는 것의 **자료형이 같기** 때문이다. 못 담는 63.8%의 대부분은 **역할 선택으로 애초에 구현 불가**(PlayStyles·능력치·세트피스 시스템) — 결함이 아니라 레버가 다르다 (obs#121) |
| 2026-07-31 | **세션 #4 결정 12건** (압축 — obs#100~#122, 상세는 각 obs) | **SSOT 1순위=`MAPS` 커널**(모든 적합값의 뿌리인데 툴에만 있어 백업 없음) · **`ANIM_*`·`COLL_CAL`은 DB로 안 옮김**(전자는 삽화라 `source`를 정직히 못 채우고 DB에 넣으면 실측 권위를 얻는다, 후자는 그리드의 순함수라 박아두면 조용히 stale) · **전수 42,972로도 H안 유지**(297위·상위 0.69%, Δ.0131은 EA 노이즈 하한 미달) · **C6는 대가 0이어도 소프트**(대가가 없다는 것이 근거가 세졌다는 뜻이 아니다) · **고점유 전면 전환 기각**(rest-defense 칸은 부호 반대) · **요구역할 표는 실측에 맞춰 고치지 않음**(불일치는 대부분 obs#14의 의도적 예외) · **애니메이션 위치만 커널 파생, 궤적은 삽화** · **sync가 프리뷰 미러 자동 갱신**(사람이 기억할 절차는 결국 빠뜨린다) |
| 2026-07-31 | **측정·수집 규약 6건** (압축 — obs#97·#98·#103·#104·#100·#102) | **슬롯 판정 원천 = `/lineups` `players[]` 배열 순서**(`formationPlace` 전 이벤트 NULL, 좌표 밴딩은 RB·RAM이 겹쳐 원리적 불가) · **`pos_class` 보존 + `lineup_pos` 신설**(덮어쓰기 아님, 두 파생을 나란히 둬야 밴딩 오탐을 계속 감사할 수 있다) · **C5에 `focus` 선택 필드 추가**(LCB에서 갈리는 것은 role이 아니라 focus였다 — 결손은 근거가 아니라 표현력) · **AcceleRATE 경계는 보가르드·카마라 2명**(불일치 4명 ≠ 키 민감 2명) · **SOFIFA `en` 정본 = `id` 보유 항목** · **첼시 제임스는 포커스만 교체**(Δ.001 동률에서 움직임만 보고 역할을 바꾸는 것이 C5를 낳은 그 사고다) · **LCB는 `Build-Up` 유지**(Δ.049는 노이즈라 결정권이 duties로 넘어간다) · **obs#38은 폐기 아니라 개정** |

---

## 최근 세션 기록

### 2026-08-21 — 세션 #34: 소스 차단 전수 확인·Understat 경로 개설·축 3개 신설 (`433c96c`~`3377180`)

- ⭐⭐⭐ **「Playwright로 열리나?」를 확인하러 갔다가 차단의 성질이 바뀐 것을 찾았다**(obs#274).
  4클라이언트(curl·헤드리스·**헤드풀**·브라우저 MCP) 전부 403이고 **홈 HTML 자체가 403**이다
  ⇒ 오리진 fetch 우회로가 원리적으로 무효. **WhoScored·FBref·sofifa도 같이 403**이라
  P0 두 경로가 동시에 닫혔다. ⇒ 대신 **Understat**을 열어 축11을 4팀 76행 적재.
- ⭐⭐ **스크립트 산출물도 감사해야 결함이 나온다.** 매핑 99건 전수 감사에서 오매칭은 0이었지만
  **PK가 xG/슛을 부풀리는 결함**을 찾았다(콜 파머 .137→**.099**, 38%). 전체 1.2%인데 **PK 키커 14명에 집중**.
  ⇒ `penalties`·`npxg_sum` 컬럼(017) 추가 + 백필. ⛔ 기존 SofaScore 9행은 **PK 포함 여부 불명이라 NULL**.
- ⭐⭐⭐ **규약이 있는데 근거 컬럼이 비어 있었다**(obs#275). 「이름+소속·국적·포지션 3요소」(08-19 규약)를
  DB에서 실행할 수 없었다 — **생년 169/182 · 국적 148/182 결손**. FotMob 백필로 27·28로 줄였다.
  ⭐ 그리고 **Transfermarkt 스크레이퍼를 쓸 이유가 없어졌다** — TM에서 얻으려던 생년·국적·포지션·
  신장·시장가·계약만료·부상이 **전부 `playerData`에 이미 있었다**. ⇒ **새 소스보다 검증된 소스의
  미사용 필드를 먼저 본다.**
- ⛔⛔ **「표기 통일」 지시를 사실 변경으로 실행했다가 14건을 잘못 덮었다**(obs#276).
  무제한 채택이 **맥긴 CM→RW · 비르츠 CAM→LW · 말렌 LW→ST · 가르시아 RB→ST**를 만들었다 — FotMob의
  「최근 출전 분포」 라벨이 우리 판정을 덮은 것이다. 전부 복원하고 3단 규칙 확정(`canon_pos()`):
  **정규 코드면 유지 → 첫 토큰이 정규면 그것 → 정보 없으면 FotMob**. 복합 표기 5건도 재정정.
  ⭐ **이 사고를 잡은 것은 「덮지 않고 보고한다」 설계**다 — 없었으면 조용히 바뀌었다.
  ⇒ **덮어쓰기 옵션에는 반드시 「무엇을 덮었는지」 출력이 붙어야 한다.**
- ⭐⭐ **동명이인 검증의 최강 증거는 커리어 궤적이다.** 안드레스 가르시아를 포지션·생일 불일치로
  오매칭 의심했으나 커리어가 **레반테→빌라 18경기→헤타페 임대**로 notes와 정확히 일치 = 동일인.
  문제는 id가 아니라 FotMob 라벨이었다.
- ⭐ **축 3개 신설**(019·020): `player_market_values` 6,631(scisports 시계열) ·
  `player_status` 106(부상 9명 — 민테 `Leg injury`/복귀 11월/시장가 €61.1m→€49.7m) ·
  `understat_player_matches` 4,817(xA·키패스·xGChain + **경기 단위 minutes**).
  ⇒ obs#272의 민테 판단이 **정형 근거**를 얻었다.
- ⭐ **드리프트 도구가 첫 실행에서 값을 잡았다**: 맥긴 DM 0.878은 커널 최대(0.717) 초과 =
  argmax로 산출 불가능 ⇒ 구 커널 잔재로 판단해 **0.717로 정정**(역할·포커스는 불변).
- ⚠️ 자기 정정 2건: `--adopt-fotmob-position` 최초 구현이 위 14건 사고를 냈다 ·
  Understat 스크립트가 **슛 행 보유자를 대상에서 빼서 그들의 경기 지표까지 누락**시켰다
  (별개 축이므로 분리 — 왓킨스 슛 +0 / 경기 +137로 검증).

### 2026-08-21 — 세션 #33: 4팀 첫 이적 감시·ATM 스쿼드 2회 보정·AVL 영입 후보 14축 전수 (`b8c4bcd`~`9145639`)

- ⭐⭐⭐ **같은 결손을 두 번 놓쳤다** — ATM `squad_entries` 12→23→**31**. 두 번 다 원인이 같다:
  원천이 **R1 경기일 명단**인데 `squad_entries`는 **전체 스쿼드**다. 내가 「결손 4건」이라 쓴 것도
  눈에 걸린 것만 센 값이고 전수 대조하니 8건이었다.
  ⇒ **결손은 「발견한 것」이 아니라 「정본 목록과의 차집합」으로 센다.**
- ⭐⭐ **「라벨이 낡았다」는 신고가 실제로는 코드 결함이었다** — 팀 코드 하드코딩으로 ATM 선수 전원이
  백분위·유사프로필 모집단에서 빠져 있었다(POOL 88→104, 15.4% 결손). 라벨 「3팀」은 **정확한 고백**이었다.
- ⭐⭐ **「데이터 없음」의 절반은 결손이 아니라 키 불일치**였다(obs#270) — `players.name_kr` ↔
  `transfer_targets.name_kr` 표기 규약 불일치. export가 `player_id`를 싣고 화면은 id 우선·라벨 폴백.
  ⚠️ 근본 원인은 **회피했을 뿐 통일하지 않았다**(라벨 단독 조인 잔여분 미감사).
- ⭐⭐ **부상 기간 연장은 등급을 바꾸지 않는다**(obs#272, 민테) — §2 등급표는 티어×단계, §2-1은 경쟁 구도인데
  부상은 어느 축도 아니다. **등급 유지 · 성사 확률만 하방**. ⏰ 하향 트리거가 09-01에 자동 충족 → **09-02 재판정**.
- ⭐⭐ **대량 수집은 손으로 하지 않는다** — 브라우저→localhost 전송이 전부 차단돼(obs#273) 1,285행을
  컨텍스트 경유로 옮겼고, `collect_fotmob_players.py`로 그 비용을 0으로 만들었다.
  ⭐ **스크립트가 수동보다 정확했다**(푸리치 30행·오르티스 LaLiga2를 추가 발견).
- ⭐ 그 외: 사용자가 판정 소유권을 넘긴 사례(45일 규정 — 근거 강도가 다르면 다른 판정이다) ·
  서브에이전트가 내 브리핑 오류를 잡음(THB 켐파니 누락이 결론을 반대로 만들 수 있었다) ·
  **커널 값은 「배치 기하 유사도」이고 그 자체로 이식 가능성이 아니다**(obs#260~266) ·
  ATM `sort_order` 부여로 UI 회귀 해소(부작용: 깊이 차트가 「그리드 유무」를 반영 — obs#269).

## 아카이브 요약

- **#32 (08-19~20, `38e9305`~`3e39ef0`)**: ⭐⭐⭐ **SofaScore 해제(08-19, obs#256)를 08-20 두 회차가 놓쳐
  「403이 8일째」로 오보** — 원인은 런북의 「현재 차단」 서술이고 에이전트도 같은 런북을 읽어 **순환 확증**이 됐다
  (→ 「주요 결정 사항」 08-20 첫 행이 이 교훈의 정본). 첼시·리버풀 전술 정본을 26/27 실측으로 이동
  (obs#254·#255·#257, 첼시 인게임 3-4-2-1 · LIV ST 슬롯 신설) · 콘사 대체 CB 축 전담 수집
  (9개 언어·20여 이름 중 **1~2티어로 특정된 것은 톰모리 하나**) · 자기 정정 4건(완비사카 소속 오기는 #31 재발).
  ⭐ **에이전트는 우리 DB를 볼 수 없어 「없다」는 판정을 할 수 없다**(CHE 원장 누락 7건 보고가 전부 오탐).
- **#31 (08-18~19, `8d79944`~`28f4255`·`e1921c3`)**: 스즈키 CONFIRMED 승격(obs#234·#241) ·
  `site/assets/prose.js` 신설(**DB의 볼드가 한 번도 렌더된 적 없었다**, obs#240) · FC26 베이스라인 3팀 86행 +
  **선수 동일성 3요소 규약**(obs#250) · FC27 온보딩 2단계 확정(obs#249·#251) · obs#245(사칭 계정 HWG 3중 검증).
- **#30 (08-16, `475cfb9`·`8f8e4dc`·`557d8c5`)**: SofaScore 403 해소 확인 + 스즈키 39경기 전수,
  obs#220(GK는 적합이 변별하지 못한다 → 툴 5화면을 평균 위치로), 프리시즌 8경기 적재(경기 분석 19경기), obs#221.
- **#28~29 (08-14, `539b60e`·`f1b3761`)**: 슈퍼컵 사후 16명 자료 재감사(obs#201, Madjo 표본 4/6 유지)와
  완비사카 FotMob 36지표 적재(obs#202, RB `.932` 유지·공격 생산성으로 확대 안 함).
- **#26~27 (08-13~14, `ecebf05`·`3b4e539`·`9652e91`)**: 영상 분석 누락 5명 보강·duties 표본 메타·G10 강화,
  슈퍼컵 교체 5명 수집. Codex cron TCC 실증, obs#197~199(AVL 슬롯 x 유지·CHE CCB 공석·LIV 설정 유지).
- **#25 (08-13, `703c612`)**: `match_game_setups`·`match_player_prescriptions`로 MATCH ONLY 설정을 신설하고
  슈퍼컵 팀 설정·선발 11명을 이관. 시즌 정본과 경기 전용 프리셋의 분리가 여기서 시작됐다.
- **#5~24 (08-01~13, obs#123~196)**: R축 기각(재개 표본 2~3배)·GK·CAM·ST 좌우 판별 불가·Fotmob 복구·
  **발행일 우선 검증 신설** · **결손과 0은 다르다**(인터셉트 1위 카마라→오나나, 가르나초 W .936) ·
  친선 일반화 정정·Codex 이식·3팀 이적 실측·슈퍼컵 심화·경기별 리포트·**DB 선조회 원칙**.
- **#1~4 (07-30~31, obs#82~122)**: 3팀 체제, EA 정본 중앙 대칭 커널, H안·C5·C6, SSOT 1~3단계,
  42,972 라인업, A축 폐기, 히트맵 커버리지 36.2%. 이전 기록은 리포트·git log 참조.

---

## 컨텍스트 노트

**⚠️ SSOT (obs#105·#107·#115 — 1~3단계 완료)**
- **DB가 정본인 블록 3개**: `MAPS` 85커널 → `game_role_focus.kernel25` · `ROLE_VARIANTS` 217변형 →
  `game_role_variants` · 라벨 → `players.name_kr`. `AUTOGEN:*`로 자동 생성되므로 **직접 편집 금지** —
  DB → `python3 scripts/sync_transfer_ui.py`. 마커 문자열은 **정확히 일치**해야 하고(`/* AUTOGEN:NAME:START */`)
  설명은 마커 **밖**에 쓴다(마커 줄에 덧붙이면 `replace_block`이 `sys.exit`).
- 회귀 게이트: **DB 무변경 상태에서 sync를 두 번 돌려 `git diff`가 비어야** 한다.
  ⭐ sync가 프리뷰 미러(`/private/tmp/tactics-preview/`)를 **자동 갱신**한다.
- ⚠️ 4단계 이후는 **순수 additive로 불가**하다(obs#115 ④) — 축약↔풀네임 **별칭 축**이 필요하다.
- ⚠️ **신규 이적 후보 등재 시 `opt_role`·`opt_focus`를 반드시 채울 것** — 비우면 `CMP_SLOTS`·`XI_POOL`에는
  뜨는데 **`SQUAD_SLOTS` 후보에서만 조용히 빠진다**. 규약은 **`opt` = `fit` = 슬롯 유형 제한 argmax**
  (⚠️ 2026-08-13 기준 음바예 행이 이 규약을 어기고 있다 — 「다음 할 일」 P0 ⓑ).

**⚠️ 방법론의 커버리지 (obs#121 — 과도한 기대와 과도한 자기비판을 둘 다 막는 기준)**
- 요구역할 69조항 중 **히트맵이 직접 담는 것은 36.2%**(온더볼 21.7 · **관계 20.3** · 타이밍 13.0 · 세트피스 5.8 · 기능 2.9).
  ⚠️ **63.8%를 "구현 실패"로 읽지 말 것** — 상당 부분이 게임의 다른 레버이거나 AI가 알아서 한다.
- ⭐ **커버리지 0%인 슬롯이 정확히 C5를 붙인 슬롯**(파우 0 · 콘사 0 · 카마라 40 · 오나나 25) —
  낮은 칸에서 **duties가 적합을 이기는 것은 구조적 필연**이다.
- ⛔ **R(관계) 축은 세우려다 기각**(obs#123, FDR 생존 0/5) — 재시도 조건은 **표본 2~3배** + `docs/60` 재사용.
- ⛔ **방향(좌/우) 판별은 GK·CAM·ST에서 원리적으로 불가**(obs#127, 커널 변형이 좌우 대칭) ·
  ✅ **FB·CB·WM에서는 코사인이 방향을 본다**(완비사카 RB **.932** vs 같은 그리드 LB **.022**).

**⚠️ 새 축을 세울 때의 4요건** (obs#32 성공 / obs#95 약함 / **obs#85·#123 기각**에서 역산 — 2승 2패)
1. **독립 출처 2~3개가 같은 방향** — ⭐ obs#123은 확증쌍을 `player_duties` **영문 서술에서만** 뽑아
   이 요건을 **선정 단계에서** 만족시켰다(움직임 자료 미참조).
2. **교란 통제** — obs#95: LB↔RB 원시 **+0.687** → 경기별 평균을 뺀 잔차 **−0.168**. 원시 상관 금지.
3. ⭐ **대조군·사전 성공 기준·검정력을 먼저 정한다** — 없으면 그 추측이 정답처럼 출력된다.
   obs#123은 n이 필요치의 1/2~1/3이라 효과 유무를 **애초에 판정할 수 없었다**(사후에야 알았다).
4. **n · p · 다중비교 상태를 `confidence`에 명시** — 없으면 하드 제약으로 못 쓴다(obs#95가 소프트인 이유).

**📍 규약은 docs로 이관됐다 (2026-08-06)** — HANDOFF는 "지금 무슨 상태인가"만 담고, 재사용 규칙은
아래 문서가 정본이다. 값이 충돌하면 **docs가 이긴다**.

| 필요한 것 | 문서 |
|---|---|
| 슬롯 x 정본표 · **회귀 게이트 11항** · 측정 기준 7항 · 별도 구현 허용 조건 | [docs/20 「슬롯 x 정본표」](docs/20-fc-game-system.md) |
| FC26 대상 시스템 · EA 피치노트(비결정성·숙련·개명) | [docs/20 「EA 공식 피치노트」](docs/20-fc-game-system.md) |
| SofaScore 수집 절차 · **수집 함정 7종** · 좌표/그리드 규약 | [docs/30 「수집 함정」](docs/30-data-rules.md) |
| `git add -A` 금지 · sync/dump 순서 · 툴 편집 사고 사례 | [docs/40 「git 규칙」](docs/40-pipeline.md) |

특히 자주 잊는 3개만 여기 남긴다:
- ⛔ **`git add -A` 금지** — `.claude/settings.json`(Figma PAT)이 있어 푸시가 차단된다. 명시 스테이징만.
  ⚠️ 이 규칙은 **핸드오프 스킬의 wip 보존 절차보다 우선**한다.
- ⭐ **적합값 Δ 0.02~0.05는 EA가 공개한 노이즈 구간**(obs#91) — 그 차이로 인선을 가르지 않는다.
- ⭐ **결손과 0은 다르다**(obs#132) — SofaScore는 0이면 키를 생략한다. `v_event_profile`의 `n`은
  지표별 표본 수가 **아니다**.

**⚠️ 국면 축 상태 (obs#96 → obs#101 종결)**
- 공격 칸은 argmax 7/8 불변·|Δ|≤.049라 **전 국면 평균을 계속 써도 결론이 안 바뀐다**.
- ⚠️ 단 **rest-defense 칸은 저점유가 옳은 기준**(카마라 .629 / .692 / **.759**) — 고점유로 재면
  목적에 부합할수록 깎인다. 축 라벨에 `poss-hi`/`poss-lo`를 병기할 것.
- ⚠️ **경기별 그리드는 (선수, 경기) 단위라 포지션이 섞인다** — 슬롯별 비교 전에 **`lineup_pos` 필터**가
  반드시 선행돼야 한다(obs#96 ②의 자기 정정 사례. `pos_class`는 RB와 RAM을 못 가른다 — docs/30 ④).
