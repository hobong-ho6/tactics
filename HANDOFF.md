# HANDOFF

<!--
════════════════════════════════════════════════════════════
이 파일은 세션 간 핸드오프 문서입니다. (사람 + Claude 공용)

■ Claude 사용 규칙
1. [세션 시작] 이 파일을 가장 먼저 읽고, "현재 상태"와
   "다음 할 일"을 확인한 뒤 작업을 시작한다.
   시작 시 사용자에게 한 줄로 브리핑한다.
   예) "지난 세션: X 완료. 오늘 예정: Y. 시작할까요?"
2. [세션 종료] 사용자가 "핸드오프 갱신" / "세션 정리" /
   "handoff"라고 하면 이 파일을 아래 규칙으로 갱신한다.
   - "현재 상태" 블록을 새로 작성 (덮어쓰기)
   - "다음 할 일"에서 완료 항목은 [x] 처리 후
     "최근 세션 기록"의 완료 목록으로 이동
   - 새로 발견된 할 일을 "다음 할 일"에 우선순위와 함께 추가
   - "최근 세션 기록" 맨 위에 오늘 세션 엔트리 추가
   - "최근 세션 기록"은 최대 5개 유지. 넘치면 가장 오래된
     엔트리를 "아카이브 요약"에 1~2줄로 압축해 병합
3. [작성 원칙]
   - 다음 세션의 Claude가 이 파일만 읽고 즉시 작업을
     이어갈 수 있을 만큼 구체적으로 쓴다.
     (파일 경로, 커밋 해시, 이슈 번호, 명령어 포함)
   - "왜 그렇게 결정했는지"를 결과보다 우선해서 남긴다.
   - 추측과 사실을 구분한다. 미확인 사항은 (미확인) 표기.
   - 전체 파일이 300줄을 넘지 않게 유지한다.
════════════════════════════════════════════════════════════
-->

## 프로젝트 정보

- **프로젝트명**: Aston Villa → FC 전술 구현 (2026-07-30부터 **3팀 체제**)
- **한 줄 설명**: 실제 감독의 전술을 실측 데이터로 분해해 FC26 인게임 전술(포메이션·역할·포커스)로 재현한다. 대상: **에메리(빌라) / 알론소(첼시) / 이라올라(리버풀)**
- **주요 경로/저장소**: `/Users/user/Documents/tactics` (git, main)
  - DB `data/avl_analysis.db` = single source of truth / 툴 `fc26-heatmap.html` = DB 파생
  - 문서 맵은 `CLAUDE.md` 참조. 신규: `docs/11-alonso-tactics.md`, `docs/12-iraola-tactics.md`
- **관련 링크**: 스케줄 작업 `villa-transfer-watch` (매일 09/21시), 스킬 `.claude/skills/transfer-watch/SKILL.md`

---

## 현재 상태

> 마지막 갱신: 2026-07-31 (세션 #4 — 커밋 1건, **push 안 함**, HEAD `1c51818`)

**세션 #4는 "전제를 원천에서 깨고 대체 경로로 목표를 달성한 날"이다.** #3이 남긴 P0 두 건이
모두 완료됐고, **차단 요소 하나가 풀렸다**(고점유 기준 전환의 선행 수집).

**HANDOFF가 지시한 수집 방법이 틀렸다(obs#97).** `/lineups`의 `formationPlace`는 105이벤트 전
선수 NULL이고 `position`은 G/D/M/F 4단계뿐이라 RB↔LB가 구분되지 않는다 — **"라인업 원천
포지션"은 존재하지 않는다.** 대신 `players[]` **배열 순서가 포메이션 순서**임을 실측 확인해
그것을 원천으로 삼았다(좌표 역산이 아니므로 obs#96 ⑥ 경고에 저촉되지 않는다).

**부수 효과로 HANDOFF의 밴딩 보정안이 기각됐다(obs#98).** RB의 y범위 12.2–29.4와 RAM의
11.6–43.8이 **겹친다** — y≤25 규칙은 원리적으로 둘을 가를 수 없고, 이것이 "오탐 11/124"의
정체였다. 파생 `lineup_pos` 16라벨은 실측 y로 **단조 정렬**(RB 17.0 → LB 84.4)해 검증됐다.

**보가르드 RB가 8선발 704분으로 확정됐다(obs#99)** — n=1도, 좌표가 시사한 10경기도 틀렸다.
**H안 프리셋의 마지막 캐비앗(obs#84 ⑦)이 해소된다.** 단 RB 평점이 RDM보다 낮다(6.46 vs 6.84).

**옛 기준 잔존값 4건을 찾아 정정했다** — 커널 교체 후 DB에 두 기준이 혼재하고 있었다
(알리송 .847 · 가르나초 .911 등). obs#38 브리프도 obs#100으로 개정했다.

⚠️ **미해결 1건은 그대로다(obs#96)** — Roles는 오프더볼을 지배하는데 우리 적합은 전 국면 평균
히트맵 기준이다. **다만 이제 선행 수집이 끝나 전환이 가능하다** → P1이 다음 세션 출발점.

**이번 세션은 "대상 시스템(FC26)을 처음으로 정면으로 판 날"이다.** 세션 #2가 측정 시스템을
감사했다면, #3은 **측정 대상의 정의를 원천에서 확보**하고 그에 맞춰 커널·프리셋·솔버를 다시 세웠다.

**가장 큰 변화는 `placedMap` 전면 교체다(obs#93·#94).** HANDOFF가 "유일하게 실패 가능성이
있는 과제"로 표시했던 변형↔슬롯 매핑이 **전제가 틀려서 풀렸다** — 배열 순서 규칙은 존재하지
않지만 **각 변형 히트맵이 자기 x를 담고 있어 순서를 알 필요가 없었다**. 변형은 시프트가 아니라
**중앙 대칭 미러**였고, 우리 wide 커널은 전부 **좌측판**이었다. 교체 후 새 불변식 2개(질량 보존 ·
미러 대칭)가 성립한다. 대가로 **모든 적합값 기준이 이동**해 회귀 게이트를 교체했다.

**그 결과 프리셋을 전면 재설계했다(H안, 사용자 확정).** 좌측 해방의 성격이 **컷인 → 터치라인
침투**로 바뀌었고(가르나초 `wm_winger`/Attack), 아침에 깨졌던 **C2(풀백 한 명씩)가 복원**됐다.

**EA 공식 피치노트를 1차 자료로 확보했다(obs#91).** 우리 방법론의 **상한 2건**이 드러났다 —
역할 커널은 FC26에서 **의도적으로 비결정적**이고, **숙련 격차는 좁혀졌다**. 우리 37역할×85포커스는
EA 문서·fut.gg 양쪽과 **전면 일치**했다(3·4차 독립 검증).

⚠️ **미해결 1건이 남았고, 그게 다음 세션의 출발점이다(obs#96).** 사용자가 지적했듯 **Roles는
오프더볼을 지배하는데 우리 적합은 전 국면 평균 히트맵에 대해 계산된다.** 지적은 타당하나
전환에 필요한 데이터가 없어 **보류**했다 — 선행 수집이 P0다.

---

## 다음 할 일

> 세션 #4 종료 시점. **P0(선행 수집)이 완료됐다 — 기준 전환의 차단이 풀렸다.**

- [x] ~~**P0 · `player_match_positions` 확장 수집**~~ → **완료** (obs#97·#98, 커밋 `1c51818`).
  ⑴ 왓킨스·캐시 **108행 적재**(0행 → 캐시 RB 46선발/3,980분 · 왓킨스 ST 41선발/3,353분+LST 3).
  ⑵ ⚠️ **전제가 틀렸다** — `formationPlace`는 105이벤트 전 선수 NULL이고 `position`은 G/D/M/F
     4단계뿐이다. "라인업 원천 포지션"은 **존재하지 않는다**. 대신 `players[]` **배열 순서 =
     포메이션 순서**(GK → 각 라인 우→좌)를 쓴다 — 원천값이므로 좌표 역산 금지에 저촉되지 않는다.
  ⑶ `lineup_order`·`formation`·`lineup_pos` 3열 추가(`pos_class` 무변경).
     ⚠️ **밴딩 보정안(y≤25=풀백)은 채택하지 않았다** — RB y범위 12.2–29.4와 RAM 11.6–43.8이
     겹쳐 원리적으로 분리 불가다. 이것이 "오탐 11/124"의 정체다(obs#98).

- [x] ~~**P0 · 보가르드 RB 표본 확정**~~ → **완료: 8선발 704분** (obs#99). n=1도 10경기도 틀렸다.
  **H안의 마지막 캐비앗(obs#84 ⑦)이 해소됐다.** 단 RB 평점 6.46 < RDM 6.84 — 난도 미보정
  (상대에 아스날·맨유·뉴캐슬 포함)이라 인선 근거로 쓸 때 단서를 달 것.
  ⚠️ 그의 RB **그리드**는 아직 1경기 히트맵이다. 8경기 중 7경기 미수집 — 재집계하려면
  `/event/{eid}/player/1089388/heatmap` 7회 수집이 필요하다(P2).

- [ ] **P1 · 고점유 기준 전환** (obs#96 ⑧) — **차단 해제됨**. 사용자 지시 사항이고 선행 수집이
  끝났으니 이제 실행 가능하다. 국면 분리는 `lineup_pos`로 슬롯을 확정한 뒤 수행할 것
  (obs#96 ② 자기정정 사례 — `pos_class` 대신 `lineup_pos`를 쓰면 포지션 혼재가 사라진다).
  지금 전환하면 XI 11칸 중 2~3칸만 전환되고 나머지는 시즌·r6로 남아 **한 XI에 세 축이 혼재**한다.
  P0 완료 후 **한 번에** 전환할 것. 저점유 그리드는 별도 변형 프리셋(`@lowposs`)으로.

- [ ] **P1 · 전 조합 전수 계산** (사용자 지시, 기준 확정 후로 연기) — `SQUAD_SLOTS` 후보 전수
  (약 18만 라인업)를 C1·C2·C3·C5·C6로 풀어 순위표를 만든다. **그룹 분해로 계산량을 줄일 것**:
  와이드쌍·풀백쌍·피벗쌍·독립칸을 따로 최적화한 뒤 결합(중복 선수 배제 필요 —
  보가르드·린델뢰프·맥긴·만잠비·캐시·부엔디아·오나나가 다중 슬롯 후보).
  ⚠️ **기준이 바뀌면 결과가 통째로 무효**가 되므로 P1(고점유 전환) 뒤에 돌릴 것.

- [ ] **P1 · 첼시 26/27 프리셋 재산출** — 빌라만 새 커널이고 첼시(id=15)는 **옛 기준**이라
  두 팀이 서로 다른 자로 측정된 상태다. `player_role_map(team='CHE')` 실측 13행 중
  로저스 `wm_insidefwd`·팔머/엔소 `wm_wideplm`·네투 `wm_winger`가 커널 교체 영향권이다.
  docs/11의 수치(하토 .930·찰로바 .873·포파나 .953·팔머 .845·로저스 .733)도 전부 옛 기준.

- [x] ~~**P1 · obs#38 우측 윙어 브리프 재검토**~~ → **완료: obs#100으로 개정**. 목표 커널
  insidefwd → **`wm_winger`**. 폐기가 아니라 개정이다 — 요구 프로필(1티어)과 침투 서명 판별(⑧)은
  존속하고 커널 처방·수치표만 교체했다. obs#38에는 ⛔ 철회 마커만 덧붙였다(행 무변경).
  **핵심 통찰**: winger는 커널 교체로 Δ=0이었고 insidefwd만 붕괴했다 → **winger 값은 처음부터
  옳았고 insidefwd만 과대평가돼 있었다.** 브리프의 커널 판정은 측정 결함의 산물이었다.
  ⚠️ 남은 캐비앗: 두 원형이 **갈린다**(디아비 insidefwd .722 / 살라 winger .910). 살라 채택
  논거("RM 밴드 안")는 우리 추론이고 EA 근거가 아니다. 살라 그리드는 **축 라벨 미표기**.

- [x] ~~**P2 · LCB 포커스 결론**~~ → **판정: 프리셋 `Build-Up` 유지, 변경 없음.**
  ⚠️ HANDOFF의 **"명시적 obs 근거가 없다"가 부정확했다** — 근거는 3중으로 존재한다:
  obs#33(claim이 "콘사=Aggressive vs 파우=Build-Up 분화"를 명문화) · obs#13(파우 PAC 67로
  Aggressive 부적합 · 두 CB 동시 Aggressive는 rest-defense 붕괴) · `player_duties` id=3/4
  (adherence HIGH, "cb_bpd/Build-Up confirmed unambiguously"). Δ .049는 노이즈 구간(obs#91 ⑴)
  이라 결정권이 상위 축(duties)으로 넘어가고, 그 축이 Build-Up을 명시한다.
  솔버의 Aggressive는 "이탈"이 아니라 **C4가 CB를 무제약으로 두는 설계상 예상된 차이**다.
  - [ ] **P2 잔여 · C5에 `focus` 차원 추가** — 여기서 갈리는 것은 role이 아니라 **focus**인데
    현 `FUNC_AXIS`는 `{role, mode}`만 갖고 role_id만 제약한다. focus를 표현할 수 없어 미등재다.
    즉 결손은 근거가 아니라 **C5의 표현력**이었다. ⚠️ 솔버 동작이 바뀌는 변경이므로
    **사용자 확인 후** 진행할 것(프리셋·솔버 변경은 이 프로젝트에서 늘 사용자 확정 사항이었다).

- [x] ~~**P2 · 툴 버그 ⑴ 키 중복**~~ → **완료: 4건 제거**(FC_STATS 67→66 · FC_DETAILS 65→62).
  네 쌍 모두 값이 동일해 실제 데이터 손실은 없었다.
  - [ ] **P2 잔여 · `SOFIFA` 중복 8건 — 이쪽은 실제 손실이다.** 케이타·카마라(모나코)의
    `sofifa id`가 풀네임 표기 행에 덮여 `sofifaURL()`이 검색 폴백으로 열화된다.
    해법은 `{id:…, en:풀네임}` 병합이 명백하나 en 표기 정본 방침이 없어 보류.
    `FC_FAMILIARITY` 3건은 값 동일(무해).
- [ ] **P2 · 툴 버그 ⑵** 프리뷰가 `/private/tmp/tactics-preview/` 미러를 서빙하므로
  **툴 편집 후 `cp fc26-heatmap.html /private/tmp/tactics-preview/` 필수** (상시 주의사항).

- [ ] **P2 · SSOT 마이그레이션** (obs#84 ⑨) — 툴의 1124줄이 DB 없이 툴에만 있다. **P1(기준 전환)
  뒤에 할 것** — 스키마가 안정된 뒤라야 의미가 있다.

- [ ] **P2 · A축 부활 — metarank 회귀** (obs#85·#90) · **P2 · `COLL_CAL` 재산출**(축 혼재.
  ⚠️ 커널 교체와는 **무관**함이 확인됐다 — 실측 그리드끼리의 코사인이라 재산출 불요, obs#94 ⑥)

- [ ] **P3 · AcceleRATE 경계 선수 4명**(만잠비·캐시·부엔디아·에스투피냔) — EA 공식으로 계열이
  갈리는 경계라 타이브레이커로 쓸 때 신뢰도 단서를 달 것. 183cm판 16/20 · 185cm판 14/20.

- [ ] **P3 · 게상 4행 방향 재도출**(obs#84 ⑥) · **리버풀 프리셋**(개막 후) ·
  **첼시 미해결 3건** · **에메리 세트피스 정량 obs** · **Fotmob 소스 제거**(5일째 미렌더링)

### 차단 요소 / 대기 중

- **3팀 모두 공식전 0경기** — 첼시 **8/24 풀럼**, 리버풀 **8/23 뉴캐슬 원정**.
- **EA가 수치를 공개하지 않는다** — Role+/++ 정량 효과, OOP 페널티 크기, 포커스 위치 델타,
  역할별 능력치 가중치. **AcceleRATE는 분류 공식만 공개**(효과 크기는 여전히 비공개).
- **국면 분리는 보유 선수만** — 영입 4명은 빌라 경기 0이라 구조적으로 불가.

---

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-07-30 | 팀 축 = **텍스트 `team` 컬럼 + FK 없는 `teams`** (3안 중 b) | `ADD COLUMN … DEFAULT`는 기존 1,270행을 재기록하지 않음 = 불변규칙 2 준수. 스키마에 이미 텍스트 팀 축 선례 3건(`matches.team` 등). DB 분리(c)는 빌라↔첼시 동시 참조 행 6건 때문에 자체 기각 |
| 2026-07-30 | UNIQUE 충돌 **5테이블만 재작성** | 나머지는 `player_id`가 스쿼드별로 달라 컬럼 추가만으로 해결. `docs/40-pipeline.md` 파괴적 변경 조항 적용, 마이그레이션은 `data/migrations/2026-07-30_team_axis.sql` |
| 2026-07-30 | **STALE = DEAD 숨김(삭제 아님)** | 툴이 `/^DEAD/i`를 필터하므로 DEAD만으로 화면에서 사라진다. 행을 지우면 `map25`·`fit_sim`을 잃고 복원 시 재측정이 필요 |
| 2026-07-30 | **등급 강등도 DEAD 숨김으로 통일** (7/21 삭제 정책 완화) | 은디아예 .883·로우 .832 같은 실측이 재취득 비용이 큰 자산. 컴팩트화 목적은 그대로 달성 |
| 2026-07-30 | STALE 판정은 **빌라 링크 보도일 기준**(`last_news_date`), 타클럽 보도는 갱신 아님. `CONFIRMED`/`OWNED` 제외 | `rationale`의 "재확인 날짜"로 판정하면 **어떤 행도 정체되지 않는다**. 확정 선수를 숨기면 스쿼드가 깨진다 |
| 2026-07-30 | **CCB 슬롯 신설** | 백3 실측이 지지 — 하토 LCB .930 / 찰로바 **CCB .873**(RCB .744) / 포파나 RCB .953, 각자 자기 자리 최고치. 좌우 흡수 시 찰로바가 .873→.744로 깎인다 |
| 2026-07-30 | CCB를 `CMP_SLOTS`·`SLOT_EMERY_ROLE`에 **의도적 미추가** | 그 키 목록이 곧 빌라 선수비교 드롭다운 → 넣으면 빌라 화면에 첼시 슬롯이 생긴다. `pos:'CCB', t:'CB'`로 기존 타입 키 코드가 그대로 동작 |
| 2026-07-30 | 스캔·크로스체크는 **서브에이전트 위임**, 메인은 판정·실측·DB·리포트 | 검색이 토큰을 많이 쓰는데 산출물은 "이름·등급·URL"뿐. 메인 컨텍스트를 실측·커널 계산에 남긴다 |
| 2026-07-30 | **obs#28 빌라 PPDA 9.88 폐기**, 대표값 12.3/12.45 | 전사는 정확했으나 소스가 오류 의심(표본 15경기·산출사 미표기). 같은 정의 xGStat 12.3과 Δ2.4, 실측 보조지표(공격진영 태클·인터셉트 리그 최하위, obs#17 CB avg_x 33.8) 전부 반대 방향 → 불변규칙 3 적용 |
| 2026-07-30 | 로저스 첼시 행은 **빌라 실측 그리드로 적재** | 사용자 지시. 근거가 에메리 체제 표본이라는 캐비앗을 rationale에 명기 |
| 2026-07-31 | **슬롯 x 정본 = 툴 `SQUAD_SLOTS`**, 다른 문서의 x표 폐기 | 코사인이 x에 대한 **계단 함수**라 버킷이 다르면 값이 통째로 바뀐다. HANDOFF 옛 표의 `LDM 50`이 툴 `38`과 다른 버킷이었다 (obs#82 ①) |
| 2026-07-31 | **docs/20 §B 라인 보상 규칙 폐기**, 역할 보상으로 대체 | §B("수비가담 부족 → 라인 하향")가 obs#27("컴팩트하려면 라인을 **올려라**")과 정면 모순. §B의 −1.3pt→4~7단계 매핑에는 도출 근거가 없다 (obs#82 ⑤) |
| 2026-07-31 | **전역 충돌 임계값 폐기 → 슬롯쌍 상대 지수 CI** | 슬롯쌍 중앙값이 LB×RB 0.006 ~ RB×RM 0.849로 **140배** 범위. 전역선은 인접 쌍 오탐·원거리 쌍 미탐 (obs#83) |
| 2026-07-31 | **26/27 프리셋 5종 전부 폐기**, 단일 `projected:emery-2627`로 재설계 | 결함 있는 측정 기준 위에 세워졌다. DB는 `-deprecated` 표기로 근거 보존, 툴에서는 제거 (obs#82) |
| 2026-07-31 | **A축(능력치 요구 프로필) 기각** | 두 독립 검증 실패(군 내부 7/11 역전, 평점 상관 0.335 < OVR 0.514). EA가 "숙련도는 **Opta 실제 데이터 기반 AI 모델**이 부여"라 명시 — 능력치의 함수가 아니다 (obs#85·#88) |
| 2026-07-31 | **B축(플레이스타일)은 점수 아닌 플래그로만** | EA 사양상 게임 동작을 바꾸는 이산 속성이라 상관 검증 대상이 아니다. 점수화하면 A축과 같은 함정 (obs#85) |
| 2026-07-31 | **뱅크 포커스 기본값 = Support**, Defend는 보상용 | EA 정의·커널·옛 지시 매핑은 Defend를 가리키나 실측 적합이 Support 쪽(캐시 0.835 vs 0.778). 불변규칙 3 (obs#89) |
| 2026-07-31 | **RM 뱅크는 캐시**(알리송 아님) | 알리송의 RM 숙련 역할은 `윙어`이지 `와이드 미드필더`가 아니다 → 뱅크가 **미숙련**이 된다. 숙련도가 개선하는 게 정확히 **오프더볼 복귀**인데 뱅크가 그 역할 (obs#88·#89) |
| 2026-07-31 | **`placedMap` 전면 교체 — 시프트 합성 폐기, EA 정본 변형 선택** | 변형은 시프트가 아니라 **중앙 대칭 미러**(좌우 질량중심 합=4)이고 우리 wide 커널은 전부 **좌측판**이었다. EA는 wide 역할에 **열 변형을 주지 않는다** — 임의 슬롯 x로 미는 것은 소스가 지지하지 않는 조작. 교체 후 질량 보존·미러 대칭이 성립 (obs#93·#94) |
| 2026-07-31 | **회귀 게이트 값 교체** (하지무사 .923 / 알리송 .847 폐기) | 옛 값은 폐기된 합성 방식의 산물. 신규 = 역할 37 · 조합 85 · **변형 217** · 하지무사 **.821** · 알리송 **.835** + 불변식 2개 |
| 2026-07-31 | **프리셋 H안 채택** — 가르나초 `wm_winger`/Attack · RB 보가르드 `fb_wingback`/Support · LDM 고메스 | 커널 교체가 처방 4건을 뒤집었다. 좌측 해방이 **컷인 → 터치라인 침투**로, C2(풀백 한 명씩)가 **복원**. F안(.8507)이 더 높았으나 카마라를 창조 역할에 두어 **obs#32 기능축과 충돌**해 기각 |
| 2026-07-31 | **C5 기능축 제약 신설** (솔버) | 솔버가 움직임 적합만 최대화해 카마라(듀얼·인터셉트 1위 볼위너)를 창조 역할에 배정하는 사고가 났다. **obs 근거가 있는 선수만** 등재하고 require/prefer로 구분 |
| 2026-07-31 | **C6는 하드 제약이 아니라 소프트 선호** | obs#95: 잔차 상관에서 지지되는 것은 "같은 쪽이 함께 올라간다"가 아니라 **"반대쪽 풀백이 억제된다"**이고, 유의한 것은 1건(n=19, p=.010)뿐이다. 근거보다 센 주장을 코드에 새기지 않는다 |
| 2026-07-31 | **슬롯 판정 원천 = `/lineups` `players[]` 배열 순서**, 좌표 밴딩 보정안 기각 | `formationPlace`가 전 이벤트 NULL이고 `position`은 4단계뿐이라 세부 포지션이 API에 없다. 배열 순서는 원천값이라 좌표 역산 금지(obs#96 ⑥)에 저촉되지 않는다. 밴딩안은 RB(y 12.2–29.4)와 RAM(11.6–43.8)이 겹쳐 원리적으로 불가 (obs#97·#98) |
| 2026-07-31 | **`pos_class` 보존 + `lineup_pos` 신설** (덮어쓰기 아님) | 불변규칙 2. 두 파생을 나란히 두면 밴딩의 오탐을 계속 감사할 수 있다. `lineup_order`는 출장시간과 무관한 선발 슬롯이라 `pos_class`가 NULL이던 minutes<45 행도 채운다 |
| 2026-07-31 | **obs#38 브리프는 폐기 아니라 개정**(obs#100) | 뒤집힌 것은 "프로필의 게임 내 커널 이름"뿐이고 요구 프로필(1티어 서사)·침투 서명 판별(⑧)은 커널과 무관하게 유효하다. winger는 커널 교체로 Δ=0 → **winger 값은 처음부터 옳았고 insidefwd만 과대평가**돼 있었다 |
| 2026-07-31 | **LCB는 프리셋 `Build-Up` 유지** (솔버 Aggressive 채택 안 함) | Δ .049는 노이즈 구간(obs#91 ⑴)이라 결정권이 duties로 넘어가고, obs#33·#13 + `player_duties` id=3/4가 Build-Up을 명시한다. 솔버 차이는 C4가 CB를 무제약으로 두는 설계상 예상된 것이지 이탈이 아니다 |
| 2026-07-31 | **고점유 기준 전환 보류** (사용자 지시였으나 미실행) | obs#96: 전환 가능한 칸이 2~3개뿐이라 **한 XI에 세 축(고점유/시즌/r6)이 혼재**한다. 커널 기준을 이미 바꾼 날에 추적 불가능한 혼재를 더하는 위험이 이득보다 크다 — 선행 수집 후 일괄 전환 |

---

## 최근 세션 기록

### 2026-07-31 — 세션 #4: P0 선행 수집 완료 (전제 반박 → 대체 경로)

- **완료** (커밋 1건 `1c51818`, **push 안 함**):
  - **P0 수집 105경기 전수**(오류 0건) — 왓킨스·캐시 **108행 신규**, `lineup_order`/`formation`/
    `lineup_pos` 3열 추가, 373행에 순서 채움. `pos_class`는 불변규칙 2로 무변경
  - **obs#97** 라인업 세부 포지션 부재 규명 + 배열 순서 규약 확립 · **obs#98** 밴딩안 기각 근거
  - **obs#99** 보가르드 RB 8선발 704분 확정 → H안 마지막 캐비앗 해소. 툴 `n=1`·밴드 `r1` 정정
  - **obs#100** obs#38 우측 윙어 브리프 개정(insidefwd → `wm_winger`), obs#38에 ⛔ 마커
  - **옛 기준 잔존값 4건 정정** — 알리송 .847→.835 · 하지무사 .589→.821 · 은디아예 .580→.825 ·
    가르나초 .911→.804(LM x=14)
  - **툴 중복키 4건 제거** · **LCB 포커스 판정**(Build-Up 유지, 근거 3중 존재 확인)
- **진행 중 / 중단 지점**: 없음. 단 **커밋을 push하지 않았다** — 사용자 지시 없이 push하지 않음.
- **발견 / 배운 것**:
  - **HANDOFF의 지시 자체가 틀릴 수 있다** — P0⑵가 지목한 필드(`formationPlace`)는 전 이벤트
    NULL이었다. 지시를 실행하기 전에 **한 이벤트로 전제를 먼저 검증**한 것이 세션을 살렸다
  - **"원천이냐"는 좌표를 쓰냐가 아니라 API가 준 값을 해석 없이 쓰냐다** — 배열 순서는 좌표
    역산이 아니므로 obs#96 ⑥의 금지에 걸리지 않는다. 금지의 취지를 지키면서 목표를 달성했다
  - **에이전트를 믿지 말고 재현하라** — 서브에이전트가 준 가르나초 .804는 맞았지만, 내가
    RM x=85로 계산하니 **0.110**이 나왔다. 그는 좌측 그리드라 정본 x가 **LM 14**였다.
    핵심 4건을 브라우저에서 직접 재현한 것이 이 함정을 드러냈다
  - **`fit_sim`은 `fit_role`/`fit_focus`와 짝**이다(툴 2342줄이 그렇게 렌더). `opt_*`만 고치고
    `fit_*`를 두면 **화면에 잘못된 짝이 표시된다** — 값만 보고 고치지 말고 렌더 코드를 볼 것
  - **HANDOFF의 "근거 없음"도 stale일 수 있다** — LCB는 obs#33·#13 + `player_duties`로 근거가
    3중이었고, 실제 결손은 **C5가 focus를 표현하지 못하는 것**이었다

### 2026-07-31 — 세션 #3: FC26 원천 확보 → 커널 교체 → 프리셋·솔버 재설계

- **완료** (커밋 10건, `924fc4e`…`8cb9cda`, 전부 push):
  - **P0 obs#89 C안 반영** — 이후 obs#91 근거로 RB만 되돌림. obs#89 기록 **2건 정정**(보가르드 .764 재현 불가 → .808 / "미숙련 0칸"은 3안 갈리는 칸 한정)
  - **EA 공식 피치노트 1차 분석**(obs#91) — 상한 2건(역할 커널 비결정성 · 숙련 격차 축소) · Wide Mid EA 정의 · **AcceleRATE 분류 공식** · Team Press 진영별 너프 · 개명 대조표
  - **P1 characteristics 85행**(obs#92) — `game_role_focus` 신설. 뱅크 포커스 논쟁이 태그로 종결. **fut.gg 소스 결함 3종** 발견(slug 비유일 · 역할 162 오염 · 좌우 불일치 4건)
  - **P1 변형 매핑 규명 + `placedMap` 전면 교체**(obs#93·#94) — 위 「주요 결정 사항」 참조
  - **프리셋 H안 재설계** + `transfer_targets.fit_sim` 13행 재산출
  - **솔버 C5·C6 신설** + DB 프리셋 대조 + **되돌리기 버튼**(덮어쓰기 함정 제거)
  - **transfer-watch** — 퀴뇨네스 STALE · 사라 하향 · 바투리나 판정 정정. 리포트 `reports/transfer-watch/2026-07-31.md`
  - **docs** — Team Press 함의를 3팀 문서에 반영, docs/20에 EA 피치노트 절 신설
- **진행 중 / 중단 지점**: **없음**. 단 obs#96의 선행 수집이 다음 세션 P0으로 정의돼 있다.
- **발견 / 배운 것**:
  - **"실패 가능성 있는 과제"가 전제를 의심하니 풀렸다** — 배열 순서 규칙을 찾을 필요가 없었다(히트맵이 자기 x를 담고 있다)
  - **원시 상관은 교란이었다**(obs#95) — LB↔RB가 +0.687로 나온 것이 단서. 경기별 평균을 빼자 −0.168로 뒤집혔다. **잔차화 없이 경기별 지표를 상관내지 말 것**
  - **내 검증이 무효였던 사례**(obs#96 ②) — 경기별 그리드는 (선수, 경기) 단위라 **포지션이 섞인다**. 슬롯별 그리드와 비교하려면 `pos_class` 필터가 **반드시** 선행돼야 한다
  - **프리뷰는 `/private/tmp/tactics-preview/` 미러를 서빙한다** — 툴 편집 후 `cp` 안 하면 사용자가 옛 화면을 본다. 오늘 이걸 빠뜨려 "가르나초가 인사이드 포워드로 보인다"는 지적을 받았고, 캐시 문제로 오진했다
  - **에이전트 병렬화가 유효했다** — 5건 위임(AcceleRATE 검증 · Team Press 문서 · 툴↔DB 감사 · 이적 스캔 · 경쟁 링크). 특히 감사 에이전트가 `renderSolve` 덮어쓰기 결함을 찾아냈다
- **다음 세션 첫 작업**: **P0 `player_match_positions` 확장 수집**(왓킨스·캐시 80경기 + 라인업 원천 포지션). 국면 분리·보가르드 RB 표본이 모두 여기에 걸려 있다.

### 2026-07-30 — 세션 #1: 이적 감시 3회 + 팀 축 도입 + 3팀 확장

- **완료** (커밋 21건, `68444a3`…`ae2c976`):
  - **이적 감시 3회**(09/14/15시) — 리포트 `reports/transfer-watch/2026-07-30.md`. 실측 3건(에머슨 RB .950 / 로우 **RM .155 부적합→LM .832 슬롯 정정** / 카타모 .915), 아데예미 바르사행 DEAD, 경쟁 링크 강도 13명 첫 적용, **하지무사 STALE 첫 전환**(빌라 보도 7/3, 27일)
  - **팀 축 마이그레이션** Phase 0~5a — 행수 12/45/22/15/19 동일 검증, 회귀 게이트(sync 후 툴 diff 0) 통과, 사전 결함 `season='25/26'` 3행 수정
  - **알론소 첼시** — obs 23건, 실측 12명, `docs/11`(228줄), **26/27 projected 처방**(10/11칸)
  - **이라올라 리버풀** — obs 16건, 실측 12명, `docs/12`(292줄)
  - **툴** — 팀 선택기 + CCB 슬롯 + 첼시 처방 프리셋, 기존 버그 2건 수정(첼시에 에메리 구조 채점 / XI에 빌라 선수 줄세움), 브라우저 회귀 검증 완료
  - **파이프라인** — sync 팀 필터, `last_news_date` 정식 전달(NEW 뱃지의 confidence 정규식 의존 제거), 가르나초 OWNED 승격(W·WM 2행)
- **진행 중 / 중단 지점**: 없음. 모든 에이전트 완료, 작업 트리에 이 세션 산출물 없음(미커밋 3건은 세션 이전 것)
- **발견 / 배운 것**:
  - **실측이 서사를 5번 정정했다** — 팔머 CAM→**RM .845** / 하토 LB→**LCB .930** / 로우 RM→**LM** / 존스 "트렌트급" 반박(전방 0.26) / obs#37 하이라인 하향
  - **살라 RM `wm_insidefwd/Attack` .961 · 침투 전방 0.77** = 프로젝트 측정 전 후보 중 최고. FA로 떠났으므로 **obs#38 우측 윙어 브리프의 실측 원형**으로 보존
  - **SofaScore 탭 프리즈** — 7/29의 "레이트리밋" 진단은 **오진**. 홈페이지 라이브 스크립트가 렌더러를 얼린다. 회피: 새 탭 + `robots.txt` + `Promise.all`
  - **MAPS 파서 함정** — 블록 마지막 항목 `st_false9`가 개행 없이 끝나 정규식이 놓친다. 파싱 후 **역할 수 37** 확인 필수
  - **에이전트가 내 지시의 오류를 3번 잡았다** — CCB의 `CMP_SLOTS` 추가(빌라 누출), `injectTransferCandidates` 멱등성 오진, 살라 rationale 오타
- **다음 세션 첫 작업**: **matchengine.pro PPDA 표 확보 시도**(P1). 3자 압박 비교 전체가 이 값에 걸려 있다.

---

## 아카이브 요약

(없음 — 세션 #1이 첫 기록)

> 참고: 세션 #1 이전 작업(7/14~7/29)은 `reports/transfer-watch/*.md`와 git log에 남아 있다.
> 특히 obs#38(우측 윙어 브리프)·obs#39(뱅크 사다리)·프리셋 5종(A/B1/B2/C/투톱)은 7/29 세션 산출물이다.

---

## 컨텍스트 노트

**⚠️ 측정 기준 (obs#82 — 이 규칙을 어기면 값이 재현되지 않는다)**
1. **슬롯 x는 툴 `SQUAD_SLOTS`가 정본**: GK 50 · LB 13 · LCB 37 · RCB 63 · RB 87 · LDM **38** · RDM 62 ·
   LM 14 · CAM 50 · RM **85** · ST 50. 다른 문서의 x표는 전부 폐기됐다.
2. **계산은 툴의 `placedMap`·`cmpCos`를 브라우저에서 직접 호출**한다. 별도 구현 금지(MAPS 파싱 함정).
   ⚠️ `placedMap`은 2026-07-31에 **EA 정본 변형 선택**으로 교체됐다(obs#94). 시프트·미러 합성 코드를 다시 쓰지 말 것.
3. **회귀 게이트(2026-07-31 교체 — 옛 값 폐기)**: 역할 **37** · 조합 **85** · 변형 **217** ·
   하지무사 RM 최고 **.821** · 알리송 RM 최고 **.835** · 캐시 RM `wm_widemid`/Support **.835** ·
   불변식 2개(좌우 **질량 총합 동일** · 좌우 **질량중심 합 = 4**). 옛 값(.923/.915/.847)은 폐기된 합성 방식의 산물이다.
4. **모든 적합값에 축 라벨 병기**(season/top/r6). **쌍 비교는 동축끼리만.**
5. **obs#29 Δ≤0.03을 기계적 적용**(측정최적이 미숙련이고 숙련 역할이 0.03 이내면 숙련 채택). 예외는 duties 명시뿐.
6. **라인은 obs#27·#3 근거값만**(기본 52 / 강팀 43 / 약팀 67 / 리드 45). 수비가담 부족은 **역할로 보상**한다.
7. **충돌은 CI**(슬롯쌍 상대 지수) = (코사인 − 슬롯쌍 중앙값)/(p90 − 중앙값). ≥1.0 주의 / ≥1.5 감점 / ≥2.0 금지.
   **cos < 0.50이면 계산하지 않는다**(분모 붕괴 방지). 표는 툴 `COLL_CAL` 52쌍.

**FC26 대상 시스템 (obs#87·#88·#90)**
- 행동 = **OVR + HyperMotion + PlayStyles(온더볼) + Roles(오프더볼)**. 우리 커널은 **Roles=오프더볼**에 정확히 대응.
- **역할 숙련도는 능력치를 올리지 않는다** — AI 포지셔닝·런·복귀만 개선. **Opta 실제 데이터 기반 AI 모델**이 부여.
  숙련도는 **역할 레벨**이지 포커스 레벨이 아니다. 움직임 역할일수록 ++가 중요.
- **fut.gg JSON API**: `/api/fut/roles/`(49역할·219 역할×포커스×변형, heatmap·characteristics 포함) ·
  `/api/fut/metarank/player/{eaId}/`(선수별 역할 점수). API의 y=0=최전방, x=0=좌측, 값 0/30/60/100 ↔ 우리 0/3/6/X.
- ⚠️ **EA는 수치를 공개하지 않는다** — Role+/++ 정량 효과, OOP 페널티, 포커스 위치 델타, 역할별 능력치 가중치.
- ⭐ **EA 공식 피치노트가 정본이다**(obs#91, docs/20 「EA 공식 피치노트」절). 2차 자료와 충돌하면 이쪽이 이긴다.
  ⑴ 역할 커널은 **의도적으로 비결정적**("instead of strictly following what their Role dictates")
  → **적합도 Δ 0.02~0.05는 노이즈 구간**이며 그 차이로 인선을 가르지 않는다.
  ⑵ **숙련 격차가 좁혀졌다**("bring regular Roles and Roles+ closer to Roles++", OOP 페널티 "대폭 감소")
  → 미숙련 회피에 **큰 적합도를 지불하지 말 것**. Δ가 노이즈면 숙련 우선, 크면 적합 우선.
  ⑶ **개명 주의**: `Wide Mid – Balanced`→**Support** · `Attacking Wingback – Balanced`→**Support** · `Complete`→**Versatile**.
- ⚠️ **방법론 결함(미해결, obs#96)** — Roles는 **오프더볼**을 지배하는데 우리 적합은 **전 국면 평균**
  히트맵에 대해 계산된다. 고점유 기준 전환은 선행 수집(P0) 후로 보류 중이다.
- ⚠️ **경기별 그리드는 (선수, 경기) 단위라 포지션이 섞인다** — 슬롯별 그리드와 비교하려면
  포지션 필터가 반드시 선행돼야 한다(obs#96 ②의 자기 정정 사례).
  ⭐ **필터는 `pos_class`가 아니라 `lineup_pos`를 쓸 것**(obs#98) — `pos_class`는 좌표 밴딩
  파생이라 RB와 RAM을 가르지 못한다(y범위 12.2–29.4 vs 11.6–43.8이 겹침). `lineup_pos`는
  라인업 배열 순서 파생이라 원천값이고, 16라벨이 실측 y로 단조 정렬해 검증됐다.
- **`lineup_pos` 매핑 규약**: 배열은 GK → 각 라인을 **우→좌**. 백4 = 0 GK · 1 RB · 2 RCB ·
  3 LCB · 4 LB. 이후는 포메이션별(`scripts/load_lineup_order.py`의 `FORM` 표가 정본).
  ⚠️ 3백 포메이션(3-4-2-1·3-4-3·3-5-2·5-4-1)은 검증 표본이 사실상 없다(게상 니스·일링 임대).
- ⚠️ **경기별 지표를 상관낼 때는 경기별 평균을 뺀 잔차를 쓸 것**(obs#95) — 안 그러면 팀 영역
  지배도가 모든 쌍을 양의 상관으로 만든다(LB↔RB 원시 +0.687 → 잔차 −0.168).

**SofaScore 실측 수집**
- API는 **페이지 컨텍스트에서만** — `curl`은 UA/Referer를 붙여도 **403**.
- **새 탭 + `https://www.sofascore.com/robots.txt` + `Promise.all` 병렬**이 유일하게 안정적. 얼면 새 탭 재시도.
- 엔드포인트: 히트맵 `/api/v1/event/{eid}/player/{pid}/heatmap` · 경기목록 `/api/v1/player/{pid}/events/last/{page}` ·
  평균위치 `/api/v1/event/{eid}/average-positions` · 팀 스쿼드 `/api/v1/team/{tid}/players`(빌라 tid=**40**)
- 경기별 원자료는 **`player_match_grids`**에 저장돼 있다(230행·7명·45경기). 재집계 시 재수집 불필요.

**sofifa 수집**
- 선수 상세 `https://sofifa.com/player/{sofifa_id}/` — 가속력 유형(AcceleRATE)·체형·선호발·
  **역할별 숙련도+포지션+가용 포커스**를 준다. `player_fc_stats.accelerate/body_type/preferred_foot/role_detail`.
- ⚠️ WebFetch는 LLM 전사 위험이 있어 **수치 데이터에는 쓰지 않는다**. 브라우저 스크랩 사용.

**git**
- ⚠️ **툴 편집 후 `cp fc26-heatmap.html /private/tmp/tactics-preview/` 필수** — 프리뷰 데몬이
  `~/Documents` TCC 권한이 없어 **/private/tmp 미러를 서빙**한다(`.claude/launch.json` 참조).
  빠뜨리면 브라우저가 옛 화면을 보여주고, 캐시 문제로 오진하게 된다.
- **`git add -A` 금지** — `.claude/settings.json`(Figma PAT) 등 커밋 금지 파일. 항상 명시 스테이징.
- DB 변경 후 `python3 scripts/sync_transfer_ui.py` → `scripts/db_dump.sh` → 명시 스테이징 커밋.
- **회귀 게이트**: DB 무변경 상태에서 sync를 돌렸을 때 `git diff fc26-heatmap.html`이 비어야 한다.
- ⚠️ **툴 편집에 탐욕적 정규식을 쓰지 말 것** — 이번 세션에 1271줄이 삭제되는 사고가 났다(git으로 복구).
  블록 제거는 **정확한 줄 범위를 확인한 뒤** 슬라이싱할 것.
