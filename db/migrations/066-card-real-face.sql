-- 066 카드의 **리얼페이스 여부** (2026-09-25 사용자 지시 「얼티밋 선수 사이드패널 세부 정보에서
--     리얼페이스 여부도 fut.gg에서 받아와서 보여줄 수 있어?」)
--
-- 왜: fut.gg가 `playerDef.isRealFace`로 이미 주고 있는데 받지 않아 화면에 축 자체가 없었다.
--   스캔 얼굴(리얼페이스)과 제네릭 얼굴은 **같은 선수라도 카드마다 갈릴 수 있어** 카드 단위 사실이다
--   ⇒ `players`가 아니라 `player_card_items`에 둔다.
--
-- ⛔ 이것은 **겉모습 축이다.** OVR·스탯·역할과 무관하니 성능 판단의 근거로 쓰지 않는다
--   (코스메틱 진화를 카탈로그에서 거르는 것과 같은 이유 — 진화 판정에 끌어들이면 순위가 오염된다).
-- ⚠️ 결손(NULL)과 `0`을 구분한다 — NULL은 「아직 안 받았다」이고 0은 「제네릭 얼굴로 확인됐다」다.

ALTER TABLE player_card_items ADD COLUMN is_real_face INTEGER;   -- 1=리얼페이스(스캔) · 0=제네릭 · NULL=미수집
