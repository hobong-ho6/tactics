> ⚠️ **[2026-08-11 v2 컷오버]** 이 문서는 v1(fc26-heatmap.html, archive/v1/) 기준이다.
> **게이트 표의 정본은 `scripts/gates.py`**, 커널 정본은 `db/tactics.db`(game_role_variants),
> 시스템 정본은 docs/00-overview.md다. 여기 남은 EA 피치노트·역할 분석·판정 이력은 유효하다.

# FC 게임 시스템 분석·매핑 규칙 (FC26 → FC27…)

목적: 실측 분석 결과를 특정 FC 버전의 전술 시스템으로 번역하는 규칙.
게임 버전이 바뀌어도 "행 추가"로만 대응한다 (구조 재작성 금지).

## ⭐ EA 공식 피치노트 — 대상 시스템의 정본 (obs#91, 2026-07-31)

> 여기 있는 것은 **EA 1차 자료 직접 인용**이다. 2차 자료(위키·가이드)와 충돌하면 이쪽이 이긴다.
> 원문: [Gameplay Deep Dive](https://www.ea.com/games/ea-sports-fc/fc-26/news/pitch-notes-fc26-gameplay-deep-dive) ·
> [TU 1.4.0](https://www.ea.com/games/ea-sports-fc/fc-26/news/pitch-notes-fc26-title-update-1-4-0) ·
> [TU 1.5.0](https://www.ea.com/games/ea-sports-fc/fc-26/news/pitch-notes-fc26-title-update-1-5-0) ·
> [The World's Game Update](https://www.ea.com/games/ea-sports-fc/fc-26/news/pitch-notes-fc26-the-worlds-game-update)

### ① 역할 커널은 결정적이지 않다 — 방법론의 구조적 상한

EA는 FC26에서 역할 구속을 **의도적으로 풀었다**: *"Players are less constrained by their Roles"* /
*"All players will be more likely to make the best attacking run options **instead of strictly following
what their player Role dictates**"*(윙어가 박스로 컷인, 윙백이 빈 공간으로 전진) / *"Tweaked **all**
Roles' positions to allow for more space in play"*.

→ **(역할×포커스) → 결정적 히트맵**이라는 우리 가정에는 상한이 있고, 그 상한은 **고칠 수 없다**
(obs#82 ①의 계단 함수는 우리 구현 문제라 고칠 수 있다 — 성격이 다르다).

> **적용 규칙**: 적합도 **Δ 0.02~0.05는 노이즈 구간**으로 취급하고 그 차이로 인선을 가르지 않는다.
> 그 구간은 숙련도·기능축 같은 **이산 근거**로 가른다 — obs#29(Δ≤0.03 → 숙련 우선)의 독립 근거다.

### ② 숙련도(Role+/++) 격차는 FC26에서 좁혀졌다

*"**Significantly improved usage of regular Roles and Roles+, to bring them closer to Roles++**"* /
*"**Drastically reduced the negative impact of being Out of Position**"*.

→ 미숙련 회피를 위해 **큰 적합도를 지불하지 말 것**. 크기는 EA 비공개라 방향만 확정이다.
⚠️ ①과 ②는 반대로 작용한다 — ①은 "작은 Δ는 무의미", ②는 "숙련 프리미엄도 작다".
**Δ가 노이즈 구간(≤0.05)이면 숙련 우선, 그보다 크면 적합 우선**이 현재의 절충이다.

### ③ 역할·포커스 개명 대조표 (문서 인용 시 필수)

FC25/2차 자료 표기와 FC26 현행 표기가 다르다. **같은 것**이다.

| 옛 표기 | 현 표기 |
|---|---|
| Wide Mid – **Balanced** | Wide Mid – **Support** |
| Attacking Wingback – **Balanced** | Attacking Wingback – **Support** |
| Advanced Forward – **Complete** | Advanced Forward – **Versatile** |
| (신규 포커스명) **Complete** 전반 | **Versatile** |

### ④ Wide Mid 리워크 — EA 정의 원문

*"Wide Mid are now **more defensively oriented and ideal for 3-back formations**."*
- **Defend** — *"tracks back like a Wingback **protecting the DM line**"* (커널 자기진영 1.30)
- **Support** — *"supports possession and defence **when needed**"* (0.60)
- **Build-Up** — *"can move a little forward to support play, but also comes back on defence"* (0.00)

→ 에메리 4-2-3-1의 뱅크 칸은 **DM 라인 보호가 아니라 두 줄 4의 두 번째 줄**이므로 **Support가 기본**이다
(obs#89 결론을 EA 정의가 지지). ⚠️ 단 EA는 이 역할을 **백3용으로** 리워크했다고 명시한다 —
백4에서의 정합성은 미확인이고, 반대로 **알론소 3-4-2-1에는 최적**이다.

### ⑤ AcceleRATE 분류 공식 (FC26 = 4종 → **3종** 회귀)

| 유형 | 조건 (전부 AND) |
|---|---|
| **Explosive** | 민첩성 ≥65 · (민첩성−**힘**) ≥10 · 가속력 ≥80 · 키 남 ≤182cm / 여 ≤162cm |
| **Lengthy** | **힘** ≥65 · (**힘**−민첩성) ≥4 · 가속력 ≥40 · 키 남 ≥183cm / 여 ≥164cm |
| **Controlled** | 위 둘에 해당하지 않는 나머지 |

> 🔄 **[2026-07-31 obs#104] 위 표의 2차 속성을 `체력`(Stamina) → `힘`(Strength)으로 정정했다.**
> 전사 오류였다. 전수 대조(20명)로 확정: **힘** 16/20 vs **체력** 15/20 (183cm 기준),
> 힘 14/20 vs 체력 13/20 (185cm 기준). 어느 임계값에서도 `힘`이 더 잘 맞고, EA 실제 공식도 Strength다.

**⚠️ 대조 규약 — sofifa는 6종 하이브리드 표기를 쓴다.** 수집값 분포: `Controlled Lengthy` 12 ·
`Controlled` 4 · `Mostly Explosive` 2 · `Controlled Explosive` 2. 위 3종 공식과 대조할 때는
**라벨의 끝낱말을 계열로 읽는다**(`Controlled Lengthy` → **Lengthy**). 첫낱말로 읽으면 8/20으로
떨어지므로 이 규약을 지킬 것.

→ **쓸 수 있는 것**: `player_fc_stats.accelerate` 수집값의 교차 검증(위 규약으로 16/20).
→ **못 쓰는 것**: 가속 곡선의 **효과 크기는 여전히 비공개**라 적합 판정 가중치로는 쓸 수 없다.
⚠️ "TU 1.4.0에서 Lengthy 최소 키 185cm" 주장은 **원문에 없다**(웹 요약에만 존재) — 183cm를 채택한다.

**불일치 4명은 키와 무관하다**(183판·185판 동일): 캐시(힘−민첩 −5) · 만잠비(−11) — 둘 다 Lengthy
차분 요건 미달 / 부엔디아(가속 74<80) · 에스투피냔(민첩−힘 6<10) — Explosive 요건 미달.
**키 임계값에 갈리는 것은 보가르드(183)·카마라(184) 2명뿐**이고, 채택 기준 183cm에서는 정상 일치한다.
⚠️ HANDOFF가 "경계 선수 4명"이라 적었던 것은 이 **두 개념을 혼동**한 것이다 — 불일치 4명 ≠ 키 민감 2명.

### ⑥ Team Press 진영별 너프 (2026-05-28)

**수비 진영** 발동 시: 볼 소유자 압박 감소, AI 이중 압박 안 함, **마킹 타이트니스 하향**(골대에
가까울수록 더 느슨). **공격 진영** 발동 시: **종전대로** 공격적 압박 + 상황적 이중 압박.

→ 압박 재현이 **진영별 비대칭**이 됐다. **이라올라(고압박)가 가장 손해**, 에메리 미드블록은
상대적으로 유리, 알론소는 중간. 압박 강도를 게임 설정으로 옮길 때 이 비대칭을 반영할 것.

### ⑧ 포커스 판정 = 태그 조회 (`game_role_focus`, obs#92)

포커스 판정 근거는 더 이상 **웹 검색 요약문이 아니다**. 테이블 `game_role_focus`(FC26 **85행**)가
역할×포커스별로 **EA 원문 정의문 + `plus`/`equal`/`negative` 태그**를 보관한다.

```bash
sqlite3 data/avl_analysis.db "SELECT focus,plus,negative FROM game_role_focus WHERE role_id='wm_widemid'"
```

뱅크 칸(`wm_widemid`) 판정의 정본 — 커널 자기진영과 태그가 같은 방향을 가리킨다:

| 포커스 | plus | negative | 커널 자기진영 |
|---|---|---|---|
| **Support** (기본) | Wide Support, **Supports Play** | Lacks Mobility | 0.60 |
| **Defend** (보상용) | Wide Support, **Stays Back** | **Supports Attacks** | 1.30 |

> ⚠️ **fut.gg 소스 주의** (obs#92): ⑴ 역할 `slug`가 유일하지 않다 — **키는 `id`**.
> ⑵ 역할 id 162(CAM Playmaker)에 **Winger 항목 2건이 오염**돼 있다.
> ⑶ 좌/우 변형의 characteristics가 갈리는 행이 4건 있다(`side_conflict=1`).
> **설명문은 좌우 동일하고 신뢰할 수 있으니, 태그가 갈리면 설명문과 정합한 쪽을 채택한다.**

### ⑦ 미검증으로 남은 것

- **라인 높이의 실효** — *"수비 라인이 오프사이드 유도 시 예전만큼 자동으로 올라가지 않는다"*.
  우리 라인 값(52/43/67/45)은 **실측 도출값**이지 게임 내 실효 검증값이 아니다.
- **TU 1.1.0~1.3.0 원문** — EA 사이트에서 404. 변경 이력에 공백이 있다.
- **②의 크기** — Role+/++ 격차와 OOP 페널티의 정량은 EA 비공개.

## 대상 시스템의 구조 — 우리 커널이 무엇에 대응하는가 (obs#87·#88·#90)

- **선수 행동 = OVR + HyperMotion + PlayStyles(온더볼) + Roles(오프더볼).**
  ⭐ 우리 커널이 대응하는 것은 **Roles = 오프더볼**이며, 정확히 그 층까지만 대응한다.
  히트맵이 요구역할의 36.2%만 담는 이유가 여기 있다(obs#121) — 온더볼은 다른 레버다.
- **역할 숙련도(Role+/++)는 능력치를 올리지 않는다** — AI 포지셔닝·런·복귀만 개선한다.
  **Opta 실제 데이터 기반 AI 모델**이 부여하며, 숙련도는 **역할 레벨**이지 포커스 레벨이 아니다.
  움직임 비중이 큰 역할일수록 ++의 값어치가 크다.
- ⚠️ **EA는 수치를 공개하지 않는다** — Role+/++ 정량 효과 · OOP 페널티 크기 · 포커스 위치 델타 ·
  역할별 능력치 가중치. 이 넷은 추정만 가능하고, 추정을 하드 제약으로 쓰지 않는다.
- **fut.gg JSON API** `/api/fut/roles/` — heatmap·characteristics 포함, **커널 원천**.
  좌표는 y=0=최전방 · x=0=좌측, 값 0/30/60/100 ↔ 우리 0/3/6/X.
  ⛔ **`metarank`를 회귀 타깃으로 쓰지 말 것** — fut.gg **자체 모델의 출력**이라 그것에 우리 값을
  맞추는 것은 **순환 논증**이다(A축이 이 이유로 폐기됐다, obs#114).

## FC26 전술 구성요소 (기록 대상)

- **포메이션**: 툴에 45개 FC26 포메이션 등록 완료 (fc26-heatmap.html `FORMATIONS`).
- **역할(Role) + 포커스(Focus)**: 포지션 타입별 역할 세트는 `game_roles` 테이블
  (FC26: 37역할, 9개 포지션 타입). 각 역할의 기대 히트맵 커널은 툴의 `MAPS`에 있음
  — 장기적으로 DB로 이관 예정 (DESIGN.md 참조).
- **팀 전술 설정**: `game_tactic_params` 테이블이 버전별 설정 어휘를 보관.
  FC26 팀 전술 = 포메이션 + 빌드업 스타일(Short Passing/Balanced/Counter)
  + 수비 접근(Deep/Balanced/High/Aggressive) + 라인 높이(0–100, 접근 방식이 범위 제한)
  + 11명의 (역할, 포커스) + 공유용 전술 코드.

## 게임 내 전술 재현의 완전한 계약

하나의 재현 전술 = **`team_tactic_setups` 1행 (팀 설정) + `player_role_map` 11행 (선수 배치)**,
같은 (season, game_version, kind)로 조인. 이 두 테이블만 조회하면 게임에서 그대로
입력 가능한 전술 전체가 나와야 한다. 게임에서 실제로 만들었으면 `tactic_code`만 기록한다.
(게임 내 검증 루프는 2026-07-05 폐기 — confidence는 데이터 기준 FINAL, docs/50 참조.)

## 실측 → 게임 매핑 방법

`player_role_map`에 (player, season, game_version, kind)별로 기록:

- `kind='measured'` — 실측 평균 히트맵을 그대로 25칸 맵(map25)으로 변환한 것.
- `kind='role'` — 실측 히트맵과 가장 유사한 게임 역할(코사인/셀합 유사도) 매핑.
- `kind='optimal'` — 평점×결과 가중으로 도출한 최적 포지션+역할 (베스트 XI + 벤치).
- `kind='match:<태그>'` — 특정 경기 단건 재현 (예: `match:LIV-4-2`).

유사도 판정 시 좌표 규약(docs/30-data-rules.md)을 반드시 맞춘 뒤 비교한다.

**역할 확정 타이브레이커 (2026-07-05)**: 실측 축(모양/기능/도착)이 서로 다른 역할을
가리켜 균형(A/B)이면 **FC26 역할 숙련도**(툴 FC_FAMILIARITY, sofifa 파생)로 확정한다.
- 후보 중 숙련 역할이 하나면 그것으로 (사례: 오나나 → dm_holding/Roaming, 마르티네스 → gk_ballplaying).
- 숙련이 동률이면 기능 축 우선 (사례: 카마라 → dm_holding/Ball-Winning).
- 단, 데이터가 명확하면 숙련도보다 데이터 우선 — 숙련도는 실행 정교함일 뿐 스탯 너프가 아님 (사례: 맥긴 wm_insidefwd 유지).

## 게임 버전 추가 절차 (FC27 출시 시)

1. FC27 역할/포커스 체계 조사 → `game_roles`에 `game_version='FC27'` 행 추가.
2. 역할별 히트맵 커널 작성 → 툴의 커널 라이브러리에 FC27 세트 추가
   (툴이 JSON 로딩으로 리팩터링되면 데이터 파일 추가만으로 끝나야 함).
3. 기존 실측 데이터(레이어 1)는 그대로 재사용 → FC27용 `player_role_map` 행 생성.

## 툴(fc26-heatmap.html) 규칙

### 전술 리포트 페이지 (헤더 메뉴 `전술 리포트`)

- 에메리 25/26 전술의 철학·파운데이션·모듈레이션·국면 분석·데이터 반전 사례·팀 설정·
  최적 XI·방법론을 한 페이지로 정리한 읽기 전용 리포트.
- 내용은 DB(tactic_observations, team_match_stats, player_role_map, team_tactic_setups 등)에서
  파생된 하드코딩 — **DB 판정이 바뀌면 리포트도 함께 갱신**할 것 (갱신일 표기 유지).

### 실측 검증 모드 (헤더 메뉴)

- **왼쪽(A) = 실측 히트맵, 오른쪽(B) = 게임 역할 설정.** 툴의 기본 모드. 헤더의
  `실측 → FC26 검증` 버튼 또는 (실측)/(실측 로테이션) 포메이션 선택으로 진입.
  (헤더 모드는 검증·빌라 스쿼드 2개만 유지 — 자유 비교·로테이션 버튼은 제거,
  같은 기능은 패널 포메이션 드롭다운으로 수행.)
- 왼쪽 데이터는 `player_role_map kind='measured'`의 선수별 정밀 그리드
  (SofaScore API 좌표를 5×5 툴 좌표계로 집계; 경기별 원본은 `appearances.heat_map25`).
- 오른쪽 로스터의 **% 뱃지 = 그 역할·포커스 커널과 같은 포지션 실측 그리드의 코사인
  유사도** (초록≥75 / 노랑≥55 / 빨강<55). 역할을 바꿔가며 실측에 가장 가까운 설정을 찾는다.
- 선수별 확인: 칩 클릭(솔로 토글)이 좌우 패널에 포지션 라벨로 동기화된다.
- **실측 로스터 선수 교체**: (실측)/(실측 로테이션) 패널의 로스터는 슬롯별 드롭다운 —
  동 포지션의 DB 관리 선수 전원(주전·로테이션·영입 후보, `CMP_SLOTS` 실측 그리드 보유자)
  으로 교체해 참조할 수 있다. 교체 시 반대편 패널의 % 뱃지는 새 실측 그리드 기준으로
  재계산된다 (LW 슬롯은 LM 후보 풀 매핑). 직접 전술 구성 시 기준점을 자유롭게 바꾸는 용도.
  선수 비교 모드의 좌우 패널은 비교 선택과 동기화되므로 고정.
- **아이콘 토글**(패널 헤더): 기본 꺼짐, 좌우 패널 동기화. 켜면 히트맵 색·숫자를 숨기고
  각 선수 아이콘을 **설정된 역할·포커스 커널(실측 그리드가 있으면 그 그리드)의 평균
  위치(중심점)**에 배치 — 역할을 바꾸면 아이콘 위치가 따라 움직이는 평균 위치 맵.
- 확정한 역할 조합은 `player_role_map`(kind='role')에 역기록하는 것이 완료 조건.

### 빌라 스쿼드 모드 (헤더 메뉴 `빌라 스쿼드`)

- 상단에 팀 전술 바(4-2-3-1 Wide / 숏 패싱 Short Passing / 밸런스 Balanced / 라인 52 —
  team_tactic_setups 파생) + 슬롯별 선수 선택 UI.
- **슬롯 구조는 게임 내 4-2-3-1 Wide와 1:1** — 좌측 슬롯은 LM(WM 타입)이므로 분석 라벨
  LW의 처방은 wm_* 게임 등가 역할로, 실측 최근접은 wm 커널 코사인 재계산으로 제공
  (2026-07-02 확정 — transfer_targets LM rows, player_role_map kind='role:LM').
  모든 슬롯의 역할·포커스는 게임에서 실제 선택 가능한 조합이어야 한다.
- **최적 조합 버튼(스쿼드 모드)**: 타일 합계 최대화가 아니라 **선택된 11명의 DB 근거
  종합 추천** — ① 선수별: 최적 포지션은 성과(평점×결과) 처방, 오프 포지션은 실측
  재현 → ② **에메리 패키지 보정**(아래 조정 규칙의 알고리즘화): 로저스 10번이면
  RM `wm_widemid/Support` 고정 + LB 공격형 보상(규칙3), 드롭인형 ST(기라시·브로베이)면
  패키지 H(ST `st_false9` + LM `wm_wideplm/Build-Up`, 규칙4)를 개인 최적 위에 덮어쓰고
  '에메리 보정' 태그로 표시. 페어링 금지(규칙1·2)와 라인 보상(규칙B, 커널 기준
  수비가담 23.8% 미만)은 역할로 못 고치므로 ⚠ 경고로 표시 → ③ 에메리 구조 점수로
  현재 vs 추천을 비교. 변경이 없으면 "이미 일치"를 명시한다.
  (일반 포메이션은 기존 타일 합계 방식, 실측 고정 패널은 대상 제외.)
- **에메리 구조 점수(패널 하단 상시 표시, 0~100)**: 셀 총합·중앙 밀집이 아니라 "올바른
  구역을 겹치지 않게 점유"를 점수화 — 조정 규칙 분석의 기준치를 그대로 사용한다.
  ① 커버리지(활성 칸/21) ② 충돌 억제(쌍 코사인 0.88+ 감점 — 기준선 0.81, 금지선 0.9+)
  ③ 10번 포켓(전방 6인, 0~1행×1~3열 질량/6.2 — 과밀도 감점) ④ 수비가담(전방 6인
  자기진영 비중/25.1% — 실측 기준 31%의 커널 등가값) ⑤ 실측 유사(커널↔선수 실측
  코사인, 선수명 있을 때만) ⑥ 패키지 규칙 준수(규칙1~4 위반 감점, 스쿼드 모드만).
  가중 .25/.25/.20/.15/.15/.15 재정규화. **커널 세팅 전용** — 실측 그리드는 스케일이
  달라(선수별 피크 정규화) 평가 대상에서 제외(기준점 역할). 캘리브레이션: (최적)
  프리셋 92, 스쿼드 기본 XI 90, 패키지 H 적용 시 포켓 6.5 복원(DB rationale과 일치).
- 슬롯마다 **분석 근거가 있는 선수만** 후보로 표시하고, 이미 배치된 선수는 다른
  슬롯에서 비활성화된다. 로저스는 RM 후보에서 의도적으로 제외(배치 금지 판정).
- 11명 선택 즉시 **왼쪽 패널 = 처방(최적) 역할**, **오른쪽 패널 = 실측 최근접 역할**로
  두 버전이 구성된다. 개별 역할·포커스는 패널 로스터에서 추가 조정 가능.
- 슬롯 카드마다 선택된 선수의 안내가 표시된다: **✓/⚠ 최적 포지션 여부**(근거 한 줄 —
  `PLAYER_BEST`, player_role_map optimal + transfer_targets fit_sim에서 파생) +
  **🎯 처방 역할·포커스** + **🔁 실측 최근접 역할·포커스** (한글·영문 병기).
- 후보·역할 데이터(`SQUAD_SLOTS`)는 보유 선수만 하드코딩(player_role_map optimal/role +
  player_duties에서 파생) — DB 값이 바뀌면 함께 갱신할 것.
- **영입 후보**(`영입·` 접두)는 `transfer_targets`에서 파생: 루머 수집 → 가능성 등급 →
  최근 6경기(45분+) 실측 그리드 → 슬롯별 커널 적합도 계산 → DB에 행 추가.
  **2026-07-14 리팩터 이후로는 여기서 끝** — `SQUAD_SLOTS`/`PLAYER_BEST`/`XI_POOL`에
  옵션을 손으로 추가하지 않는다. `scripts/sync_transfer_ui.py`가 DB를 그대로
  `TRANSFER_TARGETS`/`TRANSFER_OUTGOING` 미러 배열로 재생성하고, 툴의
  `injectTransferCandidates()`가 그 미러에서 런타임에 세 곳으로 자동 주입한다
  (라벨: `likelihood='CONFIRMED'`→`<short_label>(합류확정)`, 그 외→`영입·<short_label>`).
  즉 이 파이프라인의 산출물은 DB 행(+`short_label` 컬럼)이며, 툴 갱신은 스크립트
  실행만으로 끝난다. 실측 부적합 슬롯은 애초에 그 슬롯으로 DB 행을 만들지 않는 것으로
  대신한다(아데예미 LW 0.24 사례; 로저스 RM 제외와 동일 정책 — "제공 금지" 플래그가 아니라
  행 자체를 안 만드는 것으로 구현이 바뀜).
  이적 확정/무산 시 transfer_targets의 likelihood를 갱신(`DEAD (사유)`는 자동 제외)하고
  확정 영입은 시즌 데이터가 쌓이면 players로 승격.
- 역할·포커스 표기는 전부 `한글 (English)` 병기 — 게임 내 설정 오입력 방지.
- **FC26 역할 숙련도(role familiarity) 플래그** (2026-07-05): `player_fc_stats.role_familiarity`
  (sofifa 파생, role_id별 레벨 1=+/2=++). 슬롯 카드·비교 카드에 **처방 역할이 그 선수에게
  숙련된 역할인지** ✓/⚠로 표시(툴 `FC_FAMILIARITY`, `roleFamiliar()`, W↔WM 개념 정규화).
  **숙련도는 행동(AI 실행 정교함) 영향이지 스탯 너프가 아니므로**(EA 미공개, 커뮤니티 합의),
  **데이터가 명확한 optimal은 실측 기준을 유지**하고 숙련도는 정보 플래그로 쓴다.
  **단, 실측 축이 균형인 A/B 케이스는 숙련도가 확정 타이브레이커** (위 "역할 확정 타이브레이커"
  규칙, 2026-07-05 — 오나나 dm_holding/Roaming·마르티네스 gk_ballplaying 확정이 그 산출).
  발견: 측정 최적의 상당수가 EA 기본 숙련과 불일치 — 에메리가 선수를 기본 역할 너머로 기용
  (CB Defender→BPD, 캐시 Wingback→AttWB, 맥긴 WideMid→InsideFwd). 재현 시 소프트 실행 비용만 감수.
- **자율 판단 지수(Autonomy IQ, 파생 휴리스틱·EA 공식 아님)** (2026-07-05): 로밍/다재다능 같은
  자율 포커스를 줬을 때 스스로 위치·판단을 잘 하는지. 위치선정 .40 + 시야 .25 + 반응력 .25 +
  침착 .10. **포지션 인식**: 위치선정 요인을 슬롯별로 — 공격(CAM/WM/W/ST)=공격 위치 선정,
  수비(CB/FB/DM)=방어 인식, 미드(CM)/GK=둘 평균. 툴 `autonomyIQ(name,slotT)`, ≥80 높음/
  72–79 보통/<72 낮음. 비교 카드에 표시, 스쿼드 카드는 처방 포커스가 자율형인데 <72면 ⚠.
  근거: FC IQ 상황판단 3단 스택 — ①AI(역할·포커스가 결정 선택지) ②능력치(위치선정·시야·반응이
  실제 선택·품질) ③숙련도(속도·정밀도); ②를 능력치로 근사. 예: 카마라 딥피벗은 방어인식 83
  반영해 82(높음) — 공격 위치선정 62로 오판되던 것 교정.

### 스쿼드 인선 → 전술 조정 규칙 (2026-07-03, 실측 그리드 겹침·수비가담 분석)

인선이 바뀌면 개인 역할만이 아니라 **역할 패키지**와 **팀 전술 축**(빌드업/수비 접근/라인)도
점검한다. 근거: 팀메이트 쌍 코사인(같은 칸 충돌) + 정규화 수비가담(그리드 자기진영 40% 비중).

**A. 페어링 금지/필수 규칙** (쌍 코사인, 기본 XI 오나나×카마라 0.81이 기준선):
> ⛔ **[2026-07-31 obs#82·#83] 이 전역 임계값(0.81/0.88/0.9)은 폐기됐다.** 두 가지 이유다:
> ① 베스트게임 축에서 세워졌는데 툴은 시즌 축을 쓴다(기준쌍 오나나×카마라 best **0.814** vs season **0.960**).
> ② **형식 자체가 틀렸다** — 슬롯쌍 중앙값이 LB×RB 0.006 ~ RB×RM 0.849로 **140배** 범위라,
> 전역선은 인접 슬롯쌍(더블 피벗·RB×RM·LM×CAM)을 전부 오탐하고 원거리 쌍은 전부 미탐한다.
>
> **대체: 슬롯쌍 상대 지수 CI** = (코사인 − 슬롯쌍 중앙값) / (슬롯쌍 p90 − 중앙값).
> **CI ≥1.0 주의 · ≥1.5 감점 · ≥2.0 금지**, 단 **cos < 0.50이면 계산하지 않는다**(겹침 없음 + 분모 붕괴 방지).
> 52쌍 캘리브레이션은 `fc26-heatmap.html`의 `COLL_CAL`에 있고 `collisionCI()`가 사용한다.
> 검증: 오나나×카마라 CI **1.55**(감점 — obs#32 '미분화 더블 볼위너'와 일치) / 만잠비×카마라 **−0.16**(정상) /
> 캐시×알리송 0.856이지만 CI **0.11**(정상 — 전역선이면 오탐).
> 아래 개별 금지 규칙 1~5의 **수치는 베스트게임 축 값**이므로 그대로 인용하지 말 것.

1. 바투리나×부엔디아 동시 기용 금지 — 0.95, 샘플 최고 중복. 바투리나는 부엔디아식
   좌편향 패키지의 후계/로테이션으로만.
2. 로피/모리바×틸레만스 동시 기용 금지 — 0.96/0.93 (같은 좌측 DLP 공간). 카마라
   파트너로만 (0.77/0.76).
3. 10번이 로저스(우편향 57.5)면: RM은 터치라인 고정형(맥긴 wm_widemid/Support) 필수 —
   인버티드형(산초·보웬·하지무사)은 전원 0.73+ 충돌. 좌측 점유가 18%→12%로 비므로
   LB를 공격형으로(마첸 또는 디뉴 포커스 Attack) 보상.
4. 타깃/드롭인형 ST(기라시·브로베이)는 CAM 포켓 침범 증가(0.74/0.80 vs 왓킨스 0.68) —
   패키지 H(ST false9 + LM wideplm) 적용, 왓킨스에게는 금지(채널 러너 st_advanced 0.77).
5. RB(에메르송 0.94 캐시 동형)·GK 교체는 역할 패키지 변경 불요.

### ⭐ 슬롯 x 정본표 (obs#82, 2026-07-31)

커널 코사인은 **x에 대한 계단 함수**다(`placedMap`의 시프트가 정수). 슬롯 x가 버킷을 넘으면 값이 통째로 바뀐다
— 맥긴 RM `wm_insidefwd/Balanced`는 x=60~75에서 **0.574**, x=80~100에서 **0.802**다.
**정본은 툴의 `SQUAD_SLOTS`(=`CMP_SLOTS`)이며, 다른 문서의 x 표는 폐기한다.**

| GK | LB | LCB | RCB | RB | LDM | RDM | LM | CAM | RM | ST |
|---|---|---|---|---|---|---|---|---|---|---|
| 50 | 13 | 37 | 63 | 87 | **38** | 62 | 14 | 50 | **85** | 50 |

계산은 툴의 `placedMap`·`cmpCos`를 직접 호출하는 것이 기본이고, **회귀 게이트** 통과 후에만 값을 쓴다.

> ⭐ **[2026-08-06 완화] 별도 구현은 회귀 게이트 통과를 조건으로 허용한다.** 종전 규칙은 "별도 구현
> 금지"였으나, 금지의 목적은 *"브라우저를 쓰라"*가 아니라 **MAPS 파싱 함정 방지**다 — 게이트가 그
> 목적을 직접 충족한다. 세 함수(`decodeMap`/`placedMap`/`cmpCos`)는 전부 순수 함수라 파이썬 30줄로
> 재현되고 브라우저가 필요 없다(2026-08-06 실증: 게이트 7개 전부 소수 넷째 자리까지 재현).
> ⛔ **게이트를 걸지 않은 구현은 여전히 금지.**

> ⚠️ **[2026-07-31 obs#94 — 게이트 값이 교체됐다]** `placedMap`이 시프트 합성에서 **EA 정본 변형
> 선택**으로 바뀌면서 적합값 기준이 이동했다. **옛 게이트(하지무사 .923 / 은디아예 .883 /
> 카타모 .915 / 알리송 .847)를 더 이상 쓰지 말 것** — 폐기된 합성 방식의 산물이다.
>
> | 항목 | 현행 기준값 |
> |---|---|
> | 역할 수 / 조합 / 위치 변형 | **37** / **85** / **217** |
> | ⭐ 하지무사 RM(x=85) | **.821** (`wm_winger`/Attack) — 그리드 `312691114X000140002400001` |
> | 은디아예 RM(x=86) | ~~.825~~ — 그리드가 DB·툴 어디에도 없음, 검증 불가 |
> | 알리송 RM | **.833** (`wm_widemid`/Build-Up) |
> | ⭐ 캐시 RM (`measured:season`) | **.835** (`wm_widemid`/Support — 교체 전후 불변, 단일 열 커널) |
> | Jackson ST(x=50, 28경기 전수) | **.724** (`st_false9`/Attack) |
> | Quiñones ST(x=50) | ~~.832~~ — DB에서 행 삭제됨, 검증 불가 |
> | 만잠비 CAM(x=50) | **.861** (`cam_halfwinger`/Balanced) |
> | 바투리나 CAM(x=50) | ~~.711~~ — DB에서 행 삭제됨, 검증 불가 |
> | 가르나초 LM(x=14) | **.771** (`wm_winger`/Attack) |
> | 가르나초 W(x=15·28) | **.919** (`w_winger`/Versatile) |
>
> ⚠️ **[2026-08-11 obs#140 — 표본 교체로 기준값이 다시 이동했다]** 알리송·만잠비·가르나초의
> `transfer_targets.map25`를 **4~6경기 → 25/26 전수**로 교체했다. 옛 값(알리송 .835 `wm_winger`/Balanced ·
> 만잠비 .817 · 가르나초 LM .804 · W .936)은 **작은 표본의 산물이므로 더 이상 쓰지 말 것**.
> 커널은 그대로이고 **입력 그리드만 바뀌었다** — 즉 이번 이동은 구현 회귀가 아니다.
> - ⭐ **게이트 항목에는 어느 `kind`의 그리드인지를 반드시 병기하라.** 캐시 RM .835는
>   `measured:season` 기준이고, 같은 선수의 `measured`로 재면 **.900**이 나온다(2026-08-11 실측).
>   종류를 안 적어두면 맞는 구현이 틀린 것처럼 보인다.
> - ⭐ **캐시는 `player_role_map`에 있어 영입 후보 표본 교체의 영향을 받지 않는다 — 독립 앵커로 쓸 것.**
>   `transfer_targets` 기반 항목은 표본이 갱신될 때마다 같이 움직인다.
> - ⭐ **하지무사는 그리드를 위 표에 박아뒀으니 계속 앵커로 쓸 것.** DB 행은 §2 보존정책으로 사라졌지만
>   그리드가 `CMP_SLOTS`에만 남아 있었고, 2026-08-11에 `.821`을 정확히 재현함을 확인한 뒤 문서로 옮겼다
>   (같은 날 `CMP_SLOTS`에서 그 항목을 삭제했으므로, **이제 이 문서가 유일한 사본이다**).
> - ⛔ 은디아예·Quiñones·바투리나 3건은 DB·툴 어디에도 그리드가 없어 **재현 검증이 원리적으로 불가능**하다.
>   게이트를 이 3건에 의존시키지 말 것.
>
> 추가 불변식 2개: ⑴ 좌우 커널의 **질량 총합이 같다**(옛 방식은 `clamp` 시프트로 가장자리 질량을
> 뭉갰다), ⑵ 좌우 커널 **질량중심의 합 = 4**(정확한 미러).
>
> ⭐ **[2026-08-06] 커널 교체가 흔든 것은 wide 역할(RM/LM)뿐이다.** 전수 재산출 결과 ST·CAM 4개
> (Jackson·Quiñones·만잠비·바투리나)는 교체 전후 값이 **동일**했다. 게이트가 실패하면 먼저
> **wide 역할인지**를 보라 — 중앙 역할이 틀어졌다면 원인은 커널 교체가 아니라 다른 데 있다.
> ⚠️ **게이트 값을 바꾼 커밋과 게이트를 인용하는 문서를 반드시 같이 고칠 것** — `SKILL.md`가
> 옛 값(.923/.883)을 08-06까지 들고 있어 회귀 검증이 원리적으로 통과할 수 없는 상태였다.

**측정 기준 7항 (obs#82 — 어기면 값이 재현되지 않는다)**
1. 슬롯 x는 위 `SQUAD_SLOTS` 표가 정본. 2. 계산은 툴 함수 호출(또는 게이트 통과한 재현 구현).
3. 회귀 게이트 통과 후에만 값 사용. 4. **모든 적합값에 축 라벨 병기**(`season`/`top`/`r6`/`poss-hi`/
`poss-lo`) — **쌍 비교는 동축끼리만**. 5. **obs#29 Δ≤0.03 기계 적용**(측정최적이 미숙련이고 숙련이
0.03 이내면 숙련; 예외는 `player_duties` 명시뿐). 6. **라인은 obs#27·#3 근거값만**(기본 52 / 강팀 43 /
약팀 67 / 리드 45) — 수비가담 부족은 **역할로 보상**. 7. **충돌은 CI** = (코사인 − 쌍 중앙값)/(p90 −
중앙값), ≥1.0 주의 · ≥1.5 감점 · ≥2.0 금지, **cos<0.50이면 계산 금지**(표는 `COLL_CAL` 52쌍).
⚠️ CI는 **겹침만 보고 보완은 못 본다**(R축 기각, obs#123).

**B. 팀 전술 축 조정** (기준선 team_tactic_setups optimal: 숏 패싱/Balanced/라인 52):
- **빌드업 스타일**: 대부분의 인선에서 Short Passing 유지가 정합 — 딥 드롭 크리에이터
  (틸레만스 38% def, 바투리나 LDM cos 0.78, 로피/모리바 DLP)와 false9형 ST(기라시 0.80·
  브로베이 0.75 — "타깃"으로 알려졌지만 실측은 링크형)까지 숏 빌드업 지원형. Counter 는
  전방 질주형 다수 조합(로저스10+왓킨스+하지무사류)의 접전/원정 변형 여지 정도
  (@tight 그리드 산개 근거 — 약한 추론임을 명시하고 데이터-파이널로 보류).
- **수비 접근/라인**: ⛔ **[2026-07-31 obs#82 — 라인 보상 규칙 폐기]** 아래 '라인 52→45~48' 권고는
  **obs#27과 정면으로 모순된다**. obs#27은 *"컴팩트하게 하려면 라인을 내리는 게 아니라 **올려야** 한다
  (백4를 미드필드선으로 밀어 두 줄 간격을 압축)"*로 정반대를 말하며, 전방이 안 내려오는데 라인까지 내리면
  두 줄 간격이 오히려 벌어진다. 게다가 '−1.3pt → 4~7단계' 매핑에는 **도출 근거가 없다**(결론만 기재).
  → **기본 라인은 obs#27의 52를 유지**하고, 부족분은 **역할 보상**(뱅크 사이드 `wm_widemid` 포커스
  Support 0.60 → Defend/Balanced **1.30**)으로 처리한다. 라인 변형은 obs#3의 **실측 근거값**
  (강팀 43 · 약팀 67 · 리드 45)만 사용한다.
  지표 자체(전방 6인 정규화 수비가담)는 유효하며 **기준선 31.0%는 두 축 모두에서 재현**된다
  (25/26 기본 XI: best 30.9% · season 30.3%). 아래는 폐기된 원문이다 —
  -1.3pt 이상 빠지는 조합은 라인 보상 권장: 태버니어L 29.7 / 틸레만스+카마라 피벗 29.7 /
  바투리나L+로저스10 28.8(최저, 좌측 뒷공간 비대칭 노출 겹침). 권장 보상 = Balanced 유지
  + 라인 52→45~48, 또는 역할 보상(LM 포커스 Defend 계열). 로피+카마라(30.7)·기라시ST
  (30.2)는 축 변경 불요. CB·GK 인선은 라인 축 변경 근거 없음(실측 깊이 중심 차 2pt 내,
  GK 전원 골라인).
- 신뢰도: 팀 축 매핑은 게임 내 미검증(MEDIUM) — tactic_code 생성·비교로 검증할 것.
  변형 세팅은 team_tactic_setups(kind='optimal:<변형>')에 행 추가로 기록한다.

### 선수 비교 모드 (헤더 메뉴 `선수 비교`)

- 목적: **영입 판단** — 같은 슬롯의 후보 2명(보유 선수 vs 영입 후보 포함)을 나란히 비교.
- 선수 A/B 선택 시 **좌우 패널 = A/B 선수의 실측 히트맵**(슬롯 위치에 단독 배치,
  `선수 비교 — A/B (실측)` 포메이션)으로 자동 구성된다. 그리드 없는 후보(측정 대기)는 빈 패널.
- 슬롯 선택(4-2-3-1 Wide 11슬롯) → 선수 A/B 선택 → 3개 근거 블록:
  1. **실측 분석** — 평점 표본(빌라: v_position_profile 시즌 포지션별 / appearances 상위 경기,
     영입 후보: transfer_targets 최근 6경기) + **슬롯 적합** + 최적 포지션 ✓/⚠.
     슬롯 적합의 **기준 역할 = 스쿼드 모드와 동일한 DB 확정 최근접**(SQUAD_SLOTS.fit ←
     player_role_map kind='role' / transfer_targets fit_role)이고, %는 그 커널과 실측 그리드의
     코사인이다. 런타임 argmax(전체 커널 최대)는 시프트 반올림 차이로 DB 스캔과 동률이
     갈릴 수 있어(부엔디아 CAM: DB는 playmaker 0.59 = halfwinger 0.59 동률에서 처방과
     일치하는 playmaker 채택 — 2026-07-03 두 모드 표기 불일치 원인이었음) 기준으로 쓰지
     않고, 확정값보다 유의미하게(>0.02) 높을 때만 "참고 · 커널 최대"로 병기한다.
  2. **FC26 능력치** — `player_fc_stats`(sofifa, FC26 roster 2026-06-30) 파생 하드코딩
     `FC_STATS`: OVR/POT/6대 스탯 미러 바/나이/키/가치. GK 6대 스탯 = 다이빙~위치선정.
     미수록 선수는 "FC26 미수록"으로 표시 (에메르송 호얄 — 브라질 리그 미수록 확인).
     추가로 sofifa 상세(roster 2026-07-02, `attrs/playstyles/traits` 컬럼) 파생 `FC_DETAILS`:
     카드에 **플레이 스타일**(주황 칩)·**특산품 #특성**(파랑 칩), 하단에 접이식
     **세부 능력치 34항목**(공격5/기술5/기동력5/힘5/정신력6/수비3/GK5) 미러 바.
  3. **종합 판정** — 슬롯 적합 40% + 실측 평점 35% + FC26 OVR 25% 가중.
     비교 불가 항목은 제외 후 재정규화: '상위 경기 평균'(베스트 경기 편향) 표본이 끼면
     평점 제외, FC26 미수록이면 OVR 제외. |Δ|<0.03이면 "박빙" 판정.
- **실험 배치 (what-if)** — 선수 목록 하단 "실험 — 타 포지션 선수 (가정)" 그룹으로,
  분석 근거가 없는 슬롯에 아무 선수나 가정 배치해 역할 재정립을 테스트한다
  (예: 알리송 RM → LM Inside Forward). 원리와 한계:
  - 게임 내 움직임 자체는 **선택한 역할 커널이 결정**하므로 "움직임 예측"은 커널 그대로다.
    계산하는 것은 그 선수의 실측 그리드가 이 슬롯 커널로 재현되는 정도 + 재도출 역할
    (원본 방향과 **미러(좌우 반전) 가정** 각각 top-2, 높은 쪽 채택·명시).
  - **미러는 가정치** — DB 확정 판정은 원본 방향 기준(아데예미 LM 0.24 사례)이므로
    구분 표기하고, 본 포지션 평점·적합은 종합 판정에서 제외(참고용 note).
  - FC26 등록 포지션에 해당 슬롯 계열이 없으면 ⚠ 경고(게임 내 포지션 변경 필요) —
    모든 후보에 공통 적용되는 체크.
  - 실험 결과를 채택하려면 **커널 적합도 근거와 함께 player_role_map(kind='role:<슬롯>')에
    기록**해야 스쿼드 모드 정식 후보로 승격된다 (하드코딩 선행 금지; 게임 내 검증 요건은 폐기).
- 후보 데이터(`CMP_SLOTS`)와 `FC_STATS`는 DB 파생 하드코딩 — player_fc_stats /
  transfer_targets / player_role_map 갱신 시 함께 갱신할 것.
- FC26 스탯 수집법: sofifa는 curl/WebFetch 403(Cloudflare) — Chrome 페이지 컨텍스트에서
  `/players?keyword=<이름>&showCol[]=ae|hi|oa|pt|bp|vl|pac|sho|pas|dri|def|phy` 검색 페이지를
  fetch해 파싱한다 (동명이인은 팀명으로 구분). 새 영입 후보 추가 시 같은 방법으로
  player_fc_stats에 행 추가.

- 프리셋 네이밍: `아스톤 빌라 <시즌> (<종류>)` — 종류: 실측 / 역할 / 최적 / 경기 태그.
- 프리셋 JSON export는 `data/exports/`에 날짜 스탬프로 저장·커밋한다.
- 방향 규약: 툴 좌표(x=좌우, y=공격방향)와 SofaScore 좌표(x=공격방향, y=좌우 반전)가
  **다르다**. 변환 시 docs/30-data-rules.md의 규약 표를 따를 것.
- 로드맵: 하드코딩 프리셋 → `player_role_map`에서 export한 JSON 로딩으로 전환
  (DESIGN.md "Tool strategy" 참조). 새 시즌·새 버전이 코드 수정 없이 추가되는 상태가 목표.
