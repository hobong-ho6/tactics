# 데이터 수집·기록 규칙

## 스키마 (data/avl_analysis.db)

전체 설계는 [data/DESIGN.md](../data/DESIGN.md). 테이블 요약:

- `seasons(code,label)` — 시즌 코드는 `2025-26` 형식.
- `players` / `player_seasons` — 선수 정체성은 시즌 불변, 소속·등번호·출전시간은 시즌별.
- `matches(date,opponent,competition,venue,result,season,…)` — venue는 H/A/N.
- `appearances` — 선수×경기. 평점·포지션·역할·히트맵·출처·신뢰도.
- `streaks` + `match_streak` — 연승 등 구간 정의 (다대다).
- `game_roles` — FC 버전별 역할 라이브러리.
- `player_role_map` — 실측→게임 매핑 결과 (분석과 툴 사이의 계약).
- `player_match_positions` — 경기별 평균위치 실측 (avg_x/avg_y, 분·평점, pos_class).
  가변 포지션 선수의 "가장 많이/가장 잘 맡은 역할" 판정의 원천. 집계는 `v_position_profile` 뷰.
- `player_duties` — 선수×포지션 임무 vs 수행 리뷰.

### pos_class 분류 밴딩 (player_match_positions)

- 레인: y<40 = R, 40–60 = C, y>60 = L / 깊이: avg_x≥52 = 전진(LW·CAM·RM), <52 = 피봇.
- 45분 미만 출전은 평균위치가 불안정하므로 pos_class = NULL.
- 경계(레인 60 부근)에 걸치는 선수는 밴드 2개로 갈라질 수 있음 — 해석 시 합산할 것
  (사례: 틸레만스 pivot-left 11 + pivot-centre 10 = 같은 역할 21선발).

## 표기 규약

- **competition**: 표준 명칭만 사용 — `Premier League`, `Europa League`, `FA Cup`, `EFL Cup`.
  스테이지·매치위크는 **`stage` 컬럼**에 기록 (`MW5`, `R16 1st leg`, `Final` 등). 기존 데이터 정규화 완료(2026-07-02).
- **result**: 홈-원정 순 원문 스코어 (`4-2`)만. 승패는 venue와 조합해 판정.
  `W 4-0` 같은 접두 표기 금지 (기존 10건 정규화 완료).
- **source**: URL 또는 API 엔드포인트를 그대로. 여러 소스는 ` ; `로 이어붙이고
  수집 날짜를 괄호로 남긴다.
- **confidence**: `HIGH / MEDIUM-HIGH / MEDIUM / QUALITATIVE` + 무엇이 어느 등급인지
  한 문장 근거. 항목별 등급이 다르면 나눠 적는다 (예: 평점 HIGH, 히트맵 MEDIUM).

## SofaScore 좌표 규약 ★중요

API 히트맵/평균위치 좌표는 0–100 스케일:

| 축 | 의미 |
|---|---|
| x | 공격 방향. 0=자기 골, 100=상대 골 |
| y | **낮음=오른쪽 측면, 높음=왼쪽 측면** |

- 홈/원정 관계없이 동일 (2026-07-02 검증: Cash(RB) y≈15–20, Digne(LB) y≈85–90,
  홈 3경기·원정/중립 2경기 교차 확인).
- 존 분할: x 3등분(def/mid/att) × y 3등분(right/centre/left, 33.34 경계).
- 툴(fc26-heatmap.html) 좌표는 x=좌우/y=공격방향으로 **축이 다르므로** 변환 주의.

### 그리드 비교 산출물 규칙 (2026-08-03 사용자 확정)

`map25` 그리드를 둘 이상 비교해 **대화로 설명할 때는 수치 표만 내지 말고 5×5 히트맵을 함께 렌더링**한다.
요약 지표(자기진영·전방·중심행)만으로는 적합값의 **원인**이 보이지 않는다 — 그리드를 그리면
"가르나초는 위에 눌린 삼각형 / 그릴리시는 세로로 긴 기둥"처럼 **모양 차이가 곧 적합 격차의 원인**임이 드러난다
(실제 사례: `widemid/Support` 커널을 나란히 놓고서야 그릴리시 .793의 정체가 **아래 두 줄 질량**임이 확인됐다).

- **선수 그리드만 그리지 말고 관련 커널 그리드를 옆에 같이** 놓는다. 커널이 없으면 "왜 그 역할인지"가 안 보인다.
- 규약 라벨을 매번 명시: **위 = 공격 방향(행 0) · 왼쪽 = 왼쪽 터치라인(열 0)**, `X` = 피크(10).
- 단일 색 램프 농도로 칠하고(값 0은 빈 칸), 카드마다 요약 지표 한 줄을 캡션으로 붙인다.
- **표는 빼지 않고 병행**한다(정확한 수치 인용용). 순서만 그리드가 먼저다.
- ⚠️ 적용 범위는 **대화 산출물**이다. `reports/`·`docs/`는 마크다운이라 위젯을 넣을 수 없으므로,
  파일에는 종전대로 표 + 필요 시 코드블록 그리드(5줄 텍스트)를 쓴다.
- ⚠️ 비교하는 그리드는 **같은 축**이어야 한다(`measured` vs `measured:season` vs CMP 슬롯 그리드를 섞지 말 것) —
  obs#124·#127의 축 혼재 사례 참조.

## SofaScore 수집 절차

`api.sofascore.com`은 curl/서버 요청을 403으로 차단한다. **Chrome(claude-in-chrome)의
javascript_tool로 sofascore.com 페이지 컨텍스트에서 fetch** 하면 된다.

주요 엔드포인트:
```
/api/v1/search/all?q=<이름>                     → 선수/팀 ID
/api/v1/player/<pid>/events/last/<page>         → 선수의 과거 경기 목록 (event ID 확보)
/api/v1/event/<eid>/player/<pid>/heatmap        → 히트맵 좌표 배열 [{x,y},…]
/api/v1/event/<eid>/player/<pid>/statistics     → 그 경기 평점·분·터치·키패스
/api/v1/event/<eid>/lineups                     → 라인업·포메이션·전원 평점
/api/v1/event/<eid>/average-positions           → 팀 전원 경기 평균좌표 (averageX/averageY)
```

대량 수집 팁: javascript_tool 결과는 ~1KB에서 잘리므로, 루프는 window 변수에 담아
백그라운드로 돌리고 (`window.__done` 플래그 + setTimeout 폴링) 결과는 슬라이스로
나눠 회수한다. localhost POST 브리지는 Chrome의 로컬네트워크 차단으로 불가.

수집 시 기록할 것: 존 분포 %(상위부터), 중심좌표(centroid), x/y 범위, 히트포인트 수,
터치 수 → `heat_zones`(존 요약)와 `heat_summary`(움직임 서술 + 수치)로.
추가로 **5×5 툴좌표 그리드**를 `appearances.heat_map25`(문자 0–9,X=peak)와
`heat_tool_x/heat_tool_y`(툴좌표 중심)로 저장한다.
**이벤트 스탯도 반드시 함께 수집** (그리드와 같은 statistics 호출에서 나옴):
`xg`/`xa`/`key_passes` 컬럼 + `stats_json`(passes_total/acc, duels_won/lost, tackles,
interceptions, shots_on/off, dribbles_won, aerials_won, goals, assists 컴팩트 JSON).
선수별 집계는 `v_event_profile` 뷰. 용도: 평점 의존 축소 — 위치(그리드)+산출(스탯)의
2차원 판단 (docs/10 방법론 한계 참조).
변환: 툴x = 100 − SofaScore y, 툴y = SofaScore x. 선수별 통합본(경기 균등가중 평균)은
`player_role_map kind='measured'`에 기록하며 툴이 이를 렌더링한다.

**포지션-순수 규칙**: 가변 포지션 선수의 통합 그리드는 반드시 같은 포지션 경기끼리만
묶는다 (혼합하면 어느 역할과도 안 닮은 뭉개진 그리드가 됨 — 로저스 사례: 혼합 43% →
LW-순수 55%). 주 포지션 통합본 = `kind='measured'`, 나머지 포지션 =
`kind='measured:<class>'` (예: measured:CAM). 포지션당 표본 2경기 이상 확보 후 집계.

**맥락별 분리 집계**: 같은 (선수, 포지션)이라도 경기 지배도에 따라 풋프린트가 다르므로,
버킷당 2경기 이상이면 `@dom`(2골차 이상 승) / `@tight`(무·패·1골차 승)으로 분리 집계를
추가한다 — kind 예: `measured@dom`, `measured:CAM@tight`. 균등 평균의 "번짐"을 피하고
지배/접전 변형 프리셋의 근거가 된다. 표본 확장은 경기 수보다 **맥락 다양성**(홈/원정,
승/패)이 우선 — 균등가중 top-10 확장은 편향과 번짐만 키우므로 금지.

주요 ID (25/26 확보분): Konsa 827679, Buendía 783126, Cash 833956, Digne 96538,
Rogers 948261, Tielemans 331737, McGinn 250223, Onana 923973, Kamara 826204.
팀: Aston Villa 40. 그 외는 search로 확인하고, 자주 쓰는 ID는 여기에 추가해 갱신.

## ⚠️ 수집 함정 — 재현 실패의 실제 원인들

세션 간에 반복해서 사람을 잡은 것들만 모았다. **새 수집 전에 이 절을 먼저 읽을 것.**

### ① 결손과 0은 다르다 — SofaScore는 값이 0이면 키를 생략한다 (obs#131·#132, 2026-08-05)

`statistics` 응답에서 `interceptionWon` 같은 키는 **값이 0이면 아예 나오지 않는다.** 이걸 "미수집"으로
읽으면 **0인 표본이 평균에서 통째로 빠져 값이 부풀려진다.** 실제 피해: 카마라 인터셉트 **3.0 → 1.22** ·
듀얼 **8.4 → 6.9**, 그리고 **인터셉트·태클 1위가 카마라 → 오나나로 바뀌었다**(전수 재수집 142/142).
- ⚠️ **`v_event_profile`의 `n` 컬럼은 지표별 실제 표본 수가 아니다** — 그 뷰를 인용하는 판정은
  반드시 **지표별 COUNT를 따로 확인**할 것(08-05에 지표별 COUNT 컬럼을 추가했다).
- 판별법: 명시적 `0`이 표본 전체에서 **단 1건도 없으면** 그 지표는 생략형이다.

### ② 탭 프리즈는 레이트리밋이 아니다 (2026-07-30)

sofascore.com **홈에서** fetch 루프를 돌리면 라이브 스코어 스크립트가 렌더러를 얼려 CDP가 45초
타임아웃된다. 403이나 레이트리밋으로 오진하기 쉽다(2026-07-29 실제 오진).
- ✅ **새 탭 + `https://www.sofascore.com/robots.txt`(동일 오리진 경량 페이지) + `Promise.all` 병렬**이
  안정적이다. 얼면 새 탭을 만들어 재시도. 약 200회 호출은 **배치를 나눌 것**(6배치로 198경기 성공).
- ⛔ `curl`은 UA·Referer를 붙여도 **403**. 페이지 컨텍스트 외에는 방법이 없다.
- 실행 수단은 **claude-in-chrome / 인앱 브라우저 페인 둘 다 된다**(2026-08-06 후자로 수집 성공) —
  중요한 것은 도구가 아니라 **sofascore.com 오리진에서 실행되는가**이다.

### ③ 그리드 인코딩 규약 (230/230 재현으로 확정, obs#104)

`row = 4 − ⌊소파x/20⌋` · `col = ⌊(100 − 소파y)/20⌋` · `map25 = (v==max ? "X" : round(v/max*10))`
— **min-1 클램프 없음**(작은 값은 0으로 떨어지므로 `cells`가 무손실 원자료다). 히트포인트 **15 미만 제외**.
⚠️ **새 수집 전에 기존 행 하나로 회귀 검증**하고 시작할 것.

### ④ 라인업에 세부 포지션이 없다 (obs#97·#98)

`formationPlace`는 전 이벤트 **NULL**, `position`은 G/D/M/F뿐이다. 대신 `players[]` **배열 순서**를 쓴다
(GK → 각 라인 **우→좌**; 백4 = 0 GK · 1 RB · 2 RCB · 3 LCB · 4 LB, 이후는
`scripts/load_lineup_order.py`의 `FORM` 표가 정본).
- ⭐ **포지션 필터는 `pos_class`가 아니라 `lineup_pos`를 쓸 것** — `pos_class`는 좌표 밴딩 파생이라
  RB와 RAM을 가르지 못한다(y범위 12.2–29.4 vs 11.6–43.8이 겹침). `lineup_pos`는 배열 순서 파생이라
  원천값이고 16라벨이 실측 y로 단조 정렬해 검증됐다.
- ⚠️ 3백 포메이션(3-4-2-1·3-4-3·3-5-2·5-4-1)은 검증 표본이 사실상 없다.

### ⑤ 경기별 지표를 상관낼 때는 잔차를 쓴다 (obs#95)

원시 상관은 팀 영역 지배도가 모든 쌍을 양으로 만든다 — LB↔RB 원시 **+0.687** → 경기별 평균을 뺀
잔차 **−0.168**(부호가 뒤집힌다).

### ⑥ 친선경기는 실측을 주지 않는다 (obs#126)

히트맵·선수 스탯·평균좌표·팀 스탯이 **11명 전건 null**이다(라인업 배열·교체·득점만 남는다).
프리시즌 경기는 **인선 신호로만** 쓰고 그리드·적합 계산을 시도하지 말 것.

### ⑦ sofifa — 수치는 브라우저로, AcceleRATE는 끝낱말로 (obs#104)

선수 상세 `https://sofifa.com/player/{sofifa_id}/` — AcceleRATE·체형·선호발·역할 숙련도.
- ⚠️ **WebFetch는 수치 데이터에 쓰지 않는다**(LLM 전사 위험) — 브라우저 스크랩 사용.
- ⚠️ **AcceleRATE 대조 규약**: sofifa는 **6종 하이브리드 표기**이므로 **끝낱말을 계열로 읽고**
  2차 속성은 **`힘`**(Strength)을 쓴다 → **16/20** 일치. 첫낱말로 읽으면 **8/20**으로 붕괴한다.

## 평점 권위 순서

SofaScore API 공식값 > SofaScore 뉴스 > 서드파티 매체 자체 평점.
하위 소스 값을 API로 교정하면 이전 값과 교정 사유를 `heat_summary` 또는
`confidence`에 남긴다 (예: 부엔디아 본머스전 9.0(Sportsdunia)→8.5(API)).

## 영상·서사 소스 절차 (2026-08-11 성문화 → **2026-08-12 정기 수집으로 승격**)

**적용 시점(의무)**: ⑴ 선수가 시스템에 새로 추가될 때 ⑵ 새 팀 온보딩 시 스쿼드 전원
⑶ 감독 전술 축을 세울 때 ⑷ ⭐ **정기 수집(match-watch) 매 회차 — 아래 §정기 수집 규칙**.
AVL·CHE·LIV 공통.

1. **수집**: 전술 분석 사이트(TFA·Coaches Voice·Opta 등)·스카우트 리포트·영상 해설 텍스트를
   WebSearch로 확보. 소스 URL 필수.
2. **기록**: `player_duties`(선수) / `manager_profiles`(감독)에 **가설 지위**로 기록 —
   confidence에 "서사 소스(3티어급)" 명기.
3. **교차검증**: 실측 그리드·스탯·커널 적합과 대조. **위치 주장은 실측이 이긴다**
   (실증: 만잠비 "추가 스트라이커" 기각·부엔디아 원조 사례). 온더볼·기능 주장은
   그리드 밖 층이므로 가설 유지 + PlayStyles/스탯 확인으로 연결.
4. **역할 긴장 발견 시**: 커널 Δ 산출로 판정 — Δ>.05면 적합 우선(실증: 고메스 obs#144),
   Δ≤.05면 서사가 이산 근거가 될 수 있다. **Δ 산출 전 처방 변경 금지.**
5. **판정 기록**: obs에 정합/긴장/기각을 남기고 duties의 adherence에 요약.

### ⭐ 정기 수집 규칙 (2026-08-12 신설 — 사용자 지시)

**실측만 모으면 절반이다.** 히트맵·스탯이 담는 것은 요구역할의 36%뿐이고(obs#121), 나머지는
영상·서사에서만 온다. 따라서 **정기 수집 회차마다 서사 소스를 함께 훑는다.**

**소스 4종을 매 회차 확인한다** — 하나라도 빠뜨리면 리포트에 "미수행" 사유를 적는다:

| 종류 | 무엇을 얻나 | 검색 형태 | 티어 |
|---|---|---|---|
| **유튜브 전술 분석** | 움직임·패턴·관계 — 정지 지표가 못 잡는 층 | `<선수/감독> tactical analysis` · 채널명(Tifo·TFA·Statsbomb) | 3티어(영상 자체) / 원출처가 1~2티어면 승격 |
| **전술 블로그** | 구조 서술·도해 | TFA · Breaking The Lines · Spielverlagerung · Coaches' Voice | 2~3티어 |
| **기사** | 부상·기용 의도·감독 발언 | `<팀> <선수> <날짜>` · 클럽 전담 기자 | 1~2티어 |
| **본인 발언**(기자회견·인터뷰) | 의도 그 자체 — **1차 자료** | `<감독> press conference` | **1티어** |

**⚠️ 영상 소스의 기록 규약**:
- 채널명 · 영상 제목 · **게시일** · URL을 `source`에 모두 남긴다. 게시일이 없으면 "게시일 미확인"을 명기.
- 영상 내용을 직접 확인하지 못했으면 **"영상 설명·자막·요약 기사 기준"** 이라고 적는다.
  본 것처럼 쓰지 않는다.
- ⚠️ 유튜브 전술 분석은 **조회수·구독자와 정확도가 무관하다**. 원출처(경기 영상·데이터)를 밝히는
  채널을 우선하고, 밝히지 않으면 3티어로 고정한다.

**⚠️ 발행일 검증은 여기서도 먼저다** — transfer-watch §2-0의 4개 지표가 그대로 적용된다.
전술 분석 영상·블로그는 **재업로드·재순환이 특히 잦다**(같은 감독의 같은 전술을 매 시즌 다시 다룬다).
**체제 구분**을 반드시 하라: 에메리는 비야레알/아스날 시절, 알론소는 레버쿠젠 시절,
이라올라는 본머스/라요 시절 자료가 대량으로 돈다 — 현 팀 결론처럼 쓰면 안 된다.

**⚠️ 파일럿에서 확인된 한계 (2026-08-12 · AVL 14명 시범)**:
- ⛔ **유튜브는 본문 수집이 구조적으로 불가하다** — `WebFetch`가 영상 설명문을 가져오지 못한다.
  실제로 확보된 것은 **제목·검색 요약**뿐이고 14명 중 영상 근거가 실질적으로 쓰인 사례는 0이다.
  ⇒ 유튜브는 **"이런 분석이 존재한다"는 리드**로만 쓰고, 근거는 **같은 내용을 다룬 글 소스**로 확보하라.
  기록할 때 반드시 **"미시청 — 설명·검색요약 기준"** 을 명기한다.
- **1차 발언(quote)이 가장 약한 축이다** — 14명 중 임무를 실제로 서술한 감독·본인 발언은 소수였다.
  가장 세지만 가장 안 나온다. 못 찾으면 "미발견"으로 적고 넘어가라(억지로 채우지 말 것).
- ⭐ **정기 수집의 실질 수확은 부상·기용 정보였다** — obs#189(오나나 ACL 9개월)는 실측만 봤다면
  "출전 0"으로만 보였을 사실이다. **결장 사유는 서사에서만 온다.**

**산출 경로**:
- 선수 → `player_duties`(duties/execution/adherence/game_role_implication)
- 감독 → `manager_profiles`(11축) + `game_implication`이 있으면 `team_tactic_setups`·처방에 반영
- 판정·충돌 → `observations`
