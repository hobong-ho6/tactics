# 게임 내 검증 루프 — ⛔ 폐기 (2026-07-05)

**상태: 폐기.** 사용자가 수동 플레이(AI 자동 플레이 불가) 환경이라 체계적 검증이
불가능하다고 결정 — **데이터(실측 3축 + FC26 숙련도 타이브레이커)가 최종 심판**이다.
`team_tactic_setups.confidence`는 전부 `FINAL (data-only)`로 확정됐고, `ingame_checks`
테이블은 비운 채 보존한다(추가만 원칙). 아래 체크리스트는 참고 자료로만 남긴다 —
플레이 중 우연히 관찰한 것을 공유하면 기록은 가능하지만, 어떤 판정도 이를 전제하지 않는다.

---

(이하 원문 보존 — 참고용)

전술 재현의 최종 심판이었던 단계. 결과는 `ingame_checks` 테이블에 기록하고, 수렴하면
`team_tactic_setups`의 confidence를 HIGH로 올리고 `tactic_code`를 채우는 설계였다.

## 준비 (게임에서 1회)

1. 전술 2개 생성 — 팀 설정은 동일: **4-2-3-1 / 숏 패싱 (Short Passing) / 밸런스 (Balanced) / 라인 52**
   - A안 `(역할)`: 실측 최근접 조합 (재현 충실도) — 툴 [실측 → FC26 검증] 우측 패널 값
   - B안 `(최적)`: 처방 조합 — 툴 스쿼드 모드 왼쪽 패널 값
2. 각 전술의 **공유 코드**를 기록 → `team_tactic_setups.tactic_code` / `ingame_checks.tactic_code`

## 검증 플레이 (프리셋당 3~5경기)

경기 후 화면에서 확인할 것 — 3축 채점:

| 축 | 게임에서 보는 것 | 비교 기준 (DB) |
|---|---|---|
| 모양 shape | 선수별 히트맵 화면 (스크린샷) | `player_role_map` kind='measured'(+@dom/@tight/@poss) 그리드 |
| 기능 function | 경기 후 선수 스탯 (패스%·태클·슈팅·키패스) | `v_event_profile` (경기당 산출) |
| 도착 arrival | 득점·슈팅 위치와 유형 (헤더/6야드/에지) | `player_shot_profile` |

### 우선 관찰 포인트 (판정이 갈려 있는 자리)

- [ ] **부엔디아/로저스 10번** — 게임 히트맵이 좌편향으로 나오는가? (실측: 중심 y 56~64 좌측)
- [ ] **카마라 RDM (확정: 홀딩/Defend)** — "하프라인 우측-중앙 + 백4 스크린·안전한 배급"이 재현되는가?
  ⚠️ 옛 기준 듀얼 8.4·인터셉트 3.0은 0 생략 보정 후 **6.9·1.22**로 폐기됐고,
  인터셉트·태클 1위는 오나나다(obs#132·#195). A/B는 숙련도 타이브레이커로 2026-07-05 종결.
- [ ] **오나나 LDM (확정: 홀딩/로밍)** — 오픈플레이 깊은 로머 무브먼트 + 볼위닝 기능 + 세트피스 헤더 도착이 나오는가? (A/B 종결, 동일)
- [ ] **GK 마르티네스** — 스위퍼/빌드업이 과하게 튀어나오지 않는가? (실측: 골라인 고정, 스위핑 상황부)
- [ ] **왓킨스** — 인박스 도착 비중 (실측 84%) + 헤더 득점 나오는가? 슛 위치 평균이 박스 안인가?
- [ ] **맥긴 RM** — 에지 도착(헤더 0, 6야드 거의 0)이 재현되는가, 아니면 박스로 파고드는가?
- [ ] **콘사 BPD** — 스텝아웃 캐리가 나오는가, 패스 성공률 90%+ 유지되는가?
- [ ] **팀 라인 높이 52** — 4-4-2 미드블록이 형성되는가? 너무 높거나 낮으면 ±5씩 조정 후 기록

## 기록 방법

경기(들) 끝나면 스크린샷과 함께 알려주면 세션에서 다음을 수행:
게임 히트맵 → 5×5 그리드 변환 → 실측과 코사인 비교, 스탯 대조, `ingame_checks` INSERT,
MISMATCH 항목은 역할/포커스 조정안 제시 → 다음 라운드.

## 종료 조건

- 우선 관찰 포인트가 전부 MATCH/PARTIAL 이상으로 수렴 →
  `team_tactic_setups.confidence='HIGH (in-game validated)'` + tactic_code 확정,
  해당 조합을 `player_role_map` kind='role'/'optimal'에 최종 반영.
- 수렴 실패 축이 있으면: 모양 불일치=역할 변경, 기능 불일치=포커스 변경,
  전체 형태 불일치=팀 설정(라인·빌드업) 변경 — 진단 분리 규칙.

---

## ⭐ 2026-09-08 경량 재개 — 데이터 경로 조사 결과와 절차 (사용자 요청)

위 폐기 결정은 「체계적 검증」에 대한 것이다. 카메라 촬영 없이 PS5 화면을 Mac으로 가져올 경로가 확인돼 **기록만 하는 경량 절차**를 연다.

### 경로 (조사 2026-09-08)
| 경로 | 무엇이 되나 | 비고 |
|---|---|---|
| **PS Remote Play(macOS)** + ⌘⇧4 창 캡처 | PS5 화면(Match Facts 선수 히트맵·전술 화면)을 Mac에 PNG로 바로 저장 | **최단.** 스트림 압축 화질이지만 5×5 판독엔 충분. 컨트롤러 그대로 사용 |
| PS5 Create 버튼 → **PS App 자동 업로드** | 콘솔 원본 화질, 앱에서 14일 보존 → 폰 → Mac | 설정: Media Gallery → 옵션 → 「PlayStation App에 업로드」 |
| PS5 Media Gallery → **USB(exFAT)** | 원본 화질 일괄 복사 | 3분 넘는 영상까지 |
| FC26 Match Facts 원자료 export | **없다.** 콘솔 세이브는 export 불가(PC 전용 툴만 존재) | 히트맵은 화면 판독이 유일 |

### 전술 코드
FC26 공유 코드는 **12자 알파뉴메릭 서버 코드**(예 형식 `BZbLCFDHL9tC`, 대소문자 구분)이며 Ultimate Team뿐 아니라 **커리어·킥오프의
팀 관리 → 전술 → 「코드 사용」에서 동일하게 동작**한다. 오프라인 생성·해독은 불가하고 원작자가 전술을 지우면 만료될 수 있다.
⇒ 게임에서 프리셋을 만들고 **전술 화면을 Remote Play로 찍어 보내면** 코드를 판독해 `team_tactic_setups.tactic_code`/`match_game_setups.tactic_code`에 기입한다.
EA 내장 감독 코드(알론소·과르디올라 등)는 우리 처방의 **대조군**으로 쓸 수 있다.

### 절차 (경기 1개 = 스크린샷 11장 + 전술 화면 1장)
1. 전술 화면 캡처 → 코드 기입(위). 2. 경기 후 Match Facts → 선수별 히트맵 캡처(공격 방향 메모).
3. `python3 scripts/ingame_heatmap_to_grid.py IMG --attack right --player-id N --save --regime R --tactic-code C`
   → `ingame_captures`에 map25·실측 대조 코사인 기록(`--role/--focus/--x`면 커널 대조).
4. **판정은 하지 않는다.** 코사인은 기록이고, Δ≤0.05는 노이즈(docs/20 ①). 같은 선수·같은 코드가 3경기 쌓이면 obs로 승격 검토.
⚠️ 스크립트는 **실험적**이다 — 색 임계값·피치 상자 자동 검출을 첫 실물 스크린샷으로 검증한 뒤 정본으로 올린다.

### PS Remote Play(macOS) 캡처 — 단계별 (2026-09-08 사용자 요청)
1. **PS5 준비(1회)**: 설정 → 시스템 → 리모트 플레이 → 「리모트 플레이 사용」 켬. 설정 → 시스템 → 절전 → 「휴식 모드에서 사용 가능한 기능」에서
   「인터넷에 연결 유지」·「네트워크로 PS5 켜기」 켬(꺼진 상태에서도 Mac에서 깨울 수 있다). 같은 PSN 계정으로 로그인.
2. **Mac 준비(1회)**: `remoteplay.dl.playstation.net/remoteplay/lang/en/ps5_mac.html`에서 PS Remote Play 설치 → 실행 → PSN 로그인 → 「PS5」 선택 →
   같은 네트워크면 자동 검색(안 되면 PS5 설정의 리모트 플레이 「기기 연결」 8자리 코드 입력). DualSense는 USB-C 케이블로 Mac에 연결하거나 블루투스 페어링.
   설정(톱니) → 비디오 품질 「고(1080p)」·프레임 「표준」 — 스크린샷 판독에는 1080p로 충분하고 720p도 5×5는 읽힌다.
3. **경기 전**: 게임에서 팀 관리 → 전술 → 우리 프리셋 입력(포메이션·빌드업·수비접근·라인·11명 역할/포커스) → 화면 위쪽 **12자 공유 코드**가 보이는 상태에서
   Mac에서 **⌘⇧4 → 스페이스바 → Remote Play 창 클릭**(창 전체가 PNG로 바탕화면에 저장된다). 파일명에 팀·날짜를 붙인다(예 `AVL-2026-09-09-tactic.png`).
4. **경기 후**: Match Facts → 선수 선택 → 히트맵 화면에서 같은 방법으로 선수마다 1장(선발 11명). 화면의 **공격 방향**(우리 골문이 왼쪽/오른쪽/위/아래)을 메모한다.
   Remote Play 창은 방해 요소가 없게 **전체 화면(⌃⌘F)** 으로 두고 찍으면 피치 자동 검출이 잘 된다.
5. **전달**: PNG를 채팅에 드롭하거나 `reports/ingame/YYYY-MM-DD-<team>/`에 넣고 알려준다. 처리는 `scripts/ingame_heatmap_to_grid.py IMG --attack <방향> --player-id N --save …`.
   ⚠️ 첫 실물 1장으로 색 임계값·`--box`를 검증한 뒤에야 EXPERIMENTAL을 뗀다.
- 대안: PS5 Create 버튼(짧게) → 스크린샷이 콘솔 Media Gallery에 저장 → PS App「캡처」에서 폰으로 내려받아 AirDrop(14일 안에) / USB(exFAT) 복사.
  콘솔 원본 화질이 필요할 때만 쓴다 — 5×5 판독엔 Remote Play 캡처로 충분하다.

### ⚠️ 사용자 조작 오염 — 인게임 히트맵 읽기 규칙 (2026-09-08, 사용자 제기)
인게임 히트맵은 「EA 커널이 만든 위치」와 「사용자가 만든 위치」의 혼합이다. 층위를 나눠 읽는다.
- **오염 큼**: 조작 중인 선수(볼 캐리어·수비 스위칭 대상)의 그리드 · 볼 배급 패턴(터치 기반 히트맵이면 직접 반영) · 팀 설정 3축의 체감.
- **오염 작음**: 조작하지 않는 10명의 **오프볼 위치** = 우리가 검증하려는 커널 산출. CB 2명·GK·상대 공격 시 풀백이 가장 깨끗하다.
- 캡처마다 **주로 조작한 선수**를 받아 `ingame_captures.note`에 「조작 오염」으로 표기하고, 커널 대조는 나머지 선수만 쓴다.
- 판별 과제: FC26 Match Facts 히트맵이 **위치 기반인지 터치 기반인지** 미확인 — 첫 실물의 GK 그리드로 판별한다(골라인 연속 분포=위치 / 점 몇 개=터치).
- 대조군 후보: 커리어 모드 **시뮬레이션 관전**(무개입)에서 Match Facts가 나오면 사용자 조작 0의 순수 커널 히트맵이 된다(제공 여부 미확인).

### ⛔ 이 검증의 한계 — 증명 가능 범위 (2026-09-08 확정, 사용자 지시)

| 구분 | 내용 |
|---|---|
| **증명할 수 있는 것** | 사용자가 **조작하지 않은 선수**의 오프볼 위치 분포가 우리가 고른 역할·포커스 커널과 닮았는가(코사인). 즉 「이 역할을 주면 게임이 이 자리에 세운다」 한 가지. |
| **증명할 수 없는 것 ①** | 실축 재현 여부. 게임 히트맵 ↔ 실측 히트맵 비교는 **사용자 플레이 스타일이 중간에 끼어** 순수 비교가 아니다. 실측 대조 코사인은 참고치로만 기록한다. |
| **증명할 수 없는 것 ②** | 팀 설정 3축(빌드업·수비접근·라인)의 효과. 점유·템포·라인 체감은 사용자 조작에 덮인다. 이 축은 게임 캡처로 검증하지 않는다. |
| **증명할 수 없는 것 ③** | 기능(패스·태클·슛)과 도착(득점 위치). 사용자가 결정하는 온볼 행동이라 v1의 3축(모양/기능/도착) 중 **모양만** 남는다. |
| **증명할 수 없는 것 ④** | 조작한 선수의 위치 성향. 오염 표기 후 대조에서 제외한다. |
| **표본 조건** | 같은 선수·같은 전술 코드·비조작 3경기 이상이 쌓여야 obs 후보. 1경기 값으로 처방을 바꾸지 않는다. |
| **처방 반영 규칙** | 게임 캡처 단독으로 `prescriptions`·`slot_canon_roles`·`team_tactic_setups`를 바꾸지 않는다. 캡처는 「게임이 커널대로 서는가」의 확인이고, 「어느 역할이 감독 전술에 맞는가」는 여전히 실측·서사·duties가 결정한다(docs/10 「데이터가 최종 심판」 유지). |
| **미확인 전제** | Match Facts 히트맵의 위치/터치 기반 여부 · 스트림 압축과 UI 오버레이가 색 분해에 주는 오차 · 시뮬레이션 관전에서 Match Facts 제공 여부. 셋 다 첫 실물에서 확인한다. |

⇒ 요약: **이 절차는 「역할 → 게임 내 위치」 사상의 확인 도구이지 「실축 → 게임」 재현의 심판이 아니다.** 2026-07-05 폐기 결정의 근거(체계적 검증 불가)는 그대로 유효하다.

### ✅ 첫 실물 검증 결과 (2026-09-08, 테스트 1경기 15장 · obs#536)
- **스크립트 정본화**: 피치 상자 = 외곽선 열 프로파일(좌·우) + **GK 포메이션 점 기준**(세로, 점 중심 = 하단선 −48px) → 15/15. 히트 = 밝은 초록 육각(g>r+40·g>b+40·g>110), g값 가중.
  전제: Remote Play 720p 창 캡처(Retina 2×, 피치 759×1013px). 창 크기가 바뀌면 `PH/PW/GK_OFF` 재측정.
- **미확인 전제 해소**: FC26 히트맵은 **위치 기반**(14분 출전 선수도 연속 분포). ⇒ 실측(터치 기반)과 원천이 다르다 — 실측 코사인은 참고치, 판정 축은 **게임 커널 코사인**.
- **관측 사실**: 게임 분포는 커널 예측보다 세로로 넓다(풀백 자기진영 44~66% vs 커널 32%) — 93분 총합에 상대 점유 국면이 섞여서로 추정.
  ⇒ **매 경기 「팀 통계」 화면(점유율·양 팀 슛/패스)과 상대 포메이션을 함께 받는다.** 상대 히트맵은 불필요.
- 첫 A/B: 테스트 2 코드 `AZYMfMjy8otg` = 맥긴만 `wm_widemid/Support`(정본). 판별 지표 = 맥긴·가르나초 자기진영 비중과 정본 커널 코사인.
- 파일 규약: `reports/ingame/<날짜>-<팀>-<라벨>/shot_NN.png`(경기 1개 = 폴더 1개). 조작 선수는 `--controlled`.

### ✅ A/B 1차 (2026-09-08, 테스트 2 · obs#537)
- 맥긴 RM `wideplm/Attack → widemid/Support` 한 슬롯 변경 → **자기진영 비중 5.9% → 38.9%**, T1↔T2 유사도 .71(다른 92분 선수 .83~.91). 뱅크 형성 확인.
- **재현성 기준선**: 같은 역할·같은 선수 두 경기 유사도 0.86~0.91 → 0.75 미만이면 「변화」로 읽는다. 팀 전체 자기진영 비중은 경기마다 ±10pt 움직인다(국면 몫) — 슬롯 효과는 이 기준선 위에서만 읽는다.
- 스크립트 보강: GK 선택 화면(GK 점 흰색)은 하단 외곽선 행으로 대체 · 하단이 잘린 캡처 허용 · ⛔ 선수표 세로 강조선은 목록 스크롤에 따라 움직여 기준점으로 쓸 수 없다(−150~+216px). 배경이 외곽선을 덮은 1장만 `--box` 수동.
- 부수: GK 포커스(sweeper/Build-Up)는 게임 위치 분포를 바꾸지 못했다(goalkeeper/Defend .921 vs sweeper .489) · 가르나초 winger/Attack 2경기 연속 정본(wideplm) 우위.
