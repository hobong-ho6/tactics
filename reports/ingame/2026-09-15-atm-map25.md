# ATM map25 수집 — 「11명 결손」 재조사 → 실제 5명, 2명 닫음 (2026-09-15)

사용자 지시: 「ATM map25 수집해줘」 — HANDOFF 「다음 할 일 7」·전날 PROSE 정리 보고서가 지목한
「ATM 11명 map25 전량 결손」의 실행. 관련 obs: **#746·747·748**.

---

## 0. 먼저 발견한 것 — 결손은 11명이 아니라 **5명**이었다

`player_duties` 15행이 「map25 0행」이라고 말하고 있었지만, `squad_entries`를 직접 조회하니
**9명은 이미 실측 map25가 채워져 있었다**(무소·푸빌·그리말도·카르도소·J.알바레스·쇠를로트·쿠티로메로·르마르·오베드바르가스).

⛔ **이건 내 오류였다.** 어제(09-15) PROSE 88행을 정리하면서 duty 원문(대부분 08-19 작성)을
그대로 인용해 「재판정 조건: map25 수집」이라고 적었는데, **원문은 작성 시점 스냅샷이고
그 이후 다른 회차에서 이미 수집이 끝나 있었다.** ⇒ 9행의 `applied_note`를 정정했다
(PROSE 유지, 사유만 「map25는 이미 있다」로 갱신). **교훈: 재판정 조건을 적을 때는 duty 원문이 아니라
`squad_entries` 현재 상태를 직접 조회해야 한다**(obs#746).

**진짜 결손 5명**: 호르헤 카스티요·미겔 요렌테·알바로 모레노·알렉사 푸리치·호르헤 라하도 — 전원 스쿼드 밖 유스/뎁스.

---

## 1. 수집 완료 2명

| 선수 | 슬롯 | 표본 | map25 | 결과 |
|---|---|---|---|---|
| **호르헤 카스티요** | CM | 친선 2경기(맨유 08-01 61분 · 마르세유 08-14 20분), n=64 | `00070000181043X1824900213` | **cm_halfwinger/Balanced .5856** |
| **알렉사 푸리치** | CB | **25/26 시즌** 비야레알 원정 45분, n=25 | `011000000000143043X100031` | **cb_bpd/Aggressive .8448**(RCB — LCB .3049 대비 뚜렷) |

- 경로: `team/2836/events/last` 페이지네이션으로 이벤트 id 회수 → `average-positions`로 표본 확인 →
  `player/{id}/heatmap`으로 25칸 원자료 획득 → `core.encode.cells_from_points`/`encode`로 map25 인코딩 →
  `core.kernel.Kernel.best_fit`로 역할·포커스 산출(core 재구현 없음). curl 대신 브라우저 오리진에서 JS fetch(docs/30 규칙).
- ⭐ **부수 발견** — 알렉사 푸리치의 `sofascore_id`가 DB에 **NULL**이었다(이름 발음부호 「Purić」 매칭 실패로 추정).
  실제 id 1100964로 정정했다.
- ⚠️ **둘 다 MEDIUM confidence로 명시**했다 — 카스티요는 공식전이 아닌 **친선뿐**, 푸리치는 **시즌 경계를 넘은 표본**
  (25/26)이라 26/27 시메오네 체제 실전을 대표하지 않을 수 있다. `grid_caveat`·`confidence`에 재산출 조건을 남겼다.
- `player_duties` #147·#155 → `APPLIED`(분석이 지목한 결손이 실제로 닫혔다).

---

## 2. 수집 불가 확정 3명

| 선수 | 확인 내용 |
|---|---|
| **미겔 요렌테** | 공식전 4경기 전부 unused sub. 유일한 90분 출전 2건은 **아틀레티코 마드릴레뇨(B팀)·U19** — 1군 슬롯과 무관 |
| **알바로 모레노** | 3번 GK. 평생 unused sub 1건(2026-03-15 게타페전)뿐 |
| **호르헤 라하도** | 공식전 0경기. UEFA 유스리그만 출전 |

FotMob 전 경력·SofaScore 전 경기 `average-positions`를 각각 조회해 1군 포지션 데이터가
**존재하지 않음을 확인**했다 — 값을 발명하지 않는다. `player_duties` 3행 유지, 사유만 「시도 완료·불가 확정」으로 갱신.

### ⭐⭐ 부수 발견 — 호르헤 라하도가 이적했을 수 있다

FotMob 선수 페이지가 그의 소속을 **「Real Madrid C」**로 표시한다. 우리 `squad_entries`는 여전히
`lh=OWNED`(ATM)이고 `transfer_outgoing`에 그의 행이 없다. **단일 소스라 squad_entries를 임의로 고치지 않았다**
(불변규칙: `transfer_outgoing(CONFIRMED)` 확인 후 삭제) — **다음 transfer-watch 회차에서 교차검증 필요.**

---

## 3. 최종 상태

- `squad_entries` ATM map25 결손: 5 → **3**(전부 진짜 결손, 재수집 불가 확정).
- `player_duties` 분포: MATCH 139 · **HELD 67**(전날 78에서 −11, map25 사유 15건 중 9건 사유 정정·2건 APPLIED 전환) ·
  REJECTED 19 · **PROSE 9**(map25가 이미 있어 역할코드만 없는 정상 상태) · **APPLIED 4**(+2).
- 게이트 전항 통과(G16 포함, CONFLICT 0건 유지). 회귀 검사(`core.encode.regression_check`) 4,557행 전수 통과.

## 4. 다음

1. **호르헤 라하도 소속 확인** — transfer-watch 다음 회차에서 1순위.
2. 카스티요·푸리치 값은 **잠정치**다 — 26/27 공식전 표본이 생기면 재산출(현재 조건 그대로 유지).
3. 미겔 요렌테·알바로 모레노는 공식전 출전 전까지 결손 유지 — 강제 보강 안 함.
